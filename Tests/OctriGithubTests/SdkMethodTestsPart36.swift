// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testReposCheckPrivateVulnerabilityReportingReposCheckPrivateVulneX7a2d473e10ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.checkPrivateVulnerabilityReporting(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture913Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCheckPrivateVulnerabilityReporting decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCheckVulnerabilityAlertsReposCheckVulnerabilityAlertsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.checkVulnerabilityAlerts(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testReposCodeownersErrorsReposCodeownersErrorsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.codeownersErrors(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            ref: decoded("\"ref\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture915Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCodeownersErrors decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCompareCommitsReposCompareCommitsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.compareCommits(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            basehead: decoded("\"basehead\""),
            page: decoded("1"),
            perPage: decoded("30")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture916Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCompareCommits decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateAttestationReposCreateAttestationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createAttestation(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            bundle: decoded(sdkMethodFixture917Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture917Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateAttestation decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateAutolinkReposCreateAutolinkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createAutolink(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            keyPrefix: decoded("\"TICKET-\""),
            urlTemplate: decoded("\"https://example.com/TICKET?query=<num>\""),
            isAlphanumeric: decoded("true")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture918Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateAutolink decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateCommitCommentReposCreateCommitCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createCommitComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commitSha: decoded("\"commit_sha\""),
            body: decoded("\"sample\""),
            path: decoded("\"file1.txt\""),
            position: decoded("4"),
            line: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture919Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateCommitComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateCommitSignatureProtectionReposCreateCommitSignatureProtectionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createCommitSignatureProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture920Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateCommitSignatureProtection decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateCommitStatusReposCreateCommitStatusReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createCommitStatus(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            sha: decoded("\"sha\""),
            state: decoded("\"success\""),
            targetUrl: decoded("\"https://example.com/build/status\""),
            description: decoded("\"The build succeeded!\""),
            context: decoded("\"continuous-integration/jenkins\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture921Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateCommitStatus decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateDeployKeyReposCreateDeployKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createDeployKey(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            key: decoded("\"ssh-rsa AAA...\""),
            title: decoded("\"octocat@octomac\""),
            readOnly: decoded("true")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture922Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateDeployKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateDeploymentReposCreateDeploymentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateDeploymentOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), ref: decoded("\"topic-branch\""))
        options.task = try decoded("null")
        options.autoMerge = try decoded("null")
        options.requiredContexts = try decoded("null")
        options.payload = try decoded("{\"deploy\":\"migrate\"}")
        options.environment = try decoded("null")
        options.description = try decoded("\"Deploy request from hubot\"")
        options.transientEnvironment = try decoded("null")
        options.productionEnvironment = try decoded("null")
        let actual = try await client.repos.createDeployment(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture923Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateDeployment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateDeploymentBranchPolicyReposCreateDeploymentBranchPolicyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createDeploymentBranchPolicy(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            environmentName: decoded("\"Example Name\""),
            name: decoded("\"release/*\""),
            type: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture924Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateDeploymentBranchPolicy decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateDeploymentProtectionRuleReposCreateDeploymentProtectionRuleReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createDeploymentProtectionRule(
            environmentName: decoded("\"Example Name\""),
            repo: decoded("\"repo\""),
            owner: decoded("\"owner\""),
            integrationId: decoded("5")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture925Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateDeploymentProtectionRule decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateDeploymentStatusReposCreateDeploymentStatusReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateDeploymentStatusOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), deploymentId: decoded("1"), state: decoded("\"success\""))
        options.targetUrl = try decoded("null")
        options.logUrl = try decoded("\"https://example.com/deployment/42/output\"")
        options.description = try decoded("\"Deployment finished successfully.\"")
        options.environment = try decoded("\"production\"")
        options.environmentUrl = try decoded("null")
        options.autoInactive = try decoded("null")
        let actual = try await client.repos.createDeploymentStatus(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture926Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateDeploymentStatus decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateDispatchEventReposCreateDispatchEventReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.createDispatchEvent(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            eventType: decoded("\"on-demand-test\""),
            clientPayload: decoded("{\"unit\":false,\"integration\":true}")
        )
        XCTAssertTrue(true)
    }

    func testReposCreateForAuthenticatedUserReposCreateForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateForAuthenticatedUserOptions(name: decoded("\"Hello-World\""))
        options.description = try decoded("\"This is your first repo!\"")
        options.homepage = try decoded("\"https://github.com\"")
        options.hasIssues = try decoded("null")
        options.hasProjects = try decoded("null")
        options.hasWiki = try decoded("null")
        options.hasDiscussions = try decoded("null")
        options.teamId = try decoded("null")
        options.autoInit = try decoded("null")
        options.gitignoreTemplate = try decoded("null")
        options.licenseTemplate = try decoded("null")
        options.allowSquashMerge = try decoded("null")
        options.allowMergeCommit = try decoded("null")
        options.allowRebaseMerge = try decoded("null")
        options.allowAutoMerge = try decoded("null")
        options.deleteBranchOnMerge = try decoded("null")
        options.squashMergeCommitTitle = try decoded("null")
        options.squashMergeCommitMessage = try decoded("null")
        options.mergeCommitTitle = try decoded("null")
        options.mergeCommitMessage = try decoded("null")
        options.hasDownloads = try decoded("null")
        options.isTemplate = try decoded("true")
        let actual = try await client.repos.createForAuthenticatedUser(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture928Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateForkReposCreateForkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createFork(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            organization: decoded("\"octocat\""),
            name: decoded("\"Hello-World\""),
            defaultBranchOnly: decoded("true")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture929Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateFork decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateInOrgReposCreateInOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateInOrgOptions(org: decoded("\"org\""), name: decoded("\"Hello-World\""))
        options.description = try decoded("\"This is your first repository\"")
        options.homepage = try decoded("\"https://github.com\"")
        options.visibility = try decoded("null")
        options.hasIssues = try decoded("true")
        options.hasProjects = try decoded("true")
        options.hasWiki = try decoded("true")
        options.hasDownloads = try decoded("null")
        options.isTemplate = try decoded("null")
        options.teamId = try decoded("null")
        options.autoInit = try decoded("null")
        options.gitignoreTemplate = try decoded("null")
        options.licenseTemplate = try decoded("null")
        options.allowSquashMerge = try decoded("null")
        options.allowMergeCommit = try decoded("null")
        options.allowRebaseMerge = try decoded("null")
        options.allowAutoMerge = try decoded("null")
        options.deleteBranchOnMerge = try decoded("null")
        options.useSquashPrTitleAsDefault = try decoded("null")
        options.squashMergeCommitTitle = try decoded("null")
        options.squashMergeCommitMessage = try decoded("null")
        options.mergeCommitTitle = try decoded("null")
        options.mergeCommitMessage = try decoded("null")
        options.customProperties = try decoded("null")
        let actual = try await client.repos.createInOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture930Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateInOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateOrgRulesetReposCreateOrgRulesetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createOrgRuleset(
            org: decoded("\"org\""),
            name: decoded("\"super cool ruleset\""),
            enforcement: decoded("\"active\""),
            target: decoded("\"branch\""),
            bypassActors: decoded(sdkMethodFixture931Value1),
            conditions: decoded(sdkMethodFixture931Value2),
            rules: decoded(sdkMethodFixture931Value3)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture931Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateOrgRuleset decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateOrUpdateEnvironmentReposCreateOrUpdateEnvironmentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createOrUpdateEnvironment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            environmentName: decoded("\"Example Name\""),
            waitTimer: decoded("30"),
            preventSelfReview: decoded("false"),
            reviewers: decoded("[{\"type\":\"User\",\"id\":1},{\"type\":\"Team\",\"id\":1}]"),
            deploymentBranchPolicy: decoded("{\"protected_branches\":false,\"custom_branch_policies\":true}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture932Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateOrUpdateEnvironment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateOrUpdateFileContentsReposCreateOrUpdateFileContentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateOrUpdateFileContentsOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), path: decoded("\"path\""), message: decoded("\"my commit message\""), content: decoded("\"bXkgbmV3IGZpbGUgY29udGVudHM=\""))
        options.sha = try decoded("null")
        options.branch = try decoded("null")
        options.committer = try decoded("{\"name\":\"Monalisa Octocat\",\"email\":\"octocat@github.com\"}")
        options.author = try decoded("null")
        let actual = try await client.repos.createOrUpdateFileContents(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture933Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateOrUpdateFileContents decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreatePagesDeploymentReposCreatePagesDeploymentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createPagesDeployment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pagesBuildVersion: decoded("\"4fd754f7e594640989b406850d0bc8f06a121251\""),
            oidcToken: decoded(sdkMethodFixture934Value1),
            artifactId: decoded("null"),
            artifactUrl: decoded("\"https://downloadcontent/\""),
            environment: decoded("\"github-pages\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture934Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreatePagesDeployment decoded a response " +
                "that differs from the mock"
        )
    }
}
