// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testCodeScanningGetAnalysisCodeScanningGetAnalysisReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getAnalysis(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            analysisId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture343Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetAnalysis decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningGetAutofixCodeScanningGetAutofixReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getAutofix(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture344Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetAutofix decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningGetCodeqlDatabaseCodeScanningGetCodeqlDatabaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getCodeqlDatabase(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            language: decoded("\"language\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture345Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetCodeqlDatabase decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningGetDefaultSetupCodeScanningGetDefaultSetupReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getDefaultSetup(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture346Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetDefaultSetup decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningGetSarifCodeScanningGetSarifReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getSarif(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            sarifId: decoded("\"sarif_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture347Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetSarif decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningGetVariantAnalysisCodeScanningGetVariantAnalysisReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getVariantAnalysis(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            codeqlVariantAnalysisId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture348Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetVariantAnalysis decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningGetVariantAnalysisRepoTaskCodeScanningGetVariantAXda08119484ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.getVariantAnalysisRepoTask(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            codeqlVariantAnalysisId: decoded("1"),
            repoOwner: decoded("\"repo_owner\""),
            repoName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture349Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningGetVariantAnalysisRepoTask decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningListAlertInstancesCodeScanningListAlertInstancesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.listAlertInstances(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1"),
            page: decoded("1"),
            perPage: decoded("30"),
            ref: decoded("\"ref\""),
            pr: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture350Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningListAlertInstances decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningListAlertsForOrgCodeScanningListAlertsForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeScanningMethods.CodeScanningListAlertsForOrgOptions(org: decoded("\"org\""))
        options.toolName = try decoded("\"Example Name\"")
        options.toolGuid = try decoded("\"tool_gu-1\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.page = try decoded("1")
        options.perPage = try decoded("30")
        options.direction = try decoded("\"desc\"")
        options.state = try decoded("\"open\"")
        options.sort = try decoded("\"created\"")
        options.severity = try decoded("\"critical\"")
        options.assignees = try decoded("\"assignees\"")
        let actual = try await client.codeScanning.listAlertsForOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture351Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningListAlertsForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningListAlertsForRepoCodeScanningListAlertsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeScanningMethods.CodeScanningListAlertsForRepoOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.toolName = try decoded("\"Example Name\"")
        options.toolGuid = try decoded("\"tool_gu-1\"")
        options.page = try decoded("1")
        options.perPage = try decoded("30")
        options.ref = try decoded("\"ref\"")
        options.pr = try decoded("1")
        options.direction = try decoded("\"desc\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.sort = try decoded("\"created\"")
        options.state = try decoded("\"open\"")
        options.severity = try decoded("\"critical\"")
        options.assignees = try decoded("\"assignees\"")
        let actual = try await client.codeScanning.listAlertsForRepo(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture352Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningListAlertsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningListCodeqlDatabasesCodeScanningListCodeqlDatabasesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.listCodeqlDatabases(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture353Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningListCodeqlDatabases decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningListRecentAnalysesCodeScanningListRecentAnalysesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeScanningMethods.CodeScanningListRecentAnalysesOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.toolName = try decoded("\"Example Name\"")
        options.toolGuid = try decoded("\"tool_gu-1\"")
        options.page = try decoded("1")
        options.perPage = try decoded("30")
        options.pr = try decoded("1")
        options.ref = try decoded("\"ref\"")
        options.sarifId = try decoded("\"6c81cd8e-b078-4ac3-a3be-1dad7dbd0b53\"")
        options.direction = try decoded("\"desc\"")
        options.sort = try decoded("\"created\"")
        let actual = try await client.codeScanning.listRecentAnalyses(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture354Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningListRecentAnalyses decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningUpdateAiScanEnablementCodeScanningUpdateAiScanEnablementReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.updateAiScanEnablement(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            prScan: decoded("\"enabled\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture355Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningUpdateAiScanEnablement decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningUpdateAiScanEnablementForOrgCodeScanningUpdateAiSXacd444a51fReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeScanning.updateAiScanEnablementForOrg(
            org: decoded("\"org\""),
            prScan: decoded("\"enabled\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture356Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningUpdateAiScanEnablementForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningUpdateAlertCodeScanningUpdateAlertReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeScanningMethods.CodeScanningUpdateAlertOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1")
        )
        options.state = try decoded("\"dismissed\"")
        options.dismissedReason = try decoded("\"false positive\"")
        options.dismissedComment = try decoded(sdkMethodFixture357Value1)
        options.createRequest = try decoded("true")
        options.assignees = try decoded("null")
        let actual = try await client.codeScanning.updateAlert(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture357Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningUpdateAlert decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningUpdateDefaultSetupCodeScanningUpdateDefaultSetupReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeScanningMethods.CodeScanningUpdateDefaultSetupOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.state = try decoded("\"configured\"")
        options.runnerType = try decoded("null")
        options.runnerLabel = try decoded("null")
        options.querySuite = try decoded("null")
        options.threatModel = try decoded("\"remote_and_local\"")
        options.languages = try decoded("null")
        let actual = try await client.codeScanning.updateDefaultSetup(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture358Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningUpdateDefaultSetup decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeScanningUploadSarifCodeScanningUploadSarifReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeScanningMethods.CodeScanningUploadSarifOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commitSha: decoded("\"4b6472266afd7b471e86085a6659e8c7f2b119da\""),
            ref: decoded("\"refs/heads/master\""),
            sarif: decoded(sdkMethodFixture359Value1)
        )
        options.checkoutUri = try decoded("null")
        options.startedAt = try decoded("null")
        options.toolName = try decoded("null")
        options.validate = try decoded("null")
        let actual = try await client.codeScanning.uploadSarif(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture359Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeScanningUploadSarif decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityAttachConfigurationCodeSecurityAttachConfigurationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.attachConfiguration(
            org: decoded("\"org\""),
            configurationId: decoded("1"),
            scope: decoded("\"selected\""),
            selectedRepositoryIds: decoded("[32,91]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture360Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityAttachConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityAttachEnterpriseConfigurationCodeSecurityAttachEnX03f68bb4e3ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.attachEnterpriseConfiguration(
            enterprise: decoded("\"enterprise\""),
            configurationId: decoded("1"),
            scope: decoded("\"all\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture361Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityAttachEnterpriseConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityCreateConfigurationCodeSecurityCreateConfigurationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeSecurityMethods.CodeSecurityCreateConfigurationOptions(
            org: decoded("\"org\""),
            name: decoded("\"octo-org recommended settings\"")
        )
        options.description = try decoded("\"This is a code security configuration for octo-org\"")
        options.advancedSecurity = try decoded("\"enabled\"")
        options.codeSecurity = try decoded("null")
        options.dependencyGraph = try decoded("null")
        options.dependencyGraphAutosubmitAction = try decoded("null")
        options.dependencyGraphAutosubmitActionOptions = try decoded("null")
        options.dependabotAlerts = try decoded("\"enabled\"")
        options.dependabotSecurityUpdates = try decoded("\"not_set\"")
        options.dependabotDelegatedAlertDismissal = try decoded("null")
        options.codeScanningOptions = try decoded("null")
        options.codeScanningDefaultSetup = try decoded("null")
        options.codeScanningDefaultSetupOptions = try decoded("null")
        options.codeScanningDelegatedAlertDismissal = try decoded("null")
        options.secretProtection = try decoded("null")
        options.secretScanning = try decoded("\"enabled\"")
        options.secretScanningPushProtection = try decoded("null")
        options.secretScanningDelegatedBypass = try decoded("null")
        options.secretScanningDelegatedBypassOptions = try decoded("null")
        options.secretScanningValidityChecks = try decoded("null")
        options.secretScanningNonProviderPatterns = try decoded("null")
        options.secretScanningGenericSecrets = try decoded("null")
        options.secretScanningDelegatedAlertDismissal = try decoded("null")
        options.secretScanningExtendedMetadata = try decoded("null")
        options.privateVulnerabilityReporting = try decoded("null")
        options.enforcement = try decoded("null")
        let actual = try await client.codeSecurity.createConfiguration(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture362Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityCreateConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityCreateConfigurationForEnterpriseCodeSecurityCreatX1ac013184eReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeSecurityMethods.CodeSecurityCreateConfigurationForEnterpriseOptions(
            enterprise: decoded("\"enterprise\""),
            name: decoded("\"High rish settings\"")
        )
        options.description = try decoded("\"This is a code security configuration for octo-enterprise\"")
        options.advancedSecurity = try decoded("\"enabled\"")
        options.codeSecurity = try decoded("null")
        options.dependencyGraph = try decoded("null")
        options.dependencyGraphAutosubmitAction = try decoded("null")
        options.dependencyGraphAutosubmitActionOptions = try decoded("null")
        options.dependabotAlerts = try decoded("\"enabled\"")
        options.dependabotSecurityUpdates = try decoded("\"not_set\"")
        options.codeScanningOptions = try decoded("null")
        options.codeScanningDefaultSetup = try decoded("null")
        options.codeScanningDefaultSetupOptions = try decoded("null")
        options.codeScanningDelegatedAlertDismissal = try decoded("null")
        options.secretProtection = try decoded("null")
        options.secretScanning = try decoded("\"enabled\"")
        options.secretScanningPushProtection = try decoded("null")
        options.secretScanningValidityChecks = try decoded("null")
        options.secretScanningNonProviderPatterns = try decoded("null")
        options.secretScanningGenericSecrets = try decoded("null")
        options.secretScanningDelegatedAlertDismissal = try decoded("null")
        options.secretScanningExtendedMetadata = try decoded("null")
        options.privateVulnerabilityReporting = try decoded("null")
        options.enforcement = try decoded("null")
        let actual = try await client.codeSecurity.createConfigurationForEnterprise(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture363Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityCreateConfigurationForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }
}
