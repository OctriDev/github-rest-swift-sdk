// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testActivityMarkNotificationsAsReadActivityMarkNotificationsAsReadReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.markNotificationsAsRead(
            lastReadAt: decoded("\"2022-06-10T00:00:00Z\""),
            read: decoded("true")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture212Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityMarkNotificationsAsRead decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityMarkRepoNotificationsAsReadActivityMarkRepoNotificationsAsReadReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.markRepoNotificationsAsRead(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            lastReadAt: decoded("\"2019-01-01T00:00:00Z\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture213Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityMarkRepoNotificationsAsRead decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityMarkThreadAsDoneActivityMarkThreadAsDoneReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.activity.markThreadAsDone(
            threadId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testActivityMarkThreadAsReadActivityMarkThreadAsReadReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.activity.markThreadAsRead(
            threadId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testActivitySetRepoSubscriptionActivitySetRepoSubscriptionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.setRepoSubscription(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            subscribed: decoded("true"),
            ignored: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture216Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activitySetRepoSubscription decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivitySetThreadSubscriptionActivitySetThreadSubscriptionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.setThreadSubscription(
            threadId: decoded("1"),
            ignored: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture217Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activitySetThreadSubscription decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityStarRepoForAuthenticatedUserActivityStarRepoForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.activity.starRepoForAuthenticatedUser(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testActivityUnstarRepoForAuthenticatedUserActivityUnstarRepoForAuX6902ad76e1ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.activity.unstarRepoForAuthenticatedUser(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testAgentsAddSelectedRepoToOrgSecretAgentsAddSelectedRepoToOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.agents.addSelectedRepoToOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            repositoryId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testAgentsAddSelectedRepoToOrgVariableAgentsAddSelectedRepoToOrgVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.agents.addSelectedRepoToOrgVariable(
            org: decoded("\"org\""),
            name: decoded("\"Example Name\""),
            repositoryId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testAgentsCreateOrgVariableAgentsCreateOrgVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.createOrgVariable(
            org: decoded("\"org\""),
            name: decoded("\"USERNAME\""),
            value: decoded("\"octocat\""),
            visibility: decoded("\"selected\""),
            selectedRepositoryIds: decoded("[1296269,1296280]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture222Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsCreateOrgVariable decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsCreateOrUpdateOrgSecretAgentsCreateOrUpdateOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.createOrUpdateOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            encryptedValue: decoded("\"c2VjcmV0\""),
            keyId: decoded("\"012345678912345678\""),
            visibility: decoded("\"selected\""),
            selectedRepositoryIds: decoded("[1296269,1296280]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture223Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsCreateOrUpdateOrgSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsCreateOrUpdateRepoSecretAgentsCreateOrUpdateRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.createOrUpdateRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\""),
            encryptedValue: decoded("\"c2VjcmV0\""),
            keyId: decoded("\"012345678912345678\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture224Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsCreateOrUpdateRepoSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsCreateRepoVariableAgentsCreateRepoVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.createRepoVariable(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"USERNAME\""),
            value: decoded("\"octocat\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture225Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsCreateRepoVariable decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsDeleteOrgSecretAgentsDeleteOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.agents.deleteOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testAgentsDeleteOrgVariableAgentsDeleteOrgVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.agents.deleteOrgVariable(
            org: decoded("\"org\""),
            name: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testAgentsDeleteRepoSecretAgentsDeleteRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.agents.deleteRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testAgentsDeleteRepoVariableAgentsDeleteRepoVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.agents.deleteRepoVariable(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testAgentsGetOrgPublicKeyAgentsGetOrgPublicKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.getOrgPublicKey(
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture230Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsGetOrgPublicKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsGetOrgSecretAgentsGetOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.getOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture231Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsGetOrgSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsGetOrgVariableAgentsGetOrgVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.getOrgVariable(
            org: decoded("\"org\""),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture232Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsGetOrgVariable decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsGetRepoPublicKeyAgentsGetRepoPublicKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.getRepoPublicKey(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture233Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsGetRepoPublicKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsGetRepoSecretAgentsGetRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.getRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture234Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsGetRepoSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsGetRepoVariableAgentsGetRepoVariableReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.getRepoVariable(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture235Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsGetRepoVariable decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsListOrgSecretsAgentsListOrgSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.listOrgSecrets(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture236Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsListOrgSecrets decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsListOrgVariablesAgentsListOrgVariablesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.listOrgVariables(
            org: decoded("\"org\""),
            perPage: decoded("10"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture237Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsListOrgVariables decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsListRepoOrganizationSecretsAgentsListRepoOrganizationSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.listRepoOrganizationSecrets(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture238Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsListRepoOrganizationSecrets decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsListRepoOrganizationVariablesAgentsListRepoOrganizationVariablesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.listRepoOrganizationVariables(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("10"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture239Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsListRepoOrganizationVariables decoded a response " +
                "that differs from the mock"
        )
    }

    func testAgentsListRepoSecretsAgentsListRepoSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.agents.listRepoSecrets(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture240Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "agentsListRepoSecrets decoded a response " +
                "that differs from the mock"
        )
    }
}
