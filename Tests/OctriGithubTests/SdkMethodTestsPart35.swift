// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testPullsUpdateReviewCommentPullsUpdateReviewCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.updateReviewComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            body: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture886Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsUpdateReviewComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testRateLimitGetRateLimitGetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.rateLimit.get()
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture887Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "rateLimitGet decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsCreateForCommitCommentReactionsCreateForCommitCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.createForCommitComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            content: decoded("\"heart\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture888Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsCreateForCommitComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsCreateForIssueReactionsCreateForIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.createForIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            content: decoded("\"heart\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture889Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsCreateForIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsCreateForIssueCommentReactionsCreateForIssueCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.createForIssueComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            content: decoded("\"heart\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture890Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsCreateForIssueComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsCreateForPullRequestReviewCommentReactionsCreateForPXbde21e2dceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.createForPullRequestReviewComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            content: decoded("\"heart\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture891Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsCreateForPullRequestReviewComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsCreateForReleaseReactionsCreateForReleaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.createForRelease(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            releaseId: decoded("1"),
            content: decoded("\"heart\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture892Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsCreateForRelease decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsDeleteForCommitCommentReactionsDeleteForCommitCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.reactions.deleteForCommitComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            reactionId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReactionsDeleteForIssueReactionsDeleteForIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.reactions.deleteForIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            reactionId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReactionsDeleteForIssueCommentReactionsDeleteForIssueCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.reactions.deleteForIssueComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            reactionId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReactionsDeleteForPullRequestCommentReactionsDeleteForPullRequestCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.reactions.deleteForPullRequestComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            reactionId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReactionsDeleteForReleaseReactionsDeleteForReleaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.reactions.deleteForRelease(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            releaseId: decoded("1"),
            reactionId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReactionsListForCommitCommentReactionsListForCommitCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.listForCommitComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            content: decoded("\"+1\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture898Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsListForCommitComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsListForIssueReactionsListForIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.listForIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            content: decoded("\"+1\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture899Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsListForIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsListForIssueCommentReactionsListForIssueCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.listForIssueComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            content: decoded("\"+1\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture900Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsListForIssueComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsListForPullRequestReviewCommentReactionsListForPullRX489d0545ddReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.listForPullRequestReviewComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            content: decoded("\"+1\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture901Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsListForPullRequestReviewComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReactionsListForReleaseReactionsListForReleaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.reactions.listForRelease(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            releaseId: decoded("1"),
            content: decoded("\"+1\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture902Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reactionsListForRelease decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposAcceptInvitationForAuthenticatedUserReposAcceptInvitatioX3bec86422bReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.acceptInvitationForAuthenticatedUser(
            invitationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposAddAppAccessRestrictionsReposAddAppAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.addAppAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            apps: decoded("[\"octoapp\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture904Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposAddAppAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposAddCollaboratorReposAddCollaboratorReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.addCollaborator(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            username: decoded("\"Example Name\""),
            permission: decoded("\"triage\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture905Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposAddCollaborator decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposAddStatusCheckContextsReposAddStatusCheckContextsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.addStatusCheckContexts(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            body: decoded(sdkMethodFixture906Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture906Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposAddStatusCheckContexts decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposAddTeamAccessRestrictionsReposAddTeamAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.addTeamAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            body: decoded("{\"teams\":[\"justice-league\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture907Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposAddTeamAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposAddUserAccessRestrictionsReposAddUserAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.addUserAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            users: decoded("[\"octocat\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture908Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposAddUserAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCancelPagesDeploymentReposCancelPagesDeploymentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.cancelPagesDeployment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pagesDeploymentId: decoded("30")
        )
        XCTAssertTrue(true)
    }

    func testReposCheckAutomatedSecurityFixesReposCheckAutomatedSecurityFixesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.checkAutomatedSecurityFixes(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture910Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCheckAutomatedSecurityFixes decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCheckCollaboratorReposCheckCollaboratorReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.checkCollaborator(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testReposCheckImmutableReleasesReposCheckImmutableReleasesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.checkImmutableReleases(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture912Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCheckImmutableReleases decoded a response " +
                "that differs from the mock"
        )
    }
}
