// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testAppsRemoveRepoFromInstallationForAuthenticatedUserAppsRemoveRX93b12ebad7ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.apps.removeRepoFromInstallationForAuthenticatedUser(
            installationId: decoded("1"),
            repositoryId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testAppsResetTokenAppsResetTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.apps.resetToken(
            clientId: decoded("\"client_-1\""),
            accessToken: decoded("\"e72e16c7e42f292c6912e7710c838347ae178b4a\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture295Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "appsResetToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testAppsRevokeInstallationAccessTokenAppsRevokeInstallationAccessTokenReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.apps.revokeInstallationAccessToken()
        XCTAssertTrue(true)
    }

    func testAppsScopeTokenAppsScopeTokenReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.apps.scopeToken(
            clientId: decoded("\"client_-1\""),
            accessToken: decoded("\"e72e16c7e42f292c6912e7710c838347ae178b4a\""),
            target: decoded("\"octocat\""),
            targetId: decoded("null"),
            repositories: decoded("null"),
            repositoryIds: decoded("null"),
            permissions: decoded("{\"metadata\":\"read\",\"issues\":\"write\",\"contents\":\"read\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture297Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "appsScopeToken decoded a response " +
                "that differs from the mock"
        )
    }

    func testAppsSuspendInstallationAppsSuspendInstallationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.apps.suspendInstallation(
            installationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testAppsUnsuspendInstallationAppsUnsuspendInstallationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.apps.unsuspendInstallation(
            installationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testAppsUpdateWebhookConfigForAppAppsUpdateWebhookConfigForAppReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.apps.updateWebhookConfigForApp(
            url: decoded("\"https://example.com/webhook\""),
            contentType: decoded("\"json\""),
            secret: decoded("\"********\""),
            insecureSsl: decoded("{}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture300Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "appsUpdateWebhookConfigForApp decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingCreateOrganizationBudgetBillingCreateOrganizationBudgetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try BillingMethods.BillingCreateOrganizationBudgetOptions(org: decoded("\"org\""))
        options.budgetAmount = try decoded("500")
        options.preventFurtherUsage = try decoded("true")
        options.budgetAlerting = try decoded("{\"will_alert\":false,\"alert_recipients\":[]}")
        options.budgetScope = try decoded("\"organization\"")
        options.budgetEntityName = try decoded("\"\"")
        options.budgetType = try decoded("\"BundlePricing\"")
        options.budgetProductSku = try decoded("\"actions\"")
        options.user = try decoded("null")
        options.expiresAt = try decoded("null")
        let actual = try await client.billing.createOrganizationBudget(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture301Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingCreateOrganizationBudget decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingDeleteBudgetOrgBillingDeleteBudgetOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.deleteBudgetOrg(
            org: decoded("\"org\""),
            budgetId: decoded("\"budget_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture302Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingDeleteBudgetOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetAllBudgetsOrgBillingGetAllBudgetsOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.getAllBudgetsOrg(
            org: decoded("\"org\""),
            page: decoded("1"),
            perPage: decoded("10"),
            scope: decoded("\"enterprise\""),
            user: decoded("\"user\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture303Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetAllBudgetsOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetBudgetOrgBillingGetBudgetOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.getBudgetOrg(
            org: decoded("\"org\""),
            budgetId: decoded("\"budget_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture304Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetBudgetOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingAiCreditUsageReportOrgBillingGetGithubX3f159dc50dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubAiCreditUsageReportOrg(
            org: decoded("\"org\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1"),
            user: decoded("\"user\""),
            model: decoded("\"model\""),
            product: decoded("\"product\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture305Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingAiCreditUsageReportOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingAiCreditUsageReportUserBillingGetGithuXfe4ac35b1fReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubAiCreditUsageReportUser(
            username: decoded("\"Example Name\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1"),
            model: decoded("\"model\""),
            product: decoded("\"product\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture306Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingAiCreditUsageReportUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingPremiumRequestUsageReportOrgBillingGetXc8e62b7eb4ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubPremiumRequestUsageReportOrg(
            org: decoded("\"org\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1"),
            user: decoded("\"user\""),
            model: decoded("\"model\""),
            product: decoded("\"product\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture307Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingPremiumRequestUsageReportOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingPremiumRequestUsageReportUserBillingGeX56ee32da1dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubPremiumRequestUsageReportUser(
            username: decoded("\"Example Name\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1"),
            model: decoded("\"model\""),
            product: decoded("\"product\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture308Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingPremiumRequestUsageReportUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingUsageReportOrgBillingGetGithubBillingUXdb1f75481cReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubUsageReportOrg(
            org: decoded("\"org\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture309Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingUsageReportOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingUsageReportUserBillingGetGithubBillingXb5246615d0ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubUsageReportUser(
            username: decoded("\"Example Name\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture310Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingUsageReportUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingUsageSummaryReportOrgBillingGetGithubBXf6f6b645f5ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubUsageSummaryReportOrg(
            org: decoded("\"org\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1"),
            repository: decoded("\"repository\""),
            product: decoded("\"product\""),
            sku: decoded("\"sku\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture311Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingUsageSummaryReportOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingGetGithubBillingUsageSummaryReportUserBillingGetGithubX5eb8bb63ceReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.billing.billingGetGithubUsageSummaryReportUser(
            username: decoded("\"Example Name\""),
            year: decoded("1"),
            month: decoded("1"),
            day: decoded("1"),
            repository: decoded("\"repository\""),
            product: decoded("\"product\""),
            sku: decoded("\"sku\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture312Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingGetGithubBillingUsageSummaryReportUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testBillingUpdateBudgetOrgBillingUpdateBudgetOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try BillingMethods.BillingUpdateBudgetOrgOptions(
            org: decoded("\"org\""),
            budgetId: decoded("\"budget_-1\"")
        )
        options.budgetAmount = try decoded("10")
        options.preventFurtherUsage = try decoded("false")
        options.budgetAlerting = try decoded("{\"will_alert\":false,\"alert_recipients\":[]}")
        options.budgetScope = try decoded("null")
        options.budgetEntityName = try decoded("null")
        options.budgetType = try decoded("null")
        options.budgetProductSku = try decoded("null")
        options.user = try decoded("null")
        options.expiresAt = try decoded("null")
        let actual = try await client.billing.updateBudgetOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture313Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "billingUpdateBudgetOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testCampaignsCreateCampaignCampaignsCreateCampaignReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CampaignsMethods.CampaignsCreateCampaignOptions(
            org: decoded("\"org\""),
            name: decoded("\"Critical CodeQL alerts\""),
            description: decoded(sdkMethodFixture314Value1),
            endsAt: decoded("\"2024-03-14T00:00:00Z\"")
        )
        options.managers = try decoded("[\"octocat\"]")
        options.teamManagers = try decoded("null")
        options.contactLink = try decoded("null")
        options.codeScanningAlerts = try decoded("[{\"repository_id\":1296269,\"alert_numbers\":[1,2]}]")
        options.generateIssues = try decoded("null")
        let actual = try await client.campaigns.createCampaign(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture314Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "campaignsCreateCampaign decoded a response " +
                "that differs from the mock"
        )
    }

    func testCampaignsDeleteCampaignCampaignsDeleteCampaignReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.campaigns.deleteCampaign(
            org: decoded("\"org\""),
            campaignNumber: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testCampaignsGetCampaignSummaryCampaignsGetCampaignSummaryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.campaigns.getCampaignSummary(
            org: decoded("\"org\""),
            campaignNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture316Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "campaignsGetCampaignSummary decoded a response " +
                "that differs from the mock"
        )
    }

    func testCampaignsListOrgCampaignsCampaignsListOrgCampaignsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.campaigns.campaignsListOrg(
            org: decoded("\"org\""),
            page: decoded("1"),
            perPage: decoded("30"),
            direction: decoded("\"desc\""),
            state: decoded("\"open\""),
            sort: decoded("\"created\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture317Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "campaignsListOrgCampaigns decoded a response " +
                "that differs from the mock"
        )
    }

    func testCampaignsUpdateCampaignCampaignsUpdateCampaignReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CampaignsMethods.CampaignsUpdateCampaignOptions(
            org: decoded("\"org\""),
            campaignNumber: decoded("1")
        )
        options.name = try decoded("\"Critical CodeQL alerts\"")
        options.description = try decoded("null")
        options.managers = try decoded("null")
        options.teamManagers = try decoded("null")
        options.endsAt = try decoded("null")
        options.contactLink = try decoded("null")
        options.state = try decoded("null")
        let actual = try await client.campaigns.updateCampaign(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture318Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "campaignsUpdateCampaign decoded a response " +
                "that differs from the mock"
        )
    }
}
