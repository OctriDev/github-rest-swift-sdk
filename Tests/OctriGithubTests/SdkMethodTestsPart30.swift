// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testOrgsListPatGrantRepositoriesOrgsListPatGrantRepositoriesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listPatGrantRepositories(
            org: decoded("\"org\""),
            patId: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture758Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListPatGrantRepositories decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListPatGrantRequestRepositoriesOrgsListPatGrantRequestRepositoriesReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listPatGrantRequestRepositories(
            org: decoded("\"org\""),
            patRequestId: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture759Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListPatGrantRequestRepositories decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListPatGrantRequestsOrgsListPatGrantRequestsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try OrgsMethods.OrgsListPatGrantRequestsOptions(org: decoded("\"org\""))
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        options.sort = try decoded("\"created_at\"")
        options.direction = try decoded("\"desc\"")
        options.owner = try decoded("[\"owner[]=octocat1,owner[]=octocat2\"]")
        options.repository = try decoded("\"Hello-World\"")
        options.permission = try decoded("\"issues_read\"")
        options.lastUsedBefore = try decoded("\"2024-01-01T00:00:00Z\"")
        options.lastUsedAfter = try decoded("\"2024-01-01T00:00:00Z\"")
        options.tokenId = try decoded("[\"token_id[]=1,token_id[]=2\"]")
        let actual = try await client.orgs.listPatGrantRequests(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture760Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListPatGrantRequests decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListPatGrantsOrgsListPatGrantsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try OrgsMethods.OrgsListPatGrantsOptions(org: decoded("\"org\""))
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        options.sort = try decoded("\"created_at\"")
        options.direction = try decoded("\"desc\"")
        options.owner = try decoded("[\"owner[]=octocat1,owner[]=octocat2\"]")
        options.repository = try decoded("\"Hello-World\"")
        options.permission = try decoded("\"issues_read\"")
        options.lastUsedBefore = try decoded("\"2024-01-01T00:00:00Z\"")
        options.lastUsedAfter = try decoded("\"2024-01-01T00:00:00Z\"")
        options.tokenId = try decoded("[\"token_id[]=1,token_id[]=2\"]")
        let actual = try await client.orgs.listPatGrants(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture761Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListPatGrants decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListPendingInvitationsOrgsListPendingInvitationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listPendingInvitations(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1"),
            role: decoded("\"all\""),
            invitationSource: decoded("\"all\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture762Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListPendingInvitations decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListPublicMembersOrgsListPublicMembersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listPublicMembers(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture763Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListPublicMembers decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListSecurityManagerTeamsOrgsListSecurityManagerTeamsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listSecurityManagerTeams(
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture764Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListSecurityManagerTeams decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListWebhookDeliveriesOrgsListWebhookDeliveriesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listWebhookDeliveries(
            org: decoded("\"org\""),
            hookId: decoded("1"),
            perPage: decoded("30"),
            cursor: decoded("\"cursor\""),
            status: decoded("\"success\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture765Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListWebhookDeliveries decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsListWebhooksOrgsListWebhooksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.listWebhooks(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture766Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsListWebhooks decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsPingWebhookOrgsPingWebhookReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.pingWebhook(
            org: decoded("\"org\""),
            hookId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRedeliverWebhookDeliveryOrgsRedeliverWebhookDeliveryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.redeliverWebhookDelivery(
            org: decoded("\"org\""),
            hookId: decoded("1"),
            deliveryId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture768Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsRedeliverWebhookDelivery decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsRemoveMemberOrgsRemoveMemberReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.removeMember(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRemoveMembershipForUserOrgsRemoveMembershipForUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.removeMembershipForUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRemoveOutsideCollaboratorOrgsRemoveOutsideCollaboratorReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.removeOutsideCollaborator(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRemovePublicMembershipForAuthenticatedUserOrgsRemovePubliX1c179765a0ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.removePublicMembershipForAuthenticatedUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRemoveSecurityManagerTeamOrgsRemoveSecurityManagerTeamReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.removeSecurityManagerTeam(
            org: decoded("\"org\""),
            teamSlug: decoded("\"team_slug\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsReviewPatGrantRequestOrgsReviewPatGrantRequestReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.reviewPatGrantRequest(
            org: decoded("\"org\""),
            patRequestId: decoded("1"),
            action: decoded("\"deny\""),
            reason: decoded("\"This request is denied because the access is too broad.\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsReviewPatGrantRequestsInBulkOrgsReviewPatGrantRequestsInBulkReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.reviewPatGrantRequestsInBulk(
            org: decoded("\"org\""),
            action: decoded("\"deny\""),
            patRequestIds: decoded("[42,73]"),
            reason: decoded("\"Access is too broad.\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture775Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsReviewPatGrantRequestsInBulk decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsRevokeAllOrgRolesTeamOrgsRevokeAllOrgRolesTeamReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.revokeAllOrgRolesTeam(
            org: decoded("\"org\""),
            teamSlug: decoded("\"team_slug\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRevokeAllOrgRolesUserOrgsRevokeAllOrgRolesUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.revokeAllOrgRolesUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRevokeOrgRoleTeamOrgsRevokeOrgRoleTeamReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.revokeOrgRoleTeam(
            org: decoded("\"org\""),
            teamSlug: decoded("\"team_slug\""),
            roleId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testOrgsRevokeOrgRoleUserOrgsRevokeOrgRoleUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.revokeOrgRoleUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\""),
            roleId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testOrgsSetClusterDeploymentRecordsOrgsSetClusterDeploymentRecordsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.setClusterDeploymentRecords(
            org: decoded("\"org\""),
            cluster: decoded("\"pattern-value\""),
            logicalEnvironment: decoded("\"prod\""),
            deployments: decoded(sdkMethodFixture780Value1),
            physicalEnvironment: decoded("\"pacific-east\""),
            partialSuccess: decoded("null"),
            returnRecords: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture780Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsSetClusterDeploymentRecords decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsSetImmutableReleasesSettingsOrgsSetImmutableReleasesSettingsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.setImmutableReleasesSettings(
            org: decoded("\"org\""),
            enforcedRepositories: decoded("\"all\""),
            selectedRepositoryIds: decoded("null")
        )
        XCTAssertTrue(true)
    }

    func testOrgsSetImmutableReleasesSettingsRepositoriesOrgsSetImmutableRX3c6533e047ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.setImmutableReleasesSettingsRepositories(
            org: decoded("\"org\""),
            selectedRepositoryIds: decoded("[64780797]")
        )
        XCTAssertTrue(true)
    }

    func testOrgsSetMembershipForUserOrgsSetMembershipForUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.orgs.setMembershipForUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\""),
            role: decoded("\"member\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture783Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsSetMembershipForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testOrgsSetPublicMembershipForAuthenticatedUserOrgsSetPublicMembeX6b7ecd497dReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.setPublicMembershipForAuthenticatedUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsUnblockUserOrgsUnblockUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.orgs.unblockUser(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testOrgsUpdateOrgsUpdateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try OrgsMethods.OrgsUpdateOptions(org: decoded("\"org\""))
        options.billingEmail = try decoded("\"mona@github.com\"")
        options.company = try decoded("\"GitHub\"")
        options.email = try decoded("\"mona@github.com\"")
        options.twitterUsername = try decoded("\"github\"")
        options.location = try decoded("\"San Francisco\"")
        options.name = try decoded("\"github\"")
        options.description = try decoded("\"GitHub, the company.\"")
        options.hasOrganizationProjects = try decoded("null")
        options.hasRepositoryProjects = try decoded("null")
        options.defaultRepositoryPermission = try decoded("\"read\"")
        options.membersCanCreateRepositories = try decoded("true")
        options.membersCanCreateInternalRepositories = try decoded("null")
        options.membersCanCreatePrivateRepositories = try decoded("null")
        options.membersCanCreatePublicRepositories = try decoded("null")
        options.membersAllowedRepositoryCreationType = try decoded("\"all\"")
        options.membersCanCreatePages = try decoded("null")
        options.membersCanCreatePublicPages = try decoded("null")
        options.membersCanCreatePrivatePages = try decoded("null")
        options.membersCanForkPrivateRepositories = try decoded("null")
        options.webCommitSignoffRequired = try decoded("null")
        options.blog = try decoded("null")
        options.advancedSecurityEnabledForNewRepositories = try decoded("null")
        options.dependabotAlertsEnabledForNewRepositories = try decoded("null")
        options.dependabotSecurityUpdatesEnabledForNewRepositories = try decoded("null")
        options.dependencyGraphEnabledForNewRepositories = try decoded("null")
        options.secretScanningEnabledForNewRepositories = try decoded("null")
        options.secretScanningPushProtectionEnabledForNewRepositories = try decoded("null")
        options.secretScanningPushProtectionCustomLinkEnabled = try decoded("null")
        options.secretScanningPushProtectionCustomLink = try decoded("null")
        options.deployKeysEnabledForRepositories = try decoded("null")
        let actual = try await client.orgs.update(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture786Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "orgsUpdate decoded a response " +
                "that differs from the mock"
        )
    }
}
