// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testSecretScanningListAlertsForOrgSecretScanningListAlertsForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecretScanningMethods.SecretScanningListAlertsForOrgOptions(org: decoded("\"org\""))
        options.state = try decoded("\"open\"")
        options.secretType = try decoded("\"secret_type\"")
        options.excludeSecretTypes = try decoded("\"exclude_secret_types\"")
        options.excludeProviders = try decoded("\"exclude_providers\"")
        options.providers = try decoded("\"providers\"")
        options.resolution = try decoded("\"resolution\"")
        options.assignee = try decoded("\"assignee\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.page = try decoded("1")
        options.perPage = try decoded("30")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.validity = try decoded("\"validity\"")
        options.isPubliclyLeaked = try decoded("false")
        options.isMultiRepo = try decoded("false")
        options.hideSecret = try decoded("false")
        options.isBypassed = try decoded("false")
        options.includedMetadata = try decoded("\"included_metadata\"")
        options.ownerEmailHash = try decoded("\"user@example.com\"")
        let actual = try await client.secretScanning.listAlertsForOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1118Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningListAlertsForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningListAlertsForRepoSecretScanningListAlertsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecretScanningMethods.SecretScanningListAlertsForRepoOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.state = try decoded("\"open\"")
        options.secretType = try decoded("\"secret_type\"")
        options.excludeSecretTypes = try decoded("\"exclude_secret_types\"")
        options.excludeProviders = try decoded("\"exclude_providers\"")
        options.providers = try decoded("\"providers\"")
        options.resolution = try decoded("\"resolution\"")
        options.assignee = try decoded("\"assignee\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.page = try decoded("1")
        options.perPage = try decoded("30")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.validity = try decoded("\"validity\"")
        options.isPubliclyLeaked = try decoded("false")
        options.isMultiRepo = try decoded("false")
        options.hideSecret = try decoded("false")
        options.isBypassed = try decoded("false")
        options.includedMetadata = try decoded("\"included_metadata\"")
        options.ownerEmailHash = try decoded("\"user@example.com\"")
        let actual = try await client.secretScanning.listAlertsForRepo(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1119Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningListAlertsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningListLocationsForAlertSecretScanningListLocationsForAlertReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.listLocationsForAlert(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1"),
            page: decoded("1"),
            perPage: decoded("30")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1120Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningListLocationsForAlert decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningListOrgCustomPatternsSecretScanningListOrgCustomPatternsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.listOrgCustomPatterns(
            org: decoded("\"org\""),
            state: decoded("\"published\""),
            pushProtection: decoded("\"enabled\""),
            sort: decoded("\"created\""),
            direction: decoded("\"desc\""),
            before: decoded("\"before\""),
            after: decoded("\"after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1121Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningListOrgCustomPatterns decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningListOrgPatternConfigsSecretScanningListOrgPatternConfigsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.listOrgPatternConfigs(
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1122Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningListOrgPatternConfigs decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningListRepoCustomPatternsSecretScanningListRepoCustomPatternsReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecretScanningMethods.SecretScanningListRepoCustomPatternsOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.state = try decoded("\"published\"")
        options.pushProtection = try decoded("\"enabled\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        let actual = try await client.secretScanning.listRepoCustomPatterns(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1123Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningListRepoCustomPatterns decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningUpdateAlertSecretScanningUpdateAlertReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecretScanningMethods.SecretScanningUpdateAlertOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1")
        )
        options.state = try decoded("\"resolved\"")
        options.resolution = try decoded("\"false_positive\"")
        options.resolutionComment = try decoded("null")
        options.assignee = try decoded("null")
        options.validity = try decoded("null")
        let actual = try await client.secretScanning.updateAlert(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1124Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningUpdateAlert decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningUpdateOrgCustomPatternSecretScanningUpdateOrgCustomPatternReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecretScanningMethods.SecretScanningUpdateOrgCustomPatternOptions(
            org: decoded("\"org\""),
            patternId: decoded("1"),
            customPatternVersion: decoded("\"0ujsswThIGTUYm2K8FjOOfXtY1K\"")
        )
        options.pattern = try decoded("\"updated_secret_[0-9A-Z]{16}\"")
        options.startDelimiter = try decoded("\"[^0-9A-Za-z]\"")
        options.endDelimiter = try decoded("\"[^0-9A-Za-z]\"")
        options.mustMatch = try decoded("[\"updated_secret_[0-9]{2}*\"]")
        options.mustNotMatch = try decoded("[\"updated_secret_1234567890ABCDEF\"]")
        let actual = try await client.secretScanning.updateOrgCustomPattern(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1125Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningUpdateOrgCustomPattern decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningUpdateOrgPatternConfigsSecretScanningUpdateOrgPXb050b4783aReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.updateOrgPatternConfigs(
            org: decoded("\"org\""),
            patternConfigVersion: decoded("\"0ujsswThIGTUYm2K8FjOOfXtY1K\""),
            providerPatternSettings: decoded(sdkMethodFixture1126Value1),
            customPatternSettings: decoded(sdkMethodFixture1126Value2)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1126Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningUpdateOrgPatternConfigs decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningUpdateRepoCustomPatternSecretScanningUpdateRepoXb3a49c7715ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecretScanningMethods.SecretScanningUpdateRepoCustomPatternOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            patternId: decoded("1"),
            customPatternVersion: decoded("\"0ujsswThIGTUYm2K8FjOOfXtY1K\"")
        )
        options.pattern = try decoded("\"updated_secret_[0-9A-Z]{16}\"")
        options.startDelimiter = try decoded("\"[^0-9A-Za-z]\"")
        options.endDelimiter = try decoded("\"[^0-9A-Za-z]\"")
        options.mustMatch = try decoded("[\"updated_secret_[0-9]{2}*\"]")
        options.mustNotMatch = try decoded("[\"updated_secret_1234567890ABCDEF\"]")
        let actual = try await client.secretScanning.updateRepoCustomPattern(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1127Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningUpdateRepoCustomPattern decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesCreateForkSecurityAdvisoriesCreateForkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.securityAdvisories.createFork(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            ghsaId: decoded("\"ghsa_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1128Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesCreateFork decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesCreatePrivateVulnerabilityReportSecurityAdvXffa0ae23dcReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecurityAdvisoriesMethods.SecurityAdvisoriesCreatePrivateVulnerabilityReportOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            summary: decoded("\"A newly discovered vulnerability\""),
            description: decoded("\"A more in-depth description of what the problem is.\"")
        )
        options.vulnerabilities = try decoded(sdkMethodFixture1129Value1)
        options.cweIds = try decoded("[\"CWE-123\"]")
        options.severity = try decoded("\"high\"")
        options.cvssVectorString = try decoded("null")
        options.startPrivateFork = try decoded("null")
        let actual = try await client.securityAdvisories.createPrivateVulnerabilityReport(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1129Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesCreatePrivateVulnerabilityReport decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesCreateRepositoryAdvisorySecurityAdvisoriesCX0c6ccfa16eReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecurityAdvisoriesMethods.SecurityAdvisoriesCreateRepositoryAdvisoryOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            summary: decoded("\"A new important advisory\""),
            description: decoded("\"A more in-depth description of what the problem is.\""),
            vulnerabilities: decoded(sdkMethodFixture1130Value1)
        )
        options.cveId = try decoded("\"cve_-1\"")
        options.cweIds = try decoded("[\"CWE-1101\",\"CWE-20\"]")
        options.credits = try decoded(sdkMethodFixture1130Value2)
        options.severity = try decoded("\"high\"")
        options.cvssVectorString = try decoded("null")
        options.startPrivateFork = try decoded("null")
        let actual = try await client.securityAdvisories.createRepositoryAdvisory(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1130Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesCreateRepositoryAdvisory decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesCreateRepositoryAdvisoryCveRequestSecurityAX2539915bdfReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.securityAdvisories.createRepositoryAdvisoryCveRequest(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            ghsaId: decoded("\"ghsa_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1131Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesCreateRepositoryAdvisoryCveRequest decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesGetGlobalAdvisorySecurityAdvisoriesGetGlobalAdvisoryReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.securityAdvisories.getGlobalAdvisory(
            ghsaId: decoded("\"ghsa_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1132Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesGetGlobalAdvisory decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesGetRepositoryAdvisorySecurityAdvisoriesGetRX83a89cc6ccReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.securityAdvisories.getRepositoryAdvisory(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            ghsaId: decoded("\"ghsa_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1133Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesGetRepositoryAdvisory decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesListGlobalAdvisoriesSecurityAdvisoriesListGX40d5c63c74ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = SecurityAdvisoriesMethods.SecurityAdvisoriesListGlobalAdvisoriesOptions()
        options.ghsaId = try decoded("\"ghsa_-1\"")
        options.type = try decoded("\"reviewed\"")
        options.cveId = try decoded("\"cve_-1\"")
        options.ecosystem = try decoded("\"rubygems\"")
        options.severity = try decoded("\"unknown\"")
        options.cwes = try decoded("\"cwes\"")
        options.isWithdrawn = try decoded("false")
        options.affects = try decoded("\"affects\"")
        options.published = try decoded("\"published\"")
        options.updated = try decoded("\"2024-01-01T00:00:00Z\"")
        options.modified = try decoded("\"modified\"")
        options.epssPercentage = try decoded("\"epss_percentage\"")
        options.epssPercentile = try decoded("\"epss_percentile\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.direction = try decoded("\"desc\"")
        options.perPage = try decoded("30")
        options.sort = try decoded("\"published\"")
        let actual = try await client.securityAdvisories.listGlobalAdvisories(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1134Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesListGlobalAdvisories decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesListOrgRepositoryAdvisoriesSecurityAdvisoriXd2cb1bf494ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.securityAdvisories.listOrgRepositoryAdvisories(
            org: decoded("\"org\""),
            direction: decoded("\"desc\""),
            sort: decoded("\"created\""),
            before: decoded("\"before\""),
            after: decoded("\"after\""),
            perPage: decoded("30"),
            state: decoded("\"triage\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1135Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesListOrgRepositoryAdvisories decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecurityAdvisoriesListRepositoryAdvisoriesSecurityAdvisoriesLXfebda494aeReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try SecurityAdvisoriesMethods.SecurityAdvisoriesListRepositoryAdvisoriesOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.direction = try decoded("\"desc\"")
        options.sort = try decoded("\"created\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.perPage = try decoded("30")
        options.state = try decoded("\"triage\"")
        let actual = try await client.securityAdvisories.listRepositoryAdvisories(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1136Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "securityAdvisoriesListRepositoryAdvisories decoded a response " +
                "that differs from the mock"
        )
    }
}
