// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testDependabotCreateOrUpdateRepoSecretDependabotCreateOrUpdateRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.createOrUpdateRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\""),
            encryptedValue: decoded("\"c2VjcmV0\""),
            keyId: decoded("\"012345678912345678\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture492Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotCreateOrUpdateRepoSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotDeleteOrgSecretDependabotDeleteOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.deleteOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testDependabotDeleteRepoSecretDependabotDeleteRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.deleteRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testDependabotGetAlertDependabotGetAlertReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.getAlert(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture495Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotGetAlert decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotGetOrgPublicKeyDependabotGetOrgPublicKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.getOrgPublicKey(
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture496Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotGetOrgPublicKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotGetOrgSecretDependabotGetOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.getOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture497Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotGetOrgSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotGetRepoPublicKeyDependabotGetRepoPublicKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.getRepoPublicKey(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture498Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotGetRepoPublicKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotGetRepoSecretDependabotGetRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.getRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture499Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotGetRepoSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotListAlertsForEnterpriseDependabotListAlertsForEnterpriseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try DependabotMethods.DependabotListAlertsForEnterpriseOptions(enterprise: decoded("\"enterprise\""))
        options.classification = try decoded("\"classification\"")
        options.state = try decoded("\"state\"")
        options.severity = try decoded("\"severity\"")
        options.ecosystem = try decoded("\"ecosystem\"")
        options.package = try decoded("\"package\"")
        options.epssPercentage = try decoded("\"epss_percentage\"")
        options.has = try decoded("\"has\"")
        options.assignee = try decoded("\"assignee\"")
        options.scope = try decoded("\"development\"")
        options.relationship = try decoded("\"relationship\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.perPage = try decoded("30")
        let actual = try await client.dependabot.listAlertsForEnterprise(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture500Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotListAlertsForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotListAlertsForOrgDependabotListAlertsForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try DependabotMethods.DependabotListAlertsForOrgOptions(org: decoded("\"org\""))
        options.classification = try decoded("\"classification\"")
        options.state = try decoded("\"state\"")
        options.severity = try decoded("\"severity\"")
        options.ecosystem = try decoded("\"ecosystem\"")
        options.package = try decoded("\"package\"")
        options.epssPercentage = try decoded("\"epss_percentage\"")
        options.artifactRegistryUrl = try decoded("\"https://example.com\"")
        options.artifactRegistry = try decoded("\"artifact_registry\"")
        options.has = try decoded("\"has\"")
        options.assignee = try decoded("\"assignee\"")
        options.runtimeRisk = try decoded("\"2024-01-01T00:00:00Z\"")
        options.scope = try decoded("\"development\"")
        options.relationship = try decoded("\"relationship\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.perPage = try decoded("30")
        let actual = try await client.dependabot.listAlertsForOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture501Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotListAlertsForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotListAlertsForRepoDependabotListAlertsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try DependabotMethods.DependabotListAlertsForRepoOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""))
        options.classification = try decoded("\"classification\"")
        options.state = try decoded("\"state\"")
        options.severity = try decoded("\"severity\"")
        options.ecosystem = try decoded("\"ecosystem\"")
        options.package = try decoded("\"package\"")
        options.manifest = try decoded("\"manifest\"")
        options.epssPercentage = try decoded("\"epss_percentage\"")
        options.has = try decoded("\"has\"")
        options.assignee = try decoded("\"assignee\"")
        options.scope = try decoded("\"development\"")
        options.relationship = try decoded("\"relationship\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.before = try decoded("\"before\"")
        options.after = try decoded("\"after\"")
        options.perPage = try decoded("30")
        let actual = try await client.dependabot.listAlertsForRepo(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture502Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotListAlertsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotListOrgSecretsDependabotListOrgSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.listOrgSecrets(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture503Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotListOrgSecrets decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotListRepoSecretsDependabotListRepoSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.listRepoSecrets(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture504Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotListRepoSecrets decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotListSelectedReposForOrgSecretDependabotListSelectedX91aad6ca0fReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.listSelectedReposForOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            page: decoded("1"),
            perPage: decoded("30")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture505Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotListSelectedReposForOrgSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotRemoveSelectedRepoFromOrgSecretDependabotRemoveSeleX12af682b82ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.removeSelectedRepoFromOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            repositoryId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testDependabotRepositoryAccessForEnterpriseDependabotRepositoryAcXa6f34ed044ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.repositoryAccessForEnterprise(
            enterprise: decoded("\"enterprise\""),
            page: decoded("1"),
            perPage: decoded("30")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture507Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotRepositoryAccessForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotRepositoryAccessForOrgDependabotRepositoryAccessForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependabot.repositoryAccessForOrg(
            org: decoded("\"org\""),
            page: decoded("1"),
            perPage: decoded("30")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture508Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotRepositoryAccessForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotSetRepositoryAccessDefaultLevelDependabotSetRepositX4784336337ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.setRepositoryAccessDefaultLevel(
            org: decoded("\"org\""),
            defaultLevel: decoded("\"public\"")
        )
        XCTAssertTrue(true)
    }

    func testDependabotSetRepositoryAccessDefaultLevelForEnterpriseDependaX8e6cf8f080ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.setRepositoryAccessDefaultLevelForEnterprise(
            enterprise: decoded("\"enterprise\""),
            defaultLevel: decoded("\"public\"")
        )
        XCTAssertTrue(true)
    }

    func testDependabotSetSelectedReposForOrgSecretDependabotSetSelectedReX5277be0fc7ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.setSelectedReposForOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            selectedRepositoryIds: decoded("[64780797]")
        )
        XCTAssertTrue(true)
    }

    func testDependabotUpdateAlertDependabotUpdateAlertReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try DependabotMethods.DependabotUpdateAlertOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), alertNumber: decoded("1"))
        options.state = try decoded("\"dismissed\"")
        options.dismissedReason = try decoded("\"tolerable_risk\"")
        options.dismissedComment = try decoded("\"This alert is accurate but we use a sanitizer.\"")
        options.assignees = try decoded("null")
        options.agentAssignment = try decoded("null")
        let actual = try await client.dependabot.updateAlert(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture512Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependabotUpdateAlert decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependabotUpdateRepositoryAccessForEnterpriseDependabotUpdateX1d6e3dcbf8ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.updateRepositoryAccessForEnterprise(
            enterprise: decoded("\"enterprise\""),
            repositoryIdsToAdd: decoded("[123,456]"),
            repositoryIdsToRemove: decoded("null")
        )
        XCTAssertTrue(true)
    }

    func testDependabotUpdateRepositoryAccessForOrgDependabotUpdateRepositX4330294b86ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependabot.updateRepositoryAccessForOrg(
            org: decoded("\"org\""),
            repositoryIdsToAdd: decoded("[123,456]"),
            repositoryIdsToRemove: decoded("null")
        )
        XCTAssertTrue(true)
    }

    func testDependencyGraphCreateRepositorySnapshotDependencyGraphCreateRX24259b9c68ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependencyGraph.createRepositorySnapshot(
            options: DependencyGraphMethods.DependencyGraphCreateRepositorySnapshotOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), version: decoded("0"), job: decoded(sdkMethodFixture515Value1), sha: decoded("\"ce587453ced02b1526dfb4cb910479d431683101\""), ref: decoded("\"refs/heads/main\""), detector: decoded(sdkMethodFixture515Value2))
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture515Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependencyGraphCreateRepositorySnapshot decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependencyGraphDiffRangeDependencyGraphDiffRangeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependencyGraph.diffRange(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            basehead: decoded("\"basehead\""),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture516Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependencyGraphDiffRange decoded a response " +
                "that differs from the mock"
        )
    }
}
