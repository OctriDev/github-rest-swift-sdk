// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testReposUpdateOrgRulesetReposUpdateOrgRulesetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdateOrgRulesetOptions(org: decoded("\"org\""), rulesetId: decoded("1"))
        options.name = try decoded("\"super cool ruleset\"")
        options.target = try decoded("\"branch\"")
        options.enforcement = try decoded("\"active\"")
        options.bypassActors = try decoded(sdkMethodFixture1095Value1)
        options.conditions = try decoded(sdkMethodFixture1095Value2)
        options.rules = try decoded(sdkMethodFixture1095Value3)
        let actual = try await client.repos.updateOrgRuleset(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1095Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateOrgRuleset decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdatePullRequestReviewProtectionReposUpdatePullRequestRX3b2b2babd5ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdatePullRequestReviewProtectionOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), branch: decoded("\"branch\""))
        options.dismissalRestrictions = try decoded(sdkMethodFixture1096Value1)
        options.dismissStaleReviews = try decoded("true")
        options.requireCodeOwnerReviews = try decoded("true")
        options.requiredApprovingReviewCount = try decoded("2")
        options.requireLastPushApproval = try decoded("true")
        options.bypassPullRequestAllowances = try decoded(sdkMethodFixture1096Value2)
        let actual = try await client.repos.updatePullRequestReviewProtection(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1096Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdatePullRequestReviewProtection decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateReleaseReposUpdateReleaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdateReleaseOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), releaseId: decoded("1"))
        options.tagName = try decoded("\"v1.0.0\"")
        options.targetCommitish = try decoded("\"master\"")
        options.name = try decoded("\"v1.0.0\"")
        options.body = try decoded("\"sample\"")
        options.draft = try decoded("false")
        options.prerelease = try decoded("false")
        options.makeLatest = try decoded("null")
        options.discussionCategoryName = try decoded("null")
        let actual = try await client.repos.updateRelease(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1097Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateRelease decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateReleaseAssetReposUpdateReleaseAssetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.updateReleaseAsset(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            assetId: decoded("1"),
            name: decoded("\"foo-1.0.0-osx.zip\""),
            label: decoded("\"Mac binary\""),
            state: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1098Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateReleaseAsset decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateRepoRulesetReposUpdateRepoRulesetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdateRepoRulesetOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), rulesetId: decoded("1"))
        options.name = try decoded("\"super cool ruleset\"")
        options.target = try decoded("\"branch\"")
        options.enforcement = try decoded("\"active\"")
        options.bypassActors = try decoded(sdkMethodFixture1099Value1)
        options.conditions = try decoded(sdkMethodFixture1099Value2)
        options.rules = try decoded(sdkMethodFixture1099Value3)
        let actual = try await client.repos.updateRepoRuleset(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1099Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateRepoRuleset decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateStatusCheckProtectionReposUpdateStatusCheckProtectionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.updateStatusCheckProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            strict: decoded("true"),
            contexts: decoded("[\"continuous-integration/travis-ci\"]"),
            checks: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1100Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateStatusCheckProtection decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateWebhookReposUpdateWebhookReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdateWebhookOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), hookId: decoded("1"))
        options.config = try decoded("null")
        options.events = try decoded("null")
        options.addEvents = try decoded("[\"pull_request\"]")
        options.removeEvents = try decoded("null")
        options.active = try decoded("true")
        let actual = try await client.repos.updateWebhook(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1101Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateWebhook decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateWebhookConfigForRepoReposUpdateWebhookConfigForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.updateWebhookConfigForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            hookId: decoded("1"),
            url: decoded("\"https://example.com/webhook\""),
            contentType: decoded("\"json\""),
            secret: decoded("null"),
            insecureSsl: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1102Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateWebhookConfigForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUploadReleaseAssetReposUploadReleaseAssetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.uploadReleaseAsset(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            releaseId: decoded("1"),
            name: decoded("\"Example Name\""),
            label: decoded("\"label\""),
            body: Data("@example.zip".utf8)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1103Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUploadReleaseAsset decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchCodeSearchCodeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.code(
            q: decoded("\"q\""),
            sort: decoded("\"indexed\""),
            order: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1104Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchCode decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchCommitsSearchCommitsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.commits(
            q: decoded("\"q\""),
            sort: decoded("\"author-date\""),
            order: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1105Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchCommits decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchIssuesAndPullRequestsSearchIssuesAndPullRequestsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.issuesAndPullRequests(
            q: decoded("\"q\""),
            sort: decoded("\"comments\""),
            order: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1"),
            advancedSearch: decoded("\"advanced_search\""),
            searchType: decoded("\"semantic\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1106Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchIssuesAndPullRequests decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchLabelsSearchLabelsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.labels(
            repositoryId: decoded("1"),
            q: decoded("\"q\""),
            sort: decoded("\"created\""),
            order: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1107Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchLabels decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchReposSearchReposReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.repos(
            q: decoded("\"q\""),
            sort: decoded("\"stars\""),
            order: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1108Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchRepos decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchTopicsSearchTopicsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.topics(
            q: decoded("\"q\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1109Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchTopics decoded a response " +
                "that differs from the mock"
        )
    }

    func testSearchUsersSearchUsersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.search.users(
            q: decoded("\"q\""),
            sort: decoded("\"followers\""),
            order: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1110Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "searchUsers decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningBulkCreateOrgCustomPatternsSecretScanningBulkCrX1538568927ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.bulkCreateOrgCustomPatterns(
            org: decoded("\"org\""),
            patterns: decoded(sdkMethodFixture1111Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1111Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningBulkCreateOrgCustomPatterns decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningBulkCreateRepoCustomPatternsSecretScanningBulkCX4511bfb679ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.bulkCreateRepoCustomPatterns(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            patterns: decoded(sdkMethodFixture1112Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1112Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningBulkCreateRepoCustomPatterns decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningBulkDeleteOrgCustomPatternsSecretScanningBulkDeX4f781a0ea6ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.secretScanning.bulkDeleteOrgCustomPatterns(
            org: decoded("\"org\""),
            patterns: decoded(sdkMethodFixture1113Value1),
            postDeleteAction: decoded("null")
        )
        XCTAssertTrue(true)
    }

    func testSecretScanningBulkDeleteRepoCustomPatternsSecretScanningBulkDX6614eb889cReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.secretScanning.bulkDeleteRepoCustomPatterns(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            patterns: decoded(sdkMethodFixture1114Value1),
            postDeleteAction: decoded("null")
        )
        XCTAssertTrue(true)
    }

    func testSecretScanningCreatePushProtectionBypassSecretScanningCreatePX3d4cea889cReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.createPushProtectionBypass(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            reason: decoded("\"will_fix_later\""),
            placeholderId: decoded("\"2k4dM4tseyC5lPIsjl5emX9sPNk\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1115Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningCreatePushProtectionBypass decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningGetAlertSecretScanningGetAlertReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.getAlert(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            alertNumber: decoded("1"),
            hideSecret: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1116Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningGetAlert decoded a response " +
                "that differs from the mock"
        )
    }

    func testSecretScanningGetScanHistorySecretScanningGetScanHistoryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.secretScanning.getScanHistory(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1117Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "secretScanningGetScanHistory decoded a response " +
                "that differs from the mock"
        )
    }
}
