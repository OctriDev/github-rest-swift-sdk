// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testDependencyGraphExportSbomDependencyGraphExportSbomReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependencyGraph.exportSbom(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture517Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependencyGraphExportSbom decoded a response " +
                "that differs from the mock"
        )
    }

    func testDependencyGraphFetchSbomReportDependencyGraphFetchSbomReportReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.dependencyGraph.fetchSbomReport(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            sbomUuid: decoded("\"sbom_uu-1\"")
        )
        XCTAssertTrue(true)
    }

    func testDependencyGraphGenerateSbomReportDependencyGraphGenerateSbomReportReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.dependencyGraph.generateSbomReport(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture519Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "dependencyGraphGenerateSbomReport decoded a response " +
                "that differs from the mock"
        )
    }

    func testEmojisGetEmojisGetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.emojis.get()
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture520Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "emojisGet decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsAddEnterpriseTeamMembershipsAddReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamMemberships.add(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            username: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture521Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamMembershipsAdd decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsBulkAddEnterpriseTeamMembershipsBulkAddReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamMemberships.bulkAdd(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            usernames: decoded("[\"monalisa\",\"octocat\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture522Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamMembershipsBulkAdd decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsBulkRemoveEnterpriseTeamMembershipsBulkRemoveReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamMemberships.bulkRemove(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            usernames: decoded("[\"monalisa\",\"octocat\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture523Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamMembershipsBulkRemove decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsGetEnterpriseTeamMembershipsGetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamMemberships.get(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            username: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture524Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamMembershipsGet decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsListEnterpriseTeamMembershipsListReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamMemberships.list(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture525Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamMembershipsList decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsListTeamsForUserEnterpriseTeamMemberXc7f16fd460ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamMemberships.listTeamsForUser(
            enterprise: decoded("\"enterprise\""),
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture526Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamMembershipsListTeamsForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamMembershipsRemoveEnterpriseTeamMembershipsRemoveReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.enterpriseTeamMemberships.remove(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testEnterpriseTeamOrganizationsAddEnterpriseTeamOrganizationsAddReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamOrganizations.add(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture528Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamOrganizationsAdd decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamOrganizationsBulkAddEnterpriseTeamOrganizationsBulkAddReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamOrganizations.bulkAdd(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            organizationSlugs: decoded("[\"github\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture529Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamOrganizationsBulkAdd decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamOrganizationsBulkRemoveEnterpriseTeamOrganizatiXa205b59840ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.enterpriseTeamOrganizations.bulkRemove(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            organizationSlugs: decoded("[\"github\"]")
        )
        XCTAssertTrue(true)
    }

    func testEnterpriseTeamOrganizationsDeleteEnterpriseTeamOrganizationsDeleteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.enterpriseTeamOrganizations.delete(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            org: decoded("\"org\"")
        )
        XCTAssertTrue(true)
    }

    func testEnterpriseTeamOrganizationsGetAssignmentEnterpriseTeamOrganizXfd9c5a0e33ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamOrganizations.getAssignment(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture532Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamOrganizationsGetAssignment decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamOrganizationsGetAssignmentsEnterpriseTeamOrganiX75fde3de7aReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeamOrganizations.getAssignments(
            enterprise: decoded("\"enterprise\""),
            enterpriseTeam: decoded("\"enterprise-team\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture533Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamOrganizationsGetAssignments decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamsCreateEnterpriseTeamsCreateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeams.create(
            enterprise: decoded("\"enterprise\""),
            name: decoded("\"Justice League\""),
            description: decoded("\"A great team.\""),
            syncToOrganizations: decoded("null"),
            organizationSelectionType: decoded("null"),
            groupId: decoded("\"62ab9291-fae2-468e-974b-7e45096d5021\""),
            notificationSetting: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture534Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamsCreate decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamsDeleteEnterpriseTeamsDeleteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.enterpriseTeams.delete(
            enterprise: decoded("\"enterprise\""),
            teamSlug: decoded("\"team_slug\"")
        )
        XCTAssertTrue(true)
    }

    func testEnterpriseTeamsGetEnterpriseTeamsGetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeams.get(
            enterprise: decoded("\"enterprise\""),
            teamSlug: decoded("\"team_slug\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture536Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamsGet decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamsListEnterpriseTeamsListReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.enterpriseTeams.list(
            enterprise: decoded("\"enterprise\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture537Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamsList decoded a response " +
                "that differs from the mock"
        )
    }

    func testEnterpriseTeamsUpdateEnterpriseTeamsUpdateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try EnterpriseTeamsMethods.EnterpriseTeamsUpdateOptions(enterprise: decoded("\"enterprise\""), teamSlug: decoded("\"team_slug\""))
        options.name = try decoded("\"Justice League\"")
        options.description = try decoded("\"A great team.\"")
        options.syncToOrganizations = try decoded("null")
        options.organizationSelectionType = try decoded("null")
        options.groupId = try decoded("\"62ab9291-fae2-468e-974b-7e45096d5021\"")
        options.notificationSetting = try decoded("null")
        let actual = try await client.enterpriseTeams.update(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture538Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "enterpriseTeamsUpdate decoded a response " +
                "that differs from the mock"
        )
    }

    func testGistsCheckIsStarredGistsCheckIsStarredReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.gists.checkIsStarred(
            gistId: decoded("\"gist_-1\"")
        )
        XCTAssertTrue(true)
    }

    func testGistsCreateGistsCreateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.gists.create(
            files: decoded("{\"README.md\":{\"content\":\"Hello World\"}}"),
            description: decoded("\"Example of a gist\""),
            public: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture540Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "gistsCreate decoded a response " +
                "that differs from the mock"
        )
    }

    func testGistsCreateCommentGistsCreateCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.gists.createComment(
            gistId: decoded("\"gist_-1\""),
            body: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture541Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "gistsCreateComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testGistsDeleteGistsDeleteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.gists.delete(
            gistId: decoded("\"gist_-1\"")
        )
        XCTAssertTrue(true)
    }

    func testGistsDeleteCommentGistsDeleteCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.gists.deleteComment(
            gistId: decoded("\"gist_-1\""),
            commentId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testGistsForkGistsForkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.gists.fork(
            gistId: decoded("\"gist_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture544Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "gistsFork decoded a response " +
                "that differs from the mock"
        )
    }
}
