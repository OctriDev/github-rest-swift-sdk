// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testReposCreatePagesSiteReposCreatePagesSiteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createPagesSite(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            buildType: decoded("null"),
            source: decoded("{\"branch\":\"main\",\"path\":\"/docs\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture935Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreatePagesSite decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateReleaseReposCreateReleaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateReleaseOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            tagName: decoded("\"v1.0.0\"")
        )
        options.targetCommitish = try decoded("\"master\"")
        options.name = try decoded("\"v1.0.0\"")
        options.body = try decoded("\"sample\"")
        options.draft = try decoded("false")
        options.prerelease = try decoded("false")
        options.discussionCategoryName = try decoded("null")
        options.generateReleaseNotes = try decoded("false")
        options.makeLatest = try decoded("null")
        let actual = try await client.repos.createRelease(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture936Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateRelease decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateRepoRulesetReposCreateRepoRulesetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposCreateRepoRulesetOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"super cool ruleset\""),
            enforcement: decoded("\"active\"")
        )
        options.target = try decoded("\"branch\"")
        options.bypassActors = try decoded(sdkMethodFixture937Value1)
        options.conditions = try decoded(sdkMethodFixture937Value2)
        options.rules = try decoded(sdkMethodFixture937Value3)
        let actual = try await client.repos.createRepoRuleset(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture937Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateRepoRuleset decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateUsingTemplateReposCreateUsingTemplateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createUsingTemplate(
            templateOwner: decoded("\"template_owner\""),
            templateRepo: decoded("\"template_repo\""),
            name: decoded("\"Hello-World\""),
            owner: decoded("\"octocat\""),
            description: decoded("\"This is your first repository\""),
            includeAllBranches: decoded("false"),
            private: decoded("false")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture938Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateUsingTemplate decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCreateWebhookReposCreateWebhookReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.createWebhook(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"web\""),
            config: decoded(sdkMethodFixture939Value1),
            events: decoded("[\"push\",\"pull_request\"]"),
            active: decoded("true")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture939Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCreateWebhook decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposCustomPropertiesForReposCreateOrUpdateRepositoryValuesReX6b1375d7fcReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.reposCustomPropertiesForCreateOrUpdateRepositoryValues(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            properties: decoded(sdkMethodFixture940Value1)
        )
        XCTAssertTrue(true)
    }

    func testReposCustomPropertiesForReposGetRepositoryValuesReposCustomPrXe6d0b1b365ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.reposCustomPropertiesForGetRepositoryValues(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture941Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposCustomPropertiesForReposGetRepositoryValues decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposDeclineInvitationForAuthenticatedUserReposDeclineInvitatX8731767dc1ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.declineInvitationForAuthenticatedUser(
            invitationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteReposDeleteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.delete(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteAccessRestrictionsReposDeleteAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteAdminBranchProtectionReposDeleteAdminBranchProtectionReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteAdminBranchProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteAnEnvironmentReposDeleteAnEnvironmentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteAnEnvironment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            environmentName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteAutolinkReposDeleteAutolinkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteAutolink(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            autolinkId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteBranchProtectionReposDeleteBranchProtectionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteBranchProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteCommitCommentReposDeleteCommitCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteCommitComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteCommitSignatureProtectionReposDeleteCommitSignatureProtectionReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteCommitSignatureProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteDeployKeyReposDeleteDeployKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteDeployKey(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            keyId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteDeploymentReposDeleteDeploymentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteDeployment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            deploymentId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteDeploymentBranchPolicyReposDeleteDeploymentBranchPolicyReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteDeploymentBranchPolicy(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            environmentName: decoded("\"Example Name\""),
            branchPolicyId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteFileReposDeleteFileReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposDeleteFileOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            path: decoded("\"path\""),
            message: decoded("\"my commit message\""),
            sha: decoded("\"329688480d39049927147c162b9d2deaf885005f\"")
        )
        options.branch = try decoded("null")
        options.committer = try decoded("{\"name\":\"Monalisa Octocat\",\"email\":\"octocat@github.com\"}")
        options.author = try decoded("null")
        let actual = try await client.repos.deleteFile(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture954Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposDeleteFile decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposDeleteInvitationReposDeleteInvitationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteInvitation(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            invitationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteOrgRulesetReposDeleteOrgRulesetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteOrgRuleset(
            org: decoded("\"org\""),
            rulesetId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeletePagesSiteReposDeletePagesSiteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deletePagesSite(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeletePullRequestReviewProtectionReposDeletePullRequestRX64f0575479ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deletePullRequestReviewProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteReleaseReposDeleteReleaseReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteRelease(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            releaseId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteReleaseAssetReposDeleteReleaseAssetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteReleaseAsset(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            assetId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteRepoRulesetReposDeleteRepoRulesetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteRepoRuleset(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            rulesetId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDeleteWebhookReposDeleteWebhookReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.deleteWebhook(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            hookId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDisableAutomatedSecurityFixesReposDisableAutomatedSecurityFixesReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.disableAutomatedSecurityFixes(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDisableDeploymentProtectionRuleReposDisableDeploymentProtectionRuleReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.disableDeploymentProtectionRule(
            environmentName: decoded("\"Example Name\""),
            repo: decoded("\"repo\""),
            owner: decoded("\"owner\""),
            protectionRuleId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposDisableImmutableReleasesReposDisableImmutableReleasesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.disableImmutableReleases(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDisablePrivateVulnerabilityReportingReposDisablePrivateVX8c9ea9dc85ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.disablePrivateVulnerabilityReporting(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testReposDisableVulnerabilityAlertsReposDisableVulnerabilityAlertsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.disableVulnerabilityAlerts(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }
}
