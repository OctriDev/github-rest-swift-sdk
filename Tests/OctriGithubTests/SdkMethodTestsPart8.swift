// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testActivityDeleteRepoSubscriptionActivityDeleteRepoSubscriptionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.activity.deleteRepoSubscription(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testActivityDeleteThreadSubscriptionActivityDeleteThreadSubscriptionReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.activity.deleteThreadSubscription(
            threadId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testActivityGetFeedsActivityGetFeedsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.getFeeds()
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture189Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityGetFeeds decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityGetRepoSubscriptionActivityGetRepoSubscriptionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.getRepoSubscription(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture190Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityGetRepoSubscription decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityGetStargazerCountForRepoActivityGetStargazerCountForRepoReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.getStargazerCountForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture191Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityGetStargazerCountForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityGetStargazerHistoryForRepoActivityGetStargazerHistoryForRepoReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.getStargazerHistoryForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture192Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityGetStargazerHistoryForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityGetThreadActivityGetThreadReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.getThread(
            threadId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture193Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityGetThread decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityGetThreadSubscriptionForAuthenticatedUserActivityGetTXd20f60f231ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.getThreadSubscriptionForAuthenticatedUser(
            threadId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture194Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityGetThreadSubscriptionForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListEventsForAuthenticatedUserActivityListEventsForAuX659253862bReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listEventsForAuthenticatedUser(
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture195Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListEventsForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListNotificationsForAuthenticatedUserActivityListNotiX831bc9d8ceReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listNotificationsForAuthenticatedUser(
            all: decoded("false"),
            participating: decoded("false"),
            since: decoded("\"2024-01-01T00:00:00Z\""),
            before: decoded("\"2024-01-01T00:00:00Z\""),
            page: decoded("1"),
            perPage: decoded("50")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture196Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListNotificationsForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListOrgEventsForAuthenticatedUserActivityListOrgEventXee27165d9aReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listOrgEventsForAuthenticatedUser(
            username: decoded("\"Example Name\""),
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture197Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListOrgEventsForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListPublicEventsActivityListPublicEventsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listPublicEvents(
            perPage: decoded("15"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture198Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListPublicEvents decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListPublicEventsForRepoNetworkActivityListPublicEventX2ddba86091ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listPublicEventsForRepoNetwork(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture199Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListPublicEventsForRepoNetwork decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListPublicEventsForUserActivityListPublicEventsForUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listPublicEventsForUser(
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture200Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListPublicEventsForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListPublicOrgEventsActivityListPublicOrgEventsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listPublicOrgEvents(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture201Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListPublicOrgEvents decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListReceivedEventsForUserActivityListReceivedEventsForUserReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listReceivedEventsForUser(
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture202Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListReceivedEventsForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListReceivedPublicEventsForUserActivityListReceivedPuX42bf15f5d8ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listReceivedPublicEventsForUser(
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture203Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListReceivedPublicEventsForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListRepoEventsActivityListRepoEventsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listRepoEvents(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture204Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListRepoEvents decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListRepoNotificationsForAuthenticatedUserActivityListXaa7d5ebb12ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ActivityMethods.ActivityListRepoNotificationsForAuthenticatedUserOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.all = try decoded("false")
        options.participating = try decoded("false")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.before = try decoded("\"2024-01-01T00:00:00Z\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.activity.listRepoNotificationsForAuthenticatedUser(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture205Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListRepoNotificationsForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListReposStarredByAuthenticatedUserActivityListReposSX835e356f21ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listReposStarredByAuthenticatedUser(
            sort: decoded("\"created\""),
            direction: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture206Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListReposStarredByAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListReposStarredByUserActivityListReposStarredByUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listReposStarredByUser(
            username: decoded("\"Example Name\""),
            sort: decoded("\"created\""),
            direction: decoded("\"desc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture207Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListReposStarredByUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListReposWatchedByUserActivityListReposWatchedByUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listReposWatchedByUser(
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture208Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListReposWatchedByUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListStargazersForRepoActivityListStargazersForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listStargazersForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture209Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListStargazersForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListWatchedReposForAuthenticatedUserActivityListWatchX0471b841adReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listWatchedReposForAuthenticatedUser(
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture210Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListWatchedReposForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testActivityListWatchersForRepoActivityListWatchersForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.activity.listWatchersForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture211Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "activityListWatchersForRepo decoded a response " +
                "that differs from the mock"
        )
    }
}
