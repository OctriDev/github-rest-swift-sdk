// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testIssuesListEventsIssuesListEventsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listEvents(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture625Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListEvents decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListEventsForRepoIssuesListEventsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listEventsForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture626Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListEventsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListEventsForTimelineIssuesListEventsForTimelineReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listEventsForTimeline(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1"),
            exclude: decoded("\"exclude\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture627Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListEventsForTimeline decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListForAuthenticatedUserIssuesListForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = IssuesMethods.IssuesListForAuthenticatedUserOptions()
        options.filter = try decoded("\"assigned\"")
        options.state = try decoded("\"open\"")
        options.labels = try decoded("\"labels\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.issues.listForAuthenticatedUser(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture628Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListForOrgIssuesListForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try IssuesMethods.IssuesListForOrgOptions(org: decoded("\"org\""))
        options.filter = try decoded("\"assigned\"")
        options.state = try decoded("\"open\"")
        options.labels = try decoded("\"labels\"")
        options.type = try decoded("\"type\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.issues.listForOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture629Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListForRepoIssuesListForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try IssuesMethods.IssuesListForRepoOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""))
        options.milestone = try decoded("\"milestone\"")
        options.state = try decoded("\"open\"")
        options.assignee = try decoded("\"assignee\"")
        options.type = try decoded("\"type\"")
        options.creator = try decoded("\"creator\"")
        options.mentioned = try decoded("\"mentioned\"")
        options.issueFieldValues = try decoded("\"issue_field_values\"")
        options.labels = try decoded("\"labels\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.issues.listForRepo(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture630Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListIssueFieldValuesForIssueIssuesListIssueFieldValuesForIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listIssueFieldValuesForIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture631Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListIssueFieldValuesForIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListLabelsForMilestoneIssuesListLabelsForMilestoneReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listLabelsForMilestone(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            milestoneNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture632Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListLabelsForMilestone decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListLabelsForRepoIssuesListLabelsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listLabelsForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture633Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListLabelsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListLabelsOnIssueIssuesListLabelsOnIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listLabelsOnIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture634Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListLabelsOnIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListMilestonesIssuesListMilestonesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listMilestones(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            state: decoded("\"open\""),
            sort: decoded("\"due_on\""),
            direction: decoded("\"asc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture635Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListMilestones decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListSubIssuesIssuesListSubIssuesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.issuesListSub(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture636Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListSubIssues decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListSuggestionsIssuesListSuggestionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listSuggestions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            state: decoded("\"pending\""),
            action: decoded("\"set_type\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture637Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListSuggestions decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesLockIssuesLockReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.lock(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            lockReason: decoded("\"off-topic\"")
        )
        XCTAssertTrue(true)
    }

    func testIssuesPinCommentIssuesPinCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.pinComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture639Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesPinComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesRemoveAllLabelsIssuesRemoveAllLabelsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.removeAllLabels(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testIssuesRemoveAssigneesIssuesRemoveAssigneesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.removeAssignees(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            assignees: decoded("[\"hubot\",\"other_user\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture641Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesRemoveAssignees decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesRemoveDependencyBlockedByIssuesRemoveDependencyBlockedByReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.removeDependencyBlockedBy(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            issueId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture642Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesRemoveDependencyBlockedBy decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesRemoveLabelIssuesRemoveLabelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.removeLabel(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture643Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesRemoveLabel decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesRemoveSubIssueIssuesRemoveSubIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.removeSubIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            subIssueId: decoded("6")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture644Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesRemoveSubIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesReprioritizeSubIssueIssuesReprioritizeSubIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.reprioritizeSubIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            subIssueId: decoded("6"),
            afterId: decoded("5"),
            beforeId: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture645Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesReprioritizeSubIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesSetIssueFieldValuesIssuesSetIssueFieldValuesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.setIssueFieldValues(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            issueFieldValues: decoded(sdkMethodFixture646Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture646Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesSetIssueFieldValues decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesSetLabelsIssuesSetLabelsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.setLabels(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            body: decoded("{\"labels\":[\"bug\",\"enhancement\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture647Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesSetLabels decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesUnlockIssuesUnlockReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.unlock(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testIssuesUnpinCommentIssuesUnpinCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.unpinComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        XCTAssertTrue(true)
    }
}
