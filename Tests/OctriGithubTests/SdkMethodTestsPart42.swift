// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testReposMergeUpstreamReposMergeUpstreamReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.mergeUpstream(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"main\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1070Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposMergeUpstream decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposPingWebhookReposPingWebhookReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.pingWebhook(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            hookId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposRedeliverWebhookDeliveryReposRedeliverWebhookDeliveryReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.redeliverWebhookDelivery(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            hookId: decoded("1"),
            deliveryId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1072Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRedeliverWebhookDelivery decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposRemoveAppAccessRestrictionsReposRemoveAppAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.removeAppAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            apps: decoded("[\"my-app\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1073Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRemoveAppAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposRemoveCollaboratorReposRemoveCollaboratorReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.removeCollaborator(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testReposRemoveStatusCheckContextsReposRemoveStatusCheckContextsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.removeStatusCheckContexts(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            body: decoded("{\"contexts\":[\"continuous-integration/jenkins\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1075Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRemoveStatusCheckContexts decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposRemoveStatusCheckProtectionReposRemoveStatusCheckProtectionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.removeStatusCheckProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        XCTAssertTrue(true)
    }

    func testReposRemoveTeamAccessRestrictionsReposRemoveTeamAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.removeTeamAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            body: decoded("{\"teams\":[\"octocats\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1077Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRemoveTeamAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposRemoveUserAccessRestrictionsReposRemoveUserAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.removeUserAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            users: decoded("[\"octocat\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1078Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRemoveUserAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposRenameBranchReposRenameBranchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.renameBranch(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            newName: decoded("\"my_renamed_branch\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1079Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRenameBranch decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposReplaceAllTopicsReposReplaceAllTopicsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.replaceAllTopics(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            names: decoded("[\"octocat\",\"atom\",\"electron\",\"api\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1080Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposReplaceAllTopics decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposRequestPagesBuildReposRequestPagesBuildReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.requestPagesBuild(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1081Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposRequestPagesBuild decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposSetAdminBranchProtectionReposSetAdminBranchProtectionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.setAdminBranchProtection(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1082Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposSetAdminBranchProtection decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposSetAppAccessRestrictionsReposSetAppAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.setAppAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            apps: decoded("[\"octoapp\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1083Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposSetAppAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposSetStatusCheckContextsReposSetStatusCheckContextsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.setStatusCheckContexts(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            body: decoded("{\"contexts\":[\"continuous-integration/travis-ci\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1084Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposSetStatusCheckContexts decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposSetTeamAccessRestrictionsReposSetTeamAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.setTeamAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            body: decoded("{\"teams\":[\"justice-league\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1085Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposSetTeamAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposSetUserAccessRestrictionsReposSetUserAccessRestrictionsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.setUserAccessRestrictions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            branch: decoded("\"branch\""),
            users: decoded("[\"octocat\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1086Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposSetUserAccessRestrictions decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposTestPushWebhookReposTestPushWebhookReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.testPushWebhook(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            hookId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testReposTransferReposTransferReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.transfer(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            newOwner: decoded("\"github\""),
            newName: decoded("\"octorepo\""),
            teamIds: decoded("[12,345]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1088Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposTransfer decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateReposUpdateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdateOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""))
        options.name = try decoded("\"Hello-World\"")
        options.description = try decoded("\"This is your first repository\"")
        options.homepage = try decoded("\"https://github.com\"")
        options.visibility = try decoded("null")
        options.securityAndAnalysis = try decoded("null")
        options.hasIssues = try decoded("true")
        options.hasProjects = try decoded("true")
        options.hasWiki = try decoded("true")
        options.hasPullRequests = try decoded("null")
        options.pullRequestCreationPolicy = try decoded("null")
        options.isTemplate = try decoded("null")
        options.defaultBranch = try decoded("null")
        options.allowSquashMerge = try decoded("null")
        options.allowMergeCommit = try decoded("null")
        options.allowRebaseMerge = try decoded("null")
        options.allowAutoMerge = try decoded("null")
        options.deleteBranchOnMerge = try decoded("null")
        options.allowUpdateBranch = try decoded("null")
        options.useSquashPrTitleAsDefault = try decoded("null")
        options.squashMergeCommitTitle = try decoded("null")
        options.squashMergeCommitMessage = try decoded("null")
        options.mergeCommitTitle = try decoded("null")
        options.mergeCommitMessage = try decoded("null")
        options.archived = try decoded("null")
        options.allowForking = try decoded("null")
        options.webCommitSignoffRequired = try decoded("null")
        let actual = try await client.repos.update(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1089Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdate decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateBranchProtectionReposUpdateBranchProtectionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposUpdateBranchProtectionOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), branch: decoded("\"branch\""), requiredStatusChecks: decoded("{\"strict\":true,\"contexts\":[\"continuous-integration/travis-ci\"]}"), enforceAdmins: decoded("true"), requiredPullRequestReviews: decoded(sdkMethodFixture1090Value1), restrictions: decoded(sdkMethodFixture1090Value2))
        options.requiredLinearHistory = try decoded("true")
        options.allowForcePushes = try decoded("true")
        options.allowDeletions = try decoded("true")
        options.blockCreations = try decoded("true")
        options.requiredConversationResolution = try decoded("true")
        options.lockBranch = try decoded("true")
        options.allowForkSyncing = try decoded("true")
        let actual = try await client.repos.updateBranchProtection(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1090Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateBranchProtection decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateCommitCommentReposUpdateCommitCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.updateCommitComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1"),
            body: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1091Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateCommitComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateDeploymentBranchPolicyReposUpdateDeploymentBranchPolicyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.updateDeploymentBranchPolicy(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            environmentName: decoded("\"Example Name\""),
            branchPolicyId: decoded("1"),
            name: decoded("\"release/*\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1092Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateDeploymentBranchPolicy decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposUpdateInformationAboutPagesSiteReposUpdateInformationAboutPagesSiteReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.repos.updateInformationAboutPagesSite(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            cname: decoded("\"octocatblog.com\""),
            httpsEnforced: decoded("null"),
            buildType: decoded("null"),
            source: decoded("\"gh-pages\"")
        )
        XCTAssertTrue(true)
    }

    func testReposUpdateInvitationReposUpdateInvitationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.updateInvitation(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            invitationId: decoded("1"),
            permissions: decoded("\"write\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1094Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposUpdateInvitation decoded a response " +
                "that differs from the mock"
        )
    }
}
