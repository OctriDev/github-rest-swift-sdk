// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testCodespacesCreateOrUpdateRepoSecretCodespacesCreateOrUpdateRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.createOrUpdateRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\""),
            encryptedValue: decoded("\"c2VjcmV0\""),
            keyId: decoded("\"012345678912345678\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture388Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCreateOrUpdateRepoSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesCreateOrUpdateSecretForAuthenticatedUserCodespacesCXdee6fed529ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.createOrUpdateSecretForAuthenticatedUser(
            secretName: decoded("\"Example Name\""),
            keyId: decoded("\"012345678912345678\""),
            encryptedValue: decoded("\"c2VjcmV0\""),
            selectedRepositoryIds: decoded("[\"1234567\",\"2345678\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture389Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCreateOrUpdateSecretForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesCreateWithPrForAuthenticatedUserCodespacesCreateWitXde3b9b7b7aReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodespacesMethods.CodespacesCreateWithPrForAuthenticatedUserOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), pullNumber: decoded("1"))
        options.location = try decoded("null")
        options.geo = try decoded("null")
        options.clientIp = try decoded("null")
        options.machine = try decoded("null")
        options.devcontainerPath = try decoded("null")
        options.multiRepoPermissionsOptOut = try decoded("null")
        options.workingDirectory = try decoded("null")
        options.idleTimeoutMinutes = try decoded("null")
        options.displayName = try decoded("null")
        options.retentionPeriodMinutes = try decoded("null")
        let actual = try await client.codespaces.createWithPrForAuthenticatedUser(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture390Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCreateWithPrForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesCreateWithRepoForAuthenticatedUserCodespacesCreateWXdde387fc12ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodespacesMethods.CodespacesCreateWithRepoForAuthenticatedUserOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""))
        options.ref = try decoded("\"main\"")
        options.location = try decoded("null")
        options.geo = try decoded("null")
        options.clientIp = try decoded("null")
        options.machine = try decoded("\"standardLinux32gb\"")
        options.devcontainerPath = try decoded("null")
        options.multiRepoPermissionsOptOut = try decoded("null")
        options.workingDirectory = try decoded("null")
        options.idleTimeoutMinutes = try decoded("null")
        options.displayName = try decoded("null")
        options.retentionPeriodMinutes = try decoded("null")
        let actual = try await client.codespaces.createWithRepoForAuthenticatedUser(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture391Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCreateWithRepoForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesDeleteCodespacesAccessUsersCodespacesDeleteCodespacX1a5cb1b670ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codespaces.codespacesDeleteAccessUsers(
            org: decoded("\"org\""),
            selectedUsernames: decoded("[\"johnDoe\",\"atomIO\"]")
        )
        XCTAssertTrue(true)
    }

    func testCodespacesDeleteForAuthenticatedUserCodespacesDeleteForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.deleteForAuthenticatedUser(
            codespaceName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture393Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesDeleteForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesDeleteFromOrganizationCodespacesDeleteFromOrganizationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.deleteFromOrganization(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\""),
            codespaceName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture394Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesDeleteFromOrganization decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesDeleteOrgSecretCodespacesDeleteOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codespaces.deleteOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testCodespacesDeleteRepoSecretCodespacesDeleteRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codespaces.deleteRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testCodespacesDeleteSecretForAuthenticatedUserCodespacesDeleteSecX7ccb97ecceReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codespaces.deleteSecretForAuthenticatedUser(
            secretName: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testCodespacesExportForAuthenticatedUserCodespacesExportForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.exportForAuthenticatedUser(
            codespaceName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture398Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesExportForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetCodespacesForUserInOrgCodespacesGetCodespacesForUserInOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.codespacesGetForUserInOrg(
            org: decoded("\"org\""),
            username: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture399Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetCodespacesForUserInOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetExportDetailsForAuthenticatedUserCodespacesGetExX17fc18b83eReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getExportDetailsForAuthenticatedUser(
            codespaceName: decoded("\"Example Name\""),
            exportId: decoded("\"export_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture400Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetExportDetailsForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetForAuthenticatedUserCodespacesGetForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getForAuthenticatedUser(
            codespaceName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture401Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetOrgPublicKeyCodespacesGetOrgPublicKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getOrgPublicKey(
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture402Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetOrgPublicKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetOrgSecretCodespacesGetOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture403Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetOrgSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetPublicKeyForAuthenticatedUserCodespacesGetPublicX2db05f9b5fReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getPublicKeyForAuthenticatedUser()
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture404Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetPublicKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetRepoPublicKeyCodespacesGetRepoPublicKeyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getRepoPublicKey(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture405Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetRepoPublicKey decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetRepoSecretCodespacesGetRepoSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getRepoSecret(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture406Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetRepoSecret decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesGetSecretForAuthenticatedUserCodespacesGetSecretForX58ae54fb54ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.getSecretForAuthenticatedUser(
            secretName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture407Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesGetSecretForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesListDevcontainersInRepositoryForAuthenticatedUserCoX9c57a6b345ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.listDevcontainersInRepositoryForAuthenticatedUser(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture408Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesListDevcontainersInRepositoryForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesListForAuthenticatedUserCodespacesListForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.listForAuthenticatedUser(
            perPage: decoded("30"),
            page: decoded("1"),
            repositoryId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture409Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesListForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesListInOrganizationCodespacesListInOrganizationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.listInOrganization(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture410Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesListInOrganization decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesListInRepositoryForAuthenticatedUserCodespacesListIXb34f8bd84eReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.listInRepositoryForAuthenticatedUser(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture411Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesListInRepositoryForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesListOrgSecretsCodespacesListOrgSecretsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.listOrgSecrets(
            org: decoded("\"org\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture412Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesListOrgSecrets decoded a response " +
                "that differs from the mock"
        )
    }
}
