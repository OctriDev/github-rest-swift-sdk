// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testTeamsRemoveMembershipForUserInOrgTeamsRemoveMembershipForUserInOrgReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.teams.removeMembershipForUserInOrg(
            org: decoded("\"org\""),
            teamSlug: decoded("\"team_slug\""),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testTeamsRemoveMembershipForUserLegacyTeamsRemoveMembershipForUserLegacyReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.teams.removeMembershipForUserLegacy(
            teamId: decoded("1"),
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testTeamsRemoveRepoInOrgTeamsRemoveRepoInOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.teams.removeRepoInOrg(
            org: decoded("\"org\""),
            teamSlug: decoded("\"team_slug\""),
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testTeamsRemoveRepoLegacyTeamsRemoveRepoLegacyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.teams.removeRepoLegacy(
            teamId: decoded("1"),
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        XCTAssertTrue(true)
    }

    func testTeamsUpdateInOrgTeamsUpdateInOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try TeamsMethods.TeamsUpdateInOrgOptions(
            org: decoded("\"org\""),
            teamSlug: decoded("\"team_slug\"")
        )
        options.name = try decoded("\"new team name\"")
        options.description = try decoded("\"new team description\"")
        options.privacy = try decoded("\"closed\"")
        options.notificationSetting = try decoded("\"notifications_enabled\"")
        options.permission = try decoded("null")
        options.parentTeamId = try decoded("null")
        options.parentTeamSlug = try decoded("null")
        let actual = try await client.teams.updateInOrg(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1168Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "teamsUpdateInOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testTeamsUpdateLegacyTeamsUpdateLegacyReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try TeamsMethods.TeamsUpdateLegacyOptions(
            teamId: decoded("1"),
            name: decoded("\"new team name\"")
        )
        options.description = try decoded("\"new team description\"")
        options.privacy = try decoded("\"closed\"")
        options.notificationSetting = try decoded("\"notifications_enabled\"")
        options.permission = try decoded("null")
        options.parentTeamId = try decoded("null")
        options.parentTeamSlug = try decoded("null")
        let actual = try await client.teams.updateLegacy(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1169Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "teamsUpdateLegacy decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersAddEmailForAuthenticatedUserUsersAddEmailForAuthenticatedUserReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.addEmailForAuthenticatedUser(
            body: decoded(sdkMethodFixture1170Value1)
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1170Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersAddEmailForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersAddSocialAccountForAuthenticatedUserUsersAddSocialAccounX437f912f1aReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.addSocialAccountForAuthenticatedUser(
            accountUrls: decoded("[\"https://facebook.com/GitHub\",\"https://www.youtube.com/@GitHub\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1171Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersAddSocialAccountForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersBlockUsersBlockReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.block(
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testUsersCheckBlockedUsersCheckBlockedReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.checkBlocked(
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testUsersCheckFollowingForUserUsersCheckFollowingForUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.checkFollowingForUser(
            username: decoded("\"Example Name\""),
            targetUser: decoded("\"target_user\"")
        )
        XCTAssertTrue(true)
    }

    func testUsersCheckPersonIsFollowedByAuthenticatedUsersCheckPersonIsFoX9284b01eedReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.checkPersonIsFollowedByAuthenticated(
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testUsersCreateGpgKeyForAuthenticatedUserUsersCreateGpgKeyForAuthX5af8ba0297ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.createGpgKeyForAuthenticatedUser(
            armoredPublicKey: decoded(sdkMethodFixture1176Value1),
            name: decoded("\"Octocat's GPG Key\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1176Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersCreateGpgKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersCreatePublicSshKeyForAuthenticatedUserUsersCreatePublicSX4d053a12f0ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.createPublicSshKeyForAuthenticatedUser(
            key: decoded("\"ssh-rsa \""),
            title: decoded("\"ssh-rsa AAAAB3NzaC1yc2EAAA\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1177Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersCreatePublicSshKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersCreateSshSigningKeyForAuthenticatedUserUsersCreateSshSigXb2be43d6daReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.createSshSigningKeyForAuthenticatedUser(
            key: decoded("\"ssh-rsa \""),
            title: decoded("\"ssh-rsa AAAAB3NzaC1yc2EAAA\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1178Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersCreateSshSigningKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersDeleteAttestationsBulkUsersDeleteAttestationsBulkReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteAttestationsBulk(
            username: decoded("\"Example Name\""),
            body: decoded("{\"subject_digests\":[\"sha256:abc123\",\"sha512:def456\"]}")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeleteAttestationsByIdUsersDeleteAttestationsByIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteAttestationsById(
            username: decoded("\"Example Name\""),
            attestationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeleteAttestationsBySubjectDigestUsersDeleteAttestationsXfaed73af27ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteAttestationsBySubjectDigest(
            username: decoded("\"Example Name\""),
            subjectDigest: decoded("\"subject_digest\"")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeleteEmailForAuthenticatedUserUsersDeleteEmailForAuthenticatedUserReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteEmailForAuthenticatedUser(
            body: decoded("{\"emails\":[\"octocat@github.com\",\"mona@github.com\"]}")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeleteGpgKeyForAuthenticatedUserUsersDeleteGpgKeyForAuthX0c01bda996ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteGpgKeyForAuthenticatedUser(
            gpgKeyId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeletePublicSshKeyForAuthenticatedUserUsersDeletePublicSXd7c1a1e641ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deletePublicSshKeyForAuthenticatedUser(
            keyId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeleteSocialAccountForAuthenticatedUserUsersDeleteSocialX5f8a7b59ffReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteSocialAccountForAuthenticatedUser(
            accountUrls: decoded("[\"https://facebook.com/GitHub\",\"https://www.youtube.com/@GitHub\"]")
        )
        XCTAssertTrue(true)
    }

    func testUsersDeleteSshSigningKeyForAuthenticatedUserUsersDeleteSshSigX8e7a15da8fReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.deleteSshSigningKeyForAuthenticatedUser(
            sshSigningKeyId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testUsersFollowUsersFollowReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.users.follow(
            username: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testUsersGetAuthenticatedUsersGetAuthenticatedReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getAuthenticated()
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1188Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetAuthenticated decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersGetByIdUsersGetByIdReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getById(
            accountId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1189Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetById decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersGetByUsernameUsersGetByUsernameReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getByUsername(
            username: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1190Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetByUsername decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersGetContextForUserUsersGetContextForUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getContextForUser(
            username: decoded("\"Example Name\""),
            subjectType: decoded("\"organization\""),
            subjectId: decoded("\"subject_-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1191Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetContextForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersGetGpgKeyForAuthenticatedUserUsersGetGpgKeyForAuthenticatedUserReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getGpgKeyForAuthenticatedUser(
            gpgKeyId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1192Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetGpgKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersGetPublicSshKeyForAuthenticatedUserUsersGetPublicSshKeyFX792550ec66ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getPublicSshKeyForAuthenticatedUser(
            keyId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1193Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetPublicSshKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testUsersGetSshSigningKeyForAuthenticatedUserUsersGetSshSigningKeXfc995d9fa6ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.users.getSshSigningKeyForAuthenticatedUser(
            sshSigningKeyId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1194Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "usersGetSshSigningKeyForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }
}
