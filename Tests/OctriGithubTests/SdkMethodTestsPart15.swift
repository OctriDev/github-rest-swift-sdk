// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testCodeSecurityDeleteConfigurationCodeSecurityDeleteConfigurationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codeSecurity.deleteConfiguration(
            org: decoded("\"org\""),
            configurationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testCodeSecurityDeleteConfigurationForEnterpriseCodeSecurityDeletX12474c0cf8ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codeSecurity.deleteConfigurationForEnterprise(
            enterprise: decoded("\"enterprise\""),
            configurationId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testCodeSecurityDetachConfigurationCodeSecurityDetachConfigurationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codeSecurity.detachConfiguration(
            org: decoded("\"org\""),
            selectedRepositoryIds: decoded("[32,91]")
        )
        XCTAssertTrue(true)
    }

    func testCodeSecurityGetConfigurationCodeSecurityGetConfigurationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getConfiguration(
            org: decoded("\"org\""),
            configurationId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture367Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetConfigurationForRepositoryCodeSecurityGetConfiX125310812dReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getConfigurationForRepository(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture368Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetConfigurationForRepository decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetConfigurationsForEnterpriseCodeSecurityGetConfX0bb362556cReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getConfigurationsForEnterprise(
            enterprise: decoded("\"enterprise\""),
            perPage: decoded("30"),
            before: decoded("\"before\""),
            after: decoded("\"after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture369Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetConfigurationsForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetConfigurationsForOrgCodeSecurityGetConfigurationsForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getConfigurationsForOrg(
            org: decoded("\"org\""),
            targetType: decoded("\"all\""),
            perPage: decoded("30"),
            before: decoded("\"before\""),
            after: decoded("\"after\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture370Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetConfigurationsForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetDefaultConfigurationsCodeSecurityGetDefaultConfigurationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getDefaultConfigurations(
            org: decoded("\"org\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture371Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetDefaultConfigurations decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetDefaultConfigurationsForEnterpriseCodeSecurityXced9c38b7bReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getDefaultConfigurationsForEnterprise(
            enterprise: decoded("\"enterprise\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture372Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetDefaultConfigurationsForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetRepositoriesForConfigurationCodeSecurityGetRepXdda4e18796ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getRepositoriesForConfiguration(
            org: decoded("\"org\""),
            configurationId: decoded("1"),
            perPage: decoded("30"),
            before: decoded("\"before\""),
            after: decoded("\"after\""),
            status: decoded("\"all\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture373Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetRepositoriesForConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetRepositoriesForEnterpriseConfigurationCodeSecuX89b41c7795ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getRepositoriesForEnterpriseConfiguration(
            enterprise: decoded("\"enterprise\""),
            configurationId: decoded("1"),
            perPage: decoded("30"),
            before: decoded("\"before\""),
            after: decoded("\"after\""),
            status: decoded("\"all\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture374Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetRepositoriesForEnterpriseConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityGetSingleConfigurationForEnterpriseCodeSecurityGeX2b15181a7aReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.getSingleConfigurationForEnterprise(
            enterprise: decoded("\"enterprise\""),
            configurationId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture375Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityGetSingleConfigurationForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecuritySetConfigurationAsDefaultCodeSecuritySetConfiguraX8a168f879fReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.setConfigurationAsDefault(
            org: decoded("\"org\""),
            configurationId: decoded("1"),
            defaultForNewRepos: decoded("\"all\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture376Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecuritySetConfigurationAsDefault decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecuritySetConfigurationAsDefaultForEnterpriseCodeSecuritX08af3becbfReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codeSecurity.setConfigurationAsDefaultForEnterprise(
            enterprise: decoded("\"enterprise\""),
            configurationId: decoded("1"),
            defaultForNewRepos: decoded("\"all\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture377Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecuritySetConfigurationAsDefaultForEnterprise decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityUpdateConfigurationCodeSecurityUpdateConfigurationReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeSecurityMethods.CodeSecurityUpdateConfigurationOptions(org: decoded("\"org\""), configurationId: decoded("1"))
        options.name = try decoded("\"octo-org recommended settings v2\"")
        options.description = try decoded("null")
        options.advancedSecurity = try decoded("null")
        options.codeSecurity = try decoded("null")
        options.dependencyGraph = try decoded("null")
        options.dependencyGraphAutosubmitAction = try decoded("null")
        options.dependencyGraphAutosubmitActionOptions = try decoded("null")
        options.dependabotAlerts = try decoded("null")
        options.dependabotSecurityUpdates = try decoded("null")
        options.dependabotDelegatedAlertDismissal = try decoded("null")
        options.codeScanningDefaultSetup = try decoded("\"enabled\"")
        options.codeScanningDefaultSetupOptions = try decoded("null")
        options.codeScanningOptions = try decoded("null")
        options.codeScanningDelegatedAlertDismissal = try decoded("null")
        options.secretProtection = try decoded("null")
        options.secretScanning = try decoded("\"disabled\"")
        options.secretScanningPushProtection = try decoded("null")
        options.secretScanningDelegatedBypass = try decoded("null")
        options.secretScanningDelegatedBypassOptions = try decoded("null")
        options.secretScanningValidityChecks = try decoded("null")
        options.secretScanningNonProviderPatterns = try decoded("null")
        options.secretScanningGenericSecrets = try decoded("null")
        options.secretScanningDelegatedAlertDismissal = try decoded("null")
        options.secretScanningExtendedMetadata = try decoded("null")
        options.privateVulnerabilityReporting = try decoded("null")
        options.enforcement = try decoded("null")
        let actual = try await client.codeSecurity.updateConfiguration(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture378Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityUpdateConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodeSecurityUpdateEnterpriseConfigurationCodeSecurityUpdateEnX67036ffaf7ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try CodeSecurityMethods.CodeSecurityUpdateEnterpriseConfigurationOptions(enterprise: decoded("\"enterprise\""), configurationId: decoded("1"))
        options.name = try decoded("\"octo-enterprise recommended settings v2\"")
        options.description = try decoded("null")
        options.advancedSecurity = try decoded("null")
        options.codeSecurity = try decoded("null")
        options.dependencyGraph = try decoded("null")
        options.dependencyGraphAutosubmitAction = try decoded("null")
        options.dependencyGraphAutosubmitActionOptions = try decoded("null")
        options.dependabotAlerts = try decoded("null")
        options.dependabotSecurityUpdates = try decoded("null")
        options.codeScanningDefaultSetup = try decoded("\"enabled\"")
        options.codeScanningDefaultSetupOptions = try decoded("null")
        options.codeScanningOptions = try decoded("null")
        options.codeScanningDelegatedAlertDismissal = try decoded("null")
        options.secretProtection = try decoded("null")
        options.secretScanning = try decoded("\"disabled\"")
        options.secretScanningPushProtection = try decoded("null")
        options.secretScanningValidityChecks = try decoded("null")
        options.secretScanningNonProviderPatterns = try decoded("null")
        options.secretScanningGenericSecrets = try decoded("null")
        options.secretScanningDelegatedAlertDismissal = try decoded("null")
        options.secretScanningExtendedMetadata = try decoded("null")
        options.privateVulnerabilityReporting = try decoded("null")
        options.enforcement = try decoded("null")
        let actual = try await client.codeSecurity.updateEnterpriseConfiguration(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture379Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codeSecurityUpdateEnterpriseConfiguration decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodesOfConductGetAllCodesOfConductCodesOfConductGetAllCodesOfConductReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codesOfConduct.codesOfConductGetAll()
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture380Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codesOfConductGetAllCodesOfConduct decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodesOfConductGetConductCodeCodesOfConductGetConductCodeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codesOfConduct.getConductCode(
            key: decoded("\"key\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture381Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codesOfConductGetConductCode decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesAddRepositoryForSecretForAuthenticatedUserCodespaceX8a91ff2044ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codespaces.addRepositoryForSecretForAuthenticatedUser(
            secretName: decoded("\"Example Name\""),
            repositoryId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testCodespacesAddSelectedRepoToOrgSecretCodespacesAddSelectedRepoToOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.codespaces.addSelectedRepoToOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            repositoryId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testCodespacesCheckPermissionsForDevcontainerCodespacesCheckPermiX1e052cc88fReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.checkPermissionsForDevcontainer(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            ref: decoded("\"master\""),
            devcontainerPath: decoded("\".devcontainer/example/devcontainer.json\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture384Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCheckPermissionsForDevcontainer decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesCodespaceMachinesForAuthenticatedUserCodespacesCodeX868ff43758ReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.codespaceMachinesForAuthenticatedUser(
            codespaceName: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture385Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCodespaceMachinesForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesCreateForAuthenticatedUserCodespacesCreateForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.createForAuthenticatedUser(
            body: decoded("{\"repository_id\":1,\"ref\":\"main\",\"geo\":\"UsWest\"}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture386Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCreateForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testCodespacesCreateOrUpdateOrgSecretCodespacesCreateOrUpdateOrgSecretReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.codespaces.createOrUpdateOrgSecret(
            org: decoded("\"org\""),
            secretName: decoded("\"Example Name\""),
            visibility: decoded("\"selected\""),
            encryptedValue: decoded("\"c2VjcmV0\""),
            keyId: decoded("\"012345678912345678\""),
            selectedRepositoryIds: decoded("[1296269,1296280]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture387Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "codespacesCreateOrUpdateOrgSecret decoded a response " +
                "that differs from the mock"
        )
    }
}
