// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testIssuesAddLabelsIssuesAddLabelsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.addLabels(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            body: decoded("{\"labels\":[\"bug\",\"enhancement\"]}")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture599Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesAddLabels decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesAddSubIssueIssuesAddSubIssueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.addSubIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            subIssueId: decoded("1"),
            replaceParent: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture600Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesAddSubIssue decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesApproveSuggestionIssuesApproveSuggestionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.approveSuggestion(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            suggestionId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture601Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesApproveSuggestion decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesCheckUserCanBeAssignedIssuesCheckUserCanBeAssignedReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.checkUserCanBeAssigned(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            assignee: decoded("\"assignee\"")
        )
        XCTAssertTrue(true)
    }

    func testIssuesCheckUserCanBeAssignedToIssueIssuesCheckUserCanBeAssignedToIssueReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.checkUserCanBeAssignedToIssue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            assignee: decoded("\"assignee\"")
        )
        XCTAssertTrue(true)
    }

    func testIssuesCreateIssuesCreateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try IssuesMethods.IssuesCreateOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            title: decoded("\"Found a bug\"")
        )
        options.body = try decoded("\"sample\"")
        options.assignee = try decoded("null")
        options.milestone = try decoded("\"1\"")
        options.labels = try decoded("[\"bug\"]")
        options.assignees = try decoded("[\"octocat\"]")
        options.issueFieldValues = try decoded("null")
        options.type = try decoded("null")
        options.parentIssueId = try decoded("null")
        let actual = try await client.issues.create(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture604Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesCreate decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesCreateCommentIssuesCreateCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.createComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            body: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture605Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesCreateComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesCreateLabelIssuesCreateLabelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.createLabel(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"bug\""),
            color: decoded("\"f29513\""),
            description: decoded("\"Something isn't working\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture606Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesCreateLabel decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesCreateMilestoneIssuesCreateMilestoneReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.createMilestone(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            title: decoded("\"v1.0\""),
            state: decoded("\"open\""),
            description: decoded("\"Tracking milestone for version 1.0\""),
            dueOn: decoded("\"2012-10-09T23:39:01Z\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture607Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesCreateMilestone decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesDeleteCommentIssuesDeleteCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.deleteComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testIssuesDeleteIssueFieldValueIssuesDeleteIssueFieldValueReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.deleteIssueFieldValue(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            issueFieldId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testIssuesDeleteLabelIssuesDeleteLabelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.deleteLabel(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"Example Name\"")
        )
        XCTAssertTrue(true)
    }

    func testIssuesDeleteMilestoneIssuesDeleteMilestoneReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.issues.deleteMilestone(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            milestoneNumber: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testIssuesDismissSuggestionIssuesDismissSuggestionReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.dismissSuggestion(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            suggestionId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture612Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesDismissSuggestion decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesGetIssuesGetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.get(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture613Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesGet decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesGetCommentIssuesGetCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.getComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture614Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesGetComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesGetEventIssuesGetEventReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.getEvent(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            eventId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture615Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesGetEvent decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesGetLabelIssuesGetLabelReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.getLabel(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            name: decoded("\"Example Name\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture616Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesGetLabel decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesGetMilestoneIssuesGetMilestoneReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.getMilestone(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            milestoneNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture617Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesGetMilestone decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesGetParentIssuesGetParentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.getParent(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture618Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesGetParent decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListIssuesListReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = IssuesMethods.IssuesListOptions()
        options.filter = try decoded("\"assigned\"")
        options.state = try decoded("\"open\"")
        options.labels = try decoded("\"labels\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"desc\"")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.collab = try decoded("false")
        options.orgs = try decoded("false")
        options.owned = try decoded("false")
        options.pulls = try decoded("false")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.issues.list(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture619Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesList decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListAssigneesIssuesListAssigneesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listAssignees(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture620Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListAssignees decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListCommentsIssuesListCommentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listComments(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            since: decoded("\"2024-01-01T00:00:00Z\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture621Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListComments decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListCommentsForRepoIssuesListCommentsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listCommentsForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            sort: decoded("\"created\""),
            direction: decoded("\"asc\""),
            since: decoded("\"2024-01-01T00:00:00Z\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture622Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListCommentsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListDependenciesBlockedByIssuesListDependenciesBlockedByReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listDependenciesBlockedBy(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture623Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListDependenciesBlockedBy decoded a response " +
                "that differs from the mock"
        )
    }

    func testIssuesListDependenciesBlockingIssuesListDependenciesBlockingReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.issues.listDependenciesBlocking(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            issueNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture624Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "issuesListDependenciesBlocking decoded a response " +
                "that differs from the mock"
        )
    }
}
