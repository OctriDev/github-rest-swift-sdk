// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testPullsCreateReviewCommentPullsCreateReviewCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try PullsMethods.PullsCreateReviewCommentOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), pullNumber: decoded("1"), body: decoded("\"sample\""), commitId: decoded("\"6dcb09b5b57875f334f61aebed695e2e4193db5e\""), path: decoded("\"file1.txt\""))
        options.position = try decoded("null")
        options.side = try decoded("\"RIGHT\"")
        options.line = try decoded("2")
        options.startLine = try decoded("1")
        options.startSide = try decoded("\"RIGHT\"")
        options.inReplyTo = try decoded("null")
        options.subjectType = try decoded("null")
        let actual = try await client.pulls.createReviewComment(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture862Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsCreateReviewComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsDeletePendingReviewPullsDeletePendingReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.deletePendingReview(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture863Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsDeletePendingReview decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsDeleteReviewCommentPullsDeleteReviewCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        _ = try await client.pulls.deleteReviewComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        XCTAssertTrue(true)
    }

    func testPullsDismissReviewPullsDismissReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.dismissReview(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewId: decoded("1"),
            message: decoded("\"You are dismissed\""),
            event: decoded("\"DISMISS\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture865Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsDismissReview decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsGetPullsGetReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.get(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture866Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsGet decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsGetMergeAsyncResultPullsGetMergeAsyncResultReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.getMergeAsyncResult(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            uuid: decoded("\"uu-1\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture867Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsGetMergeAsyncResult decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsGetReviewPullsGetReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.getReview(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture868Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsGetReview decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsGetReviewCommentPullsGetReviewCommentReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.getReviewComment(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commentId: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture869Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsGetReviewComment decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListPullsListReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try PullsMethods.PullsListOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""))
        options.state = try decoded("\"open\"")
        options.head = try decoded("\"head\"")
        options.base = try decoded("\"base\"")
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"asc\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.pulls.list(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture870Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsList decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListCommentsForReviewPullsListCommentsForReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.listCommentsForReview(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewId: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture871Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListCommentsForReview decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListCommitsPullsListCommitsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.listCommits(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture872Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListCommits decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListFilesPullsListFilesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.listFiles(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture873Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListFiles decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListRequestedReviewersPullsListRequestedReviewersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.listRequestedReviewers(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture874Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListRequestedReviewers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListReviewCommentsPullsListReviewCommentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try PullsMethods.PullsListReviewCommentsOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), pullNumber: decoded("1"))
        options.sort = try decoded("\"created\"")
        options.direction = try decoded("\"asc\"")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.pulls.listReviewComments(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture875Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListReviewComments decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListReviewCommentsForRepoPullsListReviewCommentsForRepoReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.listReviewCommentsForRepo(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            sort: decoded("\"created\""),
            direction: decoded("\"asc\""),
            since: decoded("\"2024-01-01T00:00:00Z\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture876Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListReviewCommentsForRepo decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsListReviewsPullsListReviewsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.listReviews(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture877Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsListReviews decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsMergePullsMergeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.merge(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            commitTitle: decoded("\"Expand enum\""),
            commitMessage: decoded("\"Add a new value to the merge_method enum\""),
            sha: decoded("null"),
            mergeMethod: decoded("null")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture878Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsMerge decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsMergeAsyncPullsMergeAsyncReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try PullsMethods.PullsMergeAsyncOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), pullNumber: decoded("1"))
        options.commitTitle = try decoded("\"Fix race condition\"")
        options.commitMessage = try decoded(sdkMethodFixture879Value1)
        options.sha = try decoded("\"6358cd125586d9def8c3e5943f23506202da81cc\"")
        options.mergeMethod = try decoded("\"squash\"")
        options.mergeAction = try decoded("\"default\"")
        let actual = try await client.pulls.mergeAsync(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture879Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsMergeAsync decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsRemoveRequestedReviewersPullsRemoveRequestedReviewersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.removeRequestedReviewers(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewers: decoded("[\"octocat\",\"hubot\",\"other_user\"]"),
            teamReviewers: decoded("[\"justice-league\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture880Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsRemoveRequestedReviewers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsRequestReviewersPullsRequestReviewersReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.requestReviewers(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewers: decoded("[\"octocat\",\"hubot\",\"other_user\"]"),
            teamReviewers: decoded("[\"justice-league\"]")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture881Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsRequestReviewers decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsSubmitReviewPullsSubmitReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.submitReview(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewId: decoded("1"),
            event: decoded("\"REQUEST_CHANGES\""),
            body: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture882Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsSubmitReview decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsUpdatePullsUpdateReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try PullsMethods.PullsUpdateOptions(owner: decoded("\"owner\""), repo: decoded("\"repo\""), pullNumber: decoded("1"))
        options.title = try decoded("\"new title\"")
        options.body = try decoded("\"sample\"")
        options.state = try decoded("\"open\"")
        options.base = try decoded("\"master\"")
        options.maintainerCanModify = try decoded("null")
        let actual = try await client.pulls.update(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture883Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsUpdate decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsUpdateBranchPullsUpdateBranchReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.updateBranch(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            expectedHeadSha: decoded("\"6dcb09b5b57875f334f61aebed695e2e4193db5e\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture884Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsUpdateBranch decoded a response " +
                "that differs from the mock"
        )
    }

    func testPullsUpdateReviewPullsUpdateReviewReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.pulls.updateReview(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            pullNumber: decoded("1"),
            reviewId: decoded("1"),
            body: decoded("\"sample\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture885Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "pullsUpdateReview decoded a response " +
                "that differs from the mock"
        )
    }
}
