// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation
@testable import OctriGithub
import XCTest

extension SdkMethodTests {
    func testReposListContributorsReposListContributorsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listContributors(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            anon: decoded("\"anon\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1046Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListContributors decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListCustomDeploymentRuleIntegrationsReposListCustomDeploX961bccc655ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listCustomDeploymentRuleIntegrations(
            environmentName: decoded("\"Example Name\""),
            repo: decoded("\"repo\""),
            owner: decoded("\"owner\""),
            page: decoded("1"),
            perPage: decoded("30")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1047Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListCustomDeploymentRuleIntegrations decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListDeployKeysReposListDeployKeysReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listDeployKeys(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1048Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListDeployKeys decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListDeploymentBranchPoliciesReposListDeploymentBranchPoliciesReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listDeploymentBranchPolicies(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            environmentName: decoded("\"Example Name\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1049Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListDeploymentBranchPolicies decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListDeploymentsReposListDeploymentsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = try ReposMethods.ReposListDeploymentsOptions(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        options.sha = try decoded("\"none\"")
        options.ref = try decoded("\"none\"")
        options.task = try decoded("\"none\"")
        options.environment = try decoded("\"none\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        let actual = try await client.repos.listDeployments(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1050Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListDeployments decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListDeploymentStatusesReposListDeploymentStatusesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listDeploymentStatuses(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            deploymentId: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1051Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListDeploymentStatuses decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListForAuthenticatedUserReposListForAuthenticatedUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        var options = ReposMethods.ReposListForAuthenticatedUserOptions()
        options.visibility = try decoded("\"all\"")
        options.affiliation = try decoded("\"owner,collaborator,organization_member\"")
        options.type = try decoded("\"all\"")
        options.sort = try decoded("\"full_name\"")
        options.direction = try decoded("\"asc\"")
        options.perPage = try decoded("30")
        options.page = try decoded("1")
        options.since = try decoded("\"2024-01-01T00:00:00Z\"")
        options.before = try decoded("\"2024-01-01T00:00:00Z\"")
        let actual = try await client.repos.listForAuthenticatedUser(
            options: options
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1052Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListForksReposListForksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listForks(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            sort: decoded("\"newest\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1053Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListForks decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListForOrgReposListForOrgReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listForOrg(
            org: decoded("\"org\""),
            type: decoded("\"all\""),
            sort: decoded("\"created\""),
            direction: decoded("\"asc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1054Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListForOrg decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListForUserReposListForUserReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listForUser(
            username: decoded("\"Example Name\""),
            type: decoded("\"owner\""),
            sort: decoded("\"full_name\""),
            direction: decoded("\"asc\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1055Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListForUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListInvitationsReposListInvitationsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listInvitations(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1056Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListInvitations decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListInvitationsForAuthenticatedUserReposListInvitationsFXe71084f762ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listInvitationsForAuthenticatedUser(
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1057Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListInvitationsForAuthenticatedUser decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListIssueTypesReposListIssueTypesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listIssueTypes(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1058Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListIssueTypes decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListLanguagesReposListLanguagesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listLanguages(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1059Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListLanguages decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListPagesBuildsReposListPagesBuildsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listPagesBuilds(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1060Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListPagesBuilds decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListPublicReposListPublicReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listPublic(
            since: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1061Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListPublic decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListPullRequestsAssociatedWithCommitReposListPullRequestXab6d960ef1ReachesMockAndDecodesResponse(
    ) async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listPullRequestsAssociatedWithCommit(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            commitSha: decoded("\"commit_sha\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1062Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListPullRequestsAssociatedWithCommit decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListReleaseAssetsReposListReleaseAssetsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listReleaseAssets(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            releaseId: decoded("1"),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1063Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListReleaseAssets decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListReleasesReposListReleasesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listReleases(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1064Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListReleases decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListTagsReposListTagsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listTags(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1065Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListTags decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListTeamsReposListTeamsReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listTeams(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1066Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListTeams decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListWebhookDeliveriesReposListWebhookDeliveriesReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listWebhookDeliveries(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            hookId: decoded("1"),
            perPage: decoded("30"),
            cursor: decoded("\"cursor\""),
            status: decoded("\"success\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1067Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListWebhookDeliveries decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposListWebhooksReposListWebhooksReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.listWebhooks(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            perPage: decoded("30"),
            page: decoded("1")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1068Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposListWebhooks decoded a response " +
                "that differs from the mock"
        )
    }

    func testReposMergeReposMergeReachesMockAndDecodesResponse() async throws {
        guard let client = configuredClient() else {
            return
        }
        _ = client
        let actual = try await client.repos.merge(
            owner: decoded("\"owner\""),
            repo: decoded("\"repo\""),
            base: decoded("\"master\""),
            head: decoded("\"cool_feature\""),
            commitMessage: decoded("\"Shipped cool_feature!\"")
        )
        let expected = try JSONSerialization.jsonObject(
            with: Data(sdkMethodFixture1069Expected.utf8),
            options: [.fragmentsAllowed]
        )
        XCTAssertNil(
            try mismatch(jsonValue(actual), expected),
            "reposMerge decoded a response " +
                "that differs from the mock"
        )
    }
}
