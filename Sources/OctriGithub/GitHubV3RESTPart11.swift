// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class EnterpriseTeamMembershipsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists all enterprise teams that a user is a member of. This endpoint is available only for enterprises using the
    /// new enterprise teams experience. The authenticated user must be an enterprise owner or have the
    /// `enterprise_teams:read` permission.
    public func listTeamsForUser(
        enterprise: String,
        username: String,
        perPage: Int?,
        page: Int?
    ) async throws -> [EnterpriseTeam] {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsListTeamsForUser(
            config: config,
            enterprise: enterprise,
            username: username,
            perPage: perPage,
            page: page
        )
    }

    /// Lists the members of an enterprise team. Use `enterprise` and `enterprise-team` to identify the enterprise and
    /// team, and use `page` and `per_page` to paginate the results.
    ///
    /// Lists all team members in an enterprise team.
    public func list(
        enterprise: String,
        enterpriseTeam: String,
        perPage: Int?,
        page: Int?
    ) async throws -> [SimpleUser] {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsList(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            perPage: perPage,
            page: page
        )
    }

    /// Adds multiple GitHub users to an enterprise team. Supply the user handles in the required `usernames` array,
    /// using `enterprise` and `enterprise-team` to identify the target team.
    ///
    /// Add multiple team members to an enterprise team.
    public func bulkAdd(enterprise: String, enterpriseTeam: String, usernames: [String]) async throws -> [SimpleUser] {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsBulkAdd(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            usernames: usernames
        )
    }

    /// Remove multiple team members from an enterprise team.
    public func bulkRemove(
        enterprise: String,
        enterpriseTeam: String,
        usernames: [String]
    ) async throws -> [SimpleUser] {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsBulkRemove(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            usernames: usernames
        )
    }

    /// Returns whether the user is a member of the enterprise team.
    public func get(enterprise: String, enterpriseTeam: String, username: String) async throws -> SimpleUser {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsGet(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            username: username
        )
    }

    /// Adds one GitHub user to an enterprise team. Supply `enterprise`, `enterprise-team`, and `username` to identify
    /// the enterprise, team, and user account.
    ///
    /// Add a team member to an enterprise team.
    public func add(enterprise: String, enterpriseTeam: String, username: String) async throws -> SimpleUser {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsAdd(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            username: username
        )
    }

    /// Remove membership of a specific user from a particular team in an enterprise.
    public func remove(enterprise: String, enterpriseTeam: String, username: String) async throws -> SdkEmptyResponse {
        try await EnterpriseTeamMembershipsMethods.enterpriseTeamMembershipsRemove(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            username: username
        )
    }
}

public class EnterpriseTeamsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// List all teams in the enterprise for the authenticated user
    public func list(enterprise: String, perPage: Int?, page: Int?) async throws -> [EnterpriseTeam] {
        try await EnterpriseTeamsMethods.enterpriseTeamsList(
            config: config,
            enterprise: enterprise,
            perPage: perPage,
            page: page
        )
    }

    /// To create an enterprise team, the authenticated user must be an owner of the enterprise.
    public func create(
        enterprise: String,
        name: String,
        description: SdkOptional<String>?,
        syncToOrganizations: EnterpriseTeamsCreateRequestBodySyncToOrganizations?,
        organizationSelectionType: EnterpriseTeamsCreateRequestBodyOrganizationSelectionType?,
        groupId: SdkOptional<String>?,
        notificationSetting: EnterpriseTeamsCreateRequestBodyNotificationSetting?
    ) async throws -> EnterpriseTeam {
        try await EnterpriseTeamsMethods.enterpriseTeamsCreate(
            config: config,
            enterprise: enterprise,
            name: name,
            description: description,
            syncToOrganizations: syncToOrganizations,
            organizationSelectionType: organizationSelectionType,
            groupId: groupId,
            notificationSetting: notificationSetting
        )
    }

    /// Gets a team using the team's slug. To create the slug, GitHub replaces special characters in the name string,
    /// changes all words to lowercase, and replaces spaces with a `-` separator and adds the "ent:" prefix. For
    /// example, "My TEam Näme" would become `ent:my-team-name`.
    public func get(enterprise: String, teamSlug: String) async throws -> EnterpriseTeamWithMemberCount {
        try await EnterpriseTeamsMethods.enterpriseTeamsGet(config: config, enterprise: enterprise, teamSlug: teamSlug)
    }

    /// To edit a team, the authenticated user must be an enterprise owner.
    public func update(options: EnterpriseTeamsMethods.EnterpriseTeamsUpdateOptions) async throws -> EnterpriseTeam {
        try await EnterpriseTeamsMethods.enterpriseTeamsUpdate(config: config, options: options)
    }

    /// To delete an enterprise team, the authenticated user must be an enterprise owner. If you are an enterprise
    /// owner, deleting an enterprise team will delete all of its IdP mappings as well.
    public func delete(enterprise: String, teamSlug: String) async throws -> SdkEmptyResponse {
        try await EnterpriseTeamsMethods.enterpriseTeamsDelete(
            config: config,
            enterprise: enterprise,
            teamSlug: teamSlug
        )
    }
}

public class EnterpriseTeamOrganizationsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Get all organizations assigned to an enterprise team
    public func getAssignments(
        enterprise: String,
        enterpriseTeam: String,
        perPage: Int?,
        page: Int?
    ) async throws -> [OrganizationSimple] {
        try await EnterpriseTeamOrganizationsMethods.enterpriseTeamOrganizationsGetAssignments(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            perPage: perPage,
            page: page
        )
    }

    /// Assign an enterprise team to multiple organizations.
    public func bulkAdd(
        enterprise: String,
        enterpriseTeam: String,
        organizationSlugs: [String]
    ) async throws -> [OrganizationSimple] {
        try await EnterpriseTeamOrganizationsMethods.enterpriseTeamOrganizationsBulkAdd(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            organizationSlugs: organizationSlugs
        )
    }

    /// Unassign an enterprise team from multiple organizations.
    public func bulkRemove(
        enterprise: String,
        enterpriseTeam: String,
        organizationSlugs: [String]
    ) async throws -> SdkEmptyResponse {
        try await EnterpriseTeamOrganizationsMethods.enterpriseTeamOrganizationsBulkRemove(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            organizationSlugs: organizationSlugs
        )
    }

    /// Check if an enterprise team is assigned to an organization
    public func getAssignment(
        enterprise: String,
        enterpriseTeam: String,
        org: String
    ) async throws -> OrganizationSimple {
        try await EnterpriseTeamOrganizationsMethods.enterpriseTeamOrganizationsGetAssignment(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            org: org
        )
    }

    /// Assigns an enterprise team to an organization. Provide the enterprise slug, enterprise team slug or ID, and
    /// organization name to create the assignment.
    ///
    /// Assign an enterprise team to an organization.
    public func add(enterprise: String, enterpriseTeam: String, org: String) async throws -> OrganizationSimple {
        try await EnterpriseTeamOrganizationsMethods.enterpriseTeamOrganizationsAdd(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            org: org
        )
    }

    /// Deletes an assignment between an enterprise team and an organization. Provide the enterprise, enterprise team,
    /// and organization identifiers that define the assignment to remove.
    ///
    /// Unassign an enterprise team from an organization.
    public func delete(enterprise: String, enterpriseTeam: String, org: String) async throws -> SdkEmptyResponse {
        try await EnterpriseTeamOrganizationsMethods.enterpriseTeamOrganizationsDelete(
            config: config,
            enterprise: enterprise,
            enterpriseTeam: enterpriseTeam,
            org: org
        )
    }
}

public class ActivityNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// > [!NOTE] > This API is not built to serve real-time use cases. Depending on the time of day, event latency can
    /// be anywhere from 30s to 6h.
    public func listPublicEvents(perPage: Int?, page: Int?) async throws -> [Event] {
        try await ActivityMethods.activityListPublicEvents(config: config, perPage: perPage, page: page)
    }

    /// Lists the feeds available to the authenticated user. The response provides a URL for each feed. You can then get
    /// a specific feed by sending a request to one of the feed URLs. * **Timeline**: The GitHub global public timeline
    /// * **User**: The public timeline for any user, using `uri_template`. For more information, see
    /// "[Hypermedia](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia)." *
    /// **Current user public**: The public timeline for the authenticated user * **Current user**: The private timeline
    /// for the authenticated user * **Current user actor**: The private timeline for activity created by the
    /// authenticated user * **Current user organizations**: The private timeline for the organizations the
    /// authenticated user is a member of. * **Security advisories**: A collection of public announcements that provide
    /// information about security-related vulnerabilities in software on GitHub. By default, timeline resources are
    /// returned in JSON. You can specify the `application/atom+xml` type in the `Accept` header to return timeline
    /// resources in Atom format. For more information, see "[Media
    /// types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." >
    /// [!NOTE] > Private feeds are only returned when [authenticating via Basic
    /// Auth](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication)
    /// since current feed URIs use the older, non revocable auth tokens.
    public func getFeeds() async throws -> Feed {
        try await ActivityMethods.activityGetFeeds(config: config)
    }

    /// > [!NOTE] > This API is not built to serve real-time use cases. Depending on the time of day, event latency can
    /// be anywhere from 30s to 6h.
    public func listPublicEventsForRepoNetwork(
        owner: String,
        repo: String,
        perPage: Int?,
        page: Int?
    ) async throws -> [Event] {
        try await ActivityMethods.activityListPublicEventsForRepoNetwork(
            config: config,
            owner: owner,
            repo: repo,
            perPage: perPage,
            page: page
        )
    }

    /// List all notifications for the current user, sorted by most recently updated.
    public func listNotificationsForAuthenticatedUser(
        all: Bool?,
        participating: Bool?,
        since: Date?,
        before: Date?,
        page: Int?,
        perPage: Int?
    ) async throws -> [Thread] {
        try await ActivityMethods.activityListNotificationsForAuthenticatedUser(
            config: config,
            all: all,
            participating: participating,
            since: since,
            before: before,
            page: page,
            perPage: perPage
        )
    }

    /// Marks all notifications as "read" for the current user. If the number of notifications is too large to complete
    /// in one request, you will receive a `202 Accepted` status and GitHub will run an asynchronous process to mark
    /// notifications as "read." To check whether any "unread" notifications remain, you can use the [List notifications
    /// for the authenticated
    /// user](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)
    /// endpoint and pass the query parameter `all=false`.
    public func markNotificationsAsRead(
        lastReadAt: Date?,
        read: Bool?
    ) async throws -> ActivityMarkNotificationsAsReadResponse {
        try await ActivityMethods.activityMarkNotificationsAsRead(config: config, lastReadAt: lastReadAt, read: read)
    }

    /// Retrieves a notification thread by its unique identifier. Use `thread_id` from the `id` field returned by the
    /// notifications list to inspect the thread's repository, subject, read state, and subscription links.
    ///
    /// Gets information about a notification thread.
    public func getThread(threadId: Int) async throws -> Thread {
        try await ActivityMethods.activityGetThread(config: config, threadId: threadId)
    }

    /// Marks a thread as "read." Marking a thread as "read" is equivalent to clicking a notification in your
    /// notification inbox on GitHub: https://github.com/notifications.
    public func markThreadAsRead(threadId: Int) async throws -> SdkEmptyResponse {
        try await ActivityMethods.activityMarkThreadAsRead(config: config, threadId: threadId)
    }

    /// Marks a thread as "done." Marking a thread as "done" is equivalent to marking a notification in your
    /// notification inbox on GitHub as done: https://github.com/notifications.
    public func markThreadAsDone(threadId: Int) async throws -> SdkEmptyResponse {
        try await ActivityMethods.activityMarkThreadAsDone(config: config, threadId: threadId)
    }
}

public extension ActivityNamespace {
    /// This checks to see if the current user is subscribed to a thread. You can also [get a repository
    /// subscription](https://docs.github.com/rest/activity/watching#get-a-repository-subscription). Note that
    /// subscriptions are only generated if a user is participating in a conversation--for example, they've replied to
    /// the thread, were **@mentioned**, or manually subscribe to a thread.
    func getThreadSubscriptionForAuthenticatedUser(threadId: Int) async throws -> ThreadSubscription {
        try await ActivityMethods.activityGetThreadSubscriptionForAuthenticatedUser(config: config, threadId: threadId)
    }

    /// If you are watching a repository, you receive notifications for all threads by default. Use this endpoint to
    /// ignore future notifications for threads until you comment on the thread or get an **@mention**. You can also use
    /// this endpoint to subscribe to threads that you are currently not receiving notifications for or to subscribed to
    /// threads that you have previously ignored. Unsubscribing from a conversation in a repository that you are not
    /// watching is functionally equivalent to the [Delete a thread
    /// subscription](https://docs.github.com/rest/activity/notifications#delete-a-thread-subscription) endpoint.
    func setThreadSubscription(threadId: Int, ignored: Bool?) async throws -> ThreadSubscription {
        try await ActivityMethods.activitySetThreadSubscription(config: config, threadId: threadId, ignored: ignored)
    }

    /// Mutes all future notifications for a conversation until you comment on the thread or get an **@mention**. If you
    /// are watching the repository of the thread, you will still receive notifications. To ignore future notifications
    /// for a repository you are watching, use the [Set a thread
    /// subscription](https://docs.github.com/rest/activity/notifications#set-a-thread-subscription) endpoint and set
    /// `ignore` to `true`.
    func deleteThreadSubscription(threadId: Int) async throws -> SdkEmptyResponse {
        try await ActivityMethods.activityDeleteThreadSubscription(config: config, threadId: threadId)
    }

    /// > [!NOTE] > This API is not built to serve real-time use cases. Depending on the time of day, event latency can
    /// be anywhere from 30s to 6h.
    func listPublicOrgEvents(org: String, perPage: Int?, page: Int?) async throws -> [Event] {
        try await ActivityMethods.activityListPublicOrgEvents(config: config, org: org, perPage: perPage, page: page)
    }

    /// > [!NOTE] > This API is not built to serve real-time use cases. Depending on the time of day, event latency can
    /// be anywhere from 30s to 6h.
    func listRepoEvents(owner: String, repo: String, perPage: Int?, page: Int?) async throws -> [Event] {
        try await ActivityMethods.activityListRepoEvents(
            config: config,
            owner: owner,
            repo: repo,
            perPage: perPage,
            page: page
        )
    }

    /// Lists all notifications for the current user in the specified repository.
    func listRepoNotificationsForAuthenticatedUser(options: ActivityMethods
        .ActivityListRepoNotificationsForAuthenticatedUserOptions) async throws -> [Thread] {
        try await ActivityMethods.activityListRepoNotificationsForAuthenticatedUser(config: config, options: options)
    }

    /// Marks all notifications in a repository as "read" for the current user. If the number of notifications is too
    /// large to complete in one request, you will receive a `202 Accepted` status and GitHub will run an asynchronous
    /// process to mark notifications as "read." To check whether any "unread" notifications remain, you can use the
    /// [List repository notifications for the authenticated user](https://docs.github.com/rest/activity/notifications#list-repository-notifications-for-the-authenticated-user)
    /// endpoint and pass the query parameter `all=false`.
    func markRepoNotificationsAsRead(
        owner: String,
        repo: String,
        lastReadAt: Date?
    ) async throws -> ActivityMarkRepoNotificationsAsReadResponse {
        try await ActivityMethods.activityMarkRepoNotificationsAsRead(
            config: config,
            owner: owner,
            repo: repo,
            lastReadAt: lastReadAt
        )
    }

    /// List stargazers
    ///
    /// Lists the people that have starred the repository. This endpoint supports the following custom media types. For
    /// more information, see "[Media
    /// types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." -
    /// **`application/vnd.github.star+json`**: Includes a timestamp of when the star was created.
    func listStargazersForRepo(
        owner: String,
        repo: String,
        perPage: Int?,
        page: Int?
    ) async throws -> ActivityListStargazersForRepoResponse {
        try await ActivityMethods.activityListStargazersForRepo(
            config: config,
            owner: owner,
            repo: repo,
            perPage: perPage,
            page: page
        )
    }
}
