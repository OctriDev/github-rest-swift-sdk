// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

extension ActionsNamespace {
/// Creates a repository variable that you can reference in a GitHub Actions workflow. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createRepoVariable(owner: String, repo: String, name: String, value: String) async throws -> EmptyObject {
        return try await ActionsMethods.actionsCreateRepoVariable(config: config, owner: owner, repo: repo, name: name, value: value)
    }

/// Gets a specific variable in a repository. The authenticated user must have collaborator access to the repository to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func getRepoVariable(owner: String, repo: String, name: String) async throws -> ActionsVariable {
        return try await ActionsMethods.actionsGetRepoVariable(config: config, owner: owner, repo: repo, name: name)
    }

/// Updates a repository variable that you can reference in a GitHub Actions workflow. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func updateRepoVariable(owner: String, repo: String, name: String, name2: String?, value: String?) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsUpdateRepoVariable(config: config, owner: owner, repo: repo, name: name, name2: name2, value: value)
    }

/// Deletes a repository variable using the variable name. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func deleteRepoVariable(owner: String, repo: String, name: String) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsDeleteRepoVariable(config: config, owner: owner, repo: repo, name: name)
    }

/// Lists the workflows in a repository. Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func listRepoWorkflows(owner: String, repo: String, perPage: Int?, page: Int?) async throws -> ActionsListRepoWorkflowsResponse {
        return try await ActionsMethods.actionsListRepoWorkflows(config: config, owner: owner, repo: repo, perPage: perPage, page: page)
    }

/// Gets a specific workflow. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getWorkflow(owner: String, repo: String, workflowId: ActionsGetWorkflowParameter) async throws -> Workflow {
        return try await ActionsMethods.actionsGetWorkflow(config: config, owner: owner, repo: repo, workflowId: workflowId)
    }

/// Disables a workflow and sets the `state` of the workflow to `disabled_manually`. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func disableWorkflow(owner: String, repo: String, workflowId: ActionsGetWorkflowParameter) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsDisableWorkflow(config: config, owner: owner, repo: repo, workflowId: workflowId)
    }

/// You can use this endpoint to manually trigger a GitHub Actions workflow run. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. You must configure your GitHub Actions workflow to run when the `workflow_dispatch` webhook event occurs. The `inputs` are configured in the workflow file. For more information about how to configure the `workflow_dispatch` event in the workflow file, see "Events that trigger workflows." OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createWorkflowDispatch(owner: String, repo: String, workflowId: ActionsGetWorkflowParameter, ref: String, inputs: [String: JSONValue]?, returnRunDetails: Bool?) async throws -> WorkflowDispatchResponse {
        return try await ActionsMethods.actionsCreateWorkflowDispatch(config: config, owner: owner, repo: repo, workflowId: workflowId, ref: ref, inputs: inputs, returnRunDetails: returnRunDetails)
    }
}

extension ActionsNamespace {
/// Enables a workflow and sets the `state` of the workflow to `active`. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func enableWorkflow(owner: String, repo: String, workflowId: ActionsGetWorkflowParameter) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsEnableWorkflow(config: config, owner: owner, repo: repo, workflowId: workflowId)
    }

/// List all workflow runs for a workflow. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. You can use parameters to narrow the list of results. For more information about using parameters, see [Parameters](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#parameters). Anyone with read access to the repository can use this endpoint OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository. This endpoint will return up to 1,000 results for each search when using the following parameters: `actor`, `branch`, `check_suite_id`, `created`, `event`, `head_sha`, `status`.
    public func listWorkflowRuns(options: ActionsMethods.ActionsListWorkflowRunsOptions) async throws -> ActionsListWorkflowRunsResponse {
        return try await ActionsMethods.actionsListWorkflowRuns(config: config, options: options)
    }

/// > [!WARNING] > This endpoint is in the process of closing down. Refer to "[Actions Get workflow usage and Get workflow run usage endpoints closing down](https://github.blog/changelog/2025-02-02-actions-get-workflow-usage-and-get-workflow-run-usage-endpoints-closing-down/)" for more information. Gets the number of billable minutes used by a specific workflow during the current billing cycle. Billable minutes only apply to workflows in private repositories that use GitHub-hosted runners. Usage is listed for each GitHub-hosted runner operating system in milliseconds. Any job re-runs are also included in the usage. The usage does not include the multiplier for macOS and Windows runners and is not rounded up to the nearest whole minute. For more information, see "[Managing billing for GitHub Actions](https://docs.github.com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-actions)". You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getWorkflowUsage(owner: String, repo: String, workflowId: ActionsGetWorkflowParameter) async throws -> WorkflowUsage {
        return try await ActionsMethods.actionsGetWorkflowUsage(config: config, owner: owner, repo: repo, workflowId: workflowId)
    }

/// Lists all secrets available in an environment without revealing their encrypted values. Authenticated users must have collaborator access to a repository to create, update, or read secrets. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func listEnvironmentSecrets(owner: String, repo: String, environmentName: String, perPage: Int?, page: Int?) async throws -> ActionsListEnvironmentSecretsResponse {
        return try await ActionsMethods.actionsListEnvironmentSecrets(config: config, owner: owner, repo: repo, environmentName: environmentName, perPage: perPage, page: page)
    }

/// Get the public key for an environment, which you need to encrypt environment secrets. You need to encrypt a secret before you can create or update secrets. Anyone with read access to the repository can use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func getEnvironmentPublicKey(owner: String, repo: String, environmentName: String) async throws -> ActionsPublicKey {
        return try await ActionsMethods.actionsGetEnvironmentPublicKey(config: config, owner: owner, repo: repo, environmentName: environmentName)
    }

/// Gets a single environment secret without revealing its encrypted value. Authenticated users must have collaborator access to a repository to create, update, or read secrets. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func getEnvironmentSecret(owner: String, repo: String, environmentName: String, secretName: String) async throws -> ActionsSecret {
        return try await ActionsMethods.actionsGetEnvironmentSecret(config: config, owner: owner, repo: repo, environmentName: environmentName, secretName: secretName)
    }

/// Creates or updates an environment secret with an encrypted value. Encrypt your secret using [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)." Authenticated users must have collaborator access to a repository to create, update, or read secrets. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createOrUpdateEnvironmentSecret(owner: String, repo: String, environmentName: String, secretName: String, encryptedValue: String, keyId: String) async throws -> EmptyObject {
        return try await ActionsMethods.actionsCreateOrUpdateEnvironmentSecret(config: config, owner: owner, repo: repo, environmentName: environmentName, secretName: secretName, encryptedValue: encryptedValue, keyId: keyId)
    }

/// Deletes a secret in an environment using the secret name. Authenticated users must have collaborator access to a repository to create, update, or read secrets. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func deleteEnvironmentSecret(owner: String, repo: String, environmentName: String, secretName: String) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsDeleteEnvironmentSecret(config: config, owner: owner, repo: repo, environmentName: environmentName, secretName: secretName)
    }
}

extension ActionsNamespace {
/// Lists all environment variables. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func listEnvironmentVariables(owner: String, repo: String, environmentName: String, perPage: Int?, page: Int?) async throws -> ActionsListEnvironmentVariablesResponse {
        return try await ActionsMethods.actionsListEnvironmentVariables(config: config, owner: owner, repo: repo, environmentName: environmentName, perPage: perPage, page: page)
    }

/// Create an environment variable that you can reference in a GitHub Actions workflow. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createEnvironmentVariable(owner: String, repo: String, environmentName: String, name: String, value: String) async throws -> EmptyObject {
        return try await ActionsMethods.actionsCreateEnvironmentVariable(config: config, owner: owner, repo: repo, environmentName: environmentName, name: name, value: value)
    }

/// Gets a specific variable in an environment. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func getEnvironmentVariable(owner: String, repo: String, environmentName: String, name: String) async throws -> ActionsVariable {
        return try await ActionsMethods.actionsGetEnvironmentVariable(config: config, owner: owner, repo: repo, environmentName: environmentName, name: name)
    }

/// Updates an environment variable that you can reference in a GitHub Actions workflow. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func updateEnvironmentVariable(owner: String, repo: String, name: String, environmentName: String, name2: String?, value: String?) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsUpdateEnvironmentVariable(config: config, owner: owner, repo: repo, name: name, environmentName: environmentName, name2: name2, value: value)
    }

/// Deletes an environment variable using the variable name. Authenticated users must have collaborator access to a repository to create, update, or read variables. OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func deleteEnvironmentVariable(owner: String, repo: String, name: String, environmentName: String) async throws -> SdkEmptyResponse {
        return try await ActionsMethods.actionsDeleteEnvironmentVariable(config: config, owner: owner, repo: repo, name: name, environmentName: environmentName)
    }
}

public class OidcNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists the repository custom properties that are included in the OIDC token for repository actions in an enterprise. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func oidcListCustomPropertyInclusionsForEnterprise(enterprise: String) async throws -> [OidcCustomPropertyInclusion] {
        return try await OidcMethods.oidcListOidcCustomPropertyInclusionsForEnterprise(config: config, enterprise: enterprise)
    }

/// Adds a repository custom property to be included in the OIDC token for repository actions in an enterprise. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func oidcCreateCustomPropertyInclusionForEnterprise(enterprise: String, customPropertyName: String) async throws -> OidcCustomPropertyInclusion {
        return try await OidcMethods.oidcCreateOidcCustomPropertyInclusionForEnterprise(config: config, enterprise: enterprise, customPropertyName: customPropertyName)
    }

/// Removes a repository custom property from being included in the OIDC token for repository actions in an enterprise. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func oidcDeleteCustomPropertyInclusionForEnterprise(enterprise: String, customPropertyName: String) async throws -> SdkEmptyResponse {
        return try await OidcMethods.oidcDeleteOidcCustomPropertyInclusionForEnterprise(config: config, enterprise: enterprise, customPropertyName: customPropertyName)
    }

/// Lists the repository custom properties that are included in the OIDC token for repository actions in an organization. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
    public func oidcListCustomPropertyInclusionsForOrg(org: String) async throws -> [OidcCustomPropertyInclusion] {
        return try await OidcMethods.oidcListOidcCustomPropertyInclusionsForOrg(config: config, org: org)
    }

/// Adds a repository custom property to be included in the OIDC token for repository actions in an organization. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    public func oidcCreateCustomPropertyInclusionForOrg(org: String, customPropertyName: String) async throws -> OidcCustomPropertyInclusion {
        return try await OidcMethods.oidcCreateOidcCustomPropertyInclusionForOrg(config: config, org: org, customPropertyName: customPropertyName)
    }

/// Removes a repository custom property from being included in the OIDC token for repository actions in an organization. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    public func oidcDeleteCustomPropertyInclusionForOrg(org: String, customPropertyName: String) async throws -> SdkEmptyResponse {
        return try await OidcMethods.oidcDeleteOidcCustomPropertyInclusionForOrg(config: config, org: org, customPropertyName: customPropertyName)
    }

/// Gets the customization template for an OpenID Connect (OIDC) subject claim. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
    public func oidcGetCustomSubTemplateForOrg(org: String) async throws -> OidcCustomSub {
        return try await OidcMethods.oidcGetOidcCustomSubTemplateForOrg(config: config, org: org)
    }

/// Creates or updates the customization template for an OpenID Connect (OIDC) subject claim. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
    public func oidcUpdateCustomSubTemplateForOrg(org: String, includeClaimKeys: [String]?, useImmutableSubject: Bool?) async throws -> EmptyObject {
        return try await OidcMethods.oidcUpdateOidcCustomSubTemplateForOrg(config: config, org: org, includeClaimKeys: includeClaimKeys, useImmutableSubject: useImmutableSubject)
    }
}

public class CodeSecurityNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists all code security configurations available in an enterprise. The authenticated user must be an administrator of the enterprise in order to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
    public func getConfigurationsForEnterprise(enterprise: String, perPage: Int?, before: String?, after: String?) async throws -> [CodeSecurityConfiguration] {
        return try await CodeSecurityMethods.codeSecurityGetConfigurationsForEnterprise(config: config, enterprise: enterprise, perPage: perPage, before: before, after: after)
    }

/// Creates a code security configuration in an enterprise. The authenticated user must be an administrator of the enterprise in order to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func createConfigurationForEnterprise(options: CodeSecurityMethods.CodeSecurityCreateConfigurationForEnterpriseOptions) async throws -> CodeSecurityConfiguration {
        return try await CodeSecurityMethods.codeSecurityCreateConfigurationForEnterprise(config: config, options: options)
    }

/// Lists the default code security configurations for an enterprise. The authenticated user must be an administrator of the enterprise in order to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
    public func getDefaultConfigurationsForEnterprise(enterprise: String) async throws -> CodeSecurityDefaultConfigurations {
        return try await CodeSecurityMethods.codeSecurityGetDefaultConfigurationsForEnterprise(config: config, enterprise: enterprise)
    }

/// Gets a code security configuration available in an enterprise. The authenticated user must be an administrator of the enterprise in order to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
    public func getSingleConfigurationForEnterprise(enterprise: String, configurationId: Int) async throws -> CodeSecurityConfiguration {
        return try await CodeSecurityMethods.codeSecurityGetSingleConfigurationForEnterprise(config: config, enterprise: enterprise, configurationId: configurationId)
    }

/// Updates a code security configuration in an enterprise. The authenticated user must be an administrator of the enterprise in order to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func updateEnterpriseConfiguration(options: CodeSecurityMethods.CodeSecurityUpdateEnterpriseConfigurationOptions) async throws -> CodeSecurityConfiguration {
        return try await CodeSecurityMethods.codeSecurityUpdateEnterpriseConfiguration(config: config, options: options)
    }

/// Deletes a code security configuration from an enterprise. Repositories attached to the configuration will retain their settings but will no longer be associated with the configuration. The authenticated user must be an administrator for the enterprise to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func deleteConfigurationForEnterprise(enterprise: String, configurationId: Int) async throws -> SdkEmptyResponse {
        return try await CodeSecurityMethods.codeSecurityDeleteConfigurationForEnterprise(config: config, enterprise: enterprise, configurationId: configurationId)
    }

/// Attaches an enterprise code security configuration to repositories. If the repositories specified are already attached to a configuration, they will be re-attached to the provided configuration. If insufficient GHAS licenses are available to attach the configuration to a repository, only free features will be enabled. The authenticated user must be an administrator for the enterprise to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func attachEnterpriseConfiguration(enterprise: String, configurationId: Int, scope: CodeSecurityAttachEnterpriseConfigurationRequestBodyScope) async throws -> [String: JSONValue] {
        return try await CodeSecurityMethods.codeSecurityAttachEnterpriseConfiguration(config: config, enterprise: enterprise, configurationId: configurationId, scope: scope)
    }

/// Sets a code security configuration as a default to be applied to new repositories in your enterprise. This configuration will be applied by default to the matching repository type when created, but only for organizations within the enterprise that do not already have a default code security configuration set. The authenticated user must be an administrator for the enterprise to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    public func setConfigurationAsDefaultForEnterprise(enterprise: String, configurationId: Int, defaultForNewRepos: CodeSecuritySetConfigurationAsDefaultForEnterpriseRequestBodyXd7696bf466?) async throws -> CodeSecuritySetConfigurationAsDefaultForEnterpriseResponse {
        return try await CodeSecurityMethods.codeSecuritySetConfigurationAsDefaultForEnterprise(config: config, enterprise: enterprise, configurationId: configurationId, defaultForNewRepos: defaultForNewRepos)
    }
}
