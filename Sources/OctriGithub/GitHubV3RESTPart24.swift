// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public extension PackagesNamespace {
/// Delete a package version for the authenticated user
    ///
    /// Deletes a specific package version for a package owned by the authenticated user. If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance. The authenticated user must have admin permissions in the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func deletePackageVersionForAuthenticatedUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, packageVersionId: Int) async throws -> SdkEmptyResponse {
        return try await PackagesMethods.packagesDeletePackageVersionForAuthenticatedUser(config: config, packageType: packageType, packageName: packageName, packageVersionId: packageVersionId)
    }

/// Restore a package version for the authenticated user
    ///
    /// Restores a package version owned by the authenticated user. You can restore a deleted package version under the following conditions: - The package was deleted within the last 30 days. - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first. OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func restorePackageVersionForAuthenticatedUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, packageVersionId: Int) async throws -> SdkEmptyResponse {
        return try await PackagesMethods.packagesRestorePackageVersionForAuthenticatedUser(config: config, packageType: packageType, packageName: packageName, packageVersionId: packageVersionId)
    }

/// Get list of conflicting packages during Docker migration for user
    ///
    /// Lists all packages that are in a specific user's namespace, that the requesting user has access to, and that encountered a conflict during Docker migration. OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
    public func packagesListDockerMigrationConflictingForUser(username: String) async throws -> [Package] {
        return try await PackagesMethods.packagesListDockerMigrationConflictingPackagesForUser(config: config, username: username)
    }

/// List packages for a user
    ///
    /// Lists all packages in a user's namespace for which the requesting user has access. OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func packagesListForUser(username: String, packageType: PackagesListPackagesForUserParameter, visibility: PackagesListPackagesForOrganizationParameter?, page: Int?, perPage: Int?) async throws -> [Package] {
        return try await PackagesMethods.packagesListPackagesForUser(config: config, username: username, packageType: packageType, visibility: visibility, page: page, perPage: perPage)
    }

/// Get a package for a user
    ///
    /// Gets a specific package metadata for a public package owned by a user. OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func getPackageForUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, username: String) async throws -> Package {
        return try await PackagesMethods.packagesGetPackageForUser(config: config, packageType: packageType, packageName: packageName, username: username)
    }

/// Delete a package for a user
    ///
    /// Deletes an entire package for a user. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)." OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func deletePackageForUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, username: String) async throws -> SdkEmptyResponse {
        return try await PackagesMethods.packagesDeletePackageForUser(config: config, packageType: packageType, packageName: packageName, username: username)
    }

/// Restore a package for a user
    ///
    /// Restores an entire package for a user. You can restore a deleted package under the following conditions: - The package was deleted within the last 30 days. - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)." OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func restorePackageForUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, username: String, token: String?) async throws -> SdkEmptyResponse {
        return try await PackagesMethods.packagesRestorePackageForUser(config: config, packageType: packageType, packageName: packageName, username: username, token: token)
    }

/// List package versions for a package owned by a user
    ///
    /// Lists package versions for a public package owned by a specified user. OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func getAllPackageVersionsForPackageOwnedByUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, username: String) async throws -> [PackageVersion] {
        return try await PackagesMethods.packagesGetAllPackageVersionsForPackageOwnedByUser(config: config, packageType: packageType, packageName: packageName, username: username)
    }
}

public extension PackagesNamespace {
/// Get a package version for a user
    ///
    /// Gets a specific package version for a public package owned by a specified user. OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func getPackageVersionForUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, packageVersionId: Int, username: String) async throws -> PackageVersion {
        return try await PackagesMethods.packagesGetPackageVersionForUser(config: config, packageType: packageType, packageName: packageName, packageVersionId: packageVersionId, username: username)
    }

/// Delete package version for a user
    ///
    /// Deletes a specific package version for a user. If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)." OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func deletePackageVersionForUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, username: String, packageVersionId: Int) async throws -> SdkEmptyResponse {
        return try await PackagesMethods.packagesDeletePackageVersionForUser(config: config, packageType: packageType, packageName: packageName, username: username, packageVersionId: packageVersionId)
    }

/// Restore package version for a user
    ///
    /// Restores a specific package version for a user. You can restore a deleted package under the following conditions: - The package was deleted within the last 30 days. - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)." OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
    public func restorePackageVersionForUser(packageType: PackagesDeletePackageForOrgParameter, packageName: String, username: String, packageVersionId: Int) async throws -> SdkEmptyResponse {
        return try await PackagesMethods.packagesRestorePackageVersionForUser(config: config, packageType: packageType, packageName: packageName, username: username, packageVersionId: packageVersionId)
    }
}

public class InteractionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Shows which type of GitHub user can interact with this organization and when the restriction expires. If there is no restrictions, you will see an empty response.
    public func getRestrictionsForOrg(org: String) async throws -> InteractionsGetRestrictionsForOrgResponse {
        return try await InteractionsMethods.interactionsGetRestrictionsForOrg(config: config, org: org)
    }

/// Temporarily restricts interactions to a certain type of GitHub user in any public repository in the given organization. You must be an organization owner to set these restrictions. Setting the interaction limit at the organization level will overwrite any interaction limits that are set for individual repositories owned by the organization.
    public func setRestrictionsForOrg(org: String, limit: InteractionGroup, expiry: InteractionExpiry?) async throws -> InteractionLimitResponse {
        return try await InteractionsMethods.interactionsSetRestrictionsForOrg(config: config, org: org, limit: limit, expiry: expiry)
    }

/// Removes all interaction restrictions from public repositories in the given organization. You must be an organization owner to remove restrictions.
    public func removeRestrictionsForOrg(org: String) async throws -> SdkEmptyResponse {
        return try await InteractionsMethods.interactionsRemoveRestrictionsForOrg(config: config, org: org)
    }

/// Gets the pull request creation cap configuration for an organization. The cap limits the total number of open pull requests a user can have across all public repositories in the organization at one time. Only users with admin access to the organization can view the cap configuration.
    public func getPullRequestCreationCapForOrg(org: String) async throws -> InteractionsGetPullRequestCreationCapForOrgResponse {
        return try await InteractionsMethods.interactionsGetPullRequestCreationCapForOrg(config: config, org: org)
    }

/// Updates the pull request creation cap for an organization. The cap limits the total number of open pull requests a user can have across all public repositories in the organization at one time. Only users with admin access to the organization can configure the cap.
    public func updatePullRequestCreationCapForOrg(org: String, enabled: Bool, maxOpenPullRequests: Int?) async throws -> InteractionsUpdatePullRequestCreationCapForOrgResponse {
        return try await InteractionsMethods.interactionsUpdatePullRequestCreationCapForOrg(config: config, org: org, enabled: enabled, maxOpenPullRequests: maxOpenPullRequests)
    }

/// Shows which type of GitHub user can interact with this repository and when the restriction expires. If there are no restrictions, you will see an empty response.
    public func getRestrictionsForRepo(owner: String, repo: String) async throws -> InteractionsGetRestrictionsForRepoResponse {
        return try await InteractionsMethods.interactionsGetRestrictionsForRepo(config: config, owner: owner, repo: repo)
    }

/// Temporarily restricts interactions to a certain type of GitHub user within the given repository. You must have owner or admin access to set these restrictions. If an interaction limit is set for the user or organization that owns this repository, you will receive a `409 Conflict` response and will not be able to use this endpoint to change the interaction limit for a single repository.
    public func setRestrictionsForRepo(owner: String, repo: String, limit: InteractionGroup, expiry: InteractionExpiry?) async throws -> InteractionLimitResponse {
        return try await InteractionsMethods.interactionsSetRestrictionsForRepo(config: config, owner: owner, repo: repo, limit: limit, expiry: expiry)
    }

/// Removes all interaction restrictions from the given repository. You must have owner or admin access to remove restrictions. If the interaction limit is set for the user or organization that owns this repository, you will receive a `409 Conflict` response and will not be able to use this endpoint to change the interaction limit for a single repository.
    public func removeRestrictionsForRepo(owner: String, repo: String) async throws -> SdkEmptyResponse {
        return try await InteractionsMethods.interactionsRemoveRestrictionsForRepo(config: config, owner: owner, repo: repo)
    }
}

public extension InteractionsNamespace {
/// Lists the users that are on the pull request creation cap bypass list for a repository. Users on this list can create pull requests regardless of any configured pull request creation cap. Only users with maintainer permissions can view the bypass list.
    public func getPullRequestBypassListForRepo(owner: String, repo: String) async throws -> [SimpleUser] {
        return try await InteractionsMethods.interactionsGetPullRequestBypassListForRepo(config: config, owner: owner, repo: repo)
    }

/// Adds users to the pull request creation cap bypass list for a repository. Users on this list can create pull requests regardless of any configured pull request creation cap. Only users with maintainer permissions can modify the bypass list. You can add a maximum of 100 users per request. The bypass list can only hold a maximum of 100 users.
    public func setPullRequestBypassListForRepo(owner: String, repo: String, users: [String]) async throws -> SdkEmptyResponse {
        return try await InteractionsMethods.interactionsSetPullRequestBypassListForRepo(config: config, owner: owner, repo: repo, users: users)
    }

/// Removes users from the pull request creation cap bypass list for a repository. Removed users will be subject to any configured pull request creation cap. Only users with maintainer permissions can modify the bypass list. You can remove a maximum of 100 users per request.
    public func removePullRequestBypassListForRepo(owner: String, repo: String, users: [String]) async throws -> SdkEmptyResponse {
        return try await InteractionsMethods.interactionsRemovePullRequestBypassListForRepo(config: config, owner: owner, repo: repo, users: users)
    }

/// Gets the pull request creation cap configuration for a repository. The cap limits the number of open pull requests a user can have at one time. Only users with admin access to the repository can view the cap configuration.
    public func getPullRequestCreationCapForRepo(owner: String, repo: String) async throws -> InteractionsGetPullRequestCreationCapForRepoResponse {
        return try await InteractionsMethods.interactionsGetPullRequestCreationCapForRepo(config: config, owner: owner, repo: repo)
    }

/// Updates the pull request creation cap for a repository. The cap limits the number of open pull requests a user can have at one time. Only users with admin access to the repository can configure the cap.
    public func updatePullRequestCreationCapForRepo(owner: String, repo: String, enabled: Bool, maxOpenPullRequests: Int?) async throws -> InteractionsUpdatePullRequestCreationCapForRepoResponse {
        return try await InteractionsMethods.interactionsUpdatePullRequestCreationCapForRepo(config: config, owner: owner, repo: repo, enabled: enabled, maxOpenPullRequests: maxOpenPullRequests)
    }

/// Get interaction restrictions for your public repositories
    ///
    /// Shows which type of GitHub user can interact with your public repositories and when the restriction expires.
    public func getRestrictionsForAuthenticatedUser() async throws -> InteractionsGetRestrictionsForAuthenticatedUserResponse {
        return try await InteractionsMethods.interactionsGetRestrictionsForAuthenticatedUser(config: config)
    }

/// Set interaction restrictions for your public repositories
    ///
    /// Temporarily restricts which type of GitHub user can interact with your public repositories. Setting the interaction limit at the user level will overwrite any interaction limits that are set for individual repositories owned by the user.
    public func setRestrictionsForAuthenticatedUser(limit: InteractionGroup, expiry: InteractionExpiry?) async throws -> InteractionLimitResponse {
        return try await InteractionsMethods.interactionsSetRestrictionsForAuthenticatedUser(config: config, limit: limit, expiry: expiry)
    }

/// Remove interaction restrictions from your public repositories
    ///
    /// Removes any interaction restrictions from your public repositories.
    public func removeRestrictionsForAuthenticatedUser() async throws -> SdkEmptyResponse {
        return try await InteractionsMethods.interactionsRemoveRestrictionsForAuthenticatedUser(config: config)
    }
}
