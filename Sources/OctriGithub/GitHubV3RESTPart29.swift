// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public extension ReposNamespace {
/// Lists the commit comments for a specified repository. Comments are ordered by ascending ID. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type. - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`. - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`. - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
    public func listCommitCommentsForRepo(owner: String, repo: String, perPage: Int?, page: Int?) async throws -> [CommitComment] {
        return try await ReposMethods.reposListCommitCommentsForRepo(config: config, owner: owner, repo: repo, perPage: perPage, page: page)
    }

/// Gets a specified commit comment. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type. - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`. - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`. - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
    public func getCommitComment(owner: String, repo: String, commentId: Int) async throws -> CommitComment {
        return try await ReposMethods.reposGetCommitComment(config: config, owner: owner, repo: repo, commentId: commentId)
    }

/// Updates the contents of a specified commit comment. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type. - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`. - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`. - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
    public func updateCommitComment(owner: String, repo: String, commentId: Int, body: String) async throws -> CommitComment {
        return try await ReposMethods.reposUpdateCommitComment(config: config, owner: owner, repo: repo, commentId: commentId, body: body)
    }

/// Deletes a specific commit comment from a repository. Use `owner`, `repo`, and `comment_id` to identify the repository and comment to remove. The deletion permanently removes the comment and does not return a response body.
    public func deleteCommitComment(owner: String, repo: String, commentId: Int) async throws -> SdkEmptyResponse {
        return try await ReposMethods.reposDeleteCommitComment(config: config, owner: owner, repo: repo, commentId: commentId)
    }

/// **Signature verification object** The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object: | Name | Type | Description | | ---- | ---- | ----------- | | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. | | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. | | `signature` | `string` | The signature that was extracted from the commit. | | `payload` | `string` | The value that was signed. | | `verified_at` | `string` | The date the signature was verified by GitHub. | These are the possible values for `reason` in the `verification` object: | Value | Description | | ----- | ----------- | | `expired_key` | The key that made the signature is expired. | | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. | | `gpgverify_error` | There was an error communicating with the signature verification service. | | `gpgverify_unavailable` | The signature verification service is currently unavailable. | | `unsigned` | The object does not include a signature. | | `unknown_signature_type` | A non-PGP signature was found in the commit. | | `no_user` | No user was associated with the `committer` email address in the commit. | | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. | | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. | | `unknown_key` | The key that made the signature has not been registered with any user's account. | | `malformed_signature` | There was an error parsing the signature. | | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the…
    public func listCommits(options: ReposMethods.ReposListCommitsOptions) async throws -> [Commit] {
        return try await ReposMethods.reposListCommits(config: config, options: options)
    }

/// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation. Returns all branches where the given commit SHA is the HEAD, or latest commit for the branch.
    public func listBranchesForHeadCommit(owner: String, repo: String, commitSha: String) async throws -> [BranchShort] {
        return try await ReposMethods.reposListBranchesForHeadCommit(config: config, owner: owner, repo: repo, commitSha: commitSha)
    }

/// Lists the comments for a specified commit. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type. - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`. - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`. - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
    public func listCommentsForCommit(owner: String, repo: String, commitSha: String, perPage: Int?, page: Int?) async throws -> [CommitComment] {
        return try await ReposMethods.reposListCommentsForCommit(config: config, owner: owner, repo: repo, commitSha: commitSha, perPage: perPage, page: page)
    }

/// Create a comment for a commit using its `:commit_sha`. Access to commit comments can be controlled by organization owners. For more information, see "[Managing commit comments for your organization](https://docs.github.com/organizations/managing-organization-settings/managing-commit-comments-for-your-organization)". This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)." This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type. - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`. - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`. - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
    public func createCommitComment(owner: String, repo: String, commitSha: String, body: String, path: String?, position: Int?, line: Int?) async throws -> CommitComment {
        return try await ReposMethods.reposCreateCommitComment(config: config, owner: owner, repo: repo, commitSha: commitSha, body: body, path: path, position: position, line: line)
    }
}

public extension ReposNamespace {
/// Lists the merged pull request that introduced the commit to the repository. If the commit is not present in the default branch, it will return merged and open pull requests associated with the commit. To list the open or merged pull requests associated with a branch, you can set the `commit_sha` parameter to the branch name.
    public func listPullRequestsAssociatedWithCommit(owner: String, repo: String, commitSha: String, perPage: Int?, page: Int?) async throws -> [PullRequestSimple] {
        return try await ReposMethods.reposListPullRequestsAssociatedWithCommit(config: config, owner: owner, repo: repo, commitSha: commitSha, perPage: perPage, page: page)
    }

/// Returns the contents of a single commit reference. You must have `read` access for the repository to use this endpoint. > [!NOTE] > If there are more than 300 files in the commit diff and the default JSON media type is requested, the response will include pagination link headers for the remaining files, up to a limit of 3000 files. Each page contains the static commit information, and the only changes are to the file listing. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." Pagination query parameters are not supported for these media types. - **`application/vnd.github.diff`**: Returns the diff of the commit. Larger diffs may time out and return a 5xx status code. - **`application/vnd.github.patch`**: Returns the patch of the commit. Diffs with binary data will have no `patch` property. Larger diffs may time out and return a 5xx status code. - **`application/vnd.github.sha`**: Returns the commit's SHA-1 hash. You can use this endpoint to check if a remote reference's SHA-1 hash is the same as your local reference's SHA-1 hash by providing the local SHA-1 reference as the ETag. **Signature verification object** The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object: | Name | Type | Description | | ---- | ---- | ----------- | | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. | | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. | | `signature` | `string` | The signature that was extracted from the commit. | | `payload` | `string` | The value that was signed. | | `verified_at` | `string` | The date the signature was verified by GitHub. | These are the possible values for `reason` in…
    public func getCommit(owner: String, repo: String, ref: String, page: Int?, perPage: Int?) async throws -> Commit {
        return try await ReposMethods.reposGetCommit(config: config, owner: owner, repo: repo, ref: ref, page: page, perPage: perPage)
    }

/// Users with pull access in a repository can access a combined view of commit statuses for a given ref. The ref can be a SHA, a branch name, or a tag name. Additionally, a combined `state` is returned. The `state` is one of: * **failure** if any of the contexts report as `error` or `failure` * **pending** if there are no statuses or a context is `pending` * **success** if the latest status for all contexts is `success`
    public func getCombinedStatusForRef(owner: String, repo: String, ref: String, perPage: Int?, page: Int?) async throws -> CombinedCommitStatus {
        return try await ReposMethods.reposGetCombinedStatusForRef(config: config, owner: owner, repo: repo, ref: ref, perPage: perPage, page: page)
    }

/// Users with pull access in a repository can view commit statuses for a given ref. The ref can be a SHA, a branch name, or a tag name. Statuses are returned in reverse chronological order. The first status in the list will be the latest one. This resource is also available via a legacy route: `GET /repos/:owner/:repo/statuses/:ref`.
    public func listCommitStatusesForRef(owner: String, repo: String, ref: String, perPage: Int?, page: Int?) async throws -> [Status] {
        return try await ReposMethods.reposListCommitStatusesForRef(config: config, owner: owner, repo: repo, ref: ref, perPage: perPage, page: page)
    }

/// Returns all community profile metrics for a repository. The repository cannot be a fork. The returned metrics include an overall health score, the repository description, the presence of documentation, the detected code of conduct, the detected license, and the presence of ISSUE\_TEMPLATE, PULL\_REQUEST\_TEMPLATE, README, and CONTRIBUTING files. The `health_percentage` score is defined as a percentage of how many of the recommended community health files are present. For more information, see "[About community profiles for public repositories](https://docs.github.com/communities/setting-up-your-project-for-healthy-contributions/about-community-profiles-for-public-repositories)." `content_reports_enabled` is only returned for organization-owned repositories.
    public func getCommunityProfileMetrics(owner: String, repo: String) async throws -> CommunityProfile {
        return try await ReposMethods.reposGetCommunityProfileMetrics(config: config, owner: owner, repo: repo)
    }

/// Compares two commits against one another. You can compare refs (branches or tags) and commit SHAs in the same repository, or you can compare refs and commit SHAs that exist in different repositories within the same repository network, including fork branches. For more information about how to view a repository's network, see "[Understanding connections between repositories](https://docs.github.com/repositories/viewing-activity-and-data-for-your-repository/understanding-connections-between-repositories)." This endpoint is equivalent to running the `git log BASE..HEAD` command, but it returns commits in a different order. The `git log BASE..HEAD` command returns commits in reverse chronological order, whereas the API returns commits in chronological order. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github.diff`**: Returns the diff of the commit. - **`application/vnd.github.patch`**: Returns the patch of the commit. Diffs with binary data will have no `patch` property. The API response includes details about the files that were changed between the two commits. This includes the status of the change (if a file was added, removed, modified, or renamed), and details of the change itself. For example, files with a `renamed` status have a `previous_filename` field showing the previous filename of the file, and files with a `modified` status have a `patch` field showing the changes made to the file. When calling this endpoint without any paging parameter (`per_page` or `page`), the returned list is limited to 250 commits, and the last commit in the list is the most recent of the entire comparison. **Working with large comparisons** To process a response with a large number of commits, use a query parameter (`per_page` or `page`) to paginate the results. When using pagination: - The list of changed files…
    public func compareCommits(owner: String, repo: String, basehead: String, page: Int?, perPage: Int?) async throws -> CommitComparison {
        return try await ReposMethods.reposCompareCommits(config: config, owner: owner, repo: repo, basehead: basehead, page: page, perPage: perPage)
    }

/// Retrieves a file, directory, symlink, or submodule from a repository. Supply the repository identifiers and the target `path`, with `ref` optionally selecting a commit, branch, or tag instead of the default branch. Directory responses are limited to 1,000 files, and files larger than 100 MB are not supported.
    ///
    /// Gets the contents of a file or directory in a repository. Specify the file path or directory with the `path` parameter. If you omit the `path` parameter, you will receive the contents of the repository's root directory. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github.raw+json`**: Returns the raw file contents for files and symlinks. - **`application/vnd.github.html+json`**: Returns the file contents in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup). - **`application/vnd.github.object+json`**: Returns the contents in a consistent object format regardless of the content type. For example, instead of an array of objects for a directory, the response will be an object with an `entries` attribute containing the array of objects. If the content is a directory, the response will be an array of objects, one object for each item in the directory. When listing the contents of a directory, submodules have their "type" specified as "file". Logically, the value _should_ be "submodule". This behavior exists [for backwards compatibility purposes](https://git.io/v1YCW). In the next major version of the API, the type will be returned as "submodule". If the content is a symlink and the symlink's target is a normal file in the repository, then the API responds with the content of the file. Otherwise, the API responds with an object describing the symlink itself. If the content is a submodule, the `submodule_git_url` field identifies the location of the submodule repository, and the `sha` identifies a specific commit within the submodule repository. Git uses the given URL when cloning the submodule repository, and checks out the submodule at that specific commit. If the submodule repository is not hosted on github.com, the Git URLs…
    public func getContent(owner: String, repo: String, path: String, ref: String?) async throws -> ContentTree {
        return try await ReposMethods.reposGetContent(config: config, owner: owner, repo: repo, path: path, ref: ref)
    }

/// Creates a new file or replaces an existing file in a repository. > [!NOTE] > If you use this endpoint and the "[Delete a file](https://docs.github.com/rest/repos/contents/#delete-a-file)" endpoint in parallel, the concurrent requests will conflict and you will receive errors. You must use these endpoints serially instead. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. The `workflow` scope is also required in order to modify files in the `.github/workflows` directory.
    public func createOrUpdateFileContents(options: ReposMethods.ReposCreateOrUpdateFileContentsOptions) async throws -> FileCommit {
        return try await ReposMethods.reposCreateOrUpdateFileContents(config: config, options: options)
    }
}

public extension ReposNamespace {
/// Deletes a file in a repository. You can provide an additional `committer` parameter, which is an object containing information about the committer. Or, you can provide an `author` parameter, which is an object containing information about the author. The `author` section is optional and is filled in with the `committer` information if omitted. If the `committer` information is omitted, the authenticated user's information is used. You must provide values for both `name` and `email`, whether you choose to use `author` or `committer`. Otherwise, you'll receive a `422` status code. > [!NOTE] > If you use this endpoint and the "[Create or update file contents](https://docs.github.com/rest/repos/contents/#create-or-update-file-contents)" endpoint in parallel, the concurrent requests will conflict and you will receive errors. You must use these endpoints serially instead.
    public func deleteFile(options: ReposMethods.ReposDeleteFileOptions) async throws -> FileCommit {
        return try await ReposMethods.reposDeleteFile(config: config, options: options)
    }

/// Lists contributors to the specified repository and sorts them by the number of commits per contributor in descending order. This endpoint may return information that is a few hours old because the GitHub REST API caches contributor data to improve performance. GitHub identifies contributors by author email address. This endpoint groups contribution counts by GitHub user, which includes all associated email addresses. To improve performance, only the first 500 author email addresses in the repository link to GitHub users. The rest will appear as anonymous contributors without associated GitHub user information.
    public func listContributors(owner: String, repo: String, anon: String?, perPage: Int?, page: Int?) async throws -> [Contributor] {
        return try await ReposMethods.reposListContributors(config: config, owner: owner, repo: repo, anon: anon, perPage: perPage, page: page)
    }

/// Simple filtering of deployments is available via query parameters:
    public func listDeployments(options: ReposMethods.ReposListDeploymentsOptions) async throws -> [Deployment] {
        return try await ReposMethods.reposListDeployments(config: config, options: options)
    }

/// Creates a deployment for a repository reference such as a branch, tag, or SHA. Supply `ref` and use `environment`, `task`, `required_contexts`, and merge settings to control how the deployment is prepared and validated. A deployment can trigger an automatic merge or return a merged-branch response when the requested reference is behind the default branch.
    ///
    /// Deployments offer a few configurable parameters with certain defaults. The `ref` parameter can be any named branch, tag, or SHA. At GitHub we often deploy branches and verify them before we merge a pull request. The `environment` parameter allows deployments to be issued to different runtime environments. Teams often have multiple environments for verifying their applications, such as `production`, `staging`, and `qa`. This parameter makes it easier to track which environments have requested deployments. The default environment is `production`. The `auto_merge` parameter is used to ensure that the requested ref is not behind the repository's default branch. If the ref _is_ behind the default branch for the repository, we will attempt to merge it for you. If the merge succeeds, the API will return a successful merge commit. If merge conflicts prevent the merge from succeeding, the API will return a failure response. By default, [commit statuses](https://docs.github.com/rest/commits/statuses) for every submitted context must be in a `success` state. The `required_contexts` parameter allows you to specify a subset of contexts that must be `success`, or to specify contexts that have not yet been submitted. You are not required to use commit statuses to deploy. If you do not require any contexts or create any commit statuses, the deployment will always succeed. The `payload` parameter is available for any extra information that a deployment system might need. It is a JSON text field that will be passed on when a deployment event is dispatched. The `task` parameter is used by the deployment system to allow different execution paths. In the web world this might be `deploy:migrations` to run schema changes on the system. In the compiled world this could be a flag to compile an application with debugging enabled. Merged branch response: You will see this response when GitHub automatically merges the base branch into the topic branch instead of creating a deployment. This…
    public func createDeployment(options: ReposMethods.ReposCreateDeploymentOptions) async throws -> Deployment {
        return try await ReposMethods.reposCreateDeployment(config: config, options: options)
    }

/// Retrieves a deployment for a repository by its numeric identifier. Use `owner`, `repo`, and `deployment_id` to identify the repository and deployment to retrieve. The response contains the deployment reference, environment, creator, payload, status links, and timestamps.
    public func getDeployment(owner: String, repo: String, deploymentId: Int) async throws -> Deployment {
        return try await ReposMethods.reposGetDeployment(config: config, owner: owner, repo: repo, deploymentId: deploymentId)
    }

/// If the repository only has one deployment, you can delete the deployment regardless of its status. If the repository has more than one deployment, you can only delete inactive deployments. This ensures that repositories with multiple deployments will always have an active deployment. To set a deployment as inactive, you must: * Create a new deployment that is active so that the system has a record of the current state, then delete the previously active deployment. * Mark the active deployment as inactive by adding any non-successful deployment status. For more information, see "[Create a deployment](https://docs.github.com/rest/deployments/deployments/#create-a-deployment)" and "[Create a deployment status](https://docs.github.com/rest/deployments/statuses#create-a-deployment-status)." OAuth app tokens and personal access tokens (classic) need the `repo` or `repo_deployment` scope to use this endpoint.
    public func deleteDeployment(owner: String, repo: String, deploymentId: Int) async throws -> SdkEmptyResponse {
        return try await ReposMethods.reposDeleteDeployment(config: config, owner: owner, repo: repo, deploymentId: deploymentId)
    }

/// Users with pull access can view deployment statuses for a deployment:
    public func listDeploymentStatuses(owner: String, repo: String, deploymentId: Int, perPage: Int?, page: Int?) async throws -> [DeploymentStatus] {
        return try await ReposMethods.reposListDeploymentStatuses(config: config, owner: owner, repo: repo, deploymentId: deploymentId, perPage: perPage, page: page)
    }

/// Users with `push` access can create deployment statuses for a given deployment. OAuth app tokens and personal access tokens (classic) need the `repo_deployment` scope to use this endpoint.
    public func createDeploymentStatus(options: ReposMethods.ReposCreateDeploymentStatusOptions) async throws -> DeploymentStatus {
        return try await ReposMethods.reposCreateDeploymentStatus(config: config, options: options)
    }
}

public extension ReposNamespace {
/// Retrieves a status for a specific deployment in a repository. Use `owner`, `repo`, `deployment_id`, and `status_id` to identify the deployment status. The response includes the status state, associated environment, URLs, creator, and timestamps.
    ///
    /// Users with pull access can view a deployment status for a deployment:
    public func getDeploymentStatus(owner: String, repo: String, deploymentId: Int, statusId: Int) async throws -> DeploymentStatus {
        return try await ReposMethods.reposGetDeploymentStatus(config: config, owner: owner, repo: repo, deploymentId: deploymentId, statusId: statusId)
    }

/// You can use this endpoint to trigger a webhook event called `repository_dispatch` when you want activity that happens outside of GitHub to trigger a GitHub Actions workflow or GitHub App webhook. You must configure your GitHub Actions workflow or GitHub App to run when the `repository_dispatch` event occurs. For an example `repository_dispatch` webhook payload, see "[RepositoryDispatchEvent](https://docs.github.com/webhooks/event-payloads/#repository_dispatch)." The `client_payload` parameter is available for any extra information that your workflow might need. This parameter is a JSON payload that will be passed on when the webhook event is dispatched. For example, the `client_payload` can include a message that a user would like to send using a GitHub Actions workflow. Or the `client_payload` can be used as a test to debug your workflow. This input example shows how you can use the `client_payload` as a test to debug your workflow. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createDispatchEvent(owner: String, repo: String, eventType: String, clientPayload: [String: JSONValue]?) async throws -> SdkEmptyResponse {
        return try await ReposMethods.reposCreateDispatchEvent(config: config, owner: owner, repo: repo, eventType: eventType, clientPayload: clientPayload)
    }

/// Lists the environments for a repository. Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getAllEnvironments(owner: String, repo: String, perPage: Int?, page: Int?) async throws -> ReposGetAllEnvironmentsResponse {
        return try await ReposMethods.reposGetAllEnvironments(config: config, owner: owner, repo: repo, perPage: perPage, page: page)
    }

/// > [!NOTE] > To get information about name patterns that branches must match in order to deploy to this environment, see "Get a deployment branch policy." Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getEnvironment(owner: String, repo: String, environmentName: String) async throws -> Environment {
        return try await ReposMethods.reposGetEnvironment(config: config, owner: owner, repo: repo, environmentName: environmentName)
    }

/// Create or update an environment with protection rules, such as required reviewers. For more information about environment protection rules, see "Environments." > [!NOTE] > To create or update name patterns that branches must match in order to deploy to this environment, see "Deployment branch policies." > [!NOTE] > To create or update secrets for an environment, see "GitHub Actions secrets." OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createOrUpdateEnvironment(owner: String, repo: String, environmentName: String, waitTimer: WaitTimer?, preventSelfReview: PreventSelfReview?, reviewers: SdkOptional<[ReposCreateOrUpdateEnvironmentRequestBodyReviewersItem]>?, deploymentBranchPolicy: SdkOptional<DeploymentBranchPolicySettings>?) async throws -> Environment {
        return try await ReposMethods.reposCreateOrUpdateEnvironment(config: config, owner: owner, repo: repo, environmentName: environmentName, waitTimer: waitTimer, preventSelfReview: preventSelfReview, reviewers: reviewers, deploymentBranchPolicy: deploymentBranchPolicy)
    }

/// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func deleteAnEnvironment(owner: String, repo: String, environmentName: String) async throws -> SdkEmptyResponse {
        return try await ReposMethods.reposDeleteAnEnvironment(config: config, owner: owner, repo: repo, environmentName: environmentName)
    }

/// Lists the deployment branch policies for an environment. Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func listDeploymentBranchPolicies(owner: String, repo: String, environmentName: String, perPage: Int?, page: Int?) async throws -> ReposListDeploymentBranchPoliciesResponse {
        return try await ReposMethods.reposListDeploymentBranchPolicies(config: config, owner: owner, repo: repo, environmentName: environmentName, perPage: perPage, page: page)
    }

/// Creates a deployment branch or tag policy for an environment. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createDeploymentBranchPolicy(owner: String, repo: String, environmentName: String, name: String, type: DeploymentBranchPolicyNamePatternWithTypeType?) async throws -> DeploymentBranchPolicy {
        return try await ReposMethods.reposCreateDeploymentBranchPolicy(config: config, owner: owner, repo: repo, environmentName: environmentName, name: name, type: type)
    }
}

public extension ReposNamespace {
/// Gets a deployment branch or tag policy for an environment. Anyone with read access to the repository can use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getDeploymentBranchPolicy(owner: String, repo: String, environmentName: String, branchPolicyId: Int) async throws -> DeploymentBranchPolicy {
        return try await ReposMethods.reposGetDeploymentBranchPolicy(config: config, owner: owner, repo: repo, environmentName: environmentName, branchPolicyId: branchPolicyId)
    }

/// Updates a deployment branch or tag policy for an environment. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func updateDeploymentBranchPolicy(owner: String, repo: String, environmentName: String, branchPolicyId: Int, name: String) async throws -> DeploymentBranchPolicy {
        return try await ReposMethods.reposUpdateDeploymentBranchPolicy(config: config, owner: owner, repo: repo, environmentName: environmentName, branchPolicyId: branchPolicyId, name: name)
    }

/// Deletes a deployment branch or tag policy for an environment. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func deleteDeploymentBranchPolicy(owner: String, repo: String, environmentName: String, branchPolicyId: Int) async throws -> SdkEmptyResponse {
        return try await ReposMethods.reposDeleteDeploymentBranchPolicy(config: config, owner: owner, repo: repo, environmentName: environmentName, branchPolicyId: branchPolicyId)
    }

/// Gets all custom deployment protection rules that are enabled for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)." For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app). OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getAllDeploymentProtectionRules(environmentName: String, repo: String, owner: String) async throws -> ReposGetAllDeploymentProtectionRulesResponse {
        return try await ReposMethods.reposGetAllDeploymentProtectionRules(config: config, environmentName: environmentName, repo: repo, owner: owner)
    }

/// Enable a custom deployment protection rule for an environment. The authenticated user must have admin or owner permissions to the repository to use this endpoint. For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app), as well as the [guide to creating custom deployment protection rules](https://docs.github.com/actions/managing-workflow-runs-and-deployments/managing-deployments/creating-custom-deployment-protection-rules). OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createDeploymentProtectionRule(environmentName: String, repo: String, owner: String, integrationId: Int?) async throws -> DeploymentProtectionRule {
        return try await ReposMethods.reposCreateDeploymentProtectionRule(config: config, environmentName: environmentName, repo: repo, owner: owner, integrationId: integrationId)
    }

/// Gets all custom deployment protection rule integrations that are available for an environment. The authenticated user must have admin or owner permissions to the repository to use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)." For more information about the app that is providing this custom deployment rule, see "[GET an app](https://docs.github.com/rest/apps/apps#get-an-app)". OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func listCustomDeploymentRuleIntegrations(environmentName: String, repo: String, owner: String, page: Int?, perPage: Int?) async throws -> ReposListCustomDeploymentRuleIntegrationsResponse {
        return try await ReposMethods.reposListCustomDeploymentRuleIntegrations(config: config, environmentName: environmentName, repo: repo, owner: owner, page: page, perPage: perPage)
    }

/// Gets an enabled custom deployment protection rule for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)." For more information about the app that is providing this custom deployment rule, see [`GET /apps/{app_slug}`](https://docs.github.com/rest/apps/apps#get-an-app). OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
    public func getCustomDeploymentProtectionRule(owner: String, repo: String, environmentName: String, protectionRuleId: Int) async throws -> DeploymentProtectionRule {
        return try await ReposMethods.reposGetCustomDeploymentProtectionRule(config: config, owner: owner, repo: repo, environmentName: environmentName, protectionRuleId: protectionRuleId)
    }

/// Disables a custom deployment protection rule for an environment. The authenticated user must have admin or owner permissions to the repository to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func disableDeploymentProtectionRule(environmentName: String, repo: String, owner: String, protectionRuleId: Int) async throws -> SdkEmptyResponse {
        return try await ReposMethods.reposDisableDeploymentProtectionRule(config: config, environmentName: environmentName, repo: repo, owner: owner, protectionRuleId: protectionRuleId)
    }
}
