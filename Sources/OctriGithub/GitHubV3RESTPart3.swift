// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class ClassroomNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Retrieves a classroom assignment by its unique identifier. This operation is closing down and will be removed on
    /// August 28, 2026.
    ///
    /// > [!WARNING] > **Closed notice:** This operation is no longer available as of August 28, 2026. > For more
    /// information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func getAnAssignment(assignmentId: Int) async throws -> SdkEmptyResponse {
        try await ClassroomMethods.classroomGetAnAssignment(config: config, assignmentId: assignmentId)
    }

    /// Lists the accepted assignments associated with a classroom assignment. This operation is closing down and will
    /// be removed on August 28, 2026.
    ///
    /// > [!WARNING] > **Closed notice:** This operation is no longer available as of August 28, 2026. > For more
    /// information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func listAcceptedAssignmentsForAnAssignment(assignmentId: Int) async throws -> SdkEmptyResponse {
        try await ClassroomMethods.classroomListAcceptedAssignmentsForAnAssignment(
            config: config,
            assignmentId: assignmentId
        )
    }

    /// Retrieves grades for a specific classroom assignment. Supply `assignment_id` to identify the assignment; this
    /// operation is no longer available. This operation is closing down and will be removed on August 28, 2026.
    ///
    /// > [!WARNING] > **Closed notice:** This operation is no longer available as of August 28, 2026. > For more
    /// information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func getAssignmentGrades(assignmentId: Int) async throws -> SdkEmptyResponse {
        try await ClassroomMethods.classroomGetAssignmentGrades(config: config, assignmentId: assignmentId)
    }

    /// Lists classrooms available through the Classroom service. This operation is no longer available, so you cannot
    /// use it to retrieve classroom data. This operation is closing down and will be removed on August 28, 2026.
    ///
    /// > [!WARNING] > **Closed notice:** This operation is no longer available as of August 28, 2026. > For more
    /// information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func listClassrooms() async throws -> SdkEmptyResponse {
        try await ClassroomMethods.classroomListClassrooms(config: config)
    }

    /// Retrieves a specific classroom by its identifier. Supply `classroom_id` to identify the classroom, but note that
    /// this operation is no longer available. This operation is closing down and will be removed on August 28, 2026.
    ///
    /// > [!WARNING] > **Closed notice:** This operation is no longer available as of August 28, 2026. > For more
    /// information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func classroomGetA(classroomId: Int) async throws -> SdkEmptyResponse {
        try await ClassroomMethods.classroomGetAClassroom(config: config, classroomId: classroomId)
    }

    /// Lists assignments associated with a specific classroom. Supply `classroom_id` to identify the classroom, but
    /// note that this operation is no longer available. This operation is closing down and will be removed on August
    /// 28, 2026.
    ///
    /// > [!WARNING] > **Closed notice:** This operation is no longer available as of August 28, 2026. > For more
    /// information, see the [GitHub Classroom sunset notice](https://gh.io/classroom-sunset).
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func classroomListAssignmentsForA(classroomId: Int) async throws -> SdkEmptyResponse {
        try await ClassroomMethods.classroomListAssignmentsForAClassroom(config: config, classroomId: classroomId)
    }
}

public class CodesOfConductNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists all of GitHub's available codes of conduct. Use the returned collection to inspect each code's `key`,
    /// `name`, `url`, `body`, and `html_url` values.
    ///
    /// Returns array of all GitHub's codes of conduct.
    public func codesOfConductGetAll() async throws -> [CodeOfConduct] {
        try await CodesOfConductMethods.codesOfConductGetAllCodesOfConduct(config: config)
    }

    /// Retrieves a specific code of conduct by its key. Supply `key` to select the code you want to inspect, including
    /// its name, URLs, and body when available.
    ///
    /// Returns information about the specified GitHub code of conduct.
    public func getConductCode(key: String) async throws -> CodeOfConduct {
        try await CodesOfConductMethods.codesOfConductGetConductCode(config: config, key: key)
    }
}

public class CredentialsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Submit a list of credentials to be revoked. This endpoint is intended to revoke credentials the caller does not
    /// own and may have found exposed on GitHub.com or elsewhere. It can also be used for credentials associated with
    /// an old user account that you no longer have access to. Credential owners will be notified of the revocation.
    /// This endpoint currently accepts the following credential types: - Personal access tokens (classic) (`ghp_`) -
    /// Fine-grained personal access tokens (`github_pat_`) - OAuth app access tokens (`gho_`) - User-to-server tokens
    /// from GitHub Apps (`ghu_`) - Refresh tokens from GitHub Apps (`ghr_`) Revoked credentials may impact users on
    /// GitHub Free, Pro, & Team and GitHub Enterprise Cloud, and GitHub Enterprise Cloud with Enterprise Managed Users.
    /// GitHub cannot reactivate any credentials that have been revoked; new credentials will need to be generated. To
    /// prevent abuse, this API is limited to only 60 unauthenticated requests per hour and a max of 1000 tokens per API
    /// request. > [!NOTE] > Any authenticated requests will return a 403.
    public func revoke(credentials: [String]) async throws -> [String: JSONValue] {
        try await CredentialsMethods.credentialsRevoke(config: config, credentials: credentials)
    }
}

public class EmojisNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Lists all emojis available to use on GitHub. The response maps each emoji name to its corresponding image URL,
    /// so you can select the representation you need.
    ///
    /// Lists all the emojis available to use on GitHub.
    public func get() async throws -> [String: String] {
        try await EmojisMethods.emojisGet(config: config)
    }
}

public class ActionsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Gets GitHub Actions cache retention limit for an enterprise. All organizations and repositories under this
    /// enterprise may not set a higher cache retention limit. OAuth tokens and personal access tokens (classic) need
    /// the `admin:enterprise` scope to use this endpoint.
    public func actionsGetCacheRetentionLimitForEnterprise(enterprise: String) async throws
        -> ActionsCacheRetentionLimitForEnterprise {
        try await ActionsMethods.actionsGetActionsCacheRetentionLimitForEnterprise(
            config: config,
            enterprise: enterprise
        )
    }

    /// Sets GitHub Actions cache retention limit for an enterprise. All organizations and repositories under this
    /// enterprise may not set a higher cache retention limit. OAuth tokens and personal access tokens (classic) need
    /// the `admin:enterprise` scope to use this endpoint.
    public func actionsSetCacheRetentionLimitForEnterprise(
        enterprise: String,
        maxCacheRetentionDays: Int?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetActionsCacheRetentionLimitForEnterprise(
            config: config,
            enterprise: enterprise,
            maxCacheRetentionDays: maxCacheRetentionDays
        )
    }

    /// Gets GitHub Actions cache storage limit for an enterprise. All organizations and repositories under this
    /// enterprise may not set a higher cache storage limit. OAuth tokens and personal access tokens (classic) need the
    /// `admin:enterprise` scope to use this endpoint.
    public func actionsGetCacheStorageLimitForEnterprise(enterprise: String) async throws
        -> ActionsCacheStorageLimitForEnterprise {
        try await ActionsMethods.actionsGetActionsCacheStorageLimitForEnterprise(config: config, enterprise: enterprise)
    }

    /// Sets GitHub Actions cache storage limit for an enterprise. All organizations and repositories under this
    /// enterprise may not set a higher cache storage limit. OAuth tokens and personal access tokens (classic) need the
    /// `admin:enterprise` scope to use this endpoint.
    public func actionsSetCacheStorageLimitForEnterprise(
        enterprise: String,
        maxCacheSizeGb: Int?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetActionsCacheStorageLimitForEnterprise(
            config: config,
            enterprise: enterprise,
            maxCacheSizeGb: maxCacheSizeGb
        )
    }

    /// Gets GitHub Actions cache retention limit for an organization. All repositories under this organization may not
    /// set a higher cache retention limit. OAuth tokens and personal access tokens (classic) need the
    /// `admin:organization` scope to use this endpoint.
    public func actionsGetCacheRetentionLimitForOrganization(org: String) async throws
        -> ActionsCacheRetentionLimitForOrganization {
        try await ActionsMethods.actionsGetActionsCacheRetentionLimitForOrganization(config: config, org: org)
    }

    /// Sets GitHub Actions cache retention limit for an organization. All repositories under this organization may not
    /// set a higher cache retention limit. OAuth tokens and personal access tokens (classic) need the
    /// `admin:organization` scope to use this endpoint.
    public func actionsSetCacheRetentionLimitForOrganization(
        org: String,
        maxCacheRetentionDays: Int?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetActionsCacheRetentionLimitForOrganization(
            config: config,
            org: org,
            maxCacheRetentionDays: maxCacheRetentionDays
        )
    }

    /// Gets GitHub Actions cache storage limit for an organization. All repositories under this organization may not
    /// set a higher cache storage limit. OAuth tokens and personal access tokens (classic) need the
    /// `admin:organization` scope to use this endpoint.
    public func actionsGetCacheStorageLimitForOrganization(org: String) async throws
        -> ActionsCacheStorageLimitForOrganization {
        try await ActionsMethods.actionsGetActionsCacheStorageLimitForOrganization(config: config, org: org)
    }

    /// Sets GitHub Actions cache storage limit for an organization. All organizations and repositories under this
    /// organization may not set a higher cache storage limit. OAuth tokens and personal access tokens (classic) need
    /// the `admin:organization` scope to use this endpoint.
    public func actionsSetCacheStorageLimitForOrganization(
        org: String,
        maxCacheSizeGb: Int?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetActionsCacheStorageLimitForOrganization(
            config: config,
            org: org,
            maxCacheSizeGb: maxCacheSizeGb
        )
    }
}

public extension ActionsNamespace {
    /// Gets the total GitHub Actions cache usage for an organization. The data fetched using this API is refreshed
    /// approximately every 5 minutes, so values returned from this endpoint may take at least 5 minutes to get updated.
    /// OAuth tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
    func actionsGetCacheUsageForOrg(org: String) async throws -> ActionsCacheUsageOrgEnterprise {
        try await ActionsMethods.actionsGetActionsCacheUsageForOrg(config: config, org: org)
    }

    /// Lists repositories and their GitHub Actions cache usage for an organization. The data fetched using this API is
    /// refreshed approximately every 5 minutes, so values returned from this endpoint may take at least 5 minutes to
    /// get updated. OAuth tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
    func actionsGetCacheUsageByRepoForOrg(
        org: String,
        perPage: Int?,
        page: Int?
    ) async throws -> ActionsGetActionsCacheUsageByRepoForOrgResponse {
        try await ActionsMethods.actionsGetActionsCacheUsageByRepoForOrg(
            config: config,
            org: org,
            perPage: perPage,
            page: page
        )
    }

    /// Lists all GitHub-hosted runners configured in an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `manage_runner:org` scope to use this endpoint.
    func listHostedRunnersForOrg(
        org: String,
        perPage: Int?,
        page: Int?
    ) async throws -> ActionsListHostedRunnersForOrgResponse {
        try await ActionsMethods.actionsListHostedRunnersForOrg(config: config, org: org, perPage: perPage, page: page)
    }

    /// Creates a GitHub-hosted runner for an organization. OAuth tokens and personal access tokens (classic) need the
    /// `manage_runners:org` scope to use this endpoint.
    func createHostedRunnerForOrg(options: ActionsMethods
        .ActionsCreateHostedRunnerForOrgOptions) async throws -> ActionsHostedRunner {
        try await ActionsMethods.actionsCreateHostedRunnerForOrg(config: config, options: options)
    }

    /// List custom images for an organization. OAuth tokens and personal access tokens (classic) need the
    /// `manage_runners:org` scope to use this endpoint.
    func listCustomImagesForOrg(org: String) async throws -> ActionsListCustomImagesForOrgResponse {
        try await ActionsMethods.actionsListCustomImagesForOrg(config: config, org: org)
    }

    /// Get a custom image definition for GitHub Actions Hosted Runners. OAuth tokens and personal access tokens
    /// (classic) need the `manage_runners:org` scope to use this endpoint.
    func getCustomImageForOrg(org: String, imageDefinitionId: Int) async throws -> ActionsHostedRunnerCustomImage {
        try await ActionsMethods.actionsGetCustomImageForOrg(
            config: config,
            org: org,
            imageDefinitionId: imageDefinitionId
        )
    }

    /// Delete a custom image from the organization. OAuth tokens and personal access tokens (classic) need the
    /// `manage_runners:org` scope to use this endpoint.
    func deleteCustomImageFromOrg(org: String, imageDefinitionId: Int) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsDeleteCustomImageFromOrg(
            config: config,
            org: org,
            imageDefinitionId: imageDefinitionId
        )
    }

    /// List image versions of a custom image for an organization. OAuth tokens and personal access tokens (classic)
    /// need the `manage_runners:org` scope to use this endpoint.
    func listCustomImageVersionsForOrg(
        imageDefinitionId: Int,
        org: String
    ) async throws -> ActionsListCustomImageVersionsForOrgResponse {
        try await ActionsMethods.actionsListCustomImageVersionsForOrg(
            config: config,
            imageDefinitionId: imageDefinitionId,
            org: org
        )
    }
}
