// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

extension TeamsNamespace {
/// Add or update team membership for a user (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Add or update team membership for a user](https://docs.github.com/rest/teams/members#add-or-update-team-membership-for-a-user) endpoint. Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation. If the user is already a member of the team's organization, this endpoint will add the user to the team. To add a membership between an organization member and a team, the authenticated user must be an organization owner or a team maintainer. > [!NOTE] > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)." If the user is unaffiliated with the team's organization, this endpoint will send an invitation to the user via email. This newly-created membership will be in the "pending" state until the user accepts the invitation, at which point the membership will transition to the "active" state and the user will be added as a member of the team. To add a membership between an unaffiliated user and a team, the authenticated user must be an organization owner. If the user is already a member of the team, this endpoint will update the role of the team member's role. To update the membership of a team…
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func addOrUpdateMembershipForUserLegacy(teamId: Int, username: String, role: TeamsAddOrUpdateMembershipForUserLegacyRequestBodyRole?) async throws -> TeamMembership {
        return try await TeamsMethods.teamsAddOrUpdateMembershipForUserLegacy(config: config, teamId: teamId, username: username, role: role)
    }

/// Remove team membership for a user (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Remove team membership for a user](https://docs.github.com/rest/teams/members#remove-team-membership-for-a-user) endpoint. Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation. To remove a membership between a user and a team, the authenticated user must have 'admin' permissions to the team or be an owner of the organization that the team is associated with. Removing team membership does not delete the user, it just removes their membership from the team. > [!NOTE] > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func removeMembershipForUserLegacy(teamId: Int, username: String) async throws -> SdkEmptyResponse {
        return try await TeamsMethods.teamsRemoveMembershipForUserLegacy(config: config, teamId: teamId, username: username)
    }

/// List team repositories (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [List team repositories](https://docs.github.com/rest/teams/teams#list-team-repositories) endpoint.
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func listReposLegacy(teamId: Int, perPage: Int?, page: Int?) async throws -> [MinimalRepository] {
        return try await TeamsMethods.teamsListReposLegacy(config: config, teamId: teamId, perPage: perPage, page: page)
    }

/// Check team permissions for a repository (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Check team permissions for a repository](https://docs.github.com/rest/teams/teams#check-team-permissions-for-a-repository) endpoint. > [!NOTE] > Repositories inherited through a parent team will also be checked. You can also get information about the specified repository, including what permissions the team grants on it, by passing the following custom [media type](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types/) via the `Accept` header:
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func checkPermissionsForRepoLegacy(teamId: Int, owner: String, repo: String) async throws -> TeamRepository {
        return try await TeamsMethods.teamsCheckPermissionsForRepoLegacy(config: config, teamId: teamId, owner: owner, repo: repo)
    }

/// Add or update team repository permissions (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new "[Add or update team repository permissions](https://docs.github.com/rest/teams/teams#add-or-update-team-repository-permissions)" endpoint. To add a repository to a team or update the team's permission on a repository, the authenticated user must have admin access to the repository, and must be able to see the team. The repository must be owned by the organization, or a direct fork of a repository owned by the organization. You will get a `422 Unprocessable Entity` status if you attempt to add a repository to a team that is not owned by the organization. Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func addOrUpdateRepoPermissionsLegacy(teamId: Int, owner: String, repo: String, permission: TeamsAddOrUpdateRepoPermissionsLegacyRequestBodyPermission?) async throws -> SdkEmptyResponse {
        return try await TeamsMethods.teamsAddOrUpdateRepoPermissionsLegacy(config: config, teamId: teamId, owner: owner, repo: repo, permission: permission)
    }

/// Remove a repository from a team (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Remove a repository from a team](https://docs.github.com/rest/teams/teams#remove-a-repository-from-a-team) endpoint. If the authenticated user is an organization owner or a team maintainer, they can remove any repositories from the team. To remove a repository from a team as an organization member, the authenticated user must have admin access to the repository and must be able to see the team. NOTE: This does not delete the repository, it just removes it from the team.
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func removeRepoLegacy(teamId: Int, owner: String, repo: String) async throws -> SdkEmptyResponse {
        return try await TeamsMethods.teamsRemoveRepoLegacy(config: config, teamId: teamId, owner: owner, repo: repo)
    }

/// List child teams (Legacy)
    ///
    /// > [!WARNING] > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [`List child teams`](https://docs.github.com/rest/teams/teams#list-child-teams) endpoint.
    ///
    /// - Warning: This operation is deprecated and may be removed in a future release.
    public func listChildLegacy(teamId: Int, perPage: Int?, page: Int?) async throws -> [Team] {
        return try await TeamsMethods.teamsListChildLegacy(config: config, teamId: teamId, perPage: perPage, page: page)
    }

/// List teams for the authenticated user
    ///
    /// List all of the teams across all of the organizations to which the authenticated user belongs. OAuth app tokens and personal access tokens (classic) need the `user`, `repo`, or `read:org` scope to use this endpoint. When using a fine-grained personal access token, the resource owner of the token must be a single organization, and the response will only include the teams from that organization.
    public func listForAuthenticatedUser(perPage: Int?, page: Int?) async throws -> [TeamFull] {
        return try await TeamsMethods.teamsListForAuthenticatedUser(config: config, perPage: perPage, page: page)
    }
}

public class RateLimitNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// > [!NOTE] > Accessing this endpoint does not count against your REST API rate limit. Some categories of endpoints have custom rate limits that are separate from the rate limit governing the other REST API endpoints. For this reason, the API response categorizes your rate limit. Under `resources`, you'll see objects relating to different categories: * The `core` object provides your rate limit status for all non-search-related resources in the REST API. * The `search` object provides your rate limit status for the REST API for searching (excluding code searches). For more information, see "[Search](https://docs.github.com/rest/search/search)." * The `code_search` object provides your rate limit status for the REST API for searching code. For more information, see "[Search code](https://docs.github.com/rest/search/search#search-code)." * The `graphql` object provides your rate limit status for the GraphQL API. For more information, see "[Resource limitations](https://docs.github.com/graphql/overview/resource-limitations#rate-limit)." * The `integration_manifest` object provides your rate limit status for the `POST /app-manifests/{code}/conversions` operation. For more information, see "[Creating a GitHub App from a manifest](https://docs.github.com/apps/creating-github-apps/setting-up-a-github-app/creating-a-github-app-from-a-manifest#3-you-exchange-the-temporary-code-to-retrieve-the-app-configuration)." * The `dependency_snapshots` object provides your rate limit status for submitting snapshots to the dependency graph. For more information, see "[Dependency graph](https://docs.github.com/rest/dependency-graph)." * The `dependency_sbom` object provides your rate limit status for requesting SBOMs from the dependency graph. For more information, see "[Dependency graph](https://docs.github.com/rest/dependency-graph)." * The `actions_runner_registration` object provides your rate limit status for registering self-hosted runners in GitHub Actions. For more information,…
    public func get() async throws -> RateLimitOverview {
        return try await RateLimitMethods.rateLimitGet(config: config)
    }
}

public class ChecksNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Creates a new check run for a specific commit in a repository. To create a check run, you must use a GitHub App. OAuth apps and authenticated users are not able to create a check suite. In a check suite, GitHub limits the number of check runs with the same name to 1000. Once these check runs exceed 1000, GitHub will start to automatically delete older check runs. > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array.
    public func create(options: ChecksMethods.ChecksCreateOptions) async throws -> CheckRun {
        return try await ChecksMethods.checksCreate(config: config, options: options)
    }

/// Gets a single check run using its `id`. > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
    public func get(owner: String, repo: String, checkRunId: Int) async throws -> CheckRun {
        return try await ChecksMethods.checksGet(config: config, owner: owner, repo: repo, checkRunId: checkRunId)
    }

/// Updates a check run for a specific commit in a repository. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. OAuth apps and personal access tokens (classic) cannot use this endpoint.
    public func update(options: ChecksMethods.ChecksUpdateOptions) async throws -> CheckRun {
        return try await ChecksMethods.checksUpdate(config: config, options: options)
    }

/// Lists annotations for a check run using the annotation `id`. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
    public func listAnnotations(owner: String, repo: String, checkRunId: Int, perPage: Int?, page: Int?) async throws -> [CheckAnnotation] {
        return try await ChecksMethods.checksListAnnotations(config: config, owner: owner, repo: repo, checkRunId: checkRunId, perPage: perPage, page: page)
    }

/// Triggers GitHub to rerequest an existing check run, without pushing new code to a repository. This endpoint will trigger the [`check_run` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) event with the action `rerequested`. When a check run is `rerequested`, the `status` of the check suite it belongs to is reset to `queued` and the `conclusion` is cleared. The check run itself is not updated. GitHub apps recieving the [`check_run` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) with the `rerequested` action should then decide if the check run should be reset or updated and call the [update `check_run` endpoint](https://docs.github.com/rest/checks/runs#update-a-check-run) to update the check_run if desired. For more information about how to re-run GitHub Actions jobs, see "[Re-run a job from a workflow run](https://docs.github.com/rest/actions/workflow-runs#re-run-a-job-from-a-workflow-run)".
    public func rerequestRun(owner: String, repo: String, checkRunId: Int) async throws -> EmptyObject {
        return try await ChecksMethods.checksRerequestRun(config: config, owner: owner, repo: repo, checkRunId: checkRunId)
    }

/// Creates a check suite manually. By default, check suites are automatically created when you create a [check run](https://docs.github.com/rest/checks/runs). You only need to use this endpoint for manually creating check suites when you've disabled automatic creation using "[Update repository preferences for check suites](https://docs.github.com/rest/checks/suites#update-repository-preferences-for-check-suites)". > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`. OAuth apps and personal access tokens (classic) cannot use this endpoint.
    public func createSuite(owner: String, repo: String, headSha: String) async throws -> CheckSuite {
        return try await ChecksMethods.checksCreateSuite(config: config, owner: owner, repo: repo, headSha: headSha)
    }

/// Changes the default automatic flow when creating check suites. By default, a check suite is automatically created each time code is pushed to a repository. When you disable the automatic creation of check suites, you can manually [Create a check suite](https://docs.github.com/rest/checks/suites#create-a-check-suite). You must have admin permissions in the repository to set preferences for check suites.
    public func setSuitesPreferences(owner: String, repo: String, autoTriggerChecks: [ChecksSetSuitesPreferencesRequestBodyAutoTriggerChecksItem]?) async throws -> CheckSuitePreference {
        return try await ChecksMethods.checksSetSuitesPreferences(config: config, owner: owner, repo: repo, autoTriggerChecks: autoTriggerChecks)
    }

/// Gets a single check suite using its `id`. > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
    public func getSuite(owner: String, repo: String, checkSuiteId: Int) async throws -> CheckSuite {
        return try await ChecksMethods.checksGetSuite(config: config, owner: owner, repo: repo, checkSuiteId: checkSuiteId)
    }
}

extension ChecksNamespace {
/// Lists check runs for a check suite using its `id`. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
    public func listForSuite(options: ChecksMethods.ChecksListForSuiteOptions) async throws -> ChecksListForSuiteResponse {
        return try await ChecksMethods.checksListForSuite(config: config, options: options)
    }

/// Triggers GitHub to rerequest an existing check suite, without pushing new code to a repository. This endpoint will trigger the [`check_suite` webhook](https://docs.github.com/webhooks/event-payloads/#check_suite) event with the action `rerequested`. When a check suite is `rerequested`, its `status` is reset to `queued` and the `conclusion` is cleared.
    public func rerequestSuite(owner: String, repo: String, checkSuiteId: Int) async throws -> EmptyObject {
        return try await ChecksMethods.checksRerequestSuite(config: config, owner: owner, repo: repo, checkSuiteId: checkSuiteId)
    }

/// Lists check runs associated with a commit reference in a repository. Use `ref` to identify a commit SHA, branch, or tag, and use `check_name`, `status`, `filter`, or `app_id` to narrow the results; use `page` and `per_page` to paginate the results. When a reference has more than 1,000 check suites, results are limited to the 1,000 most recent suites, so use the check-suite endpoints to retrieve all check runs.
    ///
    /// Lists check runs for a commit ref. The `ref` can be a SHA, branch name, or a tag name. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. If there are more than 1000 check suites on a single git reference, this endpoint will limit check runs to the 1000 most recent check suites. To iterate over all possible check runs, use the [List check suites for a Git reference](https://docs.github.com/rest/reference/checks#list-check-suites-for-a-git-reference) endpoint and provide the `check_suite_id` parameter to the [List check runs in a check suite](https://docs.github.com/rest/reference/checks#list-check-runs-in-a-check-suite) endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
    public func listForRef(options: ChecksMethods.ChecksListForRefOptions) async throws -> ChecksListForRefResponse {
        return try await ChecksMethods.checksListForRef(config: config, options: options)
    }

/// Lists check suites for a commit `ref`. The `ref` can be a SHA, branch name, or a tag name. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
    public func listSuitesForRef(owner: String, repo: String, ref: String, appId: Int?, checkName: String?, perPage: Int?, page: Int?) async throws -> ChecksListSuitesForRefResponse {
        return try await ChecksMethods.checksListSuitesForRef(config: config, owner: owner, repo: repo, ref: ref, appId: appId, checkName: checkName, perPage: perPage, page: page)
    }
}

public class CodeQualityNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists code quality findings for a repository. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func listFindingsForRepo(owner: String, repo: String, perPage: Int?, direction: SecurityAdvisoriesListGlobalAdvisoriesParameter?, before: String?, after: String?, state: CodeQualityListFindingsForRepoParameter?) async throws -> [CodeQualityFinding] {
        return try await CodeQualityMethods.codeQualityListFindingsForRepo(config: config, owner: owner, repo: repo, perPage: perPage, direction: direction, before: before, after: after, state: state)
    }

/// Gets a single code quality finding. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getFinding(owner: String, repo: String, findingNumber: Int) async throws -> CodeQualityFinding {
        return try await CodeQualityMethods.codeQualityGetFinding(config: config, owner: owner, repo: repo, findingNumber: findingNumber)
    }

/// Gets a code quality setup configuration. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getSetup(owner: String, repo: String) async throws -> CodeQualitySetup {
        return try await CodeQualityMethods.codeQualityGetSetup(config: config, owner: owner, repo: repo)
    }

/// Updates a code quality setup configuration. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func updateSetup(owner: String, repo: String, state: CodeQualitySetupUpdateState?, runnerType: CodeQualitySetupUpdateRunnerType?, runnerLabel: SdkOptional<String>?, languages: [CodeQualitySetupUpdateLanguagesItem]?, aiFindingsOption: CodeQualitySetupUpdateAiFindingsOption?) async throws -> EmptyObject {
        return try await CodeQualityMethods.codeQualityUpdateSetup(config: config, owner: owner, repo: repo, state: state, runnerType: runnerType, runnerLabel: runnerLabel, languages: languages, aiFindingsOption: aiFindingsOption)
    }
}
