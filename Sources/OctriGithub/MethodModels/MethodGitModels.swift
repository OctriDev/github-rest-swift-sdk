// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif

/// Canonical git operation model declarations
/// An object with information about the individual creating the tag.
public struct GitCreateTagRequestBodyTagger: Codable {
    /// The name of the author of the tag
    public var name: String
    /// The email of the author of the tag
    public var email: String
    /// When this object was tagged. This is a timestamp in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
    public var date: Date?

    enum CodingKeys: String, CodingKey {
        case name
        case email
        case date
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension GitCreateTagRequestBodyTagger {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.email) else {
            throw SdkValidationError(
                field: "email",
                code: "required",
                message: "Validation failed for 'email': value is required"
            )
        }
        name = try container.sdkDecodeRequired(.name)
        email = try container.sdkDecodeRequired(.email)
        date = try container.sdkDecodeIfPresent(.date)
        if let value = date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension GitCreateTagRequestBodyTagger {
    init(name: String, email: String, date: Date? = nil) throws {
        (self.name, self.email) = (name, email)
        self.date = date
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

/// Information about the person who is making the commit. By default, `committer` will use the information set in
/// `author`. See the `author` and `committer` object below for details.
public struct GitCreateCommitRequestBodyCommitter: Codable {
    /// The name of the author (or committer) of the commit
    public var name: String?
    /// The email of the author (or committer) of the commit
    public var email: String?
    /// Indicates when this commit was authored (or committed). This is a timestamp in ISO 8601 format:
    /// `YYYY-MM-DDTHH:MM:SSZ`.
    public var date: Date?

    enum CodingKeys: String, CodingKey {
        case name
        case email
        case date
    }

    init() {
        (name, email, date) = (nil, nil, nil)
    }
}

public extension GitCreateCommitRequestBodyCommitter {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        name = try container.sdkDecodeIfPresent(.name)
        email = try container.sdkDecodeIfPresent(.email)
        date = try container.sdkDecodeIfPresent(.date)
        if let value = date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension GitCreateCommitRequestBodyCommitter {
    init(name: String? = nil, email: String? = nil, date: Date? = nil) throws {
        self.init()
        (self.name, self.email) = (name, email)
        self.date = date
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public enum GitCreateBlobResponse {
    case validationError(ValidationError)
    case repositoryRuleViolationError(RepositoryRuleViolationError)
}

extension GitCreateBlobResponse: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for GitCreateBlobResponse"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(ValidationError.self) {
            return .validationError(value)
        }
        if let value = try? container
            .decode(RepositoryRuleViolationError.self) {
            return .repositoryRuleViolationError(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .validationError(value): try container.encode(value); return true
        case let .repositoryRuleViolationError(value): try container.encode(value); return true
        }
    }
}

public struct GitCreateTreeRequestBodyTreeItem: Codable {
    /// The file referenced in the tree.
    public var path: String?
    /// The file mode; one of `100644` for file (blob), `100755` for executable (blob), `040000` for subdirectory
    /// (tree), `160000` for submodule (commit), or `120000` for a blob that specifies the path of a symlink.
    public var mode: GitCreateTreeRequestBodyTreeItemMode?
    /// Either `blob`, `tree`, or `commit`.
    public var type: GitCreateTreeRequestBodyTreeItemType?
    /// The SHA1 checksum ID of the object in the tree. Also called `tree.sha`. If the value is `null` then the file
    /// will be deleted. **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using
    /// both `tree.sha` and `content` will return an error.
    public var sha: String?
    /// The content you want this file to have. GitHub will write this blob out and use that SHA for this entry. Use
    /// either this, or `tree.sha`. **Note:** Use either `tree.sha` or `content` to specify the contents of the
    /// entry. Using both `tree.sha` and `content` will return an error.
    public var content: String?

    enum CodingKeys: String, CodingKey {
        case path
        case mode
        case type
        case sha
        case content
    }

    init() {
        (path, mode, type, sha, content) = (nil, nil, nil, nil, nil)
    }
}

public extension GitCreateTreeRequestBodyTreeItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        path = try container.sdkDecodeIfPresent(.path)
        mode = try container.sdkDecodeIfPresent(.mode)
        type = try container.sdkDecodeIfPresent(.type)
        sha = try container.sdkDecodeIfPresent(.sha)
        content = try container.sdkDecodeIfPresent(.content)
    }
}

public extension GitCreateTreeRequestBodyTreeItem {
    init(
        path: String? = nil,
        mode: GitCreateTreeRequestBodyTreeItemMode? = nil,
        type: GitCreateTreeRequestBodyTreeItemType? = nil,
        sha: String? = nil,
        content: String? = nil
    ) {
        self.init()
        (self.path, self.mode) = (path, mode)
        (self.type, self.sha) = (type, sha)
        self.content = content
    }
}

/// Information about the author of the commit. By default, the `author` will be the authenticated user and the
/// current date. See the `author` and `committer` object below for details.
public struct GitCreateCommitRequestBodyAuthor: Codable {
    /// The name of the author (or committer) of the commit
    public var name: String
    /// The email of the author (or committer) of the commit
    public var email: String
    /// Indicates when this commit was authored (or committed). This is a timestamp in ISO 8601 format:
    /// `YYYY-MM-DDTHH:MM:SSZ`.
    public var date: Date?

    enum CodingKeys: String, CodingKey {
        case name
        case email
        case date
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension GitCreateCommitRequestBodyAuthor {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.email) else {
            throw SdkValidationError(
                field: "email",
                code: "required",
                message: "Validation failed for 'email': value is required"
            )
        }
        name = try container.sdkDecodeRequired(.name)
        email = try container.sdkDecodeRequired(.email)
        date = try container.sdkDecodeIfPresent(.date)
        if let value = date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension GitCreateCommitRequestBodyAuthor {
    init(name: String, email: String, date: Date? = nil) throws {
        (self.name, self.email) = (name, email)
        self.date = date
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}
