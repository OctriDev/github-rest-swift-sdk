// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public extension ActionsNamespace {
    /// Get an image version of a custom image for GitHub Actions Hosted Runners. OAuth tokens and personal access
    /// tokens (classic) need the `manage_runners:org` scope to use this endpoint.
    func getCustomImageVersionForOrg(
        org: String,
        imageDefinitionId: Int,
        version: String
    ) async throws -> ActionsHostedRunnerCustomImageVersion {
        try await ActionsMethods.actionsGetCustomImageVersionForOrg(
            config: config,
            org: org,
            imageDefinitionId: imageDefinitionId,
            version: version
        )
    }

    /// Delete an image version of custom image from the organization. OAuth tokens and personal access tokens (classic)
    /// need the `manage_runners:org` scope to use this endpoint.
    func deleteCustomImageVersionFromOrg(
        org: String,
        imageDefinitionId: Int,
        version: String
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsDeleteCustomImageVersionFromOrg(
            config: config,
            org: org,
            imageDefinitionId: imageDefinitionId,
            version: version
        )
    }

    /// Get the list of GitHub-owned images available for GitHub-hosted runners for an organization.
    func getHostedRunnersGithubOwnedImagesForOrg(org: String) async throws
        -> ActionsGetHostedRunnersGithubOwnedImagesForOrgResponse {
        try await ActionsMethods.actionsGetHostedRunnersGithubOwnedImagesForOrg(config: config, org: org)
    }

    /// Get the list of partner images available for GitHub-hosted runners for an organization.
    func getHostedRunnersPartnerImagesForOrg(org: String) async throws
        -> ActionsGetHostedRunnersPartnerImagesForOrgResponse {
        try await ActionsMethods.actionsGetHostedRunnersPartnerImagesForOrg(config: config, org: org)
    }

    /// Get the GitHub-hosted runners limits for an organization.
    func getHostedRunnersLimitsForOrg(org: String) async throws -> ActionsHostedRunnerLimits {
        try await ActionsMethods.actionsGetHostedRunnersLimitsForOrg(config: config, org: org)
    }

    /// Get the list of machine specs available for GitHub-hosted runners for an organization.
    func getHostedRunnersMachineSpecsForOrg(org: String) async throws
        -> ActionsGetHostedRunnersMachineSpecsForOrgResponse {
        try await ActionsMethods.actionsGetHostedRunnersMachineSpecsForOrg(config: config, org: org)
    }

    /// Get the list of platforms available for GitHub-hosted runners for an organization.
    func getHostedRunnersPlatformsForOrg(org: String) async throws -> ActionsGetHostedRunnersPlatformsForOrgResponse {
        try await ActionsMethods.actionsGetHostedRunnersPlatformsForOrg(config: config, org: org)
    }

    /// Gets a GitHub-hosted runner configured in an organization. OAuth app tokens and personal access tokens (classic)
    /// need the `manage_runners:org` scope to use this endpoint.
    func getHostedRunnerForOrg(org: String, hostedRunnerId: Int) async throws -> ActionsHostedRunner {
        try await ActionsMethods.actionsGetHostedRunnerForOrg(config: config, org: org, hostedRunnerId: hostedRunnerId)
    }
}

public extension ActionsNamespace {
    /// Updates a GitHub-hosted runner for an organization. OAuth app tokens and personal access tokens (classic) need
    /// the `manage_runners:org` scope to use this endpoint.
    func updateHostedRunnerForOrg(options: ActionsMethods
        .ActionsUpdateHostedRunnerForOrgOptions) async throws -> ActionsHostedRunner {
        try await ActionsMethods.actionsUpdateHostedRunnerForOrg(config: config, options: options)
    }

    /// Deletes a GitHub-hosted runner for an organization.
    func deleteHostedRunnerForOrg(org: String, hostedRunnerId: Int) async throws -> ActionsHostedRunner {
        try await ActionsMethods.actionsDeleteHostedRunnerForOrg(
            config: config,
            org: org,
            hostedRunnerId: hostedRunnerId
        )
    }

    /// Gets the GitHub Actions permissions policy for repositories and allowed actions and reusable workflows in an
    /// organization. OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsGetGithubPermissionsOrganization(org: String) async throws -> ActionsOrganizationPermissions {
        try await ActionsMethods.actionsGetGithubActionsPermissionsOrganization(config: config, org: org)
    }

    /// Sets the GitHub Actions permissions policy for repositories and allowed actions and reusable workflows in an
    /// organization. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this
    /// endpoint.
    func actionsSetGithubPermissionsOrganization(
        org: String,
        enabledRepositories: EnabledRepositories,
        allowedActions: AllowedActions?,
        shaPinningRequired: ShaPinningRequired?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetGithubActionsPermissionsOrganization(
            config: config,
            org: org,
            enabledRepositories: enabledRepositories,
            allowedActions: allowedActions,
            shaPinningRequired: shaPinningRequired
        )
    }

    /// Gets artifact and log retention settings for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
    func getArtifactAndLogRetentionSettingsOrganization(org: String) async throws
        -> ActionsArtifactAndLogRetentionResponse {
        try await ActionsMethods.actionsGetArtifactAndLogRetentionSettingsOrganization(config: config, org: org)
    }

    /// Sets artifact and log retention settings for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
    func setArtifactAndLogRetentionSettingsOrganization(org: String, days: Int) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetArtifactAndLogRetentionSettingsOrganization(
            config: config,
            org: org,
            days: days
        )
    }

    /// Gets the fork PR contributor approval policy for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
    func getForkPrContributorApprovalPermissionsOrganization(org: String) async throws
        -> ActionsForkPrContributorApproval {
        try await ActionsMethods.actionsGetForkPrContributorApprovalPermissionsOrganization(config: config, org: org)
    }

    /// Sets the fork PR contributor approval policy for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope to use this endpoint.
    func setForkPrContributorApprovalPermissionsOrganization(
        org: String,
        approvalPolicy: ActionsForkPrContributorApprovalApprovalPolicy
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetForkPrContributorApprovalPermissionsOrganization(
            config: config,
            org: org,
            approvalPolicy: approvalPolicy
        )
    }
}

public extension ActionsNamespace {
    /// Gets the settings for whether workflows from fork pull requests can run on private repositories in an
    /// organization.
    func getPrivateRepoForkPrWorkflowsSettingsOrganization(org: String) async throws
        -> ActionsForkPrWorkflowsPrivateRepos {
        try await ActionsMethods.actionsGetPrivateRepoForkPrWorkflowsSettingsOrganization(config: config, org: org)
    }

    /// Sets the settings for whether workflows from fork pull requests can run on private repositories in an
    /// organization.
    func setPrivateRepoForkPrWorkflowsSettingsOrganization(
        org: String,
        runWorkflowsFromForkPullRequests: Bool,
        sendWriteTokensToWorkflows: Bool?,
        sendSecretsAndVariables: Bool?,
        requireApprovalForForkPrWorkflows: Bool?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetPrivateRepoForkPrWorkflowsSettingsOrganization(
            config: config,
            org: org,
            runWorkflowsFromForkPullRequests: runWorkflowsFromForkPullRequests,
            sendWriteTokensToWorkflows: sendWriteTokensToWorkflows,
            sendSecretsAndVariables: sendSecretsAndVariables,
            requireApprovalForForkPrWorkflows: requireApprovalForForkPrWorkflows
        )
    }

    /// Lists the selected repositories that are enabled for GitHub Actions in an organization. To use this endpoint,
    /// the organization permission policy for `enabled_repositories` must be configured to `selected`. For more
    /// information, see "Set GitHub Actions permissions for an organization." OAuth app tokens and personal access
    /// tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsListSelectedRepositoriesEnabledGithubOrganization(
        org: String,
        perPage: Int?,
        page: Int?
    ) async throws -> ActionsListSelectedRepositoriesEnabledGithubActionsOrganizationResponse {
        try await ActionsMethods.actionsListSelectedRepositoriesEnabledGithubActionsOrganization(
            config: config,
            org: org,
            perPage: perPage,
            page: page
        )
    }

    /// Replaces the list of selected repositories that are enabled for GitHub Actions in an organization. To use this
    /// endpoint, the organization permission policy for `enabled_repositories` must be configured to `selected`. For
    /// more information, see "Set GitHub Actions permissions for an organization." OAuth app tokens and personal access
    /// tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsSetSelectedRepositoriesEnabledGithubOrganization(
        org: String,
        selectedRepositoryIds: [Int]
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetSelectedRepositoriesEnabledGithubActionsOrganization(
            config: config,
            org: org,
            selectedRepositoryIds: selectedRepositoryIds
        )
    }

    /// Adds a repository to the list of selected repositories that are enabled for GitHub Actions in an organization.
    /// To use this endpoint, the organization permission policy for `enabled_repositories` must be must be configured
    /// to `selected`. For more information, see "Set GitHub Actions permissions for an organization." OAuth tokens and
    /// personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsEnableSelectedRepositoryGithubOrganization(
        org: String,
        repositoryId: Int
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsEnableSelectedRepositoryGithubActionsOrganization(
            config: config,
            org: org,
            repositoryId: repositoryId
        )
    }

    /// Removes a repository from the list of selected repositories that are enabled for GitHub Actions in an
    /// organization. To use this endpoint, the organization permission policy for `enabled_repositories` must be
    /// configured to `selected`. For more information, see "Set GitHub Actions permissions for an organization." OAuth
    /// tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsDisableSelectedRepositoryGithubOrganization(
        org: String,
        repositoryId: Int
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsDisableSelectedRepositoryGithubActionsOrganization(
            config: config,
            org: org,
            repositoryId: repositoryId
        )
    }

    /// Gets the selected actions and reusable workflows that are allowed in an organization. To use this endpoint, the
    /// organization permission policy for `allowed_actions` must be configured to `selected`. For more information, see
    /// "Set GitHub Actions permissions for an organization." OAuth tokens and personal access tokens (classic) need the
    /// `admin:org` scope to use this endpoint.
    func actionsGetAllowedOrganization(org: String) async throws -> SelectedActions {
        try await ActionsMethods.actionsGetAllowedActionsOrganization(config: config, org: org)
    }

    /// Sets the actions and reusable workflows that are allowed in an organization. To use this endpoint, the
    /// organization permission policy for `allowed_actions` must be configured to `selected`. For more information, see
    /// "Set GitHub Actions permissions for an organization." OAuth app tokens and personal access tokens (classic) need
    /// the `admin:org` scope to use this endpoint.
    func actionsSetAllowedOrganization(
        org: String,
        githubOwnedAllowed: Bool?,
        verifiedAllowed: Bool?,
        patternsAllowed: [String]?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetAllowedActionsOrganization(
            config: config,
            org: org,
            githubOwnedAllowed: githubOwnedAllowed,
            verifiedAllowed: verifiedAllowed,
            patternsAllowed: patternsAllowed
        )
    }
}

public extension ActionsNamespace {
    /// Gets the settings for self-hosted runners for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
    func getSelfHostedRunnersPermissionsOrganization(org: String) async throws -> SelfHostedRunnersSettings {
        try await ActionsMethods.actionsGetSelfHostedRunnersPermissionsOrganization(config: config, org: org)
    }

    /// Sets the settings for self-hosted runners for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
    func setSelfHostedRunnersPermissionsOrganization(
        org: String,
        enabledRepositories: ActionsSetSelfHostedRunnersPermissionsOrganizationRequestBodyX6ae5d89570
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetSelfHostedRunnersPermissionsOrganization(
            config: config,
            org: org,
            enabledRepositories: enabledRepositories
        )
    }

    /// Lists repositories that are allowed to use self-hosted runners in an organization. OAuth app tokens and personal
    /// access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this
    /// endpoint.
    func listSelectedRepositoriesSelfHostedRunnersOrganization(
        org: String,
        perPage: Int?,
        page: Int?
    ) async throws -> ActionsListSelectedRepositoriesSelfHostedRunnersOrganizationResponse {
        try await ActionsMethods.actionsListSelectedRepositoriesSelfHostedRunnersOrganization(
            config: config,
            org: org,
            perPage: perPage,
            page: page
        )
    }

    /// Sets repositories that are allowed to use self-hosted runners in an organization. OAuth app tokens and personal
    /// access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this
    /// endpoint.
    func setSelectedRepositoriesSelfHostedRunnersOrganization(
        org: String,
        selectedRepositoryIds: [Int]
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetSelectedRepositoriesSelfHostedRunnersOrganization(
            config: config,
            org: org,
            selectedRepositoryIds: selectedRepositoryIds
        )
    }

    /// Adds a repository to the list of repositories that are allowed to use self-hosted runners in an organization.
    /// OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies"
    /// fine-grained permission to use this endpoint.
    func enableSelectedRepositorySelfHostedRunnersOrganization(
        org: String,
        repositoryId: Int
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsEnableSelectedRepositorySelfHostedRunnersOrganization(
            config: config,
            org: org,
            repositoryId: repositoryId
        )
    }

    /// Removes a repository from the list of repositories that are allowed to use self-hosted runners in an
    /// organization. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions
    /// policies" fine-grained permission to use this endpoint.
    func disableSelectedRepositorySelfHostedRunnersOrganization(
        org: String,
        repositoryId: Int
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsDisableSelectedRepositorySelfHostedRunnersOrganization(
            config: config,
            org: org,
            repositoryId: repositoryId
        )
    }

    /// Gets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in an organization,
    /// as well as whether GitHub Actions can submit approving pull request reviews. For more information, see "[Setting
    /// the permissions of the GITHUB_TOKEN for your organization](https://docs.github.com/organizations/managing-organization-settings/disabling-or-limiting-github-actions-for-your-organization#setting-the-permissions-of-the-github_token-for-your-organization)."
    /// OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsGetGithubDefaultWorkflowPermissionsOrganization(org: String) async throws
        -> ActionsGetDefaultWorkflowPermissions {
        try await ActionsMethods.actionsGetGithubActionsDefaultWorkflowPermissionsOrganization(config: config, org: org)
    }

    /// Sets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in an organization,
    /// and sets if GitHub Actions can submit approving pull request reviews. For more information, see "[Setting the
    /// permissions of the GITHUB_TOKEN for your organization](https://docs.github.com/organizations/managing-organization-settings/disabling-or-limiting-github-actions-for-your-organization#setting-the-permissions-of-the-github_token-for-your-organization)."
    /// OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func actionsSetGithubDefaultWorkflowPermissionsOrganization(
        org: String,
        defaultWorkflowPermissions: ActionsDefaultWorkflowPermissions?,
        canApprovePullRequestReviews: ActionsCanApprovePullRequestReviews?
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetGithubActionsDefaultWorkflowPermissionsOrganization(
            config: config,
            org: org,
            defaultWorkflowPermissions: defaultWorkflowPermissions,
            canApprovePullRequestReviews: canApprovePullRequestReviews
        )
    }
}

public extension ActionsNamespace {
    /// Lists all self-hosted runner groups configured in an organization and inherited from an enterprise. OAuth app
    /// tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func listSelfHostedRunnerGroupsForOrg(
        org: String,
        perPage: Int?,
        page: Int?,
        visibleToRepository: String?
    ) async throws -> ActionsListSelfHostedRunnerGroupsForOrgResponse {
        try await ActionsMethods.actionsListSelfHostedRunnerGroupsForOrg(
            config: config,
            org: org,
            perPage: perPage,
            page: page,
            visibleToRepository: visibleToRepository
        )
    }

    /// Creates a new self-hosted runner group for an organization. OAuth tokens and personal access tokens (classic)
    /// need the `admin:org` scope to use this endpoint.
    func createSelfHostedRunnerGroupForOrg(options: ActionsMethods
        .ActionsCreateSelfHostedRunnerGroupForOrgOptions) async throws -> RunnerGroupsOrg {
        try await ActionsMethods.actionsCreateSelfHostedRunnerGroupForOrg(config: config, options: options)
    }

    /// Gets a specific self-hosted runner group for an organization. OAuth app tokens and personal access tokens
    /// (classic) need the `admin:org` scope to use this endpoint.
    func getSelfHostedRunnerGroupForOrg(org: String, runnerGroupId: Int) async throws -> RunnerGroupsOrg {
        try await ActionsMethods.actionsGetSelfHostedRunnerGroupForOrg(
            config: config,
            org: org,
            runnerGroupId: runnerGroupId
        )
    }

    /// Updates the `name` and `visibility` of a self-hosted runner group in an organization. OAuth app tokens and
    /// personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func updateSelfHostedRunnerGroupForOrg(options: ActionsMethods
        .ActionsUpdateSelfHostedRunnerGroupForOrgOptions) async throws -> RunnerGroupsOrg {
        try await ActionsMethods.actionsUpdateSelfHostedRunnerGroupForOrg(config: config, options: options)
    }

    /// Deletes a self-hosted runner group for an organization. OAuth tokens and personal access tokens (classic) need
    /// the `admin:org` scope to use this endpoint.
    func deleteSelfHostedRunnerGroupFromOrg(org: String, runnerGroupId: Int) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsDeleteSelfHostedRunnerGroupFromOrg(
            config: config,
            org: org,
            runnerGroupId: runnerGroupId
        )
    }

    /// Lists the GitHub-hosted runners in an organization group. OAuth app tokens and personal access tokens (classic)
    /// need the `admin:org` scope to use this endpoint.
    func listGithubHostedRunnersInGroupForOrg(
        org: String,
        runnerGroupId: Int,
        perPage: Int?,
        page: Int?
    ) async throws -> ActionsListGithubHostedRunnersInGroupForOrgResponse {
        try await ActionsMethods.actionsListGithubHostedRunnersInGroupForOrg(
            config: config,
            org: org,
            runnerGroupId: runnerGroupId,
            perPage: perPage,
            page: page
        )
    }

    /// Lists the repositories with access to a self-hosted runner group configured in an organization. OAuth app tokens
    /// and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func listRepoAccessToSelfHostedRunnerGroupInOrg(
        org: String,
        runnerGroupId: Int,
        page: Int?,
        perPage: Int?
    ) async throws -> ActionsListRepoAccessToSelfHostedRunnerGroupInOrgResponse {
        try await ActionsMethods.actionsListRepoAccessToSelfHostedRunnerGroupInOrg(
            config: config,
            org: org,
            runnerGroupId: runnerGroupId,
            page: page,
            perPage: perPage
        )
    }

    /// Replaces the list of repositories that have access to a self-hosted runner group configured in an organization.
    /// OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func setRepoAccessToSelfHostedRunnerGroupInOrg(
        org: String,
        runnerGroupId: Int,
        selectedRepositoryIds: [Int]
    ) async throws -> SdkEmptyResponse {
        try await ActionsMethods.actionsSetRepoAccessToSelfHostedRunnerGroupInOrg(
            config: config,
            org: org,
            runnerGroupId: runnerGroupId,
            selectedRepositoryIds: selectedRepositoryIds
        )
    }
}
