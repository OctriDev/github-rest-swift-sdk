// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public extension CodeSecurityNamespace {
    /// Lists the repositories associated with an enterprise code security configuration in an organization. The
    /// authenticated user must be an administrator of the enterprise in order to use this endpoint. OAuth app tokens
    /// and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
    func getRepositoriesForEnterpriseConfiguration(
        enterprise: String,
        configurationId: Int,
        perPage: Int?,
        before: String?,
        after: String?,
        status: String?
    ) async throws -> [CodeSecurityConfigurationRepositories] {
        try await CodeSecurityMethods.codeSecurityGetRepositoriesForEnterpriseConfiguration(
            config: config,
            enterprise: enterprise,
            configurationId: configurationId,
            perPage: perPage,
            before: before,
            after: after,
            status: status
        )
    }

    /// Lists all code security configurations available in an organization. The authenticated user must be an
    /// administrator or security manager for the organization to use this endpoint. OAuth app tokens and personal
    /// access tokens (classic) need the `read:org` scope to use this endpoint.
    func getConfigurationsForOrg(
        org: String,
        targetType: CodeSecurityGetConfigurationsForOrgParameter?,
        perPage: Int?,
        before: String?,
        after: String?
    ) async throws -> [CodeSecurityConfiguration] {
        try await CodeSecurityMethods.codeSecurityGetConfigurationsForOrg(
            config: config,
            org: org,
            targetType: targetType,
            perPage: perPage,
            before: before,
            after: after
        )
    }

    /// Creates a code security configuration in an organization. The authenticated user must be an administrator or
    /// security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens
    /// (classic) need the `write:org` scope to use this endpoint.
    func createConfiguration(options: CodeSecurityMethods
        .CodeSecurityCreateConfigurationOptions) async throws -> CodeSecurityConfiguration {
        try await CodeSecurityMethods.codeSecurityCreateConfiguration(config: config, options: options)
    }

    /// Lists the default code security configurations for an organization. The authenticated user must be an
    /// administrator or security manager for the organization to use this endpoint. OAuth app tokens and personal
    /// access tokens (classic) need the `read:org` scope to use this endpoint.
    func getDefaultConfigurations(org: String) async throws -> CodeSecurityDefaultConfigurations {
        try await CodeSecurityMethods.codeSecurityGetDefaultConfigurations(config: config, org: org)
    }

    /// Detach code security configuration(s) from a set of repositories. Repositories will retain their settings but
    /// will no longer be associated with the configuration. The authenticated user must be an administrator or security
    /// manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need
    /// the `write:org` scope to use this endpoint.
    func detachConfiguration(org: String, selectedRepositoryIds: [Int]) async throws -> SdkEmptyResponse {
        try await CodeSecurityMethods.codeSecurityDetachConfiguration(
            config: config,
            org: org,
            selectedRepositoryIds: selectedRepositoryIds
        )
    }

    /// Gets a code security configuration available in an organization. The authenticated user must be an administrator
    /// or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens
    /// (classic) need the `write:org` scope to use this endpoint.
    func getConfiguration(org: String, configurationId: Int) async throws -> CodeSecurityConfiguration {
        try await CodeSecurityMethods.codeSecurityGetConfiguration(
            config: config,
            org: org,
            configurationId: configurationId
        )
    }

    /// Updates a code security configuration in an organization. The authenticated user must be an administrator or
    /// security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens
    /// (classic) need the `write:org` scope to use this endpoint.
    func updateConfiguration(options: CodeSecurityMethods
        .CodeSecurityUpdateConfigurationOptions) async throws -> CodeSecurityConfiguration {
        try await CodeSecurityMethods.codeSecurityUpdateConfiguration(config: config, options: options)
    }

    /// Deletes the desired code security configuration from an organization. Repositories attached to the configuration
    /// will retain their settings but will no longer be associated with the configuration. The authenticated user must
    /// be an administrator or security manager for the organization to use this endpoint. OAuth app tokens and personal
    /// access tokens (classic) need the `write:org` scope to use this endpoint.
    func deleteConfiguration(org: String, configurationId: Int) async throws -> SdkEmptyResponse {
        try await CodeSecurityMethods.codeSecurityDeleteConfiguration(
            config: config,
            org: org,
            configurationId: configurationId
        )
    }
}

public extension CodeSecurityNamespace {
    /// Attach a code security configuration to a set of repositories. If the repositories specified are already
    /// attached to a configuration, they will be re-attached to the provided configuration. If insufficient GHAS
    /// licenses are available to attach the configuration to a repository, only free features will be enabled. The
    /// authenticated user must be an administrator or security manager for the organization to use this endpoint. OAuth
    /// app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
    func attachConfiguration(
        org: String,
        configurationId: Int,
        scope: CodeSecurityAttachConfigurationRequestBodyScope,
        selectedRepositoryIds: [Int]?
    ) async throws -> [String: JSONValue] {
        try await CodeSecurityMethods.codeSecurityAttachConfiguration(
            config: config,
            org: org,
            configurationId: configurationId,
            scope: scope,
            selectedRepositoryIds: selectedRepositoryIds
        )
    }

    /// Sets a code security configuration as a default to be applied to new repositories in your organization. This
    /// configuration will be applied to the matching repository type (all, none, public, private and internal) by
    /// default when they are created. The authenticated user must be an administrator or security manager for the
    /// organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `write:org`
    /// scope to use this endpoint.
    func setConfigurationAsDefault(
        org: String,
        configurationId: Int,
        defaultForNewRepos: CodeSecuritySetConfigurationAsDefaultRequestBodyDefaultForNewRepos?
    ) async throws -> CodeSecuritySetConfigurationAsDefaultResponse {
        try await CodeSecurityMethods.codeSecuritySetConfigurationAsDefault(
            config: config,
            org: org,
            configurationId: configurationId,
            defaultForNewRepos: defaultForNewRepos
        )
    }

    /// Lists the repositories associated with a code security configuration in an organization. The authenticated user
    /// must be an administrator or security manager for the organization to use this endpoint. OAuth app tokens and
    /// personal access tokens (classic) need the `read:org` scope to use this endpoint.
    func getRepositoriesForConfiguration(
        org: String,
        configurationId: Int,
        perPage: Int?,
        before: String?,
        after: String?,
        status: String?
    ) async throws -> [CodeSecurityConfigurationRepositories] {
        try await CodeSecurityMethods.codeSecurityGetRepositoriesForConfiguration(
            config: config,
            org: org,
            configurationId: configurationId,
            perPage: perPage,
            before: before,
            after: after,
            status: status
        )
    }

    /// Get the code security configuration that manages a repository's code security settings. The authenticated user
    /// must be an administrator or security manager for the organization to use this endpoint. OAuth app tokens and
    /// personal access tokens (classic) need the `repo` scope to use this endpoint.
    func getConfigurationForRepository(
        owner: String,
        repo: String
    ) async throws -> CodeSecurityConfigurationForRepository {
        try await CodeSecurityMethods.codeSecurityGetConfigurationForRepository(
            config: config,
            owner: owner,
            repo: repo
        )
    }
}

public class CopilotNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

    /// Use this endpoint to retrieve download links for the Copilot enterprise usage metrics report for a specific day.
    /// The report provides comprehensive usage data for Copilot features across the enterprise. The report contains
    /// aggregated metrics for the specified day, including usage statistics for various Copilot features, user
    /// engagement data, and feature adoption metrics. Reports are generated daily and made available for download
    /// through signed URLs with a limited expiration time. The response includes download links to the report files,
    /// along with the specific date of the report. The report covers a complete day for which data has been processed.
    /// Reports are available starting from October 10, 2025, and historical data can be accessed for up to 1 year from
    /// the current date. Enterprise owners, billing managers, and authorized users with fine-grained "View Enterprise
    /// Copilot Metrics" permission can retrieve Copilot metrics reports for the enterprise. OAuth app tokens and
    /// personal access tokens (classic) need either the `manage_billing:copilot` or `read:enterprise` scopes to use
    /// this endpoint.
    public func copilotEnterpriseOneDayUsageMetrics(
        enterprise: String,
        day: String
    ) async throws -> CopilotUsageMetrics1DayReport {
        try await CopilotMethods.copilotCopilotEnterpriseOneDayUsageMetrics(
            config: config,
            enterprise: enterprise,
            day: day
        )
    }

    /// Use this endpoint to retrieve download links for the latest 28-day enterprise Copilot usage metrics report. The
    /// report provides comprehensive usage data for Copilot features across the enterprise. The report contains
    /// aggregated metrics for the previous 28 days, including usage statistics for various Copilot features, user
    /// engagement data, and feature adoption metrics. Reports are generated daily and made available for download
    /// through signed URLs with a limited expiration time. The response includes download links to the report files,
    /// along with the specific date range covered by the report. The report covers a complete 28-day period ending on
    /// the most recent day for which data has been processed. Enterprise owners, billing managers, and authorized users
    /// with fine-grained "View Enterprise Copilot Metrics" permission can retrieve Copilot metrics reports for the
    /// enterprise. OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or
    /// `read:enterprise` scopes to use this endpoint.
    public func copilotEnterpriseUsageMetrics(enterprise: String) async throws -> CopilotUsageMetrics28DayReport {
        try await CopilotMethods.copilotCopilotEnterpriseUsageMetrics(config: config, enterprise: enterprise)
    }

    /// Use this endpoint to retrieve download links for the Copilot enterprise repository report for a specific day.
    /// The report provides per-repository pull request metrics for Copilot across the enterprise, with one entry per
    /// repository. The report contains repository-level pull request activity for the specified day, including the
    /// Copilot Coding Agent (CCA) and Copilot Code Review (CCR) breakdowns. Only repositories that had activity on the
    /// specified day are included. Reports are generated daily and made available for download through signed URLs with
    /// a limited expiration time. The response includes download links to the report files, along with the specific
    /// date of the report. The report covers a complete day for which data has been processed. Enterprise owners,
    /// billing managers, and authorized users with fine-grained "View Enterprise Copilot Metrics" permission can
    /// retrieve Copilot metrics reports for the enterprise. OAuth app tokens and personal access tokens (classic) need
    /// either the `manage_billing:copilot` or `read:enterprise` scopes to use this endpoint.
    public func copilotEnterpriseReposOneDayReport(
        enterprise: String,
        day: String
    ) async throws -> CopilotUsageMetrics1DayReport {
        try await CopilotMethods.copilotCopilotEnterpriseReposOneDayReport(
            config: config,
            enterprise: enterprise,
            day: day
        )
    }

    /// Use this endpoint to retrieve download links for the Copilot enterprise user-teams report for a specific day.
    /// The report provides user-team join data for Copilot across the enterprise, with one entry per user-team pair.
    /// The report contains user-team membership data for the specified day, enabling consumers to join with the
    /// existing enterprise user reports to compute team-level usage metrics. Reports are generated daily and made
    /// available for download through signed URLs with a limited expiration time. The response includes download links
    /// to the report files, along with the specific date of the report. The report covers a complete day for which data
    /// has been processed. Enterprise owners, billing managers, and authorized users with fine-grained "View Enterprise
    /// Copilot Metrics" permission can retrieve Copilot metrics reports for the enterprise. OAuth app tokens and
    /// personal access tokens (classic) need either the `manage_billing:copilot` or `read:enterprise` scopes to use
    /// this endpoint.
    public func copilotEnterpriseUserTeamsOneDayReport(
        enterprise: String,
        day: String
    ) async throws -> CopilotUsageMetrics1DayReport {
        try await CopilotMethods.copilotCopilotEnterpriseUserTeamsOneDayReport(
            config: config,
            enterprise: enterprise,
            day: day
        )
    }

    /// Use this endpoint to retrieve download links for the Copilot user usage metrics report for a specific day. The
    /// report provides detailed user-level usage data and engagement metrics for Copilot features across the
    /// enterprise. The report contains user-specific metrics for the specified day, including individual user
    /// engagement statistics, feature usage patterns, and adoption metrics broken down by user. This report allows
    /// authorized users to analyze Copilot usage at the user level to understand adoption patterns and identify
    /// opportunities for increased engagement. Reports are generated daily and made available for download through
    /// signed URLs with a limited expiration time. The response includes download links to the report files, along with
    /// the specific date of the report. The report covers a complete day for which data has been processed. Reports are
    /// available starting from October 10, 2025, and historical data can be accessed for up to 1 year from the current
    /// date. Enterprise owners, billing managers, and authorized users with fine-grained "View Enterprise Copilot
    /// Metrics" permission can retrieve Copilot metrics reports for the enterprise. OAuth app tokens and personal
    /// access tokens (classic) need either the `manage_billing:copilot` or `read:enterprise` scopes to use this
    /// endpoint.
    public func copilotUsersOneDayUsageMetrics(
        enterprise: String,
        day: String
    ) async throws -> CopilotUsageMetrics1DayReport {
        try await CopilotMethods.copilotCopilotUsersOneDayUsageMetrics(config: config, enterprise: enterprise, day: day)
    }

    /// Use this endpoint to retrieve download links for the latest 28-day enterprise users Copilot usage metrics
    /// report. The report provides detailed user-level usage data and engagement metrics for Copilot features across
    /// the enterprise. The report contains user-specific metrics for the previous 28 days, including individual user
    /// engagement statistics, feature usage patterns, and adoption metrics broken down by user. This report allows
    /// authorized users to analyze Copilot usage at the user level to understand adoption patterns and identify
    /// opportunities for increased engagement. Reports are generated daily and made available for download through
    /// signed URLs with a limited expiration time. The response includes download links to the report files, along with
    /// the specific date range covered by the report. The report covers a complete 28-day period ending on the most
    /// recent day for which data has been processed. Enterprise owners, billing managers, and authorized users with
    /// fine-grained "View Enterprise Copilot Metrics" permission can retrieve Copilot metrics reports for the
    /// enterprise. OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or
    /// `read:enterprise` scopes to use this endpoint.
    public func copilotUsersUsageMetrics(enterprise: String) async throws -> CopilotUsageMetrics28DayReport {
        try await CopilotMethods.copilotCopilotUsersUsageMetrics(config: config, enterprise: enterprise)
    }

    /// Sets the policy for Copilot cloud agent usage across an enterprise. Enterprise owners can configure whether
    /// Copilot cloud agent is enabled for all organizations, disabled for all organizations, configured by individual
    /// organization admins, or enabled for selected organizations only. Only enterprise owners can set the coding agent
    /// policy for their enterprise. OAuth app tokens and personal access tokens (classic) need either the
    /// `manage_billing:copilot` or `admin:enterprise` scopes to use this endpoint.
    public func setEnterpriseCodingAgentPolicy(
        enterprise: String,
        policyState: CopilotSetEnterpriseCodingAgentPolicyRequestBodyPolicyState
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotSetEnterpriseCodingAgentPolicy(
            config: config,
            enterprise: enterprise,
            policyState: policyState
        )
    }

    /// Enables Copilot cloud agent for the specified organizations within the enterprise. The enterprise's coding agent
    /// policy must be set to `enabled_for_selected_orgs` before using this endpoint. Organizations can be specified by
    /// login or matched via custom properties. Only organizations that have Copilot enabled and belong to the
    /// enterprise will be affected. Only enterprise owners can add organizations to the coding agent policy. OAuth app
    /// tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:enterprise`
    /// scopes to use this endpoint.
    public func addOrganizationsToEnterpriseCodingAgentPolicy(
        enterprise: String,
        organizations: [String]?,
        customProperties: CopilotAddOrganizationsToEnterpriseCodingAgentPolicyCustomPropertiesList?
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotAddOrganizationsToEnterpriseCodingAgentPolicy(
            config: config,
            enterprise: enterprise,
            organizations: organizations,
            customProperties: customProperties
        )
    }
}

public extension CopilotNamespace {
    /// Disables Copilot cloud agent for the specified organizations within the enterprise. The enterprise's coding
    /// agent policy must be set to `enabled_for_selected_orgs` before using this endpoint. Organizations can be
    /// specified by login or matched via custom properties. Only organizations that have Copilot enabled and belong to
    /// the enterprise will be affected. Only enterprise owners can remove organizations from the coding agent policy.
    /// OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or
    /// `admin:enterprise` scopes to use this endpoint.
    func removeOrganizationsFromEnterpriseCodingAgentPolicy(
        enterprise: String,
        organizations: [String]?,
        customProperties: CopilotRemoveOrganizationsFromEnterpriseCodingAgentPolicyCustXd9c994a561?
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotRemoveOrganizationsFromEnterpriseCodingAgentPolicy(
            config: config,
            enterprise: enterprise,
            organizations: organizations,
            customProperties: customProperties
        )
    }

    /// Retrieves Copilot subscription details for an organization, including its seat breakdown and feature policies.
    /// Supply `org` to identify the organization; only the authenticated user's organization-owner access can view
    /// these details. The endpoint is in public preview and may change.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Gets information about an
    /// organization's Copilot subscription, including seat breakdown and feature policies. To configure these settings,
    /// go to your organization's settings on GitHub.com. For more information, see "[Managing policies for Copilot in
    /// your organization](https://docs.github.com/copilot/managing-copilot/managing-policies-for-copilot-business-in-your-organization)."
    /// Only organization owners can view details about the organization's Copilot Business or Copilot Enterprise
    /// subscription. OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or
    /// `read:org` scopes to use this endpoint.
    func copilotGetOrganizationDetails(org: String) async throws -> CopilotOrganizationDetails {
        try await CopilotMethods.copilotGetCopilotOrganizationDetails(config: config, org: org)
    }

    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Lists all Copilot seats for which an
    /// organization with a Copilot Business or Copilot Enterprise subscription is currently being billed. Only
    /// organization owners can view assigned seats. Each seat object contains information about the assigned user's
    /// most recent Copilot activity. Users must have telemetry enabled in their IDE for Copilot in the IDE activity to
    /// be reflected in `last_activity_at`. For more information about activity data, see [Metrics data properties for
    /// GitHub Copilot](https://docs.github.com/copilot/reference/metrics-data). OAuth app tokens and personal access
    /// tokens (classic) need either the `manage_billing:copilot` or `read:org` scopes to use this endpoint.
    func copilotListSeats(org: String, page: Int?, perPage: Int?) async throws -> CopilotListCopilotSeatsResponse {
        try await CopilotMethods.copilotListCopilotSeats(config: config, org: org, page: page, perPage: perPage)
    }

    /// Creates Copilot seats for all users in each team specified for an organization. Use `selected_teams` to identify
    /// the teams that should receive access, and ensure the organization has a Copilot Business or Copilot Enterprise
    /// subscription with a configured suggestion matching policy. The response reports the number of newly created and
    /// refreshed seats.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Purchases a GitHub Copilot seat for all
    /// users within each specified team. The organization will be billed for each seat based on the organization's
    /// Copilot plan. For more information about Copilot pricing, see "[About billing for GitHub Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/about-billing-for-github-copilot-in-your-organization)."
    /// Only organization owners can purchase Copilot seats for their organization members. The organization must have a
    /// Copilot Business or Copilot Enterprise subscription and a configured suggestion matching policy. For more
    /// information about setting up a Copilot subscription, see "[Subscribing to Copilot for your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/subscribing-to-copilot-for-your-organization)."
    /// For more information about setting a suggestion matching policy, see "[Managing policies for Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/setting-policies-for-copilot-in-your-organization/managing-policies-for-copilot-in-your-organization#policies-for-suggestion-matching)."
    /// The response contains the total number of new seats that were created and existing seats that were refreshed.
    /// OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:org`
    /// scopes to use this endpoint.
    func copilotAddSeatsForTeams(
        org: String,
        selectedTeams: [String]
    ) async throws -> CopilotAddCopilotSeatsForTeamsResponse {
        try await CopilotMethods.copilotAddCopilotSeatsForTeams(config: config, org: org, selectedTeams: selectedTeams)
    }

    /// Removes Copilot seat assignments for all members of each specified team by marking their seats for cancellation.
    /// Use `selected_teams` to identify the teams whose members should lose access at the end of the current billing
    /// cycle, unless they retain access through another team. The response reports the number of seats marked as
    /// pending cancellation.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Sets seats for all members of each team
    /// specified to "pending cancellation". This will cause the members of the specified team(s) to lose access to
    /// GitHub Copilot at the end of the current billing cycle unless they retain access through another team. For more
    /// information about disabling access to Copilot, see "[Revoking access to Copilot for members of your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-access-to-github-copilot-in-your-organization/revoking-access-to-copilot-for-members-of-your-organization)."
    /// Only organization owners can cancel Copilot seats for their organization members. The response contains the
    /// total number of seats set to "pending cancellation". OAuth app tokens and personal access tokens (classic) need
    /// either the `manage_billing:copilot` or `admin:org` scopes to use this endpoint.
    func copilotCancelSeatAssignmentForTeams(
        org: String,
        selectedTeams: [String]
    ) async throws -> CopilotCancelCopilotSeatAssignmentForTeamsResponse {
        try await CopilotMethods.copilotCancelCopilotSeatAssignmentForTeams(
            config: config,
            org: org,
            selectedTeams: selectedTeams
        )
    }

    /// Creates Copilot seats for each user specified for an organization. Use `selected_usernames` to identify the
    /// organization members who should receive access, and ensure the organization has a Copilot Business or Copilot
    /// Enterprise subscription with a configured suggestion matching policy. The response reports the number of newly
    /// created and refreshed seats.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Purchases a GitHub Copilot seat for
    /// each user specified. The organization will be billed for each seat based on the organization's Copilot plan. For
    /// more information about Copilot pricing, see "[About billing for GitHub Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/about-billing-for-github-copilot-in-your-organization)."
    /// Only organization owners can purchase Copilot seats for their organization members. The organization must have a
    /// Copilot Business or Copilot Enterprise subscription and a configured suggestion matching policy. For more
    /// information about setting up a Copilot subscription, see "[Subscribing to Copilot for your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/subscribing-to-copilot-for-your-organization)."
    /// For more information about setting a suggestion matching policy, see "[Managing policies for Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/setting-policies-for-copilot-in-your-organization/managing-policies-for-copilot-in-your-organization#policies-for-suggestion-matching)."
    /// The response contains the total number of new seats that were created and existing seats that were refreshed.
    /// OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:org`
    /// scopes to use this endpoint.
    func copilotAddSeatsForUsers(
        org: String,
        selectedUsernames: [String]
    ) async throws -> CopilotAddCopilotSeatsForUsersResponse {
        try await CopilotMethods.copilotAddCopilotSeatsForUsers(
            config: config,
            org: org,
            selectedUsernames: selectedUsernames
        )
    }

    /// Removes Copilot seat assignments for each specified user by marking their seats for cancellation. Use
    /// `selected_usernames` to identify the organization members who should lose access at the end of the current
    /// billing cycle, unless they retain access through team membership. The response reports the number of seats
    /// marked as pending cancellation.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Sets seats for all users specified to
    /// "pending cancellation". This will cause the specified users to lose access to GitHub Copilot at the end of the
    /// current billing cycle unless they retain access through team membership. For more information about disabling
    /// access to Copilot, see "[Revoking access to Copilot for members of your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-access-to-github-copilot-in-your-organization/revoking-access-to-copilot-for-members-of-your-organization)."
    /// Only organization owners can cancel Copilot seats for their organization members. The response contains the
    /// total number of seats set to "pending cancellation". OAuth app tokens and personal access tokens (classic) need
    /// either the `manage_billing:copilot` or `admin:org` scopes to use this endpoint.
    func copilotCancelSeatAssignmentForUsers(
        org: String,
        selectedUsernames: [String]
    ) async throws -> CopilotCancelCopilotSeatAssignmentForUsersResponse {
        try await CopilotMethods.copilotCancelCopilotSeatAssignmentForUsers(
            config: config,
            org: org,
            selectedUsernames: selectedUsernames
        )
    }

    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Gets information about which
    /// repositories in an organization have been enabled or disabled for the Copilot cloud agent. Organization owners
    /// can configure whether Copilot cloud agent is enabled for all repositories, selected repositories, or no
    /// repositories owned by organization. OAuth app tokens and personal access tokens (classic) need the `admin:org`
    /// scope to use this endpoint.
    func copilotGetCodingAgentPermissionsOrganization(org: String) async throws
        -> CopilotGetCopilotCodingAgentPermissionsOrganizationResponse {
        try await CopilotMethods.copilotGetCopilotCodingAgentPermissionsOrganization(config: config, org: org)
    }
}

public extension CopilotNamespace {
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Sets the policy for which repositories
    /// in an organization can use Copilot cloud agent. Organization owners can configure whether Copilot cloud agent is
    /// enabled for all repositories, selected repositories, or no repositories owned by the organization. OAuth app
    /// tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func copilotSetCodingAgentPermissionsOrganization(
        org: String,
        enabledRepositories: CopilotSetCopilotCodingAgentPermissionsOrganizationRequestBodXd3a141df41
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotSetCopilotCodingAgentPermissionsOrganization(
            config: config,
            org: org,
            enabledRepositories: enabledRepositories
        )
    }

    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Lists the selected repositories that
    /// are enabled for Copilot cloud agent in an organization. Organization owners can use this endpoint when the
    /// coding agent repository policy is set to `selected` to see which repositories have been enabled. OAuth app
    /// tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
    func copilotListCodingAgentSelectedRepositoriesForOrganization(
        org: String,
        perPage: Int?,
        page: Int?
    ) async throws -> CopilotListCopilotCodingAgentSelectedRepositoriesForOrganizationResponse {
        try await CopilotMethods.copilotListCopilotCodingAgentSelectedRepositoriesForOrganization(
            config: config,
            org: org,
            perPage: perPage,
            page: page
        )
    }

    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Replaces the list of selected
    /// repositories that are enabled for Copilot cloud agent in an organization. This method can only be called when
    /// the cloud agent repository policy is set to `selected`. OAuth app tokens and personal access tokens (classic)
    /// need the `admin:org` scope to use this endpoint.
    func copilotSetCodingAgentSelectedRepositoriesForOrganization(
        org: String,
        selectedRepositoryIds: [Int]
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotSetCopilotCodingAgentSelectedRepositoriesForOrganization(
            config: config,
            org: org,
            selectedRepositoryIds: selectedRepositoryIds
        )
    }

    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Adds a repository to the list of
    /// selected repositories enabled for Copilot cloud agent in an organization. This method can only be called when
    /// the cloud agent repository policy is set to `selected`. OAuth app tokens and personal access tokens (classic)
    /// need the `admin:org` scope to use this endpoint.
    func copilotEnableCodingAgentForRepositoryInOrganization(
        org: String,
        repositoryId: Int
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotEnableCopilotCodingAgentForRepositoryInOrganization(
            config: config,
            org: org,
            repositoryId: repositoryId
        )
    }

    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Removes a repository from the list of
    /// selected repositories enabled for Copilot cloud agent in an organization. This method can only be called when
    /// the cloud agent repository policy is set to `selected`. OAuth app tokens and personal access tokens (classic)
    /// need the `admin:org` scopes to use this endpoint.
    func copilotDisableCodingAgentForRepositoryInOrganization(
        org: String,
        repositoryId: Int
    ) async throws -> SdkEmptyResponse {
        try await CopilotMethods.copilotDisableCopilotCodingAgentForRepositoryInOrganization(
            config: config,
            org: org,
            repositoryId: repositoryId
        )
    }

    /// Retrieves an organization's Copilot content exclusion path rules. Use `org` to identify the organization whose
    /// rules you want to inspect; the authenticated user must be an organization owner with an OAuth app token or
    /// classic personal access token authorized by the `copilot` or `read:org` scope. The response does not include
    /// comments, and duplicate keys are represented only by their final occurrence.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Gets information about an
    /// organization's Copilot content exclusion path rules. To configure these settings, go to the organization's
    /// settings on GitHub. For more information, see "[Excluding content from GitHub Copilot](https://docs.github.com/copilot/managing-copilot/configuring-and-auditing-content-exclusion/excluding-content-from-github-copilot#configuring-content-exclusions-for-your-organization)."
    /// Organization owners can view details about Copilot content exclusion rules for the organization. OAuth app
    /// tokens and personal access tokens (classic) need either the `copilot` or `read:org` scopes to use this endpoint.
    /// > [!CAUTION] > * At this time, the API does not support comments. This endpoint will not return any comments in
    /// the existing rules. > * At this time, the API does not support duplicate keys. If your content exclusion
    /// configuration contains duplicate keys, the API will return only the last occurrence of that key. For example, if
    /// duplicate entries are present, only the final value will be included in the response.
    func copilotContentExclusionForOrganization(org: String) async throws
        -> CopilotOrganizationContentExclusionDetails {
        try await CopilotMethods.copilotCopilotContentExclusionForOrganization(config: config, org: org)
    }

    /// Replaces an organization's Copilot content exclusion path rules. Use `org` to identify the organization and
    /// submit an object whose values are arrays of supported exclusion entries; the authenticated user must be an
    /// organization owner using an OAuth app token or classic personal access token with the `copilot` scope. Existing
    /// comments are deleted, and duplicate keys overwrite earlier entries so only the final value is saved.
    ///
    /// > [!NOTE] > This endpoint is in public preview and is subject to change. Sets Copilot content exclusion path
    /// rules for an organization. To configure these settings, go to the organization's settings on GitHub. For more
    /// information, see "[Excluding content from GitHub Copilot](https://docs.github.com/copilot/managing-copilot/configuring-and-auditing-content-exclusion/excluding-content-from-github-copilot#configuring-content-exclusions-for-your-organization)."
    /// Organization owners can set Copilot content exclusion rules for the organization. OAuth app tokens and personal
    /// access tokens (classic) need the `copilot` scope to use this endpoint. > [!CAUTION] > * At this time, the API
    /// does not support comments. When using this endpoint, any existing comments in your rules will be deleted. > * At
    /// this time, the API does not support duplicate keys. If you submit content exclusions through the API with
    /// duplicate keys, only the last occurrence will be saved. Earlier entries with the same key will be overwritten.
    func copilotSetContentExclusionForOrganization(
        org: String,
        body: [String: [CopilotSetCopilotContentExclusionForOrganizationRequestBodyValueItem]]
    ) async throws -> CopilotSetCopilotContentExclusionForOrganizationResponse {
        try await CopilotMethods.copilotSetCopilotContentExclusionForOrganization(config: config, org: org, body: body)
    }

    /// Use this endpoint to retrieve download links for the Copilot organization usage metrics report for a specific
    /// day. The report provides comprehensive usage data for Copilot features across the organization. The report
    /// contains aggregated metrics for the specified day, including usage statistics for various Copilot features, user
    /// engagement data, and feature adoption metrics. Reports are generated daily and made available for download
    /// through signed URLs with a limited expiration time. The response includes download links to the report files,
    /// along with the specific date of the report. The report covers a complete day for which data has been processed.
    /// Organization owners and authorized users with fine-grained "View Organization Copilot Metrics" permission can
    /// retrieve Copilot metrics reports for the organization. OAuth app tokens and personal access tokens (classic)
    /// need the `read:org` scope to use this endpoint. For more information about organization metrics attribution, see
    /// [How are metrics attributed across organizations](https://docs.github.com/copilot/concepts/copilot-metrics#how-are-metrics-attributed-across-organizations).
    func copilotOrganizationOneDayUsageMetrics(org: String, day: String) async throws -> CopilotUsageMetrics1DayReport {
        try await CopilotMethods.copilotCopilotOrganizationOneDayUsageMetrics(config: config, org: org, day: day)
    }
}
