// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// WebhookOrganizationRenamed domain models
/// Typed representation of the `WebhookOrganizationRenamed` API schema.
public struct WebhookOrganizationRenamed: Codable {
    /// Required enumerated value serialized in the `action` wire field.
    public var action: WebhookOrganizationRenamedAction
    /// A GitHub organization. Webhook payloads contain the `organization` property when the webhook is configured
    /// for an organization, or when the event occurs from activity in a repository owned by an organization.
    public var organization: OrganizationSimpleWebhooks
    /// A GitHub user.
    public var sender: SimpleUser
    /// Optional object value serialized in the `changes` wire field.
    public var changes: WebhookOrganizationRenamedChanges?
    /// An enterprise on GitHub. Webhook payloads contain the `enterprise` property when the webhook is configured
    /// on an enterprise account or an organization that's part of an enterprise account. For more information, see
    /// "About enterprise accounts."
    public var enterprise: EnterpriseWebhooks?
    /// The GitHub App installation. Webhook payloads contain the `installation` property when the event is
    /// configured for and sent to a GitHub App. For more information, see "Using webhooks with GitHub Apps."
    public var installation: SimpleInstallation?
    /// The membership between the user and the organization. Not present when the action is `member_invited`.
    public var membership: WebhooksMembership?
    /// The repository on GitHub where the event occurred. Webhook payloads contain the `repository` property when
    /// the event occurs from activity in a repository.
    public var repository: RepositoryWebhooks?

    enum CodingKeys: String, CodingKey {
        case action
        case organization
        case sender
        case changes
        case enterprise
        case installation
        case membership
        case repository
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookOrganizationRenamed {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.action) else {
            throw SdkValidationError(
                field: "action",
                code: "required",
                message: "Validation failed for 'action': value is required"
            )
        }
        guard container.contains(.organization) else {
            throw SdkValidationError(
                field: "organization",
                code: "required",
                message: "Validation failed for 'organization': value is required"
            )
        }
        guard container.contains(.sender) else {
            throw SdkValidationError(
                field: "sender",
                code: "required",
                message: "Validation failed for 'sender': value is required"
            )
        }
        action = try container.sdkDecodeRequired(.action)
        organization = try container.sdkDecodeRequired(.organization)
        sender = try container.sdkDecodeRequired(.sender)
        changes = try container.sdkDecodeIfPresent(.changes)
        enterprise = try container.sdkDecodeIfPresent(.enterprise)
        installation = try container.sdkDecodeIfPresent(.installation)
        membership = try container.sdkDecodeIfPresent(.membership)
        repository = try container.sdkDecodeIfPresent(.repository)
    }
}

public extension WebhookOrganizationRenamed {
    init(
        action: WebhookOrganizationRenamedAction,
        organization: OrganizationSimpleWebhooks,
        sender: SimpleUser,
        changes: WebhookOrganizationRenamedChanges? = nil,
        enterprise: EnterpriseWebhooks? = nil,
        installation: SimpleInstallation? = nil,
        membership: WebhooksMembership? = nil,
        repository: RepositoryWebhooks? = nil
    ) {
        (self.action, self.organization) = (action, organization)
        (self.sender, self.changes) = (sender, changes)
        (self.enterprise, self.installation) = (enterprise, installation)
        (self.membership, self.repository) = (membership, repository)
    }
}

/// Optional object value serialized in the `changes` wire field.
public struct WebhookOrganizationRenamedChanges: Codable {
    /// Optional object value serialized in the `login` wire field.
    public var login: WebhookOrganizationRenamedChangesLogin?

    enum CodingKeys: String, CodingKey {
        case login
    }

    init() {
        login = nil
    }
}

public extension WebhookOrganizationRenamedChanges {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        login = try container.sdkDecodeIfPresent(.login)
    }
}

public extension WebhookOrganizationRenamedChanges {
    init(login: WebhookOrganizationRenamedChangesLogin? = nil) {
        self.init()
        self.login = login
    }
}

/// Optional object value serialized in the `login` wire field.
public struct WebhookOrganizationRenamedChangesLogin: Codable {
    /// Optional `string` value serialized in the `from` wire field.
    public var from: String?

    enum CodingKeys: String, CodingKey {
        case from
    }

    init() {
        from = nil
    }
}

public extension WebhookOrganizationRenamedChangesLogin {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        from = try container.sdkDecodeIfPresent(.from)
    }
}

public extension WebhookOrganizationRenamedChangesLogin {
    init(from: String? = nil) {
        self.init()
        self.from = from
    }
}

/// Required enumerated value serialized in the `action` wire field.
public struct WebhookOrganizationRenamedAction: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let renamed = WebhookOrganizationRenamedAction(rawValue: "renamed")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
