// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// Billing domain models
/// Typed representation of the `BillingAiCreditUsageReportOrg` API schema.
public struct BillingAiCreditUsageReportOrg: Codable {
    /// Required object value serialized in the `timePeriod` wire field.
    public var timePeriod: BillingAiCreditUsageReportOrgTimePeriod
    /// The unique identifier of the organization.
    public var organization: String
    /// Required array value serialized in the `usageItems` wire field.
    public var usageItems: [BillingAiCreditUsageReportOrgUsageItemsItem]
    /// The name of the user for the usage report.
    public var user: String?
    /// The product for the usage report.
    public var product: String?
    /// The model for the usage report.
    public var model: String?

    enum CodingKeys: String, CodingKey {
        case timePeriod
        case organization
        case usageItems
        case user
        case product
        case model
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingAiCreditUsageReportOrg {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.timePeriod) else {
            throw SdkValidationError(
                field: "timePeriod",
                code: "required",
                message: "Validation failed for 'timePeriod': value is required"
            )
        }
        guard container.contains(.organization) else {
            throw SdkValidationError(
                field: "organization",
                code: "required",
                message: "Validation failed for 'organization': value is required"
            )
        }
        guard container.contains(.usageItems) else {
            throw SdkValidationError(
                field: "usageItems",
                code: "required",
                message: "Validation failed for 'usageItems': value is required"
            )
        }
        timePeriod = try container.sdkDecodeRequired(.timePeriod)
        organization = try container.sdkDecodeRequired(.organization)
        usageItems = try container.sdkDecodeRequired(.usageItems)
        user = try container.sdkDecodeIfPresent(.user)
        product = try container.sdkDecodeIfPresent(.product)
        model = try container.sdkDecodeIfPresent(.model)
    }
}

public extension BillingAiCreditUsageReportOrg {
    init(
        timePeriod: BillingAiCreditUsageReportOrgTimePeriod,
        organization: String,
        usageItems: [BillingAiCreditUsageReportOrgUsageItemsItem],
        user: String? = nil,
        product: String? = nil,
        model: String? = nil
    ) {
        (self.timePeriod, self.organization) = (timePeriod, organization)
        (self.usageItems, self.user) = (usageItems, user)
        (self.product, self.model) = (product, model)
    }
}

/// Required object value serialized in the `timePeriod` wire field.
public struct BillingAiCreditUsageReportOrgTimePeriod: Codable {
    /// The year for the usage report.
    public var year: Int
    /// The month for the usage report.
    public var month: Int?
    /// The day for the usage report.
    public var day: Int?

    enum CodingKeys: String, CodingKey {
        case year
        case month
        case day
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingAiCreditUsageReportOrgTimePeriod {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.year) else {
            throw SdkValidationError(
                field: "year",
                code: "required",
                message: "Validation failed for 'year': value is required"
            )
        }
        year = try container.sdkDecodeRequired(.year)
        month = try container.sdkDecodeIfPresent(.month)
        day = try container.sdkDecodeIfPresent(.day)
    }
}

public extension BillingAiCreditUsageReportOrgTimePeriod {
    init(year: Int, month: Int? = nil, day: Int? = nil) {
        (self.year, self.month) = (year, month)
        self.day = day
    }
}

/// Required object value serialized in the `usageItems[]` wire field.
public struct BillingAiCreditUsageReportOrgUsageItemsItem: Codable {
    /// Product name.
    public var product: String
    /// SKU name.
    public var sku: String
    /// Model name.
    public var model: String
    /// Unit type of the usage line item.
    public var unitType: String
    /// Price per unit of the usage line item.
    public var pricePerUnit: Double
    /// Gross quantity of the usage line item.
    public var grossQuantity: Double
    /// Gross amount of the usage line item.
    public var grossAmount: Double
    /// Discount quantity of the usage line item.
    public var discountQuantity: Double
    /// Discount amount of the usage line item.
    public var discountAmount: Double
    /// Net quantity of the usage line item.
    public var netQuantity: Double
    /// Net amount of the usage line item.
    public var netAmount: Double

    enum CodingKeys: String, CodingKey {
        case product
        case sku
        case model
        case unitType
        case pricePerUnit
        case grossQuantity
        case grossAmount
        case discountQuantity
        case discountAmount
        case netQuantity
        case netAmount
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingAiCreditUsageReportOrgUsageItemsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        product = try container.sdkDecodeRequired(.product)
        sku = try container.sdkDecodeRequired(.sku)
        model = try container.sdkDecodeRequired(.model)
        unitType = try container.sdkDecodeRequired(.unitType)
        pricePerUnit = try container.sdkDecodeRequired(.pricePerUnit)
        grossQuantity = try container.sdkDecodeRequired(.grossQuantity)
        grossAmount = try container.sdkDecodeRequired(.grossAmount)
        discountQuantity = try container.sdkDecodeRequired(.discountQuantity)
        discountAmount = try container.sdkDecodeRequired(.discountAmount)
        netQuantity = try container.sdkDecodeRequired(.netQuantity)
        netAmount = try container.sdkDecodeRequired(.netAmount)
    }
}

public extension BillingAiCreditUsageReportOrgUsageItemsItem {
    init(
        product: String,
        sku: String,
        model: String,
        unitType: String,
        pricePerUnit: Double,
        grossQuantity: Double,
        grossAmount: Double,
        discountQuantity: Double,
        discountAmount: Double,
        netQuantity: Double,
        netAmount: Double
    ) {
        (self.product, self.sku) = (product, sku)
        (self.model, self.unitType) = (model, unitType)
        (self.pricePerUnit, self.grossQuantity) = (pricePerUnit, grossQuantity)
        (self.grossAmount, self.discountQuantity) = (grossAmount, discountQuantity)
        (self.discountAmount, self.netQuantity) = (discountAmount, netQuantity)
        self.netAmount = netAmount
    }
}

/// Typed representation of the `BillingAiCreditUsageReportUser` API schema.
public struct BillingAiCreditUsageReportUser: Codable {
    /// Required object value serialized in the `timePeriod` wire field.
    public var timePeriod: BillingAiCreditUsageReportUserTimePeriod
    /// The unique identifier of the user.
    public var user: String
    /// Required array value serialized in the `usageItems` wire field.
    public var usageItems: [BillingAiCreditUsageReportUserUsageItemsItem]
    /// The product for the usage report.
    public var product: String?
    /// The model for the usage report.
    public var model: String?

    enum CodingKeys: String, CodingKey {
        case timePeriod
        case user
        case usageItems
        case product
        case model
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingAiCreditUsageReportUser {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.timePeriod) else {
            throw SdkValidationError(
                field: "timePeriod",
                code: "required",
                message: "Validation failed for 'timePeriod': value is required"
            )
        }
        guard container.contains(.user) else {
            throw SdkValidationError(
                field: "user",
                code: "required",
                message: "Validation failed for 'user': value is required"
            )
        }
        guard container.contains(.usageItems) else {
            throw SdkValidationError(
                field: "usageItems",
                code: "required",
                message: "Validation failed for 'usageItems': value is required"
            )
        }
        timePeriod = try container.sdkDecodeRequired(.timePeriod)
        user = try container.sdkDecodeRequired(.user)
        usageItems = try container.sdkDecodeRequired(.usageItems)
        product = try container.sdkDecodeIfPresent(.product)
        model = try container.sdkDecodeIfPresent(.model)
    }
}

public extension BillingAiCreditUsageReportUser {
    init(
        timePeriod: BillingAiCreditUsageReportUserTimePeriod,
        user: String,
        usageItems: [BillingAiCreditUsageReportUserUsageItemsItem],
        product: String? = nil,
        model: String? = nil
    ) {
        (self.timePeriod, self.user) = (timePeriod, user)
        (self.usageItems, self.product) = (usageItems, product)
        self.model = model
    }
}

/// Required object value serialized in the `timePeriod` wire field.
public struct BillingAiCreditUsageReportUserTimePeriod: Codable {
    /// The year for the usage report.
    public var year: Int
    /// The month for the usage report.
    public var month: Int?
    /// The day for the usage report.
    public var day: Int?

    enum CodingKeys: String, CodingKey {
        case year
        case month
        case day
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingAiCreditUsageReportUserTimePeriod {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.year) else {
            throw SdkValidationError(
                field: "year",
                code: "required",
                message: "Validation failed for 'year': value is required"
            )
        }
        year = try container.sdkDecodeRequired(.year)
        month = try container.sdkDecodeIfPresent(.month)
        day = try container.sdkDecodeIfPresent(.day)
    }
}

public extension BillingAiCreditUsageReportUserTimePeriod {
    init(year: Int, month: Int? = nil, day: Int? = nil) {
        (self.year, self.month) = (year, month)
        self.day = day
    }
}

/// Required object value serialized in the `usageItems[]` wire field.
public struct BillingAiCreditUsageReportUserUsageItemsItem: Codable {
    /// Product name.
    public var product: String
    /// SKU name.
    public var sku: String
    /// Model name.
    public var model: String
    /// Unit type of the usage line item.
    public var unitType: String
    /// Price per unit of the usage line item.
    public var pricePerUnit: Double
    /// Gross quantity of the usage line item.
    public var grossQuantity: Double
    /// Gross amount of the usage line item.
    public var grossAmount: Double
    /// Discount quantity of the usage line item.
    public var discountQuantity: Double
    /// Discount amount of the usage line item.
    public var discountAmount: Double
    /// Net quantity of the usage line item.
    public var netQuantity: Double
    /// Net amount of the usage line item.
    public var netAmount: Double

    enum CodingKeys: String, CodingKey {
        case product
        case sku
        case model
        case unitType
        case pricePerUnit
        case grossQuantity
        case grossAmount
        case discountQuantity
        case discountAmount
        case netQuantity
        case netAmount
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingAiCreditUsageReportUserUsageItemsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        product = try container.sdkDecodeRequired(.product)
        sku = try container.sdkDecodeRequired(.sku)
        model = try container.sdkDecodeRequired(.model)
        unitType = try container.sdkDecodeRequired(.unitType)
        pricePerUnit = try container.sdkDecodeRequired(.pricePerUnit)
        grossQuantity = try container.sdkDecodeRequired(.grossQuantity)
        grossAmount = try container.sdkDecodeRequired(.grossAmount)
        discountQuantity = try container.sdkDecodeRequired(.discountQuantity)
        discountAmount = try container.sdkDecodeRequired(.discountAmount)
        netQuantity = try container.sdkDecodeRequired(.netQuantity)
        netAmount = try container.sdkDecodeRequired(.netAmount)
    }
}

public extension BillingAiCreditUsageReportUserUsageItemsItem {
    init(
        product: String,
        sku: String,
        model: String,
        unitType: String,
        pricePerUnit: Double,
        grossQuantity: Double,
        grossAmount: Double,
        discountQuantity: Double,
        discountAmount: Double,
        netQuantity: Double,
        netAmount: Double
    ) {
        (self.product, self.sku) = (product, sku)
        (self.model, self.unitType) = (model, unitType)
        (self.pricePerUnit, self.grossQuantity) = (pricePerUnit, grossQuantity)
        (self.grossAmount, self.discountQuantity) = (grossAmount, discountQuantity)
        (self.discountAmount, self.netQuantity) = (discountAmount, netQuantity)
        self.netAmount = netAmount
    }
}

/// Typed representation of the `BillingPremiumRequestUsageReportOrg` API schema.
public struct BillingPremiumRequestUsageReportOrg: Codable {
    /// Required object value serialized in the `timePeriod` wire field.
    public var timePeriod: BillingPremiumRequestUsageReportOrgTimePeriod
    /// The unique identifier of the organization.
    public var organization: String
    /// Required array value serialized in the `usageItems` wire field.
    public var usageItems: [BillingPremiumRequestUsageReportOrgUsageItemsItem]
    /// The name of the user for the usage report.
    public var user: String?
    /// The product for the usage report.
    public var product: String?
    /// The model for the usage report.
    public var model: String?

    enum CodingKeys: String, CodingKey {
        case timePeriod
        case organization
        case usageItems
        case user
        case product
        case model
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingPremiumRequestUsageReportOrg {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.timePeriod) else {
            throw SdkValidationError(
                field: "timePeriod",
                code: "required",
                message: "Validation failed for 'timePeriod': value is required"
            )
        }
        guard container.contains(.organization) else {
            throw SdkValidationError(
                field: "organization",
                code: "required",
                message: "Validation failed for 'organization': value is required"
            )
        }
        guard container.contains(.usageItems) else {
            throw SdkValidationError(
                field: "usageItems",
                code: "required",
                message: "Validation failed for 'usageItems': value is required"
            )
        }
        timePeriod = try container.sdkDecodeRequired(.timePeriod)
        organization = try container.sdkDecodeRequired(.organization)
        usageItems = try container.sdkDecodeRequired(.usageItems)
        user = try container.sdkDecodeIfPresent(.user)
        product = try container.sdkDecodeIfPresent(.product)
        model = try container.sdkDecodeIfPresent(.model)
    }
}

public extension BillingPremiumRequestUsageReportOrg {
    init(
        timePeriod: BillingPremiumRequestUsageReportOrgTimePeriod,
        organization: String,
        usageItems: [BillingPremiumRequestUsageReportOrgUsageItemsItem],
        user: String? = nil,
        product: String? = nil,
        model: String? = nil
    ) {
        (self.timePeriod, self.organization) = (timePeriod, organization)
        (self.usageItems, self.user) = (usageItems, user)
        (self.product, self.model) = (product, model)
    }
}

/// Required object value serialized in the `timePeriod` wire field.
public struct BillingPremiumRequestUsageReportOrgTimePeriod: Codable {
    /// The year for the usage report.
    public var year: Int
    /// The month for the usage report.
    public var month: Int?
    /// The day for the usage report.
    public var day: Int?

    enum CodingKeys: String, CodingKey {
        case year
        case month
        case day
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingPremiumRequestUsageReportOrgTimePeriod {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.year) else {
            throw SdkValidationError(
                field: "year",
                code: "required",
                message: "Validation failed for 'year': value is required"
            )
        }
        year = try container.sdkDecodeRequired(.year)
        month = try container.sdkDecodeIfPresent(.month)
        day = try container.sdkDecodeIfPresent(.day)
    }
}

public extension BillingPremiumRequestUsageReportOrgTimePeriod {
    init(year: Int, month: Int? = nil, day: Int? = nil) {
        (self.year, self.month) = (year, month)
        self.day = day
    }
}

/// Required object value serialized in the `usageItems[]` wire field.
public struct BillingPremiumRequestUsageReportOrgUsageItemsItem: Codable {
    /// Product name.
    public var product: String
    /// SKU name.
    public var sku: String
    /// Model name.
    public var model: String
    /// Unit type of the usage line item.
    public var unitType: String
    /// Price per unit of the usage line item.
    public var pricePerUnit: Double
    /// Gross quantity of the usage line item.
    public var grossQuantity: Double
    /// Gross amount of the usage line item.
    public var grossAmount: Double
    /// Discount quantity of the usage line item.
    public var discountQuantity: Double
    /// Discount amount of the usage line item.
    public var discountAmount: Double
    /// Net quantity of the usage line item.
    public var netQuantity: Double
    /// Net amount of the usage line item.
    public var netAmount: Double

    enum CodingKeys: String, CodingKey {
        case product
        case sku
        case model
        case unitType
        case pricePerUnit
        case grossQuantity
        case grossAmount
        case discountQuantity
        case discountAmount
        case netQuantity
        case netAmount
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BillingPremiumRequestUsageReportOrgUsageItemsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        product = try container.sdkDecodeRequired(.product)
        sku = try container.sdkDecodeRequired(.sku)
        model = try container.sdkDecodeRequired(.model)
        unitType = try container.sdkDecodeRequired(.unitType)
        pricePerUnit = try container.sdkDecodeRequired(.pricePerUnit)
        grossQuantity = try container.sdkDecodeRequired(.grossQuantity)
        grossAmount = try container.sdkDecodeRequired(.grossAmount)
        discountQuantity = try container.sdkDecodeRequired(.discountQuantity)
        discountAmount = try container.sdkDecodeRequired(.discountAmount)
        netQuantity = try container.sdkDecodeRequired(.netQuantity)
        netAmount = try container.sdkDecodeRequired(.netAmount)
    }
}

public extension BillingPremiumRequestUsageReportOrgUsageItemsItem {
    init(
        product: String,
        sku: String,
        model: String,
        unitType: String,
        pricePerUnit: Double,
        grossQuantity: Double,
        grossAmount: Double,
        discountQuantity: Double,
        discountAmount: Double,
        netQuantity: Double,
        netAmount: Double
    ) {
        (self.product, self.sku) = (product, sku)
        (self.model, self.unitType) = (model, unitType)
        (self.pricePerUnit, self.grossQuantity) = (pricePerUnit, grossQuantity)
        (self.grossAmount, self.discountQuantity) = (grossAmount, discountQuantity)
        (self.discountAmount, self.netQuantity) = (discountAmount, netQuantity)
        self.netAmount = netAmount
    }
}

/// Typed representation of the `BillingPremiumRequestUsageReportUser` API schema.
public struct BillingPremiumRequestUsageReportUser: Codable {
    /// Required object value serialized in the `timePeriod` wire field.
    public var timePeriod: BillingPremiumRequestUsageReportUserTimePeriod
    /// The unique identifier of the user.
    public var user: String
    /// Required array value serialized in the `usageItems` wire field.
    public var usageItems: [BillingPremiumRequestUsageReportUserUsageItemsItem]
    /// The product for the usage report.
    public var product: String?
    /// The model for the usage report.
    public var model: String?

    enum CodingKeys: String, CodingKey {
        case timePeriod
        case user
        case usageItems
        case product
        case model
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}
