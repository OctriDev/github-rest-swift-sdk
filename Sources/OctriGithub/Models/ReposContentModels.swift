// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// ReposContent domain models
public typealias ContentDirectory = [ContentDirectoryItem]

public struct ContentDirectoryItem: Codable {
    public var type: ContentDirectoryItemType
    public var size: Int
    public var name: String
    public var path: String
    public var sha: String
    public var url: String
    public var gitUrl: String?
    public var htmlUrl: String?
    public var downloadUrl: String?
    public var links: ContentDirectoryItemLinks
    public var content: String?

    enum CodingKeys: String, CodingKey {
        case type
        case size
        case name
        case path
        case sha
        case url
        case gitUrl = "git_url"
        case htmlUrl = "html_url"
        case downloadUrl = "download_url"
        case links = "_links"
        case content
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentDirectoryItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        type = try container.sdkDecodeRequired(.type)
        size = try container.sdkDecodeRequired(.size)
        name = try container.sdkDecodeRequired(.name)
        path = try container.sdkDecodeRequired(.path)
        sha = try container.sdkDecodeRequired(.sha)
        url = try container.sdkDecodeRequired(.url)
        gitUrl = try container.sdkDecodeIfPresent(.gitUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        downloadUrl = try container.sdkDecodeIfPresent(.downloadUrl)
        links = try container.sdkDecodeRequired(.links)
        content = try container.sdkDecodeIfPresent(.content)
        try sdkValidateUri("url", url)
        if let value = gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

public extension ContentDirectoryItem {
    init(
        type: ContentDirectoryItemType,
        size: Int,
        name: String,
        path: String,
        sha: String,
        url: String,
        gitUrl: String?,
        htmlUrl: String?,
        downloadUrl: String?,
        links: ContentDirectoryItemLinks,
        content: String? = nil
    ) throws {
        (self.type, self.size) = (type, size)
        (self.name, self.path) = (name, path)
        (self.sha, self.url) = (sha, url)
        (self.gitUrl, self.htmlUrl) = (gitUrl, htmlUrl)
        (self.downloadUrl, self.links) = (downloadUrl, links)
        self.content = content
        try sdkValidateUri("url", self.url)
        if let value = self.gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = self.downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

public struct ContentDirectoryItemLinks: Codable {
    public var git: String?
    public var html: String?
    public var `self`: String

    enum CodingKeys: String, CodingKey {
        case git
        case html
        case `self`
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentDirectoryItemLinks {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.git) else {
            throw SdkValidationError(
                field: "git",
                code: "required",
                message: "Validation failed for 'git': value is required"
            )
        }
        guard container.contains(.html) else {
            throw SdkValidationError(
                field: "html",
                code: "required",
                message: "Validation failed for 'html': value is required"
            )
        }
        guard container.contains(.`self`) else {
            throw SdkValidationError(
                field: "self",
                code: "required",
                message: "Validation failed for 'self': value is required"
            )
        }
        git = try container.sdkDecodeIfPresent(.git)
        html = try container.sdkDecodeIfPresent(.html)
        self.`self` = try container.sdkDecodeRequired(.`self`)
        if let value = git {
            try sdkValidateUri("git", value)
        }
        if let value = html {
            try sdkValidateUri("html", value)
        }
        try sdkValidateUri("self", self.`self`)
    }
}

public extension ContentDirectoryItemLinks {
    init(git: String?, html: String?, self selfValue: String) throws {
        (self.git, self.html) = (git, html)
        self.`self` = selfValue
        if let value = self.git {
            try sdkValidateUri("git", value)
        }
        if let value = self.html {
            try sdkValidateUri("html", value)
        }
        try sdkValidateUri("self", self.`self`)
    }
}

/// Content File
public struct ContentFile: Codable {
    /// Required enumerated value serialized in the `type` wire field.
    public var type: ContentFileType
    /// Required `string` value serialized in the `encoding` wire field.
    public var encoding: String
    /// Required `integer` value serialized in the `size` wire field.
    public var size: Int
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `path` wire field.
    public var path: String
    /// Required `string` value serialized in the `content` wire field.
    public var content: String
    /// Required `string` value serialized in the `sha` wire field.
    public var sha: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `git_url` wire field.
    public var gitUrl: String?
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String?
    /// Required `uri`-formatted value serialized in the `download_url` wire field.
    public var downloadUrl: String?
    /// Required object value serialized in the `_links` wire field.
    public var links: ContentFileLinks
    /// Optional `string` value serialized in the `target` wire field.
    /// - Example: "\"actual/actual.md\""
    public var target: String?
    /// Optional `string` value serialized in the `submodule_git_url` wire field.
    /// - Example: "\"git://example.com/defunkt/dotjs.git\""
    public var submoduleGitUrl: String?

    enum CodingKeys: String, CodingKey {
        case type
        case encoding
        case size
        case name
        case path
        case content
        case sha
        case url
        case gitUrl = "git_url"
        case htmlUrl = "html_url"
        case downloadUrl = "download_url"
        case links = "_links"
        case target
        case submoduleGitUrl = "submodule_git_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentFile {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        type = try container.sdkDecodeRequired(.type)
        encoding = try container.sdkDecodeRequired(.encoding)
        size = try container.sdkDecodeRequired(.size)
        name = try container.sdkDecodeRequired(.name)
        path = try container.sdkDecodeRequired(.path)
        content = try container.sdkDecodeRequired(.content)
        sha = try container.sdkDecodeRequired(.sha)
        url = try container.sdkDecodeRequired(.url)
        gitUrl = try container.sdkDecodeIfPresent(.gitUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        downloadUrl = try container.sdkDecodeIfPresent(.downloadUrl)
        links = try container.sdkDecodeRequired(.links)
        target = try container.sdkDecodeIfPresent(.target)
        submoduleGitUrl = try container.sdkDecodeIfPresent(.submoduleGitUrl)
        try sdkValidateUri("url", url)
        if let value = gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

public extension ContentFile {
    init(
        type: ContentFileType,
        encoding: String,
        size: Int,
        name: String,
        path: String,
        content: String,
        sha: String,
        url: String,
        gitUrl: String?,
        htmlUrl: String?,
        downloadUrl: String?,
        links: ContentFileLinks,
        target: String? = nil,
        submoduleGitUrl: String? = nil
    ) throws {
        (self.type, self.encoding) = (type, encoding)
        (self.size, self.name) = (size, name)
        (self.path, self.content) = (path, content)
        (self.sha, self.url) = (sha, url)
        (self.gitUrl, self.htmlUrl) = (gitUrl, htmlUrl)
        (self.downloadUrl, self.links) = (downloadUrl, links)
        (self.target, self.submoduleGitUrl) = (target, submoduleGitUrl)
        try sdkValidateUri("url", self.url)
        if let value = self.gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = self.downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

/// Required object value serialized in the `_links` wire field.
public struct ContentFileLinks: Codable {
    /// Required `uri`-formatted value serialized in the `git` wire field.
    public var git: String?
    /// Required `uri`-formatted value serialized in the `html` wire field.
    public var html: String?
    /// Required `uri`-formatted value serialized in the `self` wire field.
    public var `self`: String

    enum CodingKeys: String, CodingKey {
        case git
        case html
        case `self`
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentFileLinks {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.git) else {
            throw SdkValidationError(
                field: "git",
                code: "required",
                message: "Validation failed for 'git': value is required"
            )
        }
        guard container.contains(.html) else {
            throw SdkValidationError(
                field: "html",
                code: "required",
                message: "Validation failed for 'html': value is required"
            )
        }
        guard container.contains(.`self`) else {
            throw SdkValidationError(
                field: "self",
                code: "required",
                message: "Validation failed for 'self': value is required"
            )
        }
        git = try container.sdkDecodeIfPresent(.git)
        html = try container.sdkDecodeIfPresent(.html)
        self.`self` = try container.sdkDecodeRequired(.`self`)
        if let value = git {
            try sdkValidateUri("git", value)
        }
        if let value = html {
            try sdkValidateUri("html", value)
        }
        try sdkValidateUri("self", self.`self`)
    }
}

public extension ContentFileLinks {
    init(git: String?, html: String?, self selfValue: String) throws {
        (self.git, self.html) = (git, html)
        self.`self` = selfValue
        if let value = self.git {
            try sdkValidateUri("git", value)
        }
        if let value = self.html {
            try sdkValidateUri("html", value)
        }
        try sdkValidateUri("self", self.`self`)
    }
}

/// An object describing a submodule
public struct ContentSubmodule: Codable {
    /// Required enumerated value serialized in the `type` wire field.
    public var type: ContentSubmoduleType
    /// Required `uri`-formatted value serialized in the `submodule_git_url` wire field.
    public var submoduleGitUrl: String
    /// Required `integer` value serialized in the `size` wire field.
    public var size: Int
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `path` wire field.
    public var path: String
    /// Required `string` value serialized in the `sha` wire field.
    public var sha: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `git_url` wire field.
    public var gitUrl: String?
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String?
    /// Required `uri`-formatted value serialized in the `download_url` wire field.
    public var downloadUrl: String?
    /// Required object value serialized in the `_links` wire field.
    public var links: ContentSubmoduleLinks

    enum CodingKeys: String, CodingKey {
        case type
        case submoduleGitUrl = "submodule_git_url"
        case size
        case name
        case path
        case sha
        case url
        case gitUrl = "git_url"
        case htmlUrl = "html_url"
        case downloadUrl = "download_url"
        case links = "_links"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentSubmodule {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        type = try container.sdkDecodeRequired(.type)
        submoduleGitUrl = try container.sdkDecodeRequired(.submoduleGitUrl)
        size = try container.sdkDecodeRequired(.size)
        name = try container.sdkDecodeRequired(.name)
        path = try container.sdkDecodeRequired(.path)
        sha = try container.sdkDecodeRequired(.sha)
        url = try container.sdkDecodeRequired(.url)
        gitUrl = try container.sdkDecodeIfPresent(.gitUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        downloadUrl = try container.sdkDecodeIfPresent(.downloadUrl)
        links = try container.sdkDecodeRequired(.links)
        try sdkValidateUri("submodule_git_url", submoduleGitUrl)
        try sdkValidateUri("url", url)
        if let value = gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

public extension ContentSubmodule {
    init(
        type: ContentSubmoduleType,
        submoduleGitUrl: String,
        size: Int,
        name: String,
        path: String,
        sha: String,
        url: String,
        gitUrl: String?,
        htmlUrl: String?,
        downloadUrl: String?,
        links: ContentSubmoduleLinks
    ) throws {
        (self.type, self.submoduleGitUrl) = (type, submoduleGitUrl)
        (self.size, self.name) = (size, name)
        (self.path, self.sha) = (path, sha)
        (self.url, self.gitUrl) = (url, gitUrl)
        (self.htmlUrl, self.downloadUrl) = (htmlUrl, downloadUrl)
        self.links = links
        try sdkValidateUri("submodule_git_url", self.submoduleGitUrl)
        try sdkValidateUri("url", self.url)
        if let value = self.gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = self.downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

/// Required object value serialized in the `_links` wire field.
public struct ContentSubmoduleLinks: Codable {
    /// Required `uri`-formatted value serialized in the `git` wire field.
    public var git: String?
    /// Required `uri`-formatted value serialized in the `html` wire field.
    public var html: String?
    /// Required `uri`-formatted value serialized in the `self` wire field.
    public var `self`: String

    enum CodingKeys: String, CodingKey {
        case git
        case html
        case `self`
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentSubmoduleLinks {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.git) else {
            throw SdkValidationError(
                field: "git",
                code: "required",
                message: "Validation failed for 'git': value is required"
            )
        }
        guard container.contains(.html) else {
            throw SdkValidationError(
                field: "html",
                code: "required",
                message: "Validation failed for 'html': value is required"
            )
        }
        guard container.contains(.`self`) else {
            throw SdkValidationError(
                field: "self",
                code: "required",
                message: "Validation failed for 'self': value is required"
            )
        }
        git = try container.sdkDecodeIfPresent(.git)
        html = try container.sdkDecodeIfPresent(.html)
        self.`self` = try container.sdkDecodeRequired(.`self`)
        if let value = git {
            try sdkValidateUri("git", value)
        }
        if let value = html {
            try sdkValidateUri("html", value)
        }
        try sdkValidateUri("self", self.`self`)
    }
}

public extension ContentSubmoduleLinks {
    init(git: String?, html: String?, self selfValue: String) throws {
        (self.git, self.html) = (git, html)
        self.`self` = selfValue
        if let value = self.git {
            try sdkValidateUri("git", value)
        }
        if let value = self.html {
            try sdkValidateUri("html", value)
        }
        try sdkValidateUri("self", self.`self`)
    }
}

/// An object describing a symlink
public struct ContentSymlink: Codable {
    /// Required enumerated value serialized in the `type` wire field.
    public var type: ContentSymlinkType
    /// Required `string` value serialized in the `target` wire field.
    public var target: String
    /// Required `integer` value serialized in the `size` wire field.
    public var size: Int
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `path` wire field.
    public var path: String
    /// Required `string` value serialized in the `sha` wire field.
    public var sha: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `git_url` wire field.
    public var gitUrl: String?
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String?
    /// Required `uri`-formatted value serialized in the `download_url` wire field.
    public var downloadUrl: String?
    /// Required object value serialized in the `_links` wire field.
    public var links: ContentSymlinkLinks

    enum CodingKeys: String, CodingKey {
        case type
        case target
        case size
        case name
        case path
        case sha
        case url
        case gitUrl = "git_url"
        case htmlUrl = "html_url"
        case downloadUrl = "download_url"
        case links = "_links"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension ContentSymlink {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        type = try container.sdkDecodeRequired(.type)
        target = try container.sdkDecodeRequired(.target)
        size = try container.sdkDecodeRequired(.size)
        name = try container.sdkDecodeRequired(.name)
        path = try container.sdkDecodeRequired(.path)
        sha = try container.sdkDecodeRequired(.sha)
        url = try container.sdkDecodeRequired(.url)
        gitUrl = try container.sdkDecodeIfPresent(.gitUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        downloadUrl = try container.sdkDecodeIfPresent(.downloadUrl)
        links = try container.sdkDecodeRequired(.links)
        try sdkValidateUri("url", url)
        if let value = gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}

public extension ContentSymlink {
    init(
        type: ContentSymlinkType,
        target: String,
        size: Int,
        name: String,
        path: String,
        sha: String,
        url: String,
        gitUrl: String?,
        htmlUrl: String?,
        downloadUrl: String?,
        links: ContentSymlinkLinks
    ) throws {
        (self.type, self.target) = (type, target)
        (self.size, self.name) = (size, name)
        (self.path, self.sha) = (path, sha)
        (self.url, self.gitUrl) = (url, gitUrl)
        (self.htmlUrl, self.downloadUrl) = (htmlUrl, downloadUrl)
        self.links = links
        try sdkValidateUri("url", self.url)
        if let value = self.gitUrl {
            try sdkValidateUri("git_url", value)
        }
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
        if let value = self.downloadUrl {
            try sdkValidateUri("download_url", value)
        }
    }
}
