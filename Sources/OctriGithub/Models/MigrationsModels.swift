// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// Migrations domain models
/// A repository import from an external source.
public struct Import: Codable {
    /// Required `string` value serialized in the `vcs` wire field.
    public var vcs: String?
    /// The URL of the originating repository.
    public var vcsUrl: String
    /// Required enumerated value serialized in the `status` wire field.
    public var status: ImportStatus
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String
    /// Required `uri`-formatted value serialized in the `authors_url` wire field.
    public var authorsUrl: String
    /// Required `uri`-formatted value serialized in the `repository_url` wire field.
    public var repositoryUrl: String
    /// Optional `boolean` value serialized in the `use_lfs` wire field.
    public var useLfs: Bool?
    /// Optional `string` value serialized in the `svc_root` wire field.
    public var svcRoot: String?
    /// Optional `string` value serialized in the `tfvc_project` wire field.
    public var tfvcProject: String?
    /// Optional `string` value serialized in the `status_text` wire field.
    public var statusText: String?
    /// Optional `string` value serialized in the `failed_step` wire field.
    public var failedStep: String?
    /// Optional `string` value serialized in the `error_message` wire field.
    public var errorMessage: String?
    /// Optional `integer` value serialized in the `import_percent` wire field.
    public var importPercent: Int?
    /// Optional `integer` value serialized in the `commit_count` wire field.
    public var commitCount: Int?
    /// Optional `integer` value serialized in the `push_percent` wire field.
    public var pushPercent: Int?
    /// Optional `boolean` value serialized in the `has_large_files` wire field.
    public var hasLargeFiles: Bool?
    /// Optional `integer` value serialized in the `large_files_size` wire field.
    public var largeFilesSize: Int?
    /// Optional `integer` value serialized in the `large_files_count` wire field.
    public var largeFilesCount: Int?
    /// Optional array value serialized in the `project_choices` wire field.
    public var projectChoices: [ImportProjectChoicesItem]?
    /// Optional `string` value serialized in the `message` wire field.
    public var message: String?
    /// Optional `integer` value serialized in the `authors_count` wire field.
    public var authorsCount: Int?
    /// Optional `string` value serialized in the `svn_root` wire field.
    public var svnRoot: String?

    enum CodingKeys: String, CodingKey {
        case vcs
        case vcsUrl = "vcs_url"
        case status
        case url
        case htmlUrl = "html_url"
        case authorsUrl = "authors_url"
        case repositoryUrl = "repository_url"
        case useLfs = "use_lfs"
        case svcRoot = "svc_root"
        case tfvcProject = "tfvc_project"
        case statusText = "status_text"
        case failedStep = "failed_step"
        case errorMessage = "error_message"
        case importPercent = "import_percent"
        case commitCount = "commit_count"
        case pushPercent = "push_percent"
        case hasLargeFiles = "has_large_files"
        case largeFilesSize = "large_files_size"
        case largeFilesCount = "large_files_count"
        case projectChoices = "project_choices"
        case message
        case authorsCount = "authors_count"
        case svnRoot = "svn_root"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Import {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.vcs) else {
            throw SdkValidationError(
                field: "vcs",
                code: "required",
                message: "Validation failed for 'vcs': value is required"
            )
        }
        guard container.contains(.vcsUrl) else {
            throw SdkValidationError(
                field: "vcs_url",
                code: "required",
                message: "Validation failed for 'vcs_url': value is required"
            )
        }
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        guard container.contains(.htmlUrl) else {
            throw SdkValidationError(
                field: "html_url",
                code: "required",
                message: "Validation failed for 'html_url': value is required"
            )
        }
        guard container.contains(.authorsUrl) else {
            throw SdkValidationError(
                field: "authors_url",
                code: "required",
                message: "Validation failed for 'authors_url': value is required"
            )
        }
        guard container.contains(.repositoryUrl) else {
            throw SdkValidationError(
                field: "repository_url",
                code: "required",
                message: "Validation failed for 'repository_url': value is required"
            )
        }
        vcs = try container.sdkDecodeIfPresent(.vcs)
        vcsUrl = try container.sdkDecodeRequired(.vcsUrl)
        status = try container.sdkDecodeRequired(.status)
        url = try container.sdkDecodeRequired(.url)
        htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        authorsUrl = try container.sdkDecodeRequired(.authorsUrl)
        repositoryUrl = try container.sdkDecodeRequired(.repositoryUrl)
        useLfs = try container.sdkDecodeIfPresent(.useLfs)
        svcRoot = try container.sdkDecodeIfPresent(.svcRoot)
        tfvcProject = try container.sdkDecodeIfPresent(.tfvcProject)
        statusText = try container.sdkDecodeIfPresent(.statusText)
        failedStep = try container.sdkDecodeIfPresent(.failedStep)
        errorMessage = try container.sdkDecodeIfPresent(.errorMessage)
        importPercent = try container.sdkDecodeIfPresent(.importPercent)
        commitCount = try container.sdkDecodeIfPresent(.commitCount)
        pushPercent = try container.sdkDecodeIfPresent(.pushPercent)
        hasLargeFiles = try container.sdkDecodeIfPresent(.hasLargeFiles)
        largeFilesSize = try container.sdkDecodeIfPresent(.largeFilesSize)
        largeFilesCount = try container.sdkDecodeIfPresent(.largeFilesCount)
        projectChoices = try container.sdkDecodeIfPresent(.projectChoices)
        message = try container.sdkDecodeIfPresent(.message)
        authorsCount = try container.sdkDecodeIfPresent(.authorsCount)
        svnRoot = try container.sdkDecodeIfPresent(.svnRoot)
        try sdkValidateUri("url", url)
        try sdkValidateUri("html_url", htmlUrl)
        try sdkValidateUri("authors_url", authorsUrl)
        try sdkValidateUri("repository_url", repositoryUrl)
    }
}

public extension Import {
    init(
        vcs: String?,
        vcsUrl: String,
        status: ImportStatus,
        url: String,
        htmlUrl: String,
        authorsUrl: String,
        repositoryUrl: String,
        useLfs: Bool? = nil,
        svcRoot: String? = nil,
        tfvcProject: String? = nil,
        statusText: String? = nil,
        failedStep: String? = nil,
        errorMessage: String? = nil,
        importPercent: Int? = nil,
        commitCount: Int? = nil,
        pushPercent: Int? = nil,
        hasLargeFiles: Bool? = nil,
        largeFilesSize: Int? = nil,
        largeFilesCount: Int? = nil,
        projectChoices: [ImportProjectChoicesItem]? = nil,
        message: String? = nil,
        authorsCount: Int? = nil,
        svnRoot: String? = nil
    ) throws {
        (self.vcs, self.vcsUrl) = (vcs, vcsUrl)
        (self.status, self.url) = (status, url)
        (self.htmlUrl, self.authorsUrl) = (htmlUrl, authorsUrl)
        (self.repositoryUrl, self.useLfs) = (repositoryUrl, useLfs)
        (self.svcRoot, self.tfvcProject) = (svcRoot, tfvcProject)
        (self.statusText, self.failedStep) = (statusText, failedStep)
        (self.errorMessage, self.importPercent) = (errorMessage, importPercent)
        (self.commitCount, self.pushPercent) = (commitCount, pushPercent)
        (self.hasLargeFiles, self.largeFilesSize) = (hasLargeFiles, largeFilesSize)
        (self.largeFilesCount, self.projectChoices) = (largeFilesCount, projectChoices)
        (self.message, self.authorsCount) = (message, authorsCount)
        self.svnRoot = svnRoot
        try sdkValidateUri("url", self.url)
        try sdkValidateUri("html_url", self.htmlUrl)
        try sdkValidateUri("authors_url", self.authorsUrl)
        try sdkValidateUri("repository_url", self.repositoryUrl)
    }
}

/// Required object value serialized in the `project_choices[]` wire field.
public struct ImportProjectChoicesItem: Codable {
    /// Optional `string` value serialized in the `vcs` wire field.
    public var vcs: String?
    /// Optional `string` value serialized in the `tfvc_project` wire field.
    public var tfvcProject: String?
    /// Optional `string` value serialized in the `human_name` wire field.
    public var humanName: String?

    enum CodingKeys: String, CodingKey {
        case vcs
        case tfvcProject = "tfvc_project"
        case humanName = "human_name"
    }

    init() {
        (vcs, tfvcProject, humanName) = (nil, nil, nil)
    }
}

public extension ImportProjectChoicesItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        vcs = try container.sdkDecodeIfPresent(.vcs)
        tfvcProject = try container.sdkDecodeIfPresent(.tfvcProject)
        humanName = try container.sdkDecodeIfPresent(.humanName)
    }
}

public extension ImportProjectChoicesItem {
    init(vcs: String? = nil, tfvcProject: String? = nil, humanName: String? = nil) {
        self.init()
        (self.vcs, self.tfvcProject) = (vcs, tfvcProject)
        self.humanName = humanName
    }
}

/// A migration.
public struct Migration: Codable {
    /// Required `int64`-formatted value serialized in the `id` wire field.
    /// - Example: 79
    public var id: Int
    /// A GitHub user.
    public var owner: NullableSimpleUser?
    /// Required `string` value serialized in the `guid` wire field.
    /// - Example: "0b989ba4-242f-11e5-81e1-c7b6966d2516"
    public var guid: String
    /// Required `string` value serialized in the `state` wire field.
    /// - Example: "pending"
    public var state: String
    /// Required `boolean` value serialized in the `lock_repositories` wire field.
    /// - Example: true
    public var lockRepositories: Bool
    /// Required `boolean` value serialized in the `exclude_metadata` wire field.
    public var excludeMetadata: Bool
    /// Required `boolean` value serialized in the `exclude_git_data` wire field.
    public var excludeGitData: Bool
    /// Required `boolean` value serialized in the `exclude_attachments` wire field.
    public var excludeAttachments: Bool
    /// Required `boolean` value serialized in the `exclude_releases` wire field.
    public var excludeReleases: Bool
    /// Required `boolean` value serialized in the `exclude_owner_projects` wire field.
    public var excludeOwnerProjects: Bool
    /// Required `boolean` value serialized in the `org_metadata_only` wire field.
    public var orgMetadataOnly: Bool
    /// The repositories included in the migration. Only returned for export migrations.
    public var repositories: [Repository]
    /// Required `uri`-formatted value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/orgs/octo-org/migrations/79"
    public var url: String
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    /// - Example: "2015-07-06T15:33:38-07:00"
    public var createdAt: Date
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    /// - Example: "2015-07-06T15:33:38-07:00"
    public var updatedAt: Date
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Optional `uri`-formatted value serialized in the `archive_url` wire field.
    public var archiveUrl: String?
    /// Exclude related items from being returned in the response in order to improve performance of the request.
    /// The array can include any of: `"repositories"`.
    public var exclude: [String]?

    enum CodingKeys: String, CodingKey {
        case id
        case owner
        case guid
        case state
        case lockRepositories = "lock_repositories"
        case excludeMetadata = "exclude_metadata"
        case excludeGitData = "exclude_git_data"
        case excludeAttachments = "exclude_attachments"
        case excludeReleases = "exclude_releases"
        case excludeOwnerProjects = "exclude_owner_projects"
        case orgMetadataOnly = "org_metadata_only"
        case repositories
        case url
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case nodeId = "node_id"
        case archiveUrl = "archive_url"
        case exclude
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Migration {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        owner = try container.sdkDecodeIfPresent(.owner)
        guid = try container.sdkDecodeRequired(.guid)
        state = try container.sdkDecodeRequired(.state)
        lockRepositories = try container.sdkDecodeRequired(.lockRepositories)
        excludeMetadata = try container.sdkDecodeRequired(.excludeMetadata)
        excludeGitData = try container.sdkDecodeRequired(.excludeGitData)
        excludeAttachments = try container.sdkDecodeRequired(.excludeAttachments)
        excludeReleases = try container.sdkDecodeRequired(.excludeReleases)
        excludeOwnerProjects = try container.sdkDecodeRequired(.excludeOwnerProjects)
        orgMetadataOnly = try container.sdkDecodeRequired(.orgMetadataOnly)
        repositories = try container.sdkDecodeRequired(.repositories)
        url = try container.sdkDecodeRequired(.url)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        updatedAt = try container.sdkDecodeRequired(.updatedAt)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        archiveUrl = try container.sdkDecodeIfPresent(.archiveUrl)
        exclude = try container.sdkDecodeIfPresent(.exclude)
        try sdkValidateUri("url", url)
        try sdkValidateDateTime("created_at", sdkWireString(createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(updatedAt))
        if let value = archiveUrl {
            try sdkValidateUri("archive_url", value)
        }
    }
}

public extension Migration {
    init(
        id: Int,
        owner: NullableSimpleUser?,
        guid: String,
        state: String,
        lockRepositories: Bool,
        excludeMetadata: Bool,
        excludeGitData: Bool,
        excludeAttachments: Bool,
        excludeReleases: Bool,
        excludeOwnerProjects: Bool,
        orgMetadataOnly: Bool,
        repositories: [Repository],
        url: String,
        createdAt: Date,
        updatedAt: Date,
        nodeId: String,
        archiveUrl: String? = nil,
        exclude: [String]? = nil
    ) throws {
        (self.id, self.owner) = (id, owner)
        (self.guid, self.state) = (guid, state)
        (self.lockRepositories, self.excludeMetadata) = (lockRepositories, excludeMetadata)
        (self.excludeGitData, self.excludeAttachments) = (excludeGitData, excludeAttachments)
        (self.excludeReleases, self.excludeOwnerProjects) = (excludeReleases, excludeOwnerProjects)
        (self.orgMetadataOnly, self.repositories) = (orgMetadataOnly, repositories)
        (self.url, self.createdAt) = (url, createdAt)
        (self.updatedAt, self.nodeId) = (updatedAt, nodeId)
        (self.archiveUrl, self.exclude) = (archiveUrl, exclude)
        try sdkValidateUri("url", self.url)
        try sdkValidateDateTime("created_at", sdkWireString(self.createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(self.updatedAt))
        if let value = self.archiveUrl {
            try sdkValidateUri("archive_url", value)
        }
    }
}

/// Porter Author
public struct PorterAuthor: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required `string` value serialized in the `remote_id` wire field.
    public var remoteId: String
    /// Required `string` value serialized in the `remote_name` wire field.
    public var remoteName: String
    /// Required `string` value serialized in the `email` wire field.
    public var email: String
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `import_url` wire field.
    public var importUrl: String

    enum CodingKeys: String, CodingKey {
        case id
        case remoteId = "remote_id"
        case remoteName = "remote_name"
        case email
        case name
        case url
        case importUrl = "import_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PorterAuthor {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.remoteId) else {
            throw SdkValidationError(
                field: "remote_id",
                code: "required",
                message: "Validation failed for 'remote_id': value is required"
            )
        }
        guard container.contains(.remoteName) else {
            throw SdkValidationError(
                field: "remote_name",
                code: "required",
                message: "Validation failed for 'remote_name': value is required"
            )
        }
        guard container.contains(.email) else {
            throw SdkValidationError(
                field: "email",
                code: "required",
                message: "Validation failed for 'email': value is required"
            )
        }
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        guard container.contains(.importUrl) else {
            throw SdkValidationError(
                field: "import_url",
                code: "required",
                message: "Validation failed for 'import_url': value is required"
            )
        }
        id = try container.sdkDecodeRequired(.id)
        remoteId = try container.sdkDecodeRequired(.remoteId)
        remoteName = try container.sdkDecodeRequired(.remoteName)
        email = try container.sdkDecodeRequired(.email)
        name = try container.sdkDecodeRequired(.name)
        url = try container.sdkDecodeRequired(.url)
        importUrl = try container.sdkDecodeRequired(.importUrl)
        try sdkValidateUri("url", url)
        try sdkValidateUri("import_url", importUrl)
    }
}

public extension PorterAuthor {
    init(
        id: Int,
        remoteId: String,
        remoteName: String,
        email: String,
        name: String,
        url: String,
        importUrl: String
    ) throws {
        (self.id, self.remoteId) = (id, remoteId)
        (self.remoteName, self.email) = (remoteName, email)
        (self.name, self.url) = (name, url)
        self.importUrl = importUrl
        try sdkValidateUri("url", self.url)
        try sdkValidateUri("import_url", self.importUrl)
    }
}

/// Porter Large File
public struct PorterLargeFile: Codable {
    /// Required `string` value serialized in the `ref_name` wire field.
    public var refName: String
    /// Required `string` value serialized in the `path` wire field.
    public var path: String
    /// Required `string` value serialized in the `oid` wire field.
    public var oid: String
    /// Required `integer` value serialized in the `size` wire field.
    public var size: Int

    enum CodingKeys: String, CodingKey {
        case refName = "ref_name"
        case path
        case oid
        case size
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension PorterLargeFile {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.refName) else {
            throw SdkValidationError(
                field: "ref_name",
                code: "required",
                message: "Validation failed for 'ref_name': value is required"
            )
        }
        guard container.contains(.path) else {
            throw SdkValidationError(
                field: "path",
                code: "required",
                message: "Validation failed for 'path': value is required"
            )
        }
        guard container.contains(.oid) else {
            throw SdkValidationError(
                field: "oid",
                code: "required",
                message: "Validation failed for 'oid': value is required"
            )
        }
        guard container.contains(.size) else {
            throw SdkValidationError(
                field: "size",
                code: "required",
                message: "Validation failed for 'size': value is required"
            )
        }
        refName = try container.sdkDecodeRequired(.refName)
        path = try container.sdkDecodeRequired(.path)
        oid = try container.sdkDecodeRequired(.oid)
        size = try container.sdkDecodeRequired(.size)
    }
}

public extension PorterLargeFile {
    init(refName: String, path: String, oid: String, size: Int) {
        (self.refName, self.path) = (refName, path)
        (self.oid, self.size) = (oid, size)
    }
}

/// Required enumerated value serialized in the `status` wire field.
public struct ImportStatus: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let auth = ImportStatus(rawValue: "auth")
    public static let error = ImportStatus(rawValue: "error")
    public static let none = ImportStatus(rawValue: "none")
    public static let detecting = ImportStatus(rawValue: "detecting")
    public static let choose = ImportStatus(rawValue: "choose")
    public static let authFailed = ImportStatus(rawValue: "auth_failed")
    public static let importing = ImportStatus(rawValue: "importing")
    public static let mapping = ImportStatus(rawValue: "mapping")
    public static let waitingToPush = ImportStatus(rawValue: "waiting_to_push")
    public static let pushing = ImportStatus(rawValue: "pushing")
    public static let complete = ImportStatus(rawValue: "complete")
    public static let setup = ImportStatus(rawValue: "setup")
    public static let unknown = ImportStatus(rawValue: "unknown")
    public static let detectionFoundMultiple = ImportStatus(rawValue: "detection_found_multiple")
    public static let detectionFoundNothing = ImportStatus(rawValue: "detection_found_nothing")
    public static let detectionNeedsAuth = ImportStatus(rawValue: "detection_needs_auth")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
