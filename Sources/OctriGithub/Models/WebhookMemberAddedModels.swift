// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// WebhookMemberAdded domain models
/// Typed representation of the `WebhookMemberAdded` API schema.
public struct WebhookMemberAdded: Codable {
    /// Required enumerated value serialized in the `action` wire field.
    public var action: WebhookMemberAddedAction
    /// Required object value serialized in the `member` wire field.
    public var member: WebhooksUser?
    /// The repository on GitHub where the event occurred. Webhook payloads contain the `repository` property when
    /// the event occurs from activity in a repository.
    public var repository: RepositoryWebhooks
    /// A GitHub user.
    public var sender: SimpleUser
    /// Optional object value serialized in the `changes` wire field.
    public var changes: WebhookMemberAddedChanges?
    /// An enterprise on GitHub. Webhook payloads contain the `enterprise` property when the webhook is configured
    /// on an enterprise account or an organization that's part of an enterprise account. For more information, see
    /// "About enterprise accounts."
    public var enterprise: EnterpriseWebhooks?
    /// The GitHub App installation. Webhook payloads contain the `installation` property when the event is
    /// configured for and sent to a GitHub App. For more information, see "Using webhooks with GitHub Apps."
    public var installation: SimpleInstallation?
    /// A GitHub organization. Webhook payloads contain the `organization` property when the webhook is configured
    /// for an organization, or when the event occurs from activity in a repository owned by an organization.
    public var organization: OrganizationSimpleWebhooks?

    enum CodingKeys: String, CodingKey {
        case action
        case member
        case repository
        case sender
        case changes
        case enterprise
        case installation
        case organization
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookMemberAdded {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.action) else {
            throw SdkValidationError(
                field: "action",
                code: "required",
                message: "Validation failed for 'action': value is required"
            )
        }
        guard container.contains(.member) else {
            throw SdkValidationError(
                field: "member",
                code: "required",
                message: "Validation failed for 'member': value is required"
            )
        }
        guard container.contains(.repository) else {
            throw SdkValidationError(
                field: "repository",
                code: "required",
                message: "Validation failed for 'repository': value is required"
            )
        }
        guard container.contains(.sender) else {
            throw SdkValidationError(
                field: "sender",
                code: "required",
                message: "Validation failed for 'sender': value is required"
            )
        }
        action = try container.sdkDecodeRequired(.action)
        member = try container.sdkDecodeIfPresent(.member)
        repository = try container.sdkDecodeRequired(.repository)
        sender = try container.sdkDecodeRequired(.sender)
        changes = try container.sdkDecodeIfPresent(.changes)
        enterprise = try container.sdkDecodeIfPresent(.enterprise)
        installation = try container.sdkDecodeIfPresent(.installation)
        organization = try container.sdkDecodeIfPresent(.organization)
    }
}

public extension WebhookMemberAdded {
    init(
        action: WebhookMemberAddedAction,
        member: WebhooksUser?,
        repository: RepositoryWebhooks,
        sender: SimpleUser,
        changes: WebhookMemberAddedChanges? = nil,
        enterprise: EnterpriseWebhooks? = nil,
        installation: SimpleInstallation? = nil,
        organization: OrganizationSimpleWebhooks? = nil
    ) {
        (self.action, self.member) = (action, member)
        (self.repository, self.sender) = (repository, sender)
        (self.changes, self.enterprise) = (changes, enterprise)
        (self.installation, self.organization) = (installation, organization)
    }
}

/// Optional object value serialized in the `changes` wire field.
public struct WebhookMemberAddedChanges: Codable {
    /// This field is included for legacy purposes; use the `role_name` field instead. The `maintain` role is mapped
    /// to `write` and the `triage` role is mapped to `read`. To determine the role assigned to the collaborator,
    /// use the `role_name` field instead, which will provide the full role name, including custom roles.
    public var permission: WebhookMemberAddedChangesPermission?
    /// The role assigned to the collaborator.
    public var roleName: WebhookMemberAddedChangesRoleName?

    enum CodingKeys: String, CodingKey {
        case permission
        case roleName = "role_name"
    }

    init() {
        (permission, roleName) = (nil, nil)
    }
}

public extension WebhookMemberAddedChanges {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        permission = try container.sdkDecodeIfPresent(.permission)
        roleName = try container.sdkDecodeIfPresent(.roleName)
    }
}

public extension WebhookMemberAddedChanges {
    init(permission: WebhookMemberAddedChangesPermission? = nil, roleName: WebhookMemberAddedChangesRoleName? = nil) {
        self.init()
        (self.permission, self.roleName) = (permission, roleName)
    }
}

/// This field is included for legacy purposes; use the `role_name` field instead. The `maintain` role is mapped to
/// `write` and the `triage` role is mapped to `read`. To determine the role assigned to the collaborator, use the
/// `role_name` field instead, which will provide the full role name, including custom roles.
public struct WebhookMemberAddedChangesPermission: Codable {
    /// Required enumerated value serialized in the `to` wire field.
    public var to: WebhookMemberAddedChangesPermissionTo

    enum CodingKeys: String, CodingKey {
        case to
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookMemberAddedChangesPermission {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.to) else {
            throw SdkValidationError(
                field: "to",
                code: "required",
                message: "Validation failed for 'to': value is required"
            )
        }
        to = try container.sdkDecodeRequired(.to)
    }
}

public extension WebhookMemberAddedChangesPermission {
    init(to: WebhookMemberAddedChangesPermissionTo) {
        self.to = to
    }
}

/// The role assigned to the collaborator.
public struct WebhookMemberAddedChangesRoleName: Codable {
    /// Required `string` value serialized in the `to` wire field.
    public var to: String

    enum CodingKeys: String, CodingKey {
        case to
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookMemberAddedChangesRoleName {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.to) else {
            throw SdkValidationError(
                field: "to",
                code: "required",
                message: "Validation failed for 'to': value is required"
            )
        }
        to = try container.sdkDecodeRequired(.to)
    }
}

public extension WebhookMemberAddedChangesRoleName {
    init(to: String) {
        self.to = to
    }
}

/// Required enumerated value serialized in the `action` wire field.
public struct WebhookMemberAddedAction: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let added = WebhookMemberAddedAction(rawValue: "added")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// Required enumerated value serialized in the `to` wire field.
public struct WebhookMemberAddedChangesPermissionTo: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let write = WebhookMemberAddedChangesPermissionTo(rawValue: "write")
    public static let admin = WebhookMemberAddedChangesPermissionTo(rawValue: "admin")
    public static let read = WebhookMemberAddedChangesPermissionTo(rawValue: "read")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
