// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// Issues domain models
public extension RemovedFromProjectIssueEvent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        url = try container.sdkDecodeRequired(.url)
        actor = try container.sdkDecodeRequired(.actor)
        event = try container.sdkDecodeRequired(.event)
        commitId = try container.sdkDecodeIfPresent(.commitId)
        commitUrl = try container.sdkDecodeIfPresent(.commitUrl)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        performedViaGithubApp = try container.sdkDecodeIfPresent(.performedViaGithubApp)
        projectCard = try container.sdkDecodeIfPresent(.projectCard)
    }
}

public extension RemovedFromProjectIssueEvent {
    init(
        id: Int,
        nodeId: String,
        url: String,
        actor: SimpleUser,
        event: String,
        commitId: String?,
        commitUrl: String?,
        createdAt: String,
        performedViaGithubApp: NullableIntegration?,
        projectCard: RemovedFromProjectIssueEventProjectCard? = nil
    ) {
        (self.id, self.nodeId) = (id, nodeId)
        (self.url, self.actor) = (url, actor)
        (self.event, self.commitId) = (event, commitId)
        (self.commitUrl, self.createdAt) = (commitUrl, createdAt)
        (self.performedViaGithubApp, self.projectCard) = (performedViaGithubApp, projectCard)
    }
}

/// Optional object value serialized in the `project_card` wire field.
public struct RemovedFromProjectIssueEventProjectCard: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `integer` value serialized in the `project_id` wire field.
    public var projectId: Int
    /// Required `uri`-formatted value serialized in the `project_url` wire field.
    public var projectUrl: String
    /// Required `string` value serialized in the `column_name` wire field.
    public var columnName: String
    /// Optional `string` value serialized in the `previous_column_name` wire field.
    public var previousColumnName: String?

    enum CodingKeys: String, CodingKey {
        case id
        case url
        case projectId = "project_id"
        case projectUrl = "project_url"
        case columnName = "column_name"
        case previousColumnName = "previous_column_name"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension RemovedFromProjectIssueEventProjectCard {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        guard container.contains(.projectId) else {
            throw SdkValidationError(
                field: "project_id",
                code: "required",
                message: "Validation failed for 'project_id': value is required"
            )
        }
        guard container.contains(.projectUrl) else {
            throw SdkValidationError(
                field: "project_url",
                code: "required",
                message: "Validation failed for 'project_url': value is required"
            )
        }
        guard container.contains(.columnName) else {
            throw SdkValidationError(
                field: "column_name",
                code: "required",
                message: "Validation failed for 'column_name': value is required"
            )
        }
        id = try container.sdkDecodeRequired(.id)
        url = try container.sdkDecodeRequired(.url)
        projectId = try container.sdkDecodeRequired(.projectId)
        projectUrl = try container.sdkDecodeRequired(.projectUrl)
        columnName = try container.sdkDecodeRequired(.columnName)
        previousColumnName = try container.sdkDecodeIfPresent(.previousColumnName)
        try sdkValidateUri("url", url)
        try sdkValidateUri("project_url", projectUrl)
    }
}

public extension RemovedFromProjectIssueEventProjectCard {
    init(
        id: Int,
        url: String,
        projectId: Int,
        projectUrl: String,
        columnName: String,
        previousColumnName: String? = nil
    ) throws {
        (self.id, self.url) = (id, url)
        (self.projectId, self.projectUrl) = (projectId, projectUrl)
        (self.columnName, self.previousColumnName) = (columnName, previousColumnName)
        try sdkValidateUri("url", self.url)
        try sdkValidateUri("project_url", self.projectUrl)
    }
}

/// Renamed Issue Event
public struct RenamedIssueEvent: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `string` value serialized in the `url` wire field.
    public var url: String
    /// A GitHub user.
    public var actor: SimpleUser
    /// Required `string` value serialized in the `event` wire field.
    public var event: String
    /// Required `string` value serialized in the `commit_id` wire field.
    public var commitId: String?
    /// Required `string` value serialized in the `commit_url` wire field.
    public var commitUrl: String?
    /// Required `string` value serialized in the `created_at` wire field.
    public var createdAt: String
    /// GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user
    /// accounts and granted access to specific repositories. They come with granular permissions and built-in
    /// webhooks. GitHub apps are first class actors within GitHub.
    public var performedViaGithubApp: NullableIntegration?
    /// Required object value serialized in the `rename` wire field.
    public var rename: RenamedIssueEventRename

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case url
        case actor
        case event
        case commitId = "commit_id"
        case commitUrl = "commit_url"
        case createdAt = "created_at"
        case performedViaGithubApp = "performed_via_github_app"
        case rename
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension RenamedIssueEvent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        url = try container.sdkDecodeRequired(.url)
        actor = try container.sdkDecodeRequired(.actor)
        event = try container.sdkDecodeRequired(.event)
        commitId = try container.sdkDecodeIfPresent(.commitId)
        commitUrl = try container.sdkDecodeIfPresent(.commitUrl)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        performedViaGithubApp = try container.sdkDecodeIfPresent(.performedViaGithubApp)
        rename = try container.sdkDecodeRequired(.rename)
    }
}

public extension RenamedIssueEvent {
    init(
        id: Int,
        nodeId: String,
        url: String,
        actor: SimpleUser,
        event: String,
        commitId: String?,
        commitUrl: String?,
        createdAt: String,
        performedViaGithubApp: NullableIntegration?,
        rename: RenamedIssueEventRename
    ) {
        (self.id, self.nodeId) = (id, nodeId)
        (self.url, self.actor) = (url, actor)
        (self.event, self.commitId) = (event, commitId)
        (self.commitUrl, self.createdAt) = (commitUrl, createdAt)
        (self.performedViaGithubApp, self.rename) = (performedViaGithubApp, rename)
    }
}

/// Required object value serialized in the `rename` wire field.
public struct RenamedIssueEventRename: Codable {
    /// Required `string` value serialized in the `from` wire field.
    public var from: String
    /// Required `string` value serialized in the `to` wire field.
    public var to: String

    enum CodingKeys: String, CodingKey {
        case from
        case to
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension RenamedIssueEventRename {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.from) else {
            throw SdkValidationError(
                field: "from",
                code: "required",
                message: "Validation failed for 'from': value is required"
            )
        }
        guard container.contains(.to) else {
            throw SdkValidationError(
                field: "to",
                code: "required",
                message: "Validation failed for 'to': value is required"
            )
        }
        from = try container.sdkDecodeRequired(.from)
        to = try container.sdkDecodeRequired(.to)
    }
}

public extension RenamedIssueEventRename {
    init(from: String, to: String) {
        (self.from, self.to) = (from, to)
    }
}

/// State Change Issue Event
public struct StateChangeIssueEvent: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `string` value serialized in the `url` wire field.
    public var url: String
    /// A GitHub user.
    public var actor: SimpleUser
    /// Required `string` value serialized in the `event` wire field.
    public var event: String
    /// Required `string` value serialized in the `commit_id` wire field.
    public var commitId: String?
    /// Required `string` value serialized in the `commit_url` wire field.
    public var commitUrl: String?
    /// Required `string` value serialized in the `created_at` wire field.
    public var createdAt: String
    /// GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user
    /// accounts and granted access to specific repositories. They come with granular permissions and built-in
    /// webhooks. GitHub apps are first class actors within GitHub.
    public var performedViaGithubApp: NullableIntegration?
    /// Optional `string` value serialized in the `state_reason` wire field.
    public var stateReason: String?
    /// The intent behind an agent's action on an issue, including the rationale and confidence. Present (and `null`
    /// when the event carried no agent intent) on supported event types while the issue suggestions feature is
    /// enabled for the repository; the property is omitted entirely when the feature is disabled or the event type
    /// does not support intent.
    public var intent: NullableIssueEventIntent?

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case url
        case actor
        case event
        case commitId = "commit_id"
        case commitUrl = "commit_url"
        case createdAt = "created_at"
        case performedViaGithubApp = "performed_via_github_app"
        case stateReason = "state_reason"
        case intent
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension StateChangeIssueEvent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        url = try container.sdkDecodeRequired(.url)
        actor = try container.sdkDecodeRequired(.actor)
        event = try container.sdkDecodeRequired(.event)
        commitId = try container.sdkDecodeIfPresent(.commitId)
        commitUrl = try container.sdkDecodeIfPresent(.commitUrl)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        performedViaGithubApp = try container.sdkDecodeIfPresent(.performedViaGithubApp)
        stateReason = try container.sdkDecodeIfPresent(.stateReason)
        intent = try container.sdkDecodeIfPresent(.intent)
    }
}

public extension StateChangeIssueEvent {
    init(
        id: Int,
        nodeId: String,
        url: String,
        actor: SimpleUser,
        event: String,
        commitId: String?,
        commitUrl: String?,
        createdAt: String,
        performedViaGithubApp: NullableIntegration?,
        stateReason: String? = nil,
        intent: NullableIssueEventIntent? = nil
    ) {
        (self.id, self.nodeId) = (id, nodeId)
        (self.url, self.actor) = (url, actor)
        (self.event, self.commitId) = (event, commitId)
        (self.commitUrl, self.createdAt) = (commitUrl, createdAt)
        (self.performedViaGithubApp, self.stateReason) = (performedViaGithubApp, stateReason)
        self.intent = intent
    }
}

/// Unassigned Issue Event
public struct UnassignedIssueEvent: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `string` value serialized in the `url` wire field.
    public var url: String
    /// A GitHub user.
    public var actor: SimpleUser
    /// Required `string` value serialized in the `event` wire field.
    public var event: String
    /// Required `string` value serialized in the `commit_id` wire field.
    public var commitId: String?
    /// Required `string` value serialized in the `commit_url` wire field.
    public var commitUrl: String?
    /// Required `string` value serialized in the `created_at` wire field.
    public var createdAt: String
    /// GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user
    /// accounts and granted access to specific repositories. They come with granular permissions and built-in
    /// webhooks. GitHub apps are first class actors within GitHub.
    public var performedViaGithubApp: NullableIntegration?
    /// A GitHub user.
    public var assignee: SimpleUser
    /// A GitHub user.
    public var assigner: SimpleUser

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case url
        case actor
        case event
        case commitId = "commit_id"
        case commitUrl = "commit_url"
        case createdAt = "created_at"
        case performedViaGithubApp = "performed_via_github_app"
        case assignee
        case assigner
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension UnassignedIssueEvent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        url = try container.sdkDecodeRequired(.url)
        actor = try container.sdkDecodeRequired(.actor)
        event = try container.sdkDecodeRequired(.event)
        commitId = try container.sdkDecodeIfPresent(.commitId)
        commitUrl = try container.sdkDecodeIfPresent(.commitUrl)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        performedViaGithubApp = try container.sdkDecodeIfPresent(.performedViaGithubApp)
        assignee = try container.sdkDecodeRequired(.assignee)
        assigner = try container.sdkDecodeRequired(.assigner)
    }
}

public extension UnassignedIssueEvent {
    init(
        id: Int,
        nodeId: String,
        url: String,
        actor: SimpleUser,
        event: String,
        commitId: String?,
        commitUrl: String?,
        createdAt: String,
        performedViaGithubApp: NullableIntegration?,
        assignee: SimpleUser,
        assigner: SimpleUser
    ) {
        (self.id, self.nodeId) = (id, nodeId)
        (self.url, self.actor) = (url, actor)
        (self.event, self.commitId) = (event, commitId)
        (self.commitUrl, self.createdAt) = (commitUrl, createdAt)
        (self.performedViaGithubApp, self.assignee) = (performedViaGithubApp, assignee)
        self.assigner = assigner
    }
}

/// Unlabeled Issue Event
public struct UnlabeledIssueEvent: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `string` value serialized in the `url` wire field.
    public var url: String
    /// A GitHub user.
    public var actor: SimpleUser
    /// Required `string` value serialized in the `event` wire field.
    public var event: String
    /// Required `string` value serialized in the `commit_id` wire field.
    public var commitId: String?
    /// Required `string` value serialized in the `commit_url` wire field.
    public var commitUrl: String?
    /// Required `string` value serialized in the `created_at` wire field.
    public var createdAt: String
    /// GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user
    /// accounts and granted access to specific repositories. They come with granular permissions and built-in
    /// webhooks. GitHub apps are first class actors within GitHub.
    public var performedViaGithubApp: NullableIntegration?
    /// Required object value serialized in the `label` wire field.
    public var label: UnlabeledIssueEventLabel
    /// The intent behind an agent's action on an issue, including the rationale and confidence. Present (and `null`
    /// when the event carried no agent intent) on supported event types while the issue suggestions feature is
    /// enabled for the repository; the property is omitted entirely when the feature is disabled or the event type
    /// does not support intent.
    public var intent: NullableIssueEventIntent?

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case url
        case actor
        case event
        case commitId = "commit_id"
        case commitUrl = "commit_url"
        case createdAt = "created_at"
        case performedViaGithubApp = "performed_via_github_app"
        case label
        case intent
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension UnlabeledIssueEvent {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        url = try container.sdkDecodeRequired(.url)
        actor = try container.sdkDecodeRequired(.actor)
        event = try container.sdkDecodeRequired(.event)
        commitId = try container.sdkDecodeIfPresent(.commitId)
        commitUrl = try container.sdkDecodeIfPresent(.commitUrl)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        performedViaGithubApp = try container.sdkDecodeIfPresent(.performedViaGithubApp)
        label = try container.sdkDecodeRequired(.label)
        intent = try container.sdkDecodeIfPresent(.intent)
    }
}

public extension UnlabeledIssueEvent {
    init(
        id: Int,
        nodeId: String,
        url: String,
        actor: SimpleUser,
        event: String,
        commitId: String?,
        commitUrl: String?,
        createdAt: String,
        performedViaGithubApp: NullableIntegration?,
        label: UnlabeledIssueEventLabel,
        intent: NullableIssueEventIntent? = nil
    ) {
        (self.id, self.nodeId) = (id, nodeId)
        (self.url, self.actor) = (url, actor)
        (self.event, self.commitId) = (event, commitId)
        (self.commitUrl, self.createdAt) = (commitUrl, createdAt)
        (self.performedViaGithubApp, self.label) = (performedViaGithubApp, label)
        self.intent = intent
    }
}

/// Required object value serialized in the `label` wire field.
public struct UnlabeledIssueEventLabel: Codable {
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `color` wire field.
    public var color: String

    enum CodingKeys: String, CodingKey {
        case name
        case color
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension UnlabeledIssueEventLabel {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.color) else {
            throw SdkValidationError(
                field: "color",
                code: "required",
                message: "Validation failed for 'color': value is required"
            )
        }
        name = try container.sdkDecodeRequired(.name)
        color = try container.sdkDecodeRequired(.color)
    }
}

public extension UnlabeledIssueEventLabel {
    init(name: String, color: String) {
        (self.name, self.color) = (name, color)
    }
}

/// The state of the milestone.
public struct MilestoneState: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let open = MilestoneState(rawValue: "open")
    public static let closed = MilestoneState(rawValue: "closed")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
