// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

// WebhookProjectEdited domain models
/// Typed representation of the `WebhookProjectEdited` API schema.
public struct WebhookProjectEdited: Codable {
    /// Required enumerated value serialized in the `action` wire field.
    public var action: WebhookProjectEditedAction
    /// Required object value serialized in the `project` wire field.
    public var project: WebhooksProject
    /// The changes to the project if the action was `edited`.
    public var changes: WebhookProjectEditedChanges?
    /// An enterprise on GitHub. Webhook payloads contain the `enterprise` property when the webhook is configured
    /// on an enterprise account or an organization that's part of an enterprise account. For more information, see
    /// "About enterprise accounts."
    public var enterprise: EnterpriseWebhooks?
    /// The GitHub App installation. Webhook payloads contain the `installation` property when the event is
    /// configured for and sent to a GitHub App. For more information, see "Using webhooks with GitHub Apps."
    public var installation: SimpleInstallation?
    /// A GitHub organization. Webhook payloads contain the `organization` property when the webhook is configured
    /// for an organization, or when the event occurs from activity in a repository owned by an organization.
    public var organization: OrganizationSimpleWebhooks?
    /// The repository on GitHub where the event occurred. Webhook payloads contain the `repository` property when
    /// the event occurs from activity in a repository.
    public var repository: RepositoryWebhooks?
    /// A GitHub user.
    public var sender: SimpleUser?

    enum CodingKeys: String, CodingKey {
        case action
        case project
        case changes
        case enterprise
        case installation
        case organization
        case repository
        case sender
    }

    private init(sdkCopy value: Self) { self = value }
}

extension WebhookProjectEdited {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.action) else {
            throw SdkValidationError(field: "action", code: "required", message: "Validation failed for 'action': value is required")
        }
        guard container.contains(.project) else {
            throw SdkValidationError(field: "project", code: "required", message: "Validation failed for 'project': value is required")
        }
        self.action = try container.sdkDecodeRequired(.action)
        self.project = try container.sdkDecodeRequired(.project)
        self.changes = try container.sdkDecodeIfPresent(.changes)
        self.enterprise = try container.sdkDecodeIfPresent(.enterprise)
        self.installation = try container.sdkDecodeIfPresent(.installation)
        self.organization = try container.sdkDecodeIfPresent(.organization)
        self.repository = try container.sdkDecodeIfPresent(.repository)
        self.sender = try container.sdkDecodeIfPresent(.sender)
    }
}

extension WebhookProjectEdited {
    public init(action: WebhookProjectEditedAction, project: WebhooksProject, changes: WebhookProjectEditedChanges? = nil, enterprise: EnterpriseWebhooks? = nil, installation: SimpleInstallation? = nil, organization: OrganizationSimpleWebhooks? = nil, repository: RepositoryWebhooks? = nil, sender: SimpleUser? = nil) {
        (self.action, self.project) = (action, project)
        (self.changes, self.enterprise) = (changes, enterprise)
        (self.installation, self.organization) = (installation, organization)
        (self.repository, self.sender) = (repository, sender)
    }
}

/// The changes to the project if the action was `edited`.
public struct WebhookProjectEditedChanges: Codable {
    /// Optional object value serialized in the `body` wire field.
    public var body: WebhookProjectEditedChangesBody?
    /// Optional object value serialized in the `name` wire field.
    public var name: WebhookProjectEditedChangesName?

    enum CodingKeys: String, CodingKey {
        case body
        case name
    }

    init() {
        (self.body, self.name) = (nil, nil)
    }
}

extension WebhookProjectEditedChanges {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.body = try container.sdkDecodeIfPresent(.body)
        self.name = try container.sdkDecodeIfPresent(.name)
    }
}

extension WebhookProjectEditedChanges {
    public init(body: WebhookProjectEditedChangesBody? = nil, name: WebhookProjectEditedChangesName? = nil) {
        self.init()
        (self.body, self.name) = (body, name)
    }
}

/// Optional object value serialized in the `body` wire field.
public struct WebhookProjectEditedChangesBody: Codable {
    /// The previous version of the body if the action was `edited`.
    public var from: String

    enum CodingKeys: String, CodingKey {
        case from
    }

    private init(sdkCopy value: Self) { self = value }
}

extension WebhookProjectEditedChangesBody {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.from) else {
            throw SdkValidationError(field: "from", code: "required", message: "Validation failed for 'from': value is required")
        }
        self.from = try container.sdkDecodeRequired(.from)
    }
}

extension WebhookProjectEditedChangesBody {
    public init(from: String) {
        self.from = from
    }
}

/// Optional object value serialized in the `name` wire field.
public struct WebhookProjectEditedChangesName: Codable {
    /// The changes to the project if the action was `edited`.
    public var from: String

    enum CodingKeys: String, CodingKey {
        case from
    }

    private init(sdkCopy value: Self) { self = value }
}

extension WebhookProjectEditedChangesName {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.from) else {
            throw SdkValidationError(field: "from", code: "required", message: "Validation failed for 'from': value is required")
        }
        self.from = try container.sdkDecodeRequired(.from)
    }
}

extension WebhookProjectEditedChangesName {
    public init(from: String) {
        self.from = from
    }
}

/// Required enumerated value serialized in the `action` wire field.
public struct WebhookProjectEditedAction: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) { self.rawValue = rawValue }
    public static let edited = WebhookProjectEditedAction(rawValue: "edited")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        self.init(rawValue: try container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
