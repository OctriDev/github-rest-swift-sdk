// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// DependencyGraph domain models
public typealias DependencyGraphDiff = [DependencyGraphDiffItem]

public typealias Metadata = [String: MetadataValue]

/// Typed representation of the `Dependency` API schema.
public struct Dependency: Codable {
    /// Package-url (PURL) of dependency. See github.com for more details.
    /// - Example: "pkg:/npm/%40actions/http-client@1.0.11"
    public var packageUrl: String?
    /// User-defined metadata to store domain-specific information limited to 8 keys with scalar values.
    public var metadata: Metadata?
    /// A notation of whether a dependency is requested directly by this manifest or is a dependency of another
    /// dependency.
    /// - Example: "direct"
    public var relationship: DependencyRelationship?
    /// A notation of whether the dependency is required for the primary build artifact (runtime) or is only used
    /// for development. Future versions of this specification may allow for more granular scopes.
    /// - Example: "runtime"
    public var scope: DependencyScope?
    /// Array of package-url (PURLs) of direct child dependencies.
    /// - Example: "@actions/http-client"
    public var dependencies: [String]?

    enum CodingKeys: String, CodingKey {
        case packageUrl = "package_url"
        case metadata
        case relationship
        case scope
        case dependencies
    }

    init() {
        (packageUrl, metadata, relationship, scope, dependencies) = (nil, nil, nil, nil, nil)
    }
}

public extension Dependency {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        packageUrl = try container.sdkDecodeIfPresent(.packageUrl)
        metadata = try container.sdkDecodeIfPresent(.metadata)
        relationship = try container.sdkDecodeIfPresent(.relationship)
        scope = try container.sdkDecodeIfPresent(.scope)
        dependencies = try container.sdkDecodeIfPresent(.dependencies)
        if let value = packageUrl {
            try sdkValidatePattern("package_url", value, sdkPatterna72d747a93cb)
        }
    }
}

public extension Dependency {
    init(
        packageUrl: String? = nil,
        metadata: Metadata? = nil,
        relationship: DependencyRelationship? = nil,
        scope: DependencyScope? = nil,
        dependencies: [String]? = nil
    ) throws {
        self.init()
        (self.packageUrl, self.metadata) = (packageUrl, metadata)
        (self.relationship, self.scope) = (relationship, scope)
        self.dependencies = dependencies
        if let value = self.packageUrl {
            try sdkValidatePattern("package_url", value, sdkPatterna72d747a93cb)
        }
    }
}

public struct DependencyGraphDiffItem: Codable {
    public var changeType: DependencyGraphDiffItemChangeType
    /// - Example: "path/to/package-lock.json"
    public var manifest: String
    /// - Example: "npm"
    public var ecosystem: String
    /// - Example: "@actions/core"
    public var name: String
    /// - Example: "1.0.0"
    public var version: String
    /// - Example: "pkg:/npm/%40actions/core@1.1.0"
    public var packageUrl: String?
    /// - Example: "MIT"
    public var license: String?
    /// - Example: "https://github.com/github/actions"
    public var sourceRepositoryUrl: String?
    public var vulnerabilities: [DependencyGraphDiffItemVulnerabilitiesItem]
    /// Where the dependency is utilized. `development` means that the dependency is only utilized in the
    /// development environment. `runtime` means that the dependency is utilized at runtime and in the development
    /// environment.
    public var scope: DependencyGraphDiffItemScope

    enum CodingKeys: String, CodingKey {
        case changeType = "change_type"
        case manifest
        case ecosystem
        case name
        case version
        case packageUrl = "package_url"
        case license
        case sourceRepositoryUrl = "source_repository_url"
        case vulnerabilities
        case scope
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DependencyGraphDiffItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        changeType = try container.sdkDecodeRequired(.changeType)
        manifest = try container.sdkDecodeRequired(.manifest)
        ecosystem = try container.sdkDecodeRequired(.ecosystem)
        name = try container.sdkDecodeRequired(.name)
        version = try container.sdkDecodeRequired(.version)
        packageUrl = try container.sdkDecodeIfPresent(.packageUrl)
        license = try container.sdkDecodeIfPresent(.license)
        sourceRepositoryUrl = try container.sdkDecodeIfPresent(.sourceRepositoryUrl)
        vulnerabilities = try container.sdkDecodeRequired(.vulnerabilities)
        scope = try container.sdkDecodeRequired(.scope)
    }
}

public extension DependencyGraphDiffItem {
    init(
        changeType: DependencyGraphDiffItemChangeType,
        manifest: String,
        ecosystem: String,
        name: String,
        version: String,
        packageUrl: String?,
        license: String?,
        sourceRepositoryUrl: String?,
        vulnerabilities: [DependencyGraphDiffItemVulnerabilitiesItem],
        scope: DependencyGraphDiffItemScope
    ) {
        (self.changeType, self.manifest) = (changeType, manifest)
        (self.ecosystem, self.name) = (ecosystem, name)
        (self.version, self.packageUrl) = (version, packageUrl)
        (self.license, self.sourceRepositoryUrl) = (license, sourceRepositoryUrl)
        (self.vulnerabilities, self.scope) = (vulnerabilities, scope)
    }
}

public struct DependencyGraphDiffItemVulnerabilitiesItem: Codable {
    /// - Example: "critical"
    public var severity: String
    /// - Example: "GHSA-rf4j-j272-fj86"
    public var advisoryGhsaId: String
    /// - Example: "A summary of the advisory."
    public var advisorySummary: String
    /// - Example: "https://github.com/advisories/GHSA-rf4j-j272-fj86"
    public var advisoryUrl: String

    enum CodingKeys: String, CodingKey {
        case severity
        case advisoryGhsaId = "advisory_ghsa_id"
        case advisorySummary = "advisory_summary"
        case advisoryUrl = "advisory_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DependencyGraphDiffItemVulnerabilitiesItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.severity) else {
            throw SdkValidationError(
                field: "severity",
                code: "required",
                message: "Validation failed for 'severity': value is required"
            )
        }
        guard container.contains(.advisoryGhsaId) else {
            throw SdkValidationError(
                field: "advisory_ghsa_id",
                code: "required",
                message: "Validation failed for 'advisory_ghsa_id': value is required"
            )
        }
        guard container.contains(.advisorySummary) else {
            throw SdkValidationError(
                field: "advisory_summary",
                code: "required",
                message: "Validation failed for 'advisory_summary': value is required"
            )
        }
        guard container.contains(.advisoryUrl) else {
            throw SdkValidationError(
                field: "advisory_url",
                code: "required",
                message: "Validation failed for 'advisory_url': value is required"
            )
        }
        severity = try container.sdkDecodeRequired(.severity)
        advisoryGhsaId = try container.sdkDecodeRequired(.advisoryGhsaId)
        advisorySummary = try container.sdkDecodeRequired(.advisorySummary)
        advisoryUrl = try container.sdkDecodeRequired(.advisoryUrl)
    }
}

public extension DependencyGraphDiffItemVulnerabilitiesItem {
    init(severity: String, advisoryGhsaId: String, advisorySummary: String, advisoryUrl: String) {
        (self.severity, self.advisoryGhsaId) = (severity, advisoryGhsaId)
        (self.advisorySummary, self.advisoryUrl) = (advisorySummary, advisoryUrl)
    }
}

/// A schema for the SPDX JSON format returned by the Dependency Graph.
public struct DependencyGraphSpdxSbom: Codable {
    /// Required object value serialized in the `sbom` wire field.
    public var sbom: DependencyGraphSpdxSbomSbom

    enum CodingKeys: String, CodingKey {
        case sbom
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DependencyGraphSpdxSbom {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.sbom) else {
            throw SdkValidationError(
                field: "sbom",
                code: "required",
                message: "Validation failed for 'sbom': value is required"
            )
        }
        sbom = try container.sdkDecodeRequired(.sbom)
    }
}

public extension DependencyGraphSpdxSbom {
    init(sbom: DependencyGraphSpdxSbomSbom) {
        self.sbom = sbom
    }
}

/// Required object value serialized in the `sbom` wire field.
public struct DependencyGraphSpdxSbomSbom: Codable {
    /// The SPDX identifier for the SPDX document.
    /// - Example: "SPDXRef-DOCUMENT"
    public var sPDXID: String
    /// The version of the SPDX specification that this document conforms to.
    /// - Example: "SPDX-2.3"
    public var spdxVersion: String
    /// Required object value serialized in the `creationInfo` wire field.
    public var creationInfo: DependencyGraphSpdxSbomSbomCreationInfo
    /// The name of the SPDX document.
    /// - Example: "github/github"
    public var name: String
    /// The license under which the SPDX document is licensed.
    /// - Example: "CC0-1.0"
    public var dataLicense: String
    /// The namespace for the SPDX document.
    /// - Example: "https://spdx.org/spdxdocs/protobom/15e41dd2-f961-4f4d-b8dc-f8f57ad70d57"
    public var documentNamespace: String
    /// Required array value serialized in the `packages` wire field.
    public var packages: [DependencyGraphSpdxSbomSbomPackagesItem]
    /// An optional comment about the SPDX document.
    /// - Example: "Exact versions could not be resolved for some packages. For more information:
    /// https://docs.github.com/en/code-security/supply-chain-security/understanding-your-software-supply-chain/"
    public var comment: String?
    /// Optional array value serialized in the `relationships` wire field.
    public var relationships: [DependencyGraphSpdxSbomSbomRelationshipsItem]?

    enum CodingKeys: String, CodingKey {
        case sPDXID = "SPDXID"
        case spdxVersion
        case creationInfo
        case name
        case dataLicense
        case documentNamespace
        case packages
        case comment
        case relationships
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DependencyGraphSpdxSbomSbom {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.sPDXID) else {
            throw SdkValidationError(
                field: "SPDXID",
                code: "required",
                message: "Validation failed for 'SPDXID': value is required"
            )
        }
        guard container.contains(.spdxVersion) else {
            throw SdkValidationError(
                field: "spdxVersion",
                code: "required",
                message: "Validation failed for 'spdxVersion': value is required"
            )
        }
        guard container.contains(.creationInfo) else {
            throw SdkValidationError(
                field: "creationInfo",
                code: "required",
                message: "Validation failed for 'creationInfo': value is required"
            )
        }
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.dataLicense) else {
            throw SdkValidationError(
                field: "dataLicense",
                code: "required",
                message: "Validation failed for 'dataLicense': value is required"
            )
        }
        guard container.contains(.documentNamespace) else {
            throw SdkValidationError(
                field: "documentNamespace",
                code: "required",
                message: "Validation failed for 'documentNamespace': value is required"
            )
        }
        guard container.contains(.packages) else {
            throw SdkValidationError(
                field: "packages",
                code: "required",
                message: "Validation failed for 'packages': value is required"
            )
        }
        sPDXID = try container.sdkDecodeRequired(.sPDXID)
        spdxVersion = try container.sdkDecodeRequired(.spdxVersion)
        creationInfo = try container.sdkDecodeRequired(.creationInfo)
        name = try container.sdkDecodeRequired(.name)
        dataLicense = try container.sdkDecodeRequired(.dataLicense)
        documentNamespace = try container.sdkDecodeRequired(.documentNamespace)
        packages = try container.sdkDecodeRequired(.packages)
        comment = try container.sdkDecodeIfPresent(.comment)
        relationships = try container.sdkDecodeIfPresent(.relationships)
    }
}

public extension DependencyGraphSpdxSbomSbom {
    init(
        sPDXID: String,
        spdxVersion: String,
        creationInfo: DependencyGraphSpdxSbomSbomCreationInfo,
        name: String,
        dataLicense: String,
        documentNamespace: String,
        packages: [DependencyGraphSpdxSbomSbomPackagesItem],
        comment: String? = nil,
        relationships: [DependencyGraphSpdxSbomSbomRelationshipsItem]? = nil
    ) {
        (self.sPDXID, self.spdxVersion) = (sPDXID, spdxVersion)
        (self.creationInfo, self.name) = (creationInfo, name)
        (self.dataLicense, self.documentNamespace) = (dataLicense, documentNamespace)
        (self.packages, self.comment) = (packages, comment)
        self.relationships = relationships
    }
}

/// Required object value serialized in the `creationInfo` wire field.
public struct DependencyGraphSpdxSbomSbomCreationInfo: Codable {
    /// The date and time the SPDX document was created.
    /// - Example: "2021-11-03T00:00:00Z"
    public var created: String
    /// The tools that were used to generate the SPDX document.
    public var creators: [String]

    enum CodingKeys: String, CodingKey {
        case created
        case creators
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DependencyGraphSpdxSbomSbomCreationInfo {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.created) else {
            throw SdkValidationError(
                field: "created",
                code: "required",
                message: "Validation failed for 'created': value is required"
            )
        }
        guard container.contains(.creators) else {
            throw SdkValidationError(
                field: "creators",
                code: "required",
                message: "Validation failed for 'creators': value is required"
            )
        }
        created = try container.sdkDecodeRequired(.created)
        creators = try container.sdkDecodeRequired(.creators)
    }
}

public extension DependencyGraphSpdxSbomSbomCreationInfo {
    init(created: String, creators: [String]) {
        (self.created, self.creators) = (created, creators)
    }
}

/// Required object value serialized in the `packages[]` wire field.
public struct DependencyGraphSpdxSbomSbomPackagesItem: Codable {
    /// A unique SPDX identifier for the package.
    /// - Example: "SPDXRef-Package"
    public var sPDXID: String?
    /// The name of the package.
    /// - Example: "github/github"
    public var name: String?
    /// The version of the package. If the package does not have an exact version specified, a version range is
    /// given.
    /// - Example: "1.0.0"
    public var versionInfo: String?
    /// The location where the package can be downloaded, or NOASSERTION if this has not been determined.
    /// - Example: "NOASSERTION"
    public var downloadLocation: String?
    /// Whether the package's file content has been subjected to analysis during the creation of the SPDX document.
    public var filesAnalyzed: Bool?
    /// The license of the package as determined while creating the SPDX document.
    /// - Example: "MIT"
    public var licenseConcluded: String?
    /// The license of the package as declared by its author, or NOASSERTION if this information was not available
    /// when the SPDX document was created.
    /// - Example: "NOASSERTION"
    public var licenseDeclared: String?
    /// The distribution source of this package, or NOASSERTION if this was not determined.
    /// - Example: "NOASSERTION"
    public var supplier: String?
    /// The copyright holders of the package, and any dates present with those notices, if available.
    /// - Example: "Copyright (c) 1985 GitHub.com"
    public var copyrightText: String?
    /// Optional array value serialized in the `externalRefs` wire field.
    public var externalRefs: [DependencyGraphSpdxSbomSbomPackagesItemExternalRefsItem]?

    enum CodingKeys: String, CodingKey {
        case sPDXID = "SPDXID"
        case name
        case versionInfo
        case downloadLocation
        case filesAnalyzed
        case licenseConcluded
        case licenseDeclared
        case supplier
        case copyrightText
        case externalRefs
    }

    init() {
        (sPDXID, name, versionInfo, downloadLocation, filesAnalyzed) = (nil, nil, nil, nil, nil)
        (licenseConcluded, licenseDeclared, supplier, copyrightText, externalRefs) = (nil, nil, nil, nil, nil)
    }
}

public extension DependencyGraphSpdxSbomSbomPackagesItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        sPDXID = try container.sdkDecodeIfPresent(.sPDXID)
        name = try container.sdkDecodeIfPresent(.name)
        versionInfo = try container.sdkDecodeIfPresent(.versionInfo)
        downloadLocation = try container.sdkDecodeIfPresent(.downloadLocation)
        filesAnalyzed = try container.sdkDecodeIfPresent(.filesAnalyzed)
        licenseConcluded = try container.sdkDecodeIfPresent(.licenseConcluded)
        licenseDeclared = try container.sdkDecodeIfPresent(.licenseDeclared)
        supplier = try container.sdkDecodeIfPresent(.supplier)
        copyrightText = try container.sdkDecodeIfPresent(.copyrightText)
        externalRefs = try container.sdkDecodeIfPresent(.externalRefs)
    }
}

public extension DependencyGraphSpdxSbomSbomPackagesItem {
    init(
        sPDXID: String? = nil,
        name: String? = nil,
        versionInfo: String? = nil,
        downloadLocation: String? = nil,
        filesAnalyzed: Bool? = nil,
        licenseConcluded: String? = nil,
        licenseDeclared: String? = nil,
        supplier: String? = nil,
        copyrightText: String? = nil,
        externalRefs: [DependencyGraphSpdxSbomSbomPackagesItemExternalRefsItem]? = nil
    ) {
        self.init()
        (self.sPDXID, self.name) = (sPDXID, name)
        (self.versionInfo, self.downloadLocation) = (versionInfo, downloadLocation)
        (self.filesAnalyzed, self.licenseConcluded) = (filesAnalyzed, licenseConcluded)
        (self.licenseDeclared, self.supplier) = (licenseDeclared, supplier)
        (self.copyrightText, self.externalRefs) = (copyrightText, externalRefs)
    }
}

/// Required object value serialized in the `externalRefs[]` wire field.
public struct DependencyGraphSpdxSbomSbomPackagesItemExternalRefsItem: Codable {
    /// The category of reference to an external resource this reference refers to.
    /// - Example: "PACKAGE-MANAGER"
    public var referenceCategory: String
    /// A locator for the particular external resource this reference refers to.
    /// - Example: "pkg:gem/rails@6.0.1"
    public var referenceLocator: String
    /// The category of reference to an external resource this reference refers to.
    /// - Example: "purl"
    public var referenceType: String

    enum CodingKeys: String, CodingKey {
        case referenceCategory
        case referenceLocator
        case referenceType
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension DependencyGraphSpdxSbomSbomPackagesItemExternalRefsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.referenceCategory) else {
            throw SdkValidationError(
                field: "referenceCategory",
                code: "required",
                message: "Validation failed for 'referenceCategory': value is required"
            )
        }
        guard container.contains(.referenceLocator) else {
            throw SdkValidationError(
                field: "referenceLocator",
                code: "required",
                message: "Validation failed for 'referenceLocator': value is required"
            )
        }
        guard container.contains(.referenceType) else {
            throw SdkValidationError(
                field: "referenceType",
                code: "required",
                message: "Validation failed for 'referenceType': value is required"
            )
        }
        referenceCategory = try container.sdkDecodeRequired(.referenceCategory)
        referenceLocator = try container.sdkDecodeRequired(.referenceLocator)
        referenceType = try container.sdkDecodeRequired(.referenceType)
    }
}

public extension DependencyGraphSpdxSbomSbomPackagesItemExternalRefsItem {
    init(referenceCategory: String, referenceLocator: String, referenceType: String) {
        (self.referenceCategory, self.referenceLocator) = (referenceCategory, referenceLocator)
        self.referenceType = referenceType
    }
}

/// Required object value serialized in the `relationships[]` wire field.
public struct DependencyGraphSpdxSbomSbomRelationshipsItem: Codable {
    /// The type of relationship between the two SPDX elements.
    /// - Example: "DEPENDS_ON"
    public var relationshipType: String?
    /// The SPDX identifier of the package that is the source of the relationship.
    public var spdxElementId: String?
    /// The SPDX identifier of the package that is the target of the relationship.
    public var relatedSpdxElement: String?

    enum CodingKeys: String, CodingKey {
        case relationshipType
        case spdxElementId
        case relatedSpdxElement
    }

    init() {
        (relationshipType, spdxElementId, relatedSpdxElement) = (nil, nil, nil)
    }
}

public extension DependencyGraphSpdxSbomSbomRelationshipsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        relationshipType = try container.sdkDecodeIfPresent(.relationshipType)
        spdxElementId = try container.sdkDecodeIfPresent(.spdxElementId)
        relatedSpdxElement = try container.sdkDecodeIfPresent(.relatedSpdxElement)
    }
}

public extension DependencyGraphSpdxSbomSbomRelationshipsItem {
    init(relationshipType: String? = nil, spdxElementId: String? = nil, relatedSpdxElement: String? = nil) {
        self.init()
        (self.relationshipType, self.spdxElementId) = (relationshipType, spdxElementId)
        self.relatedSpdxElement = relatedSpdxElement
    }
}
