// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// WebhookWorkflowJobWaiting domain models
/// Typed representation of the `WebhookWorkflowJobWaiting` API schema.
public struct WebhookWorkflowJobWaiting: Codable {
    /// Required enumerated value serialized in the `action` wire field.
    public var action: WebhookWorkflowJobWaitingAction
    /// The repository on GitHub where the event occurred. Webhook payloads contain the `repository` property when
    /// the event occurs from activity in a repository.
    public var repository: RepositoryWebhooks
    /// A GitHub user.
    public var sender: SimpleUser
    /// Required object value serialized in the `workflow_job` wire field.
    public var workflowJob: WebhookWorkflowJobWaitingWorkflowJob
    /// An enterprise on GitHub. Webhook payloads contain the `enterprise` property when the webhook is configured
    /// on an enterprise account or an organization that's part of an enterprise account. For more information, see
    /// "About enterprise accounts."
    public var enterprise: EnterpriseWebhooks?
    /// The GitHub App installation. Webhook payloads contain the `installation` property when the event is
    /// configured for and sent to a GitHub App. For more information, see "Using webhooks with GitHub Apps."
    public var installation: SimpleInstallation?
    /// A GitHub organization. Webhook payloads contain the `organization` property when the webhook is configured
    /// for an organization, or when the event occurs from activity in a repository owned by an organization.
    public var organization: OrganizationSimpleWebhooks?
    /// A request for a specific ref(branch,sha,tag) to be deployed
    public var deployment: Deployment?

    enum CodingKeys: String, CodingKey {
        case action
        case repository
        case sender
        case workflowJob = "workflow_job"
        case enterprise
        case installation
        case organization
        case deployment
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookWorkflowJobWaiting {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.action) else {
            throw SdkValidationError(
                field: "action",
                code: "required",
                message: "Validation failed for 'action': value is required"
            )
        }
        guard container.contains(.repository) else {
            throw SdkValidationError(
                field: "repository",
                code: "required",
                message: "Validation failed for 'repository': value is required"
            )
        }
        guard container.contains(.sender) else {
            throw SdkValidationError(
                field: "sender",
                code: "required",
                message: "Validation failed for 'sender': value is required"
            )
        }
        guard container.contains(.workflowJob) else {
            throw SdkValidationError(
                field: "workflow_job",
                code: "required",
                message: "Validation failed for 'workflow_job': value is required"
            )
        }
        action = try container.sdkDecodeRequired(.action)
        repository = try container.sdkDecodeRequired(.repository)
        sender = try container.sdkDecodeRequired(.sender)
        workflowJob = try container.sdkDecodeRequired(.workflowJob)
        enterprise = try container.sdkDecodeIfPresent(.enterprise)
        installation = try container.sdkDecodeIfPresent(.installation)
        organization = try container.sdkDecodeIfPresent(.organization)
        deployment = try container.sdkDecodeIfPresent(.deployment)
    }
}

public extension WebhookWorkflowJobWaiting {
    init(
        action: WebhookWorkflowJobWaitingAction,
        repository: RepositoryWebhooks,
        sender: SimpleUser,
        workflowJob: WebhookWorkflowJobWaitingWorkflowJob,
        enterprise: EnterpriseWebhooks? = nil,
        installation: SimpleInstallation? = nil,
        organization: OrganizationSimpleWebhooks? = nil,
        deployment: Deployment? = nil
    ) {
        (self.action, self.repository) = (action, repository)
        (self.sender, self.workflowJob) = (sender, workflowJob)
        (self.enterprise, self.installation) = (enterprise, installation)
        (self.organization, self.deployment) = (organization, deployment)
    }
}

/// Required object value serialized in the `workflow_job` wire field.
public struct WebhookWorkflowJobWaitingWorkflowJob: Codable {
    /// Required `uri`-formatted value serialized in the `check_run_url` wire field.
    public var checkRunUrl: String
    /// Required `string` value serialized in the `completed_at` wire field.
    public var completedAt: String?
    /// Required `string` value serialized in the `conclusion` wire field.
    public var conclusion: String?
    /// The time that the job created.
    public var createdAt: String
    /// Required `string` value serialized in the `head_sha` wire field.
    public var headSha: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String
    /// Required `integer` value serialized in the `id` wire field.
    public var id: Int
    /// Required array value serialized in the `labels` wire field.
    public var labels: [String]
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `integer` value serialized in the `run_attempt` wire field.
    public var runAttempt: Int
    /// Required `number` value serialized in the `run_id` wire field.
    public var runId: Double
    /// Required `uri`-formatted value serialized in the `run_url` wire field.
    public var runUrl: String
    /// Required `integer` value serialized in the `runner_group_id` wire field.
    public var runnerGroupId: Int?
    /// Required `string` value serialized in the `runner_group_name` wire field.
    public var runnerGroupName: String?
    /// Required `integer` value serialized in the `runner_id` wire field.
    public var runnerId: Int?
    /// Required `string` value serialized in the `runner_name` wire field.
    public var runnerName: String?
    /// Required `date-time`-formatted value serialized in the `started_at` wire field.
    public var startedAt: Date
    /// The name of the current branch.
    public var headBranch: String?
    /// The name of the workflow.
    public var workflowName: String?
    /// Required enumerated value serialized in the `status` wire field.
    public var status: WebhookWorkflowJobWaitingWorkflowJobStatus
    /// Required array value serialized in the `steps` wire field.
    public var steps: [WebhookWorkflowJobWaitingWorkflowJobStepsItem]
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case checkRunUrl = "check_run_url"
        case completedAt = "completed_at"
        case conclusion
        case createdAt = "created_at"
        case headSha = "head_sha"
        case htmlUrl = "html_url"
        case id
        case labels
        case name
        case nodeId = "node_id"
        case runAttempt = "run_attempt"
        case runId = "run_id"
        case runUrl = "run_url"
        case runnerGroupId = "runner_group_id"
        case runnerGroupName = "runner_group_name"
        case runnerId = "runner_id"
        case runnerName = "runner_name"
        case startedAt = "started_at"
        case headBranch = "head_branch"
        case workflowName = "workflow_name"
        case status
        case steps
        case url
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookWorkflowJobWaitingWorkflowJob {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        checkRunUrl = try container.sdkDecodeRequired(.checkRunUrl)
        completedAt = try container.sdkDecodeIfPresent(.completedAt)
        conclusion = try container.sdkDecodeIfPresent(.conclusion)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        headSha = try container.sdkDecodeRequired(.headSha)
        htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        id = try container.sdkDecodeRequired(.id)
        labels = try container.sdkDecodeRequired(.labels)
        name = try container.sdkDecodeRequired(.name)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        runAttempt = try container.sdkDecodeRequired(.runAttempt)
        runId = try container.sdkDecodeRequired(.runId)
        runUrl = try container.sdkDecodeRequired(.runUrl)
        runnerGroupId = try container.sdkDecodeIfPresent(.runnerGroupId)
        runnerGroupName = try container.sdkDecodeIfPresent(.runnerGroupName)
        runnerId = try container.sdkDecodeIfPresent(.runnerId)
        runnerName = try container.sdkDecodeIfPresent(.runnerName)
        startedAt = try container.sdkDecodeRequired(.startedAt)
        headBranch = try container.sdkDecodeIfPresent(.headBranch)
        workflowName = try container.sdkDecodeIfPresent(.workflowName)
        status = try container.sdkDecodeRequired(.status)
        steps = try container.sdkDecodeRequired(.steps)
        url = try container.sdkDecodeRequired(.url)
        try sdkValidateUri("check_run_url", checkRunUrl)
        try sdkValidateUri("html_url", htmlUrl)
        try sdkValidateUri("run_url", runUrl)
        try sdkValidateDateTime("started_at", sdkWireString(startedAt))
        try sdkValidateUri("url", url)
    }
}

public extension WebhookWorkflowJobWaitingWorkflowJob {
    init(
        checkRunUrl: String,
        completedAt: String?,
        conclusion: String?,
        createdAt: String,
        headSha: String,
        htmlUrl: String,
        id: Int,
        labels: [String],
        name: String,
        nodeId: String,
        runAttempt: Int,
        runId: Double,
        runUrl: String,
        runnerGroupId: Int?,
        runnerGroupName: String?,
        runnerId: Int?,
        runnerName: String?,
        startedAt: Date,
        headBranch: String?,
        workflowName: String?,
        status: WebhookWorkflowJobWaitingWorkflowJobStatus,
        steps: [WebhookWorkflowJobWaitingWorkflowJobStepsItem],
        url: String
    ) throws {
        (self.checkRunUrl, self.completedAt) = (checkRunUrl, completedAt)
        (self.conclusion, self.createdAt) = (conclusion, createdAt)
        (self.headSha, self.htmlUrl) = (headSha, htmlUrl)
        (self.id, self.labels) = (id, labels)
        (self.name, self.nodeId) = (name, nodeId)
        (self.runAttempt, self.runId) = (runAttempt, runId)
        (self.runUrl, self.runnerGroupId) = (runUrl, runnerGroupId)
        (self.runnerGroupName, self.runnerId) = (runnerGroupName, runnerId)
        (self.runnerName, self.startedAt) = (runnerName, startedAt)
        (self.headBranch, self.workflowName) = (headBranch, workflowName)
        (self.status, self.steps) = (status, steps)
        self.url = url
        try sdkValidateUri("check_run_url", self.checkRunUrl)
        try sdkValidateUri("html_url", self.htmlUrl)
        try sdkValidateUri("run_url", self.runUrl)
        try sdkValidateDateTime("started_at", sdkWireString(self.startedAt))
        try sdkValidateUri("url", self.url)
    }
}

/// Required object value serialized in the `steps[]` wire field.
public struct WebhookWorkflowJobWaitingWorkflowJobStepsItem: Codable {
    /// Required `string` value serialized in the `completed_at` wire field.
    public var completedAt: String?
    /// Required enumerated value serialized in the `conclusion` wire field.
    public var conclusion: WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion?
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `integer` value serialized in the `number` wire field.
    public var number: Int
    /// Required `string` value serialized in the `started_at` wire field.
    public var startedAt: String?
    /// Required enumerated value serialized in the `status` wire field.
    public var status: WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus

    enum CodingKeys: String, CodingKey {
        case completedAt = "completed_at"
        case conclusion
        case name
        case number
        case startedAt = "started_at"
        case status
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WebhookWorkflowJobWaitingWorkflowJobStepsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.completedAt) else {
            throw SdkValidationError(
                field: "completed_at",
                code: "required",
                message: "Validation failed for 'completed_at': value is required"
            )
        }
        guard container.contains(.conclusion) else {
            throw SdkValidationError(
                field: "conclusion",
                code: "required",
                message: "Validation failed for 'conclusion': value is required"
            )
        }
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.number) else {
            throw SdkValidationError(
                field: "number",
                code: "required",
                message: "Validation failed for 'number': value is required"
            )
        }
        guard container.contains(.startedAt) else {
            throw SdkValidationError(
                field: "started_at",
                code: "required",
                message: "Validation failed for 'started_at': value is required"
            )
        }
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        completedAt = try container.sdkDecodeIfPresent(.completedAt)
        conclusion = try container.sdkDecodeIfPresent(.conclusion)
        name = try container.sdkDecodeRequired(.name)
        number = try container.sdkDecodeRequired(.number)
        startedAt = try container.sdkDecodeIfPresent(.startedAt)
        status = try container.sdkDecodeRequired(.status)
    }
}

public extension WebhookWorkflowJobWaitingWorkflowJobStepsItem {
    init(
        completedAt: String?,
        conclusion: WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion?,
        name: String,
        number: Int,
        startedAt: String?,
        status: WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus
    ) {
        (self.completedAt, self.conclusion) = (completedAt, conclusion)
        (self.name, self.number) = (name, number)
        (self.startedAt, self.status) = (startedAt, status)
    }
}

/// Required enumerated value serialized in the `status` wire field.
public struct WebhookWorkflowJobWaitingWorkflowJobStatus: RawRepresentable, Hashable, Codable, Sendable,
    SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let queued = WebhookWorkflowJobWaitingWorkflowJobStatus(rawValue: "queued")
    public static let inProgress = WebhookWorkflowJobWaitingWorkflowJobStatus(rawValue: "in_progress")
    public static let completed = WebhookWorkflowJobWaitingWorkflowJobStatus(rawValue: "completed")
    public static let waiting = WebhookWorkflowJobWaitingWorkflowJobStatus(rawValue: "waiting")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// Required enumerated value serialized in the `conclusion` wire field.
public struct WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion: RawRepresentable, Hashable, Codable, Sendable,
    SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let failure = WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion(rawValue: "failure")
    public static let skipped = WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion(rawValue: "skipped")
    public static let success = WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion(rawValue: "success")
    public static let cancelled = WebhookWorkflowJobWaitingWorkflowJobStepsItemConclusion(rawValue: "cancelled")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// Required enumerated value serialized in the `action` wire field.
public struct WebhookWorkflowJobWaitingAction: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let waiting = WebhookWorkflowJobWaitingAction(rawValue: "waiting")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}

/// Required enumerated value serialized in the `status` wire field.
public struct WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus: RawRepresentable, Hashable, Codable, Sendable,
    SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let completed = WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus(rawValue: "completed")
    public static let inProgress = WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus(rawValue: "in_progress")
    public static let queued = WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus(rawValue: "queued")
    public static let pending = WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus(rawValue: "pending")
    public static let waiting = WebhookWorkflowJobWaitingWorkflowJobStepsItemStatus(rawValue: "waiting")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
