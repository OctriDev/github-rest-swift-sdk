// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

// WebhookPush domain models
/// Typed representation of the `WebhookPush` API schema.
public struct WebhookPush: Codable {
    /// The SHA of the most recent commit on `ref` after the push.
    public var after: String
    /// Required `string` value serialized in the `base_ref` wire field.
    public var baseRef: WebhooksNullableString?
    /// The SHA of the most recent commit on `ref` before the push.
    public var before: String
    /// An array of commit objects describing the pushed commits. (Pushed commits are all commits that are included
    /// in the `compare` between the `before` commit and the `after` commit.) The array includes a maximum of 2048
    /// commits. If necessary, you can use the Commits API to fetch additional commits.
    public var commits: [WebhookPushCommitsItem]
    /// URL that shows the changes in this `ref` update, from the `before` commit to the `after` commit. For a newly
    /// created `ref` that is directly based on the default branch, this is the comparison between the head of the
    /// default branch and the `after` commit. Otherwise, this shows all commits until the `after` commit.
    public var compare: String
    /// Whether this push created the `ref`.
    public var created: Bool
    /// Whether this push deleted the `ref`.
    public var deleted: Bool
    /// Whether this push was a force push of the `ref`.
    public var forced: Bool
    /// Required object value serialized in the `head_commit` wire field.
    public var headCommit: WebhookPushHeadCommit?
    /// Metaproperties for Git author/committer information.
    public var pusher: WebhookPushPusher
    /// The full git ref that was pushed. Example: `refs/heads/main` or `refs/tags/v3.14.1`.
    public var ref: String
    /// A git repository
    public var repository: WebhookPushRepository
    /// An enterprise on GitHub. Webhook payloads contain the `enterprise` property when the webhook is configured
    /// on an enterprise account or an organization that's part of an enterprise account. For more information, see
    /// "About enterprise accounts."
    public var enterprise: EnterpriseWebhooks?
    /// The GitHub App installation. Webhook payloads contain the `installation` property when the event is
    /// configured for and sent to a GitHub App. For more information, see "Using webhooks with GitHub Apps."
    public var installation: SimpleInstallation?
    /// A GitHub organization. Webhook payloads contain the `organization` property when the webhook is configured
    /// for an organization, or when the event occurs from activity in a repository owned by an organization.
    public var organization: OrganizationSimpleWebhooks?
    /// A GitHub user.
    public var sender: SimpleUser?

    enum CodingKeys: String, CodingKey {
        case after
        case baseRef = "base_ref"
        case before
        case commits
        case compare
        case created
        case deleted
        case forced
        case headCommit = "head_commit"
        case pusher
        case ref
        case repository
        case enterprise
        case installation
        case organization
        case sender
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPush {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.after = try container.sdkDecodeRequired(.after)
        self.baseRef = try container.sdkDecodeIfPresent(.baseRef)
        self.before = try container.sdkDecodeRequired(.before)
        self.commits = try container.sdkDecodeRequired(.commits)
        self.compare = try container.sdkDecodeRequired(.compare)
        self.created = try container.sdkDecodeRequired(.created)
        self.deleted = try container.sdkDecodeRequired(.deleted)
        self.forced = try container.sdkDecodeRequired(.forced)
        self.headCommit = try container.sdkDecodeIfPresent(.headCommit)
        self.pusher = try container.sdkDecodeRequired(.pusher)
        self.ref = try container.sdkDecodeRequired(.ref)
        self.repository = try container.sdkDecodeRequired(.repository)
        self.enterprise = try container.sdkDecodeIfPresent(.enterprise)
        self.installation = try container.sdkDecodeIfPresent(.installation)
        self.organization = try container.sdkDecodeIfPresent(.organization)
        self.sender = try container.sdkDecodeIfPresent(.sender)
    }
}

public extension WebhookPush {
    public init(after: String, baseRef: WebhooksNullableString?, before: String, commits: [WebhookPushCommitsItem], compare: String, created: Bool, deleted: Bool, forced: Bool, headCommit: WebhookPushHeadCommit?, pusher: WebhookPushPusher, ref: String, repository: WebhookPushRepository, enterprise: EnterpriseWebhooks? = nil, installation: SimpleInstallation? = nil, organization: OrganizationSimpleWebhooks? = nil, sender: SimpleUser? = nil) {
        (self.after, self.baseRef) = (after, baseRef)
        (self.before, self.commits) = (before, commits)
        (self.compare, self.created) = (compare, created)
        (self.deleted, self.forced) = (deleted, forced)
        (self.headCommit, self.pusher) = (headCommit, pusher)
        (self.ref, self.repository) = (ref, repository)
        (self.enterprise, self.installation) = (enterprise, installation)
        (self.organization, self.sender) = (organization, sender)
    }
}

/// Required object value serialized in the `commits[]` wire field.
public struct WebhookPushCommitsItem: Codable {
    /// Metaproperties for Git author/committer information.
    public var author: WebhookPushCommitsItemAuthor
    /// Metaproperties for Git author/committer information.
    public var committer: WebhookPushCommitsItemCommitter
    /// Whether this commit is distinct from any that have been pushed before.
    public var distinct: Bool
    /// Required `string` value serialized in the `id` wire field.
    public var id: String
    /// The commit message.
    public var message: String
    /// The ISO 8601 timestamp of the commit.
    public var timestamp: Date
    /// Required `string` value serialized in the `tree_id` wire field.
    public var treeId: String
    /// URL that points to the commit API resource.
    public var url: String
    /// An array of files added in the commit. A maximum of 3000 changed files will be reported per commit.
    public var added: [String]?
    /// An array of files modified by the commit. A maximum of 3000 changed files will be reported per commit.
    public var modified: [String]?
    /// An array of files removed in the commit. A maximum of 3000 changed files will be reported per commit.
    public var removed: [String]?

    enum CodingKeys: String, CodingKey {
        case author
        case committer
        case distinct
        case id
        case message
        case timestamp
        case treeId = "tree_id"
        case url
        case added
        case modified
        case removed
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushCommitsItem {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.author = try container.sdkDecodeRequired(.author)
        self.committer = try container.sdkDecodeRequired(.committer)
        self.distinct = try container.sdkDecodeRequired(.distinct)
        self.id = try container.sdkDecodeRequired(.id)
        self.message = try container.sdkDecodeRequired(.message)
        self.timestamp = try container.sdkDecodeRequired(.timestamp)
        self.treeId = try container.sdkDecodeRequired(.treeId)
        self.url = try container.sdkDecodeRequired(.url)
        self.added = try container.sdkDecodeIfPresent(.added)
        self.modified = try container.sdkDecodeIfPresent(.modified)
        self.removed = try container.sdkDecodeIfPresent(.removed)
            try sdkValidateDateTime("timestamp", sdkWireString(self.timestamp))
            try sdkValidateUri("url", self.url)
    }
}

public extension WebhookPushCommitsItem {
    public init(author: WebhookPushCommitsItemAuthor, committer: WebhookPushCommitsItemCommitter, distinct: Bool, id: String, message: String, timestamp: Date, treeId: String, url: String, added: [String]? = nil, modified: [String]? = nil, removed: [String]? = nil) throws {
        (self.author, self.committer) = (author, committer)
        (self.distinct, self.id) = (distinct, id)
        (self.message, self.timestamp) = (message, timestamp)
        (self.treeId, self.url) = (treeId, url)
        (self.added, self.modified) = (added, modified)
        self.removed = removed
            try sdkValidateDateTime("timestamp", sdkWireString(self.timestamp))
            try sdkValidateUri("url", self.url)
    }
}

/// Metaproperties for Git author/committer information.
public struct WebhookPushCommitsItemAuthor: Codable {
    /// Required `email`-formatted value serialized in the `email` wire field.
    public var email: String?
    /// The git author's name.
    public var name: String
    /// Optional `date-time`-formatted value serialized in the `date` wire field.
    public var date: Date?
    /// Optional `string` value serialized in the `username` wire field.
    public var username: String?

    enum CodingKeys: String, CodingKey {
        case email
        case name
        case date
        case username
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushCommitsItemAuthor {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.email) else {
            throw SdkValidationError(field: "email", code: "required", message: "Validation failed for 'email': value is required")
        }
        guard container.contains(.name) else {
            throw SdkValidationError(field: "name", code: "required", message: "Validation failed for 'name': value is required")
        }
        self.email = try container.sdkDecodeIfPresent(.email)
        self.name = try container.sdkDecodeRequired(.name)
        self.date = try container.sdkDecodeIfPresent(.date)
        self.username = try container.sdkDecodeIfPresent(.username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension WebhookPushCommitsItemAuthor {
    public init(email: String?, name: String, date: Date? = nil, username: String? = nil) throws {
        (self.email, self.name) = (email, name)
        (self.date, self.username) = (date, username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

/// Metaproperties for Git author/committer information.
public struct WebhookPushCommitsItemCommitter: Codable {
    /// Required `email`-formatted value serialized in the `email` wire field.
    public var email: String?
    /// The git author's name.
    public var name: String
    /// Optional `date-time`-formatted value serialized in the `date` wire field.
    public var date: Date?
    /// Optional `string` value serialized in the `username` wire field.
    public var username: String?

    enum CodingKeys: String, CodingKey {
        case email
        case name
        case date
        case username
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushCommitsItemCommitter {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.email) else {
            throw SdkValidationError(field: "email", code: "required", message: "Validation failed for 'email': value is required")
        }
        guard container.contains(.name) else {
            throw SdkValidationError(field: "name", code: "required", message: "Validation failed for 'name': value is required")
        }
        self.email = try container.sdkDecodeIfPresent(.email)
        self.name = try container.sdkDecodeRequired(.name)
        self.date = try container.sdkDecodeIfPresent(.date)
        self.username = try container.sdkDecodeIfPresent(.username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension WebhookPushCommitsItemCommitter {
    public init(email: String?, name: String, date: Date? = nil, username: String? = nil) throws {
        (self.email, self.name) = (email, name)
        (self.date, self.username) = (date, username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

/// Required object value serialized in the `head_commit` wire field.
public struct WebhookPushHeadCommit: Codable {
    /// Metaproperties for Git author/committer information.
    public var author: WebhookPushHeadCommitAuthor
    /// Metaproperties for Git author/committer information.
    public var committer: WebhookPushHeadCommitCommitter
    /// Whether this commit is distinct from any that have been pushed before.
    public var distinct: Bool
    /// Required `string` value serialized in the `id` wire field.
    public var id: String
    /// The commit message.
    public var message: String
    /// The ISO 8601 timestamp of the commit.
    public var timestamp: Date
    /// Required `string` value serialized in the `tree_id` wire field.
    public var treeId: String
    /// URL that points to the commit API resource.
    public var url: String
    /// An array of files added in the commit.
    public var added: [String]?
    /// An array of files modified by the commit.
    public var modified: [String]?
    /// An array of files removed in the commit.
    public var removed: [String]?

    enum CodingKeys: String, CodingKey {
        case author
        case committer
        case distinct
        case id
        case message
        case timestamp
        case treeId = "tree_id"
        case url
        case added
        case modified
        case removed
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushHeadCommit {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.author = try container.sdkDecodeRequired(.author)
        self.committer = try container.sdkDecodeRequired(.committer)
        self.distinct = try container.sdkDecodeRequired(.distinct)
        self.id = try container.sdkDecodeRequired(.id)
        self.message = try container.sdkDecodeRequired(.message)
        self.timestamp = try container.sdkDecodeRequired(.timestamp)
        self.treeId = try container.sdkDecodeRequired(.treeId)
        self.url = try container.sdkDecodeRequired(.url)
        self.added = try container.sdkDecodeIfPresent(.added)
        self.modified = try container.sdkDecodeIfPresent(.modified)
        self.removed = try container.sdkDecodeIfPresent(.removed)
            try sdkValidateDateTime("timestamp", sdkWireString(self.timestamp))
            try sdkValidateUri("url", self.url)
    }
}

public extension WebhookPushHeadCommit {
    public init(author: WebhookPushHeadCommitAuthor, committer: WebhookPushHeadCommitCommitter, distinct: Bool, id: String, message: String, timestamp: Date, treeId: String, url: String, added: [String]? = nil, modified: [String]? = nil, removed: [String]? = nil) throws {
        (self.author, self.committer) = (author, committer)
        (self.distinct, self.id) = (distinct, id)
        (self.message, self.timestamp) = (message, timestamp)
        (self.treeId, self.url) = (treeId, url)
        (self.added, self.modified) = (added, modified)
        self.removed = removed
            try sdkValidateDateTime("timestamp", sdkWireString(self.timestamp))
            try sdkValidateUri("url", self.url)
    }
}

/// Metaproperties for Git author/committer information.
public struct WebhookPushHeadCommitAuthor: Codable {
    /// Required `email`-formatted value serialized in the `email` wire field.
    public var email: String?
    /// The git author's name.
    public var name: String
    /// Optional `date-time`-formatted value serialized in the `date` wire field.
    public var date: Date?
    /// Optional `string` value serialized in the `username` wire field.
    public var username: String?

    enum CodingKeys: String, CodingKey {
        case email
        case name
        case date
        case username
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushHeadCommitAuthor {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.email) else {
            throw SdkValidationError(field: "email", code: "required", message: "Validation failed for 'email': value is required")
        }
        guard container.contains(.name) else {
            throw SdkValidationError(field: "name", code: "required", message: "Validation failed for 'name': value is required")
        }
        self.email = try container.sdkDecodeIfPresent(.email)
        self.name = try container.sdkDecodeRequired(.name)
        self.date = try container.sdkDecodeIfPresent(.date)
        self.username = try container.sdkDecodeIfPresent(.username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension WebhookPushHeadCommitAuthor {
    public init(email: String?, name: String, date: Date? = nil, username: String? = nil) throws {
        (self.email, self.name) = (email, name)
        (self.date, self.username) = (date, username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

/// Metaproperties for Git author/committer information.
public struct WebhookPushHeadCommitCommitter: Codable {
    /// Required `email`-formatted value serialized in the `email` wire field.
    public var email: String?
    /// The git author's name.
    public var name: String
    /// Optional `date-time`-formatted value serialized in the `date` wire field.
    public var date: Date?
    /// Optional `string` value serialized in the `username` wire field.
    public var username: String?

    enum CodingKeys: String, CodingKey {
        case email
        case name
        case date
        case username
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushHeadCommitCommitter {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.email) else {
            throw SdkValidationError(field: "email", code: "required", message: "Validation failed for 'email': value is required")
        }
        guard container.contains(.name) else {
            throw SdkValidationError(field: "name", code: "required", message: "Validation failed for 'name': value is required")
        }
        self.email = try container.sdkDecodeIfPresent(.email)
        self.name = try container.sdkDecodeRequired(.name)
        self.date = try container.sdkDecodeIfPresent(.date)
        self.username = try container.sdkDecodeIfPresent(.username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

public extension WebhookPushHeadCommitCommitter {
    public init(email: String?, name: String, date: Date? = nil, username: String? = nil) throws {
        (self.email, self.name) = (email, name)
        (self.date, self.username) = (date, username)
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
    }
}

/// Metaproperties for Git author/committer information.
public struct WebhookPushPusher: Codable {
    /// The git author's name.
    public var name: String
    /// Optional `date-time`-formatted value serialized in the `date` wire field.
    public var date: Date?
    /// Optional `email`-formatted value serialized in the `email` wire field.
    public var email: String?
    /// Optional `string` value serialized in the `username` wire field.
    public var username: String?

    enum CodingKeys: String, CodingKey {
        case name
        case date
        case email
        case username
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension WebhookPushPusher {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.name) else {
            throw SdkValidationError(field: "name", code: "required", message: "Validation failed for 'name': value is required")
        }
        self.name = try container.sdkDecodeRequired(.name)
        self.date = try container.sdkDecodeIfPresent(.date)
        self.email = try container.sdkDecodeIfPresent(.email)
        self.username = try container.sdkDecodeIfPresent(.username)
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
    }
}

public extension WebhookPushPusher {
    public init(name: String, date: Date? = nil, email: String? = nil, username: String? = nil) throws {
        (self.name, self.date) = (name, date)
        (self.email, self.username) = (email, username)
        if let value = self.date {
            try sdkValidateDateTime("date", sdkWireString(value))
        }
        if let value = self.email {
            try sdkValidateEmail("email", value)
        }
    }
}
