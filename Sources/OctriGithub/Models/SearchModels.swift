// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

// Search domain models
public typealias SearchResultTextMatches = [SearchResultTextMatchesItem]

/// Code Search Result Item
public struct CodeSearchResultItem: Codable {
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `path` wire field.
    public var path: String
    /// Required `string` value serialized in the `sha` wire field.
    public var sha: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `git_url` wire field.
    public var gitUrl: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String
    /// Minimal Repository
    public var repository: MinimalRepository
    /// Required `number` value serialized in the `score` wire field.
    public var score: Double
    /// Optional `integer` value serialized in the `file_size` wire field.
    public var fileSize: Int?
    /// Optional `string` value serialized in the `language` wire field.
    public var language: String?
    /// Optional `date-time`-formatted value serialized in the `last_modified_at` wire field.
    public var lastModifiedAt: Date?
    /// Optional array value serialized in the `line_numbers` wire field.
    /// - Example: ["73..77","77..78"]
    public var lineNumbers: [String]?
    /// Optional array value serialized in the `text_matches` wire field.
    public var textMatches: SearchResultTextMatches?

    enum CodingKeys: String, CodingKey {
        case name
        case path
        case sha
        case url
        case gitUrl = "git_url"
        case htmlUrl = "html_url"
        case repository
        case score
        case fileSize = "file_size"
        case language
        case lastModifiedAt = "last_modified_at"
        case lineNumbers = "line_numbers"
        case textMatches = "text_matches"
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CodeSearchResultItem {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.sdkDecodeRequired(.name)
        self.path = try container.sdkDecodeRequired(.path)
        self.sha = try container.sdkDecodeRequired(.sha)
        self.url = try container.sdkDecodeRequired(.url)
        self.gitUrl = try container.sdkDecodeRequired(.gitUrl)
        self.htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        self.repository = try container.sdkDecodeRequired(.repository)
        self.score = try container.sdkDecodeRequired(.score)
        self.fileSize = try container.sdkDecodeIfPresent(.fileSize)
        self.language = try container.sdkDecodeIfPresent(.language)
        self.lastModifiedAt = try container.sdkDecodeIfPresent(.lastModifiedAt)
        self.lineNumbers = try container.sdkDecodeIfPresent(.lineNumbers)
        self.textMatches = try container.sdkDecodeIfPresent(.textMatches)
            try sdkValidateUri("url", self.url)
            try sdkValidateUri("git_url", self.gitUrl)
            try sdkValidateUri("html_url", self.htmlUrl)
        if let value = self.lastModifiedAt {
            try sdkValidateDateTime("last_modified_at", sdkWireString(value))
        }
    }
}

extension CodeSearchResultItem {
    public init(name: String, path: String, sha: String, url: String, gitUrl: String, htmlUrl: String, repository: MinimalRepository, score: Double, fileSize: Int? = nil, language: String? = nil, lastModifiedAt: Date? = nil, lineNumbers: [String]? = nil, textMatches: SearchResultTextMatches? = nil) throws {
        (self.name, self.path) = (name, path)
        (self.sha, self.url) = (sha, url)
        (self.gitUrl, self.htmlUrl) = (gitUrl, htmlUrl)
        (self.repository, self.score) = (repository, score)
        (self.fileSize, self.language) = (fileSize, language)
        (self.lastModifiedAt, self.lineNumbers) = (lastModifiedAt, lineNumbers)
        self.textMatches = textMatches
            try sdkValidateUri("url", self.url)
            try sdkValidateUri("git_url", self.gitUrl)
            try sdkValidateUri("html_url", self.htmlUrl)
        if let value = self.lastModifiedAt {
            try sdkValidateDateTime("last_modified_at", sdkWireString(value))
        }
    }
}

/// Commit Search Result Item
public struct CommitSearchResultItem: Codable {
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `string` value serialized in the `sha` wire field.
    public var sha: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String
    /// Required `uri`-formatted value serialized in the `comments_url` wire field.
    public var commentsUrl: String
    /// Required object value serialized in the `commit` wire field.
    public var commit: CommitSearchResultItemCommit
    /// A GitHub user.
    public var author: NullableSimpleUser?
    /// Metaproperties for Git author/committer information.
    public var committer: NullableGitUser?
    /// Required array value serialized in the `parents` wire field.
    public var parents: [CommitSearchResultItemParentsItem]
    /// Minimal Repository
    public var repository: MinimalRepository
    /// Required `number` value serialized in the `score` wire field.
    public var score: Double
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Optional array value serialized in the `text_matches` wire field.
    public var textMatches: SearchResultTextMatches?

    enum CodingKeys: String, CodingKey {
        case url
        case sha
        case htmlUrl = "html_url"
        case commentsUrl = "comments_url"
        case commit
        case author
        case committer
        case parents
        case repository
        case score
        case nodeId = "node_id"
        case textMatches = "text_matches"
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CommitSearchResultItem {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.url = try container.sdkDecodeRequired(.url)
        self.sha = try container.sdkDecodeRequired(.sha)
        self.htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        self.commentsUrl = try container.sdkDecodeRequired(.commentsUrl)
        self.commit = try container.sdkDecodeRequired(.commit)
        self.author = try container.sdkDecodeIfPresent(.author)
        self.committer = try container.sdkDecodeIfPresent(.committer)
        self.parents = try container.sdkDecodeRequired(.parents)
        self.repository = try container.sdkDecodeRequired(.repository)
        self.score = try container.sdkDecodeRequired(.score)
        self.nodeId = try container.sdkDecodeRequired(.nodeId)
        self.textMatches = try container.sdkDecodeIfPresent(.textMatches)
            try sdkValidateUri("url", self.url)
            try sdkValidateUri("html_url", self.htmlUrl)
            try sdkValidateUri("comments_url", self.commentsUrl)
    }
}

extension CommitSearchResultItem {
    public init(url: String, sha: String, htmlUrl: String, commentsUrl: String, commit: CommitSearchResultItemCommit, author: NullableSimpleUser?, committer: NullableGitUser?, parents: [CommitSearchResultItemParentsItem], repository: MinimalRepository, score: Double, nodeId: String, textMatches: SearchResultTextMatches? = nil) throws {
        (self.url, self.sha) = (url, sha)
        (self.htmlUrl, self.commentsUrl) = (htmlUrl, commentsUrl)
        (self.commit, self.author) = (commit, author)
        (self.committer, self.parents) = (committer, parents)
        (self.repository, self.score) = (repository, score)
        (self.nodeId, self.textMatches) = (nodeId, textMatches)
            try sdkValidateUri("url", self.url)
            try sdkValidateUri("html_url", self.htmlUrl)
            try sdkValidateUri("comments_url", self.commentsUrl)
    }
}

/// Required object value serialized in the `commit` wire field.
public struct CommitSearchResultItemCommit: Codable {
    /// Required object value serialized in the `author` wire field.
    public var author: CommitSearchResultItemCommitAuthor
    /// Metaproperties for Git author/committer information.
    public var committer: NullableGitUser?
    /// Required `integer` value serialized in the `comment_count` wire field.
    public var commentCount: Int
    /// Required `string` value serialized in the `message` wire field.
    public var message: String
    /// Required object value serialized in the `tree` wire field.
    public var tree: CommitSearchResultItemCommitTree
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Optional object value serialized in the `verification` wire field.
    public var verification: Verification?

    enum CodingKeys: String, CodingKey {
        case author
        case committer
        case commentCount = "comment_count"
        case message
        case tree
        case url
        case verification
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CommitSearchResultItemCommit {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.author) else {
            throw SdkValidationError(field: "author", code: "required", message: "Validation failed for 'author': value is required")
        }
        guard container.contains(.committer) else {
            throw SdkValidationError(field: "committer", code: "required", message: "Validation failed for 'committer': value is required")
        }
        guard container.contains(.commentCount) else {
            throw SdkValidationError(field: "comment_count", code: "required", message: "Validation failed for 'comment_count': value is required")
        }
        guard container.contains(.message) else {
            throw SdkValidationError(field: "message", code: "required", message: "Validation failed for 'message': value is required")
        }
        guard container.contains(.tree) else {
            throw SdkValidationError(field: "tree", code: "required", message: "Validation failed for 'tree': value is required")
        }
        guard container.contains(.url) else {
            throw SdkValidationError(field: "url", code: "required", message: "Validation failed for 'url': value is required")
        }
        self.author = try container.sdkDecodeRequired(.author)
        self.committer = try container.sdkDecodeIfPresent(.committer)
        self.commentCount = try container.sdkDecodeRequired(.commentCount)
        self.message = try container.sdkDecodeRequired(.message)
        self.tree = try container.sdkDecodeRequired(.tree)
        self.url = try container.sdkDecodeRequired(.url)
        self.verification = try container.sdkDecodeIfPresent(.verification)
            try sdkValidateUri("url", self.url)
    }
}

extension CommitSearchResultItemCommit {
    public init(author: CommitSearchResultItemCommitAuthor, committer: NullableGitUser?, commentCount: Int, message: String, tree: CommitSearchResultItemCommitTree, url: String, verification: Verification? = nil) throws {
        (self.author, self.committer) = (author, committer)
        (self.commentCount, self.message) = (commentCount, message)
        (self.tree, self.url) = (tree, url)
        self.verification = verification
            try sdkValidateUri("url", self.url)
    }
}

/// Required object value serialized in the `author` wire field.
public struct CommitSearchResultItemCommitAuthor: Codable {
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `email` wire field.
    public var email: String
    /// Required `date-time`-formatted value serialized in the `date` wire field.
    public var date: Date

    enum CodingKeys: String, CodingKey {
        case name
        case email
        case date
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CommitSearchResultItemCommitAuthor {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.name) else {
            throw SdkValidationError(field: "name", code: "required", message: "Validation failed for 'name': value is required")
        }
        guard container.contains(.email) else {
            throw SdkValidationError(field: "email", code: "required", message: "Validation failed for 'email': value is required")
        }
        guard container.contains(.date) else {
            throw SdkValidationError(field: "date", code: "required", message: "Validation failed for 'date': value is required")
        }
        self.name = try container.sdkDecodeRequired(.name)
        self.email = try container.sdkDecodeRequired(.email)
        self.date = try container.sdkDecodeRequired(.date)
            try sdkValidateDateTime("date", sdkWireString(self.date))
    }
}

extension CommitSearchResultItemCommitAuthor {
    public init(name: String, email: String, date: Date) throws {
        (self.name, self.email) = (name, email)
        self.date = date
            try sdkValidateDateTime("date", sdkWireString(self.date))
    }
}

/// Required object value serialized in the `tree` wire field.
public struct CommitSearchResultItemCommitTree: Codable {
    /// Required `string` value serialized in the `sha` wire field.
    public var sha: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String

    enum CodingKeys: String, CodingKey {
        case sha
        case url
    }

    private init(sdkCopy value: Self) { self = value }
}

extension CommitSearchResultItemCommitTree {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.sha) else {
            throw SdkValidationError(field: "sha", code: "required", message: "Validation failed for 'sha': value is required")
        }
        guard container.contains(.url) else {
            throw SdkValidationError(field: "url", code: "required", message: "Validation failed for 'url': value is required")
        }
        self.sha = try container.sdkDecodeRequired(.sha)
        self.url = try container.sdkDecodeRequired(.url)
            try sdkValidateUri("url", self.url)
    }
}

extension CommitSearchResultItemCommitTree {
    public init(sha: String, url: String) throws {
        (self.sha, self.url) = (sha, url)
            try sdkValidateUri("url", self.url)
    }
}

/// Required object value serialized in the `parents[]` wire field.
public struct CommitSearchResultItemParentsItem: Codable {
    /// Optional `string` value serialized in the `url` wire field.
    public var url: String?
    /// Optional `string` value serialized in the `html_url` wire field.
    public var htmlUrl: String?
    /// Optional `string` value serialized in the `sha` wire field.
    public var sha: String?

    enum CodingKeys: String, CodingKey {
        case url
        case htmlUrl = "html_url"
        case sha
    }

    init() {
        (self.url, self.htmlUrl, self.sha) = (nil, nil, nil)
    }
}

extension CommitSearchResultItemParentsItem {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.url = try container.sdkDecodeIfPresent(.url)
        self.htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        self.sha = try container.sdkDecodeIfPresent(.sha)
    }
}

extension CommitSearchResultItemParentsItem {
    public init(url: String? = nil, htmlUrl: String? = nil, sha: String? = nil) {
        self.init()
        (self.url, self.htmlUrl) = (url, htmlUrl)
        self.sha = sha
    }
}

/// Issue Search Result Item
public struct IssueSearchResultItem: Codable {
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `repository_url` wire field.
    public var repositoryUrl: String
    /// Required `string` value serialized in the `labels_url` wire field.
    public var labelsUrl: String
    /// Required `uri`-formatted value serialized in the `comments_url` wire field.
    public var commentsUrl: String
    /// Required `uri`-formatted value serialized in the `events_url` wire field.
    public var eventsUrl: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String
    /// Required `int64`-formatted value serialized in the `id` wire field.
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `integer` value serialized in the `number` wire field.
    public var number: Int
    /// Required `string` value serialized in the `title` wire field.
    public var title: String
    /// Required `boolean` value serialized in the `locked` wire field.
    public var locked: Bool
    /// A GitHub user.
    public var user: NullableSimpleUser?
    /// Required array value serialized in the `labels` wire field.
    public var labels: [IssueSearchResultItemLabelsItem]
    /// Required `string` value serialized in the `state` wire field.
    public var state: String
    /// A GitHub user.
    public var assignee: NullableSimpleUser?
    /// A collection of related issues and pull requests.
    public var milestone: NullableMilestone?
    /// Required `integer` value serialized in the `comments` wire field.
    public var comments: Int
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    public var createdAt: Date
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    public var updatedAt: Date
    /// Required `date-time`-formatted value serialized in the `closed_at` wire field.
    public var closedAt: Date?
    /// Required `number` value serialized in the `score` wire field.
    public var score: Double
    /// How the author is associated with the repository.
    /// - Example: "OWNER"
    public var authorAssociation: AuthorAssociation
    /// Optional `string` value serialized in the `active_lock_reason` wire field.
    public var activeLockReason: String?
    /// Optional array value serialized in the `assignees` wire field.
    public var assignees: [SimpleUser]?
    /// Optional object value serialized in the `sub_issues_summary` wire field.
    public var subIssuesSummary: SubIssuesSummary?
    /// Optional object value serialized in the `issue_dependencies_summary` wire field.
    public var issueDependenciesSummary: IssueDependenciesSummary?
    /// Optional array value serialized in the `issue_field_values` wire field.
    public var issueFieldValues: [IssueFieldValue]?
    /// Optional `string` value serialized in the `state_reason` wire field.
    public var stateReason: String?
    /// Optional array value serialized in the `text_matches` wire field.
    public var textMatches: SearchResultTextMatches?
    /// Optional object value serialized in the `pull_request` wire field.
    public var pullRequest: IssueSearchResultItemPullRequest?
    /// Optional `string` value serialized in the `body` wire field.
    public var body: String?
    /// Optional `boolean` value serialized in the `draft` wire field.
    public var draft: Bool?
    /// A repository on GitHub.
    public var repository: Repository?
    /// Optional `string` value serialized in the `body_html` wire field.
    public var bodyHtml: String?
    /// Optional `string` value serialized in the `body_text` wire field.
    public var bodyText: String?
    /// Optional `uri`-formatted value serialized in the `timeline_url` wire field.
    public var timelineUrl: String?
    /// The type assigned to the issue. This is only present for issues in repositories where issue types are
    /// supported.
    public var type: IssueType?
    /// GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user
    /// accounts and granted access to specific repositories. They come with granular permissions and built-in
    /// webhooks. GitHub apps are first class actors within GitHub.
    public var performedViaGithubApp: NullableIntegration?
    /// Comments provide a way for people to collaborate on an issue.
    public var pinnedComment: NullableIssueComment?
    /// Optional object value serialized in the `reactions` wire field.
    public var reactions: ReactionRollup?

    enum CodingKeys: String, CodingKey {
        case url
        case repositoryUrl = "repository_url"
        case labelsUrl = "labels_url"
        case commentsUrl = "comments_url"
        case eventsUrl = "events_url"
        case htmlUrl = "html_url"
        case id
        case nodeId = "node_id"
        case number
        case title
        case locked
        case user
        case labels
        case state
        case assignee
        case milestone
        case comments
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case closedAt = "closed_at"
        case score
        case authorAssociation = "author_association"
        case activeLockReason = "active_lock_reason"
        case assignees
        case subIssuesSummary = "sub_issues_summary"
        case issueDependenciesSummary = "issue_dependencies_summary"
        case issueFieldValues = "issue_field_values"
        case stateReason = "state_reason"
        case textMatches = "text_matches"
        case pullRequest = "pull_request"
        case body
        case draft
        case repository
        case bodyHtml = "body_html"
        case bodyText = "body_text"
        case timelineUrl = "timeline_url"
        case type
        case performedViaGithubApp = "performed_via_github_app"
        case pinnedComment = "pinned_comment"
        case reactions
    }

    private init(sdkCopy value: Self) { self = value }
}
