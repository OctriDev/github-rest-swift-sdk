// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

// WebhookSubIssuesSubIssueRemoved domain models
/// Typed representation of the `WebhookSubIssuesSubIssueRemoved` API schema.
public struct WebhookSubIssuesSubIssueRemoved: Codable {
    /// Required enumerated value serialized in the `action` wire field.
    public var action: WebhookSubIssuesSubIssueRemovedAction
    /// The ID of the parent issue.
    public var parentIssueId: Double
    /// Issues are a great way to keep track of tasks, enhancements, and bugs for your projects.
    public var parentIssue: Issue
    /// The repository on GitHub where the event occurred. Webhook payloads contain the `repository` property when
    /// the event occurs from activity in a repository.
    public var repository: RepositoryWebhooks
    /// A GitHub user.
    public var sender: SimpleUser
    /// The ID of the sub-issue.
    public var subIssueId: Double?
    /// Issues are a great way to keep track of tasks, enhancements, and bugs for your projects.
    public var subIssue: Issue?
    /// A repository on GitHub.
    public var subIssueRepo: Repository?
    /// The GitHub App installation. Webhook payloads contain the `installation` property when the event is
    /// configured for and sent to a GitHub App. For more information, see "Using webhooks with GitHub Apps."
    public var installation: SimpleInstallation?
    /// A GitHub organization. Webhook payloads contain the `organization` property when the webhook is configured
    /// for an organization, or when the event occurs from activity in a repository owned by an organization.
    public var organization: OrganizationSimpleWebhooks?

    enum CodingKeys: String, CodingKey {
        case action
        case parentIssueId = "parent_issue_id"
        case parentIssue = "parent_issue"
        case repository
        case sender
        case subIssueId = "sub_issue_id"
        case subIssue = "sub_issue"
        case subIssueRepo = "sub_issue_repo"
        case installation
        case organization
    }

    private init(sdkCopy value: Self) { self = value }
}

extension WebhookSubIssuesSubIssueRemoved {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.action) else {
            throw SdkValidationError(field: "action", code: "required", message: "Validation failed for 'action': value is required")
        }
        guard container.contains(.parentIssueId) else {
            throw SdkValidationError(field: "parent_issue_id", code: "required", message: "Validation failed for 'parent_issue_id': value is required")
        }
        guard container.contains(.parentIssue) else {
            throw SdkValidationError(field: "parent_issue", code: "required", message: "Validation failed for 'parent_issue': value is required")
        }
        guard container.contains(.repository) else {
            throw SdkValidationError(field: "repository", code: "required", message: "Validation failed for 'repository': value is required")
        }
        guard container.contains(.sender) else {
            throw SdkValidationError(field: "sender", code: "required", message: "Validation failed for 'sender': value is required")
        }
        self.action = try container.sdkDecodeRequired(.action)
        self.parentIssueId = try container.sdkDecodeRequired(.parentIssueId)
        self.parentIssue = try container.sdkDecodeRequired(.parentIssue)
        self.repository = try container.sdkDecodeRequired(.repository)
        self.sender = try container.sdkDecodeRequired(.sender)
        self.subIssueId = try container.sdkDecodeIfPresent(.subIssueId)
        self.subIssue = try container.sdkDecodeIfPresent(.subIssue)
        self.subIssueRepo = try container.sdkDecodeIfPresent(.subIssueRepo)
        self.installation = try container.sdkDecodeIfPresent(.installation)
        self.organization = try container.sdkDecodeIfPresent(.organization)
    }
}

extension WebhookSubIssuesSubIssueRemoved {
    public init(action: WebhookSubIssuesSubIssueRemovedAction, parentIssueId: Double, parentIssue: Issue, repository: RepositoryWebhooks, sender: SimpleUser, subIssueId: Double? = nil, subIssue: Issue? = nil, subIssueRepo: Repository? = nil, installation: SimpleInstallation? = nil, organization: OrganizationSimpleWebhooks? = nil) {
        (self.action, self.parentIssueId) = (action, parentIssueId)
        (self.parentIssue, self.repository) = (parentIssue, repository)
        (self.sender, self.subIssueId) = (sender, subIssueId)
        (self.subIssue, self.subIssueRepo) = (subIssue, subIssueRepo)
        (self.installation, self.organization) = (installation, organization)
    }
}

/// Required enumerated value serialized in the `action` wire field.
public struct WebhookSubIssuesSubIssueRemovedAction: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) { self.rawValue = rawValue }
    public static let subIssueRemoved = WebhookSubIssuesSubIssueRemovedAction(rawValue: "sub_issue_removed")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        self.init(rawValue: try container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
