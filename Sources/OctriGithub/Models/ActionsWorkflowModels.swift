// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// ActionsWorkflow domain models
public typealias WorkflowRunId = Int

/// A GitHub Actions workflow
public struct Workflow: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    /// - Example: 5
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    /// - Example: "MDg6V29ya2Zsb3cxMg=="
    public var nodeId: String
    /// Required `string` value serialized in the `name` wire field.
    /// - Example: "CI"
    public var name: String
    /// Required `string` value serialized in the `path` wire field.
    /// - Example: "ruby.yaml"
    public var path: String
    /// Required enumerated value serialized in the `state` wire field.
    /// - Example: "active"
    public var state: WorkflowState
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    /// - Example: "2019-12-06T14:20:20.000Z"
    public var createdAt: Date
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    /// - Example: "2019-12-06T14:20:20.000Z"
    public var updatedAt: Date
    /// Required `string` value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/repos/actions/setup-ruby/workflows/5"
    public var url: String
    /// Required `string` value serialized in the `html_url` wire field.
    /// - Example: "https://github.com/actions/setup-ruby/blob/master/.github/workflows/ruby.yaml"
    public var htmlUrl: String
    /// Required `string` value serialized in the `badge_url` wire field.
    /// - Example: "https://github.com/actions/setup-ruby/workflows/CI/badge.svg"
    public var badgeUrl: String
    /// Optional `date-time`-formatted value serialized in the `deleted_at` wire field.
    /// - Example: "2019-12-06T14:20:20.000Z"
    public var deletedAt: Date?

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case name
        case path
        case state
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case url
        case htmlUrl = "html_url"
        case badgeUrl = "badge_url"
        case deletedAt = "deleted_at"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Workflow {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        name = try container.sdkDecodeRequired(.name)
        path = try container.sdkDecodeRequired(.path)
        state = try container.sdkDecodeRequired(.state)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        updatedAt = try container.sdkDecodeRequired(.updatedAt)
        url = try container.sdkDecodeRequired(.url)
        htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        badgeUrl = try container.sdkDecodeRequired(.badgeUrl)
        deletedAt = try container.sdkDecodeIfPresent(.deletedAt)
        try sdkValidateDateTime("created_at", sdkWireString(createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(updatedAt))
        if let value = deletedAt {
            try sdkValidateDateTime("deleted_at", sdkWireString(value))
        }
    }
}

public extension Workflow {
    init(
        id: Int,
        nodeId: String,
        name: String,
        path: String,
        state: WorkflowState,
        createdAt: Date,
        updatedAt: Date,
        url: String,
        htmlUrl: String,
        badgeUrl: String,
        deletedAt: Date? = nil
    ) throws {
        (self.id, self.nodeId) = (id, nodeId)
        (self.name, self.path) = (name, path)
        (self.state, self.createdAt) = (state, createdAt)
        (self.updatedAt, self.url) = (updatedAt, url)
        (self.htmlUrl, self.badgeUrl) = (htmlUrl, badgeUrl)
        self.deletedAt = deletedAt
        try sdkValidateDateTime("created_at", sdkWireString(self.createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(self.updatedAt))
        if let value = self.deletedAt {
            try sdkValidateDateTime("deleted_at", sdkWireString(value))
        }
    }
}

/// Response containing the workflow run ID and URLs.
public struct WorkflowDispatchResponse: Codable {
    /// The ID of the workflow run.
    public var workflowRunId: WorkflowRunId
    /// The URL to the workflow run.
    public var runUrl: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String

    enum CodingKeys: String, CodingKey {
        case workflowRunId = "workflow_run_id"
        case runUrl = "run_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WorkflowDispatchResponse {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.workflowRunId) else {
            throw SdkValidationError(
                field: "workflow_run_id",
                code: "required",
                message: "Validation failed for 'workflow_run_id': value is required"
            )
        }
        guard container.contains(.runUrl) else {
            throw SdkValidationError(
                field: "run_url",
                code: "required",
                message: "Validation failed for 'run_url': value is required"
            )
        }
        guard container.contains(.htmlUrl) else {
            throw SdkValidationError(
                field: "html_url",
                code: "required",
                message: "Validation failed for 'html_url': value is required"
            )
        }
        workflowRunId = try container.sdkDecodeRequired(.workflowRunId)
        runUrl = try container.sdkDecodeRequired(.runUrl)
        htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        try sdkValidateUri("run_url", runUrl)
        try sdkValidateUri("html_url", htmlUrl)
    }
}

public extension WorkflowDispatchResponse {
    init(workflowRunId: WorkflowRunId, runUrl: String, htmlUrl: String) throws {
        (self.workflowRunId, self.runUrl) = (workflowRunId, runUrl)
        self.htmlUrl = htmlUrl
        try sdkValidateUri("run_url", self.runUrl)
        try sdkValidateUri("html_url", self.htmlUrl)
    }
}

/// An invocation of a workflow
public struct WorkflowRun: Codable {
    /// The ID of the workflow run.
    /// - Example: 5
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    /// - Example: "MDEwOkNoZWNrU3VpdGU1"
    public var nodeId: String
    /// Required `string` value serialized in the `head_branch` wire field.
    /// - Example: "master"
    public var headBranch: String?
    /// The SHA of the head commit that points to the version of the workflow being run.
    /// - Example: "009b8a3a9ccbb128af87f9b1c0f4c62e8a304f6d"
    public var headSha: String
    /// The full path of the workflow
    /// - Example: "octocat/octo-repo/.github/workflows/ci.yml@main"
    public var path: String
    /// The auto incrementing run number for the workflow run.
    /// - Example: 106
    public var runNumber: Int
    /// Required `string` value serialized in the `event` wire field.
    /// - Example: "push"
    public var event: String
    /// Required `string` value serialized in the `status` wire field.
    /// - Example: "completed"
    public var status: String?
    /// Required `string` value serialized in the `conclusion` wire field.
    /// - Example: "neutral"
    public var conclusion: String?
    /// The ID of the parent workflow.
    /// - Example: 5
    public var workflowId: Int
    /// The URL to the workflow run.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5"
    public var url: String
    /// Required `string` value serialized in the `html_url` wire field.
    /// - Example: "https://github.com/github/hello-world/suites/4"
    public var htmlUrl: String
    /// Pull requests that are open with a `head_sha` or `head_branch` that matches the workflow run. The returned
    /// pull requests do not necessarily indicate pull requests that triggered the run.
    public var pullRequests: [PullRequestMinimal]?
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    public var createdAt: Date
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    public var updatedAt: Date
    /// The URL to the jobs for the workflow run.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5/jobs"
    public var jobsUrl: String
    /// The URL to download the logs for the workflow run.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5/logs"
    public var logsUrl: String
    /// The URL to the associated check suite.
    /// - Example: "https://api.github.com/repos/github/hello-world/check-suites/12"
    public var checkSuiteUrl: String
    /// The URL to the artifacts for the workflow run.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5/rerun/artifacts"
    public var artifactsUrl: String
    /// The URL to cancel the workflow run.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5/cancel"
    public var cancelUrl: String
    /// The URL to rerun the workflow run.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5/rerun"
    public var rerunUrl: String
    /// The URL to the workflow.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/workflows/main.yaml"
    public var workflowUrl: String
    /// A commit.
    public var headCommit: NullableSimpleCommit?
    /// Minimal Repository
    public var repository: MinimalRepository
    /// Minimal Repository
    public var headRepository: MinimalRepository
    /// The event-specific title associated with the run or the run-name if set, or the value of `run-name` if it is
    /// set in the workflow.
    /// - Example: "Simple Workflow"
    public var displayTitle: String
    /// The name of the workflow run.
    /// - Example: "Build"
    public var name: String?
    /// The ID of the associated check suite.
    /// - Example: 42
    public var checkSuiteId: Int?
    /// The node ID of the associated check suite.
    /// - Example: "MDEwOkNoZWNrU3VpdGU0Mg=="
    public var checkSuiteNodeId: String?
    /// Attempt number of the run, 1 for first attempt and higher if the workflow was re-run.
    /// - Example: 1
    public var runAttempt: Int?
    /// Optional array value serialized in the `referenced_workflows` wire field.
    public var referencedWorkflows: [ReferencedWorkflow]?
    /// A GitHub user.
    public var actor: SimpleUser?
    /// A GitHub user.
    public var triggeringActor: SimpleUser?
    /// The start time of the latest run. Resets on re-run.
    public var runStartedAt: Date?
    /// The URL to the previous attempted run of this workflow, if one exists.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5/attempts/3"
    public var previousAttemptUrl: String?
    /// Optional `integer` value serialized in the `head_repository_id` wire field.
    /// - Example: 5
    public var headRepositoryId: Int?

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case headBranch = "head_branch"
        case headSha = "head_sha"
        case path
        case runNumber = "run_number"
        case event
        case status
        case conclusion
        case workflowId = "workflow_id"
        case url
        case htmlUrl = "html_url"
        case pullRequests = "pull_requests"
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case jobsUrl = "jobs_url"
        case logsUrl = "logs_url"
        case checkSuiteUrl = "check_suite_url"
        case artifactsUrl = "artifacts_url"
        case cancelUrl = "cancel_url"
        case rerunUrl = "rerun_url"
        case workflowUrl = "workflow_url"
        case headCommit = "head_commit"
        case repository
        case headRepository = "head_repository"
        case displayTitle = "display_title"
        case name
        case checkSuiteId = "check_suite_id"
        case checkSuiteNodeId = "check_suite_node_id"
        case runAttempt = "run_attempt"
        case referencedWorkflows = "referenced_workflows"
        case actor
        case triggeringActor = "triggering_actor"
        case runStartedAt = "run_started_at"
        case previousAttemptUrl = "previous_attempt_url"
        case headRepositoryId = "head_repository_id"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WorkflowRun {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        headBranch = try container.sdkDecodeIfPresent(.headBranch)
        headSha = try container.sdkDecodeRequired(.headSha)
        path = try container.sdkDecodeRequired(.path)
        runNumber = try container.sdkDecodeRequired(.runNumber)
        event = try container.sdkDecodeRequired(.event)
        status = try container.sdkDecodeIfPresent(.status)
        conclusion = try container.sdkDecodeIfPresent(.conclusion)
        workflowId = try container.sdkDecodeRequired(.workflowId)
        url = try container.sdkDecodeRequired(.url)
        htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        pullRequests = try container.sdkDecodeIfPresent(.pullRequests)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        updatedAt = try container.sdkDecodeRequired(.updatedAt)
        jobsUrl = try container.sdkDecodeRequired(.jobsUrl)
        logsUrl = try container.sdkDecodeRequired(.logsUrl)
        checkSuiteUrl = try container.sdkDecodeRequired(.checkSuiteUrl)
        artifactsUrl = try container.sdkDecodeRequired(.artifactsUrl)
        cancelUrl = try container.sdkDecodeRequired(.cancelUrl)
        rerunUrl = try container.sdkDecodeRequired(.rerunUrl)
        workflowUrl = try container.sdkDecodeRequired(.workflowUrl)
        headCommit = try container.sdkDecodeIfPresent(.headCommit)
        repository = try container.sdkDecodeRequired(.repository)
        headRepository = try container.sdkDecodeRequired(.headRepository)
        displayTitle = try container.sdkDecodeRequired(.displayTitle)
        name = try container.sdkDecodeIfPresent(.name)
        checkSuiteId = try container.sdkDecodeIfPresent(.checkSuiteId)
        checkSuiteNodeId = try container.sdkDecodeIfPresent(.checkSuiteNodeId)
        runAttempt = try container.sdkDecodeIfPresent(.runAttempt)
        referencedWorkflows = try container.sdkDecodeIfPresent(.referencedWorkflows)
        actor = try container.sdkDecodeIfPresent(.actor)
        triggeringActor = try container.sdkDecodeIfPresent(.triggeringActor)
        runStartedAt = try container.sdkDecodeIfPresent(.runStartedAt)
        previousAttemptUrl = try container.sdkDecodeIfPresent(.previousAttemptUrl)
        headRepositoryId = try container.sdkDecodeIfPresent(.headRepositoryId)
        try sdkValidateDateTime("created_at", sdkWireString(createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(updatedAt))
        if let value = runStartedAt {
            try sdkValidateDateTime("run_started_at", sdkWireString(value))
        }
    }
}

public extension WorkflowRun {
    init(
        id: Int,
        nodeId: String,
        headBranch: String?,
        headSha: String,
        path: String,
        runNumber: Int,
        event: String,
        status: String?,
        conclusion: String?,
        workflowId: Int,
        url: String,
        htmlUrl: String,
        pullRequests: [PullRequestMinimal]?,
        createdAt: Date,
        updatedAt: Date,
        jobsUrl: String,
        logsUrl: String,
        checkSuiteUrl: String,
        artifactsUrl: String,
        cancelUrl: String,
        rerunUrl: String,
        workflowUrl: String,
        headCommit: NullableSimpleCommit?,
        repository: MinimalRepository,
        headRepository: MinimalRepository,
        displayTitle: String,
        name: String? = nil,
        checkSuiteId: Int? = nil,
        checkSuiteNodeId: String? = nil,
        runAttempt: Int? = nil,
        referencedWorkflows: [ReferencedWorkflow]? = nil,
        actor: SimpleUser? = nil,
        triggeringActor: SimpleUser? = nil,
        runStartedAt: Date? = nil,
        previousAttemptUrl: String? = nil,
        headRepositoryId: Int? = nil
    ) throws {
        (self.id, self.nodeId) = (id, nodeId)
        (self.headBranch, self.headSha) = (headBranch, headSha)
        (self.path, self.runNumber) = (path, runNumber)
        (self.event, self.status) = (event, status)
        (self.conclusion, self.workflowId) = (conclusion, workflowId)
        (self.url, self.htmlUrl) = (url, htmlUrl)
        (self.pullRequests, self.createdAt) = (pullRequests, createdAt)
        (self.updatedAt, self.jobsUrl) = (updatedAt, jobsUrl)
        (self.logsUrl, self.checkSuiteUrl) = (logsUrl, checkSuiteUrl)
        (self.artifactsUrl, self.cancelUrl) = (artifactsUrl, cancelUrl)
        (self.rerunUrl, self.workflowUrl) = (rerunUrl, workflowUrl)
        (self.headCommit, self.repository) = (headCommit, repository)
        (self.headRepository, self.displayTitle) = (headRepository, displayTitle)
        (self.name, self.checkSuiteId) = (name, checkSuiteId)
        (self.checkSuiteNodeId, self.runAttempt) = (checkSuiteNodeId, runAttempt)
        (self.referencedWorkflows, self.actor) = (referencedWorkflows, actor)
        (self.triggeringActor, self.runStartedAt) = (triggeringActor, runStartedAt)
        (self.previousAttemptUrl, self.headRepositoryId) = (previousAttemptUrl, headRepositoryId)
        try sdkValidateDateTime("created_at", sdkWireString(self.createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(self.updatedAt))
        if let value = self.runStartedAt {
            try sdkValidateDateTime("run_started_at", sdkWireString(value))
        }
    }
}

/// Workflow Run Usage
public struct WorkflowRunUsage: Codable {
    /// Required object value serialized in the `billable` wire field.
    public var billable: WorkflowRunUsageBillable
    /// Optional `integer` value serialized in the `run_duration_ms` wire field.
    public var runDurationMs: Int?

    enum CodingKeys: String, CodingKey {
        case billable
        case runDurationMs = "run_duration_ms"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WorkflowRunUsage {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.billable) else {
            throw SdkValidationError(
                field: "billable",
                code: "required",
                message: "Validation failed for 'billable': value is required"
            )
        }
        billable = try container.sdkDecodeRequired(.billable)
        runDurationMs = try container.sdkDecodeIfPresent(.runDurationMs)
    }
}

public extension WorkflowRunUsage {
    init(billable: WorkflowRunUsageBillable, runDurationMs: Int? = nil) {
        (self.billable, self.runDurationMs) = (billable, runDurationMs)
    }
}

/// Required object value serialized in the `billable` wire field.
public struct WorkflowRunUsageBillable: Codable {
    /// Optional object value serialized in the `UBUNTU` wire field.
    public var uBUNTU: WorkflowRunUsageBillableUBUNTU?
    /// Optional object value serialized in the `MACOS` wire field.
    public var mACOS: WorkflowRunUsageBillableMACOS?
    /// Optional object value serialized in the `WINDOWS` wire field.
    public var wINDOWS: WorkflowRunUsageBillableWINDOWS?

    enum CodingKeys: String, CodingKey {
        case uBUNTU = "UBUNTU"
        case mACOS = "MACOS"
        case wINDOWS = "WINDOWS"
    }

    init() {
        (uBUNTU, mACOS, wINDOWS) = (nil, nil, nil)
    }
}

public extension WorkflowRunUsageBillable {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        uBUNTU = try container.sdkDecodeIfPresent(.uBUNTU)
        mACOS = try container.sdkDecodeIfPresent(.mACOS)
        wINDOWS = try container.sdkDecodeIfPresent(.wINDOWS)
    }
}

public extension WorkflowRunUsageBillable {
    init(
        uBUNTU: WorkflowRunUsageBillableUBUNTU? = nil,
        mACOS: WorkflowRunUsageBillableMACOS? = nil,
        wINDOWS: WorkflowRunUsageBillableWINDOWS? = nil
    ) {
        self.init()
        (self.uBUNTU, self.mACOS) = (uBUNTU, mACOS)
        self.wINDOWS = wINDOWS
    }
}

/// Optional object value serialized in the `MACOS` wire field.
public struct WorkflowRunUsageBillableMACOS: Codable {
    /// Required `integer` value serialized in the `total_ms` wire field.
    public var totalMs: Int
    /// Required `integer` value serialized in the `jobs` wire field.
    public var jobs: Int
    /// Optional array value serialized in the `job_runs` wire field.
    public var jobRuns: [WorkflowRunUsageBillableMACOSJobRunsItem]?

    enum CodingKeys: String, CodingKey {
        case totalMs = "total_ms"
        case jobs
        case jobRuns = "job_runs"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WorkflowRunUsageBillableMACOS {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.totalMs) else {
            throw SdkValidationError(
                field: "total_ms",
                code: "required",
                message: "Validation failed for 'total_ms': value is required"
            )
        }
        guard container.contains(.jobs) else {
            throw SdkValidationError(
                field: "jobs",
                code: "required",
                message: "Validation failed for 'jobs': value is required"
            )
        }
        totalMs = try container.sdkDecodeRequired(.totalMs)
        jobs = try container.sdkDecodeRequired(.jobs)
        jobRuns = try container.sdkDecodeIfPresent(.jobRuns)
    }
}

public extension WorkflowRunUsageBillableMACOS {
    init(totalMs: Int, jobs: Int, jobRuns: [WorkflowRunUsageBillableMACOSJobRunsItem]? = nil) {
        (self.totalMs, self.jobs) = (totalMs, jobs)
        self.jobRuns = jobRuns
    }
}

/// Required object value serialized in the `job_runs[]` wire field.
public struct WorkflowRunUsageBillableMACOSJobRunsItem: Codable {
    /// Required `integer` value serialized in the `job_id` wire field.
    public var jobId: Int
    /// Required `integer` value serialized in the `duration_ms` wire field.
    public var durationMs: Int

    enum CodingKeys: String, CodingKey {
        case jobId = "job_id"
        case durationMs = "duration_ms"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension WorkflowRunUsageBillableMACOSJobRunsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.jobId) else {
            throw SdkValidationError(
                field: "job_id",
                code: "required",
                message: "Validation failed for 'job_id': value is required"
            )
        }
        guard container.contains(.durationMs) else {
            throw SdkValidationError(
                field: "duration_ms",
                code: "required",
                message: "Validation failed for 'duration_ms': value is required"
            )
        }
        jobId = try container.sdkDecodeRequired(.jobId)
        durationMs = try container.sdkDecodeRequired(.durationMs)
    }
}

public extension WorkflowRunUsageBillableMACOSJobRunsItem {
    init(jobId: Int, durationMs: Int) {
        (self.jobId, self.durationMs) = (jobId, durationMs)
    }
}

/// Optional object value serialized in the `UBUNTU` wire field.
public struct WorkflowRunUsageBillableUBUNTU: Codable {
    /// Required `integer` value serialized in the `total_ms` wire field.
    public var totalMs: Int
    /// Required `integer` value serialized in the `jobs` wire field.
    public var jobs: Int
    /// Optional array value serialized in the `job_runs` wire field.
    public var jobRuns: [WorkflowRunUsageBillableUBUNTUJobRunsItem]?

    enum CodingKeys: String, CodingKey {
        case totalMs = "total_ms"
        case jobs
        case jobRuns = "job_runs"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}
