// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// Actions domain models
public typealias ShaPinningRequired = Bool

/// An artifact
public struct Artifact: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    /// - Example: 5
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    /// - Example: "MDEwOkNoZWNrU3VpdGU1"
    public var nodeId: String
    /// The name of the artifact.
    /// - Example: "AdventureWorks.Framework"
    public var name: String
    /// The size in bytes of the artifact.
    /// - Example: 12345
    public var sizeInBytes: Int
    /// Required `string` value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/artifacts/5"
    public var url: String
    /// Required `string` value serialized in the `archive_download_url` wire field.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/artifacts/5/zip"
    public var archiveDownloadUrl: String
    /// Whether or not the artifact has expired.
    public var expired: Bool
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    public var createdAt: Date?
    /// Required `date-time`-formatted value serialized in the `expires_at` wire field.
    public var expiresAt: Date?
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    public var updatedAt: Date?
    /// The SHA256 digest of the artifact. This field will only be populated on artifacts uploaded with
    /// upload-artifact v4 or newer. For older versions, this field will be null.
    /// - Example: "sha256:cfc3236bdad15b5898bca8408945c9e19e1917da8704adc20eaa618444290a8c"
    public var digest: String?
    /// Optional object value serialized in the `workflow_run` wire field.
    public var workflowRun: ArtifactWorkflowRun?

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case name
        case sizeInBytes = "size_in_bytes"
        case url
        case archiveDownloadUrl = "archive_download_url"
        case expired
        case createdAt = "created_at"
        case expiresAt = "expires_at"
        case updatedAt = "updated_at"
        case digest
        case workflowRun = "workflow_run"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Artifact {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        name = try container.sdkDecodeRequired(.name)
        sizeInBytes = try container.sdkDecodeRequired(.sizeInBytes)
        url = try container.sdkDecodeRequired(.url)
        archiveDownloadUrl = try container.sdkDecodeRequired(.archiveDownloadUrl)
        expired = try container.sdkDecodeRequired(.expired)
        createdAt = try container.sdkDecodeIfPresent(.createdAt)
        expiresAt = try container.sdkDecodeIfPresent(.expiresAt)
        updatedAt = try container.sdkDecodeIfPresent(.updatedAt)
        digest = try container.sdkDecodeIfPresent(.digest)
        workflowRun = try container.sdkDecodeIfPresent(.workflowRun)
        if let value = createdAt {
            try sdkValidateDateTime("created_at", sdkWireString(value))
        }
        if let value = expiresAt {
            try sdkValidateDateTime("expires_at", sdkWireString(value))
        }
        if let value = updatedAt {
            try sdkValidateDateTime("updated_at", sdkWireString(value))
        }
    }
}

public extension Artifact {
    init(
        id: Int,
        nodeId: String,
        name: String,
        sizeInBytes: Int,
        url: String,
        archiveDownloadUrl: String,
        expired: Bool,
        createdAt: Date?,
        expiresAt: Date?,
        updatedAt: Date?,
        digest: String? = nil,
        workflowRun: ArtifactWorkflowRun? = nil
    ) throws {
        (self.id, self.nodeId) = (id, nodeId)
        (self.name, self.sizeInBytes) = (name, sizeInBytes)
        (self.url, self.archiveDownloadUrl) = (url, archiveDownloadUrl)
        (self.expired, self.createdAt) = (expired, createdAt)
        (self.expiresAt, self.updatedAt) = (expiresAt, updatedAt)
        (self.digest, self.workflowRun) = (digest, workflowRun)
        if let value = self.createdAt {
            try sdkValidateDateTime("created_at", sdkWireString(value))
        }
        if let value = self.expiresAt {
            try sdkValidateDateTime("expires_at", sdkWireString(value))
        }
        if let value = self.updatedAt {
            try sdkValidateDateTime("updated_at", sdkWireString(value))
        }
    }
}

/// Optional object value serialized in the `workflow_run` wire field.
public struct ArtifactWorkflowRun: Codable {
    /// Optional `integer` value serialized in the `id` wire field.
    /// - Example: 10
    public var id: Int?
    /// Optional `integer` value serialized in the `repository_id` wire field.
    /// - Example: 42
    public var repositoryId: Int?
    /// Optional `integer` value serialized in the `head_repository_id` wire field.
    /// - Example: 42
    public var headRepositoryId: Int?
    /// Optional `string` value serialized in the `head_branch` wire field.
    /// - Example: "main"
    public var headBranch: String?
    /// Optional `string` value serialized in the `head_sha` wire field.
    /// - Example: "009b8a3a9ccbb128af87f9b1c0f4c62e8a304f6d"
    public var headSha: String?

    enum CodingKeys: String, CodingKey {
        case id
        case repositoryId = "repository_id"
        case headRepositoryId = "head_repository_id"
        case headBranch = "head_branch"
        case headSha = "head_sha"
    }

    init() {
        (id, repositoryId, headRepositoryId, headBranch, headSha) = (nil, nil, nil, nil, nil)
    }
}

public extension ArtifactWorkflowRun {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeIfPresent(.id)
        repositoryId = try container.sdkDecodeIfPresent(.repositoryId)
        headRepositoryId = try container.sdkDecodeIfPresent(.headRepositoryId)
        headBranch = try container.sdkDecodeIfPresent(.headBranch)
        headSha = try container.sdkDecodeIfPresent(.headSha)
    }
}

public extension ArtifactWorkflowRun {
    init(
        id: Int? = nil,
        repositoryId: Int? = nil,
        headRepositoryId: Int? = nil,
        headBranch: String? = nil,
        headSha: String? = nil
    ) {
        self.init()
        (self.id, self.repositoryId) = (id, repositoryId)
        (self.headRepositoryId, self.headBranch) = (headRepositoryId, headBranch)
        self.headSha = headSha
    }
}

/// Authentication Token
public struct AuthenticationToken: Codable {
    /// The token used for authentication
    /// - Example: "v1.1f699f1069f60xxx"
    public var token: String
    /// The time this token expires
    /// - Example: "2016-07-11T22:14:10Z"
    public var expiresAt: Date
    /// Optional object value serialized in the `permissions` wire field.
    /// - Example: {"issues":"read","deployments":"write"}
    public var permissions: [String: JSONValue]?
    /// The repositories this token has access to
    public var repositories: [Repository]?
    /// Optional `string` value serialized in the `single_file` wire field.
    /// - Example: "config.yaml"
    public var singleFile: String?
    /// Describe whether all repositories have been selected or there's a selection involved
    public var repositorySelection: AuthenticationTokenRepositorySelection?

    enum CodingKeys: String, CodingKey {
        case token
        case expiresAt = "expires_at"
        case permissions
        case repositories
        case singleFile = "single_file"
        case repositorySelection = "repository_selection"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension AuthenticationToken {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.token) else {
            throw SdkValidationError(
                field: "token",
                code: "required",
                message: "Validation failed for 'token': value is required"
            )
        }
        guard container.contains(.expiresAt) else {
            throw SdkValidationError(
                field: "expires_at",
                code: "required",
                message: "Validation failed for 'expires_at': value is required"
            )
        }
        token = try container.sdkDecodeRequired(.token)
        expiresAt = try container.sdkDecodeRequired(.expiresAt)
        permissions = try container.sdkDecodeIfPresent(.permissions)
        repositories = try container.sdkDecodeIfPresent(.repositories)
        singleFile = try container.sdkDecodeIfPresent(.singleFile)
        repositorySelection = try container.sdkDecodeIfPresent(.repositorySelection)
        try sdkValidateDateTime("expires_at", sdkWireString(expiresAt))
    }
}

public extension AuthenticationToken {
    init(
        token: String,
        expiresAt: Date,
        permissions: [String: JSONValue]? = nil,
        repositories: [Repository]? = nil,
        singleFile: String? = nil,
        repositorySelection: AuthenticationTokenRepositorySelection? = nil
    ) throws {
        (self.token, self.expiresAt) = (token, expiresAt)
        (self.permissions, self.repositories) = (permissions, repositories)
        (self.singleFile, self.repositorySelection) = (singleFile, repositorySelection)
        try sdkValidateDateTime("expires_at", sdkWireString(self.expiresAt))
    }
}

/// Optional object value serialized in the `permissions` wire field.
public struct AuthenticationTokenPermissions: Codable {
    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension AuthenticationTokenPermissions {
    init() {}
}

/// An entry in the reviews log for environment deployments
public struct EnvironmentApprovals: Codable {
    /// The list of environments that were approved or rejected
    public var environments: [EnvironmentApprovalsEnvironmentsItem]
    /// Whether deployment to the environment(s) was approved or rejected or pending (with comments)
    /// - Example: "approved"
    public var state: EnvironmentApprovalsState
    /// A GitHub user.
    public var user: SimpleUser
    /// The comment submitted with the deployment review
    /// - Example: "Ship it!"
    public var comment: String

    enum CodingKeys: String, CodingKey {
        case environments
        case state
        case user
        case comment
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension EnvironmentApprovals {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.environments) else {
            throw SdkValidationError(
                field: "environments",
                code: "required",
                message: "Validation failed for 'environments': value is required"
            )
        }
        guard container.contains(.state) else {
            throw SdkValidationError(
                field: "state",
                code: "required",
                message: "Validation failed for 'state': value is required"
            )
        }
        guard container.contains(.user) else {
            throw SdkValidationError(
                field: "user",
                code: "required",
                message: "Validation failed for 'user': value is required"
            )
        }
        guard container.contains(.comment) else {
            throw SdkValidationError(
                field: "comment",
                code: "required",
                message: "Validation failed for 'comment': value is required"
            )
        }
        environments = try container.sdkDecodeRequired(.environments)
        state = try container.sdkDecodeRequired(.state)
        user = try container.sdkDecodeRequired(.user)
        comment = try container.sdkDecodeRequired(.comment)
    }
}

public extension EnvironmentApprovals {
    init(
        environments: [EnvironmentApprovalsEnvironmentsItem],
        state: EnvironmentApprovalsState,
        user: SimpleUser,
        comment: String
    ) {
        (self.environments, self.state) = (environments, state)
        (self.user, self.comment) = (user, comment)
    }
}

/// Required object value serialized in the `environments[]` wire field.
public struct EnvironmentApprovalsEnvironmentsItem: Codable {
    /// The id of the environment.
    /// - Example: 56780428
    public var id: Int?
    /// Optional `string` value serialized in the `node_id` wire field.
    /// - Example: "MDExOkVudmlyb25tZW50NTY3ODA0Mjg="
    public var nodeId: String?
    /// The name of the environment.
    /// - Example: "staging"
    public var name: String?
    /// Optional `string` value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/repos/github/hello-world/environments/staging"
    public var url: String?
    /// Optional `string` value serialized in the `html_url` wire field.
    /// - Example: "https://github.com/github/hello-world/deployments/activity_log?environments_filter=staging"
    public var htmlUrl: String?
    /// The time that the environment was created, in ISO 8601 format.
    /// - Example: "2020-11-23T22:00:40Z"
    public var createdAt: Date?
    /// The time that the environment was last updated, in ISO 8601 format.
    /// - Example: "2020-11-23T22:00:40Z"
    public var updatedAt: Date?

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case name
        case url
        case htmlUrl = "html_url"
        case createdAt = "created_at"
        case updatedAt = "updated_at"
    }

    init() {
        (id, nodeId, name, url, htmlUrl) = (nil, nil, nil, nil, nil)
        (createdAt, updatedAt) = (nil, nil)
    }
}

public extension EnvironmentApprovalsEnvironmentsItem {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeIfPresent(.id)
        nodeId = try container.sdkDecodeIfPresent(.nodeId)
        name = try container.sdkDecodeIfPresent(.name)
        url = try container.sdkDecodeIfPresent(.url)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        createdAt = try container.sdkDecodeIfPresent(.createdAt)
        updatedAt = try container.sdkDecodeIfPresent(.updatedAt)
        if let value = createdAt {
            try sdkValidateDateTime("created_at", sdkWireString(value))
        }
        if let value = updatedAt {
            try sdkValidateDateTime("updated_at", sdkWireString(value))
        }
    }
}

public extension EnvironmentApprovalsEnvironmentsItem {
    init(
        id: Int? = nil,
        nodeId: String? = nil,
        name: String? = nil,
        url: String? = nil,
        htmlUrl: String? = nil,
        createdAt: Date? = nil,
        updatedAt: Date? = nil
    ) throws {
        self.init()
        (self.id, self.nodeId) = (id, nodeId)
        (self.name, self.url) = (name, url)
        (self.htmlUrl, self.createdAt) = (htmlUrl, createdAt)
        self.updatedAt = updatedAt
        if let value = self.createdAt {
            try sdkValidateDateTime("created_at", sdkWireString(value))
        }
        if let value = self.updatedAt {
            try sdkValidateDateTime("updated_at", sdkWireString(value))
        }
    }
}

/// Information of a job execution in a workflow run
public struct Job: Codable {
    /// The id of the job.
    /// - Example: 21
    public var id: Int
    /// The id of the associated workflow run.
    /// - Example: 5
    public var runId: Int
    /// Required `string` value serialized in the `run_url` wire field.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/runs/5"
    public var runUrl: String
    /// Required `string` value serialized in the `node_id` wire field.
    /// - Example: "MDg6Q2hlY2tSdW40"
    public var nodeId: String
    /// The SHA of the commit that is being run.
    /// - Example: "009b8a3a9ccbb128af87f9b1c0f4c62e8a304f6d"
    public var headSha: String
    /// Required `string` value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/repos/github/hello-world/actions/jobs/21"
    public var url: String
    /// Required `string` value serialized in the `html_url` wire field.
    /// - Example: "https://github.com/github/hello-world/runs/4"
    public var htmlUrl: String?
    /// The phase of the lifecycle that the job is currently in.
    /// - Example: "queued"
    public var status: JobStatus
    /// The outcome of the job.
    /// - Example: "success"
    public var conclusion: JobConclusion?
    /// The time that the job created, in ISO 8601 format.
    /// - Example: "2019-08-08T08:00:00-07:00"
    public var createdAt: Date
    /// The time that the job started, in ISO 8601 format.
    /// - Example: "2019-08-08T08:00:00-07:00"
    public var startedAt: Date
    /// The time that the job finished, in ISO 8601 format.
    /// - Example: "2019-08-08T08:00:00-07:00"
    public var completedAt: Date?
    /// The name of the job.
    /// - Example: "test-coverage"
    public var name: String
    /// Required `string` value serialized in the `check_run_url` wire field.
    /// - Example: "https://api.github.com/repos/github/hello-world/check-runs/4"
    public var checkRunUrl: String
    /// Labels for the workflow job. Specified by the "runs_on" attribute in the action's workflow file.
    /// - Example: ["self-hosted","foo","bar"]
    public var labels: [String]
    /// The ID of the runner to which this job has been assigned. (If a runner hasn't yet been assigned, this will
    /// be null.)
    /// - Example: 1
    public var runnerId: Int?
    /// The name of the runner to which this job has been assigned. (If a runner hasn't yet been assigned, this will
    /// be null.)
    /// - Example: "my runner"
    public var runnerName: String?
    /// The ID of the runner group to which this job has been assigned. (If a runner hasn't yet been assigned, this
    /// will be null.)
    /// - Example: 2
    public var runnerGroupId: Int?
    /// The name of the runner group to which this job has been assigned. (If a runner hasn't yet been assigned,
    /// this will be null.)
    /// - Example: "my runner group"
    public var runnerGroupName: String?
    /// The name of the workflow.
    /// - Example: "Build"
    public var workflowName: String?
    /// The name of the current branch.
    /// - Example: "main"
    public var headBranch: String?
    /// Attempt number of the associated workflow run, 1 for first attempt and higher if the workflow was re-run.
    /// - Example: 1
    public var runAttempt: Int?
    /// Steps in this job.
    public var steps: [JobStepsItem]?

    enum CodingKeys: String, CodingKey {
        case id
        case runId = "run_id"
        case runUrl = "run_url"
        case nodeId = "node_id"
        case headSha = "head_sha"
        case url
        case htmlUrl = "html_url"
        case status
        case conclusion
        case createdAt = "created_at"
        case startedAt = "started_at"
        case completedAt = "completed_at"
        case name
        case checkRunUrl = "check_run_url"
        case labels
        case runnerId = "runner_id"
        case runnerName = "runner_name"
        case runnerGroupId = "runner_group_id"
        case runnerGroupName = "runner_group_name"
        case workflowName = "workflow_name"
        case headBranch = "head_branch"
        case runAttempt = "run_attempt"
        case steps
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Job {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        runId = try container.sdkDecodeRequired(.runId)
        runUrl = try container.sdkDecodeRequired(.runUrl)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        headSha = try container.sdkDecodeRequired(.headSha)
        url = try container.sdkDecodeRequired(.url)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        status = try container.sdkDecodeRequired(.status)
        conclusion = try container.sdkDecodeIfPresent(.conclusion)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        startedAt = try container.sdkDecodeRequired(.startedAt)
        completedAt = try container.sdkDecodeIfPresent(.completedAt)
        name = try container.sdkDecodeRequired(.name)
        checkRunUrl = try container.sdkDecodeRequired(.checkRunUrl)
        labels = try container.sdkDecodeRequired(.labels)
        runnerId = try container.sdkDecodeIfPresent(.runnerId)
        runnerName = try container.sdkDecodeIfPresent(.runnerName)
        runnerGroupId = try container.sdkDecodeIfPresent(.runnerGroupId)
        runnerGroupName = try container.sdkDecodeIfPresent(.runnerGroupName)
        workflowName = try container.sdkDecodeIfPresent(.workflowName)
        headBranch = try container.sdkDecodeIfPresent(.headBranch)
        runAttempt = try container.sdkDecodeIfPresent(.runAttempt)
        steps = try container.sdkDecodeIfPresent(.steps)
        try sdkValidateDateTime("created_at", sdkWireString(createdAt))
        try sdkValidateDateTime("started_at", sdkWireString(startedAt))
        if let value = completedAt {
            try sdkValidateDateTime("completed_at", sdkWireString(value))
        }
    }
}

public extension Job {
    init(
        id: Int,
        runId: Int,
        runUrl: String,
        nodeId: String,
        headSha: String,
        url: String,
        htmlUrl: String?,
        status: JobStatus,
        conclusion: JobConclusion?,
        createdAt: Date,
        startedAt: Date,
        completedAt: Date?,
        name: String,
        checkRunUrl: String,
        labels: [String],
        runnerId: Int?,
        runnerName: String?,
        runnerGroupId: Int?,
        runnerGroupName: String?,
        workflowName: String?,
        headBranch: String?,
        runAttempt: Int? = nil,
        steps: [JobStepsItem]? = nil
    ) throws {
        (self.id, self.runId) = (id, runId)
        (self.runUrl, self.nodeId) = (runUrl, nodeId)
        (self.headSha, self.url) = (headSha, url)
        (self.htmlUrl, self.status) = (htmlUrl, status)
        (self.conclusion, self.createdAt) = (conclusion, createdAt)
        (self.startedAt, self.completedAt) = (startedAt, completedAt)
        (self.name, self.checkRunUrl) = (name, checkRunUrl)
        (self.labels, self.runnerId) = (labels, runnerId)
        (self.runnerName, self.runnerGroupId) = (runnerName, runnerGroupId)
        (self.runnerGroupName, self.workflowName) = (runnerGroupName, workflowName)
        (self.headBranch, self.runAttempt) = (headBranch, runAttempt)
        self.steps = steps
        try sdkValidateDateTime("created_at", sdkWireString(self.createdAt))
        try sdkValidateDateTime("started_at", sdkWireString(self.startedAt))
        if let value = self.completedAt {
            try sdkValidateDateTime("completed_at", sdkWireString(value))
        }
    }
}
