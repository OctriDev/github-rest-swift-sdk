// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// ActionsRunner domain models
/// A self hosted runner
public struct Runner: Codable {
    /// The ID of the runner.
    /// - Example: 5
    public var id: Int
    /// The name of the runner.
    /// - Example: "iMac"
    public var name: String
    /// The Operating System of the runner.
    /// - Example: "macos"
    public var os: String
    /// The status of the runner.
    /// - Example: "online"
    public var status: String
    /// Required `boolean` value serialized in the `busy` wire field.
    public var busy: Bool
    /// Required array value serialized in the `labels` wire field.
    public var labels: [RunnerLabel]
    /// The ID of the runner group.
    /// - Example: 1
    public var runnerGroupId: Int?
    /// Optional `boolean` value serialized in the `ephemeral` wire field.
    public var ephemeral: Bool?
    /// The version of the GitHub Actions Runner software. This is only set if the runner has connected to the
    /// service at least once.
    /// - Example: "2.323.0"
    public var version: String?

    enum CodingKeys: String, CodingKey {
        case id
        case name
        case os
        case status
        case busy
        case labels
        case runnerGroupId = "runner_group_id"
        case ephemeral
        case version
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension Runner {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.os) else {
            throw SdkValidationError(
                field: "os",
                code: "required",
                message: "Validation failed for 'os': value is required"
            )
        }
        guard container.contains(.status) else {
            throw SdkValidationError(
                field: "status",
                code: "required",
                message: "Validation failed for 'status': value is required"
            )
        }
        guard container.contains(.busy) else {
            throw SdkValidationError(
                field: "busy",
                code: "required",
                message: "Validation failed for 'busy': value is required"
            )
        }
        guard container.contains(.labels) else {
            throw SdkValidationError(
                field: "labels",
                code: "required",
                message: "Validation failed for 'labels': value is required"
            )
        }
        id = try container.sdkDecodeRequired(.id)
        name = try container.sdkDecodeRequired(.name)
        os = try container.sdkDecodeRequired(.os)
        status = try container.sdkDecodeRequired(.status)
        busy = try container.sdkDecodeRequired(.busy)
        labels = try container.sdkDecodeRequired(.labels)
        runnerGroupId = try container.sdkDecodeIfPresent(.runnerGroupId)
        ephemeral = try container.sdkDecodeIfPresent(.ephemeral)
        version = try container.sdkDecodeIfPresent(.version)
    }
}

public extension Runner {
    init(
        id: Int,
        name: String,
        os: String,
        status: String,
        busy: Bool,
        labels: [RunnerLabel],
        runnerGroupId: Int? = nil,
        ephemeral: Bool? = nil,
        version: String? = nil
    ) {
        (self.id, self.name) = (id, name)
        (self.os, self.status) = (os, status)
        (self.busy, self.labels) = (busy, labels)
        (self.runnerGroupId, self.ephemeral) = (runnerGroupId, ephemeral)
        self.version = version
    }
}

/// Runner Application
public struct RunnerApplication: Codable {
    /// Required `string` value serialized in the `os` wire field.
    public var os: String
    /// Required `string` value serialized in the `architecture` wire field.
    public var architecture: String
    /// Required `string` value serialized in the `download_url` wire field.
    public var downloadUrl: String
    /// Required `string` value serialized in the `filename` wire field.
    public var filename: String
    /// A short lived bearer token used to download the runner, if needed.
    public var tempDownloadToken: String?
    /// Optional `string` value serialized in the `sha256_checksum` wire field.
    public var sha256Checksum: String?

    enum CodingKeys: String, CodingKey {
        case os
        case architecture
        case downloadUrl = "download_url"
        case filename
        case tempDownloadToken = "temp_download_token"
        case sha256Checksum = "sha256_checksum"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension RunnerApplication {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.os) else {
            throw SdkValidationError(
                field: "os",
                code: "required",
                message: "Validation failed for 'os': value is required"
            )
        }
        guard container.contains(.architecture) else {
            throw SdkValidationError(
                field: "architecture",
                code: "required",
                message: "Validation failed for 'architecture': value is required"
            )
        }
        guard container.contains(.downloadUrl) else {
            throw SdkValidationError(
                field: "download_url",
                code: "required",
                message: "Validation failed for 'download_url': value is required"
            )
        }
        guard container.contains(.filename) else {
            throw SdkValidationError(
                field: "filename",
                code: "required",
                message: "Validation failed for 'filename': value is required"
            )
        }
        os = try container.sdkDecodeRequired(.os)
        architecture = try container.sdkDecodeRequired(.architecture)
        downloadUrl = try container.sdkDecodeRequired(.downloadUrl)
        filename = try container.sdkDecodeRequired(.filename)
        tempDownloadToken = try container.sdkDecodeIfPresent(.tempDownloadToken)
        sha256Checksum = try container.sdkDecodeIfPresent(.sha256Checksum)
    }
}

public extension RunnerApplication {
    init(
        os: String,
        architecture: String,
        downloadUrl: String,
        filename: String,
        tempDownloadToken: String? = nil,
        sha256Checksum: String? = nil
    ) {
        (self.os, self.architecture) = (os, architecture)
        (self.downloadUrl, self.filename) = (downloadUrl, filename)
        (self.tempDownloadToken, self.sha256Checksum) = (tempDownloadToken, sha256Checksum)
    }
}

/// Typed representation of the `RunnerGroupsOrg` API schema.
public struct RunnerGroupsOrg: Codable {
    /// Required `number` value serialized in the `id` wire field.
    public var id: Double
    /// Required `string` value serialized in the `name` wire field.
    public var name: String
    /// Required `string` value serialized in the `visibility` wire field.
    public var visibility: String
    /// Required `boolean` value serialized in the `default` wire field.
    public var `default`: Bool
    /// Required `string` value serialized in the `runners_url` wire field.
    public var runnersUrl: String
    /// Required `boolean` value serialized in the `inherited` wire field.
    public var inherited: Bool
    /// Required `boolean` value serialized in the `allows_public_repositories` wire field.
    public var allowsPublicRepositories: Bool
    /// Link to the selected repositories resource for this runner group. Not present unless visibility was set to
    /// `selected`
    public var selectedRepositoriesUrl: String?
    /// Optional `string` value serialized in the `hosted_runners_url` wire field.
    public var hostedRunnersUrl: String?
    /// The identifier of a hosted compute network configuration.
    public var networkConfigurationId: String?
    /// Optional `boolean` value serialized in the `inherited_allows_public_repositories` wire field.
    public var inheritedAllowsPublicRepositories: Bool?
    /// If `true`, the `restricted_to_workflows` and `selected_workflows` fields cannot be modified.
    public var workflowRestrictionsReadOnly: Bool?
    /// If `true`, the runner group will be restricted to running only the workflows specified in the
    /// `selected_workflows` array.
    public var restrictedToWorkflows: Bool?
    /// List of workflows the runner group should be allowed to run. This setting will be ignored unless
    /// `restricted_to_workflows` is set to `true`.
    public var selectedWorkflows: [String]?

    enum CodingKeys: String, CodingKey {
        case id
        case name
        case visibility
        case `default`
        case runnersUrl = "runners_url"
        case inherited
        case allowsPublicRepositories = "allows_public_repositories"
        case selectedRepositoriesUrl = "selected_repositories_url"
        case hostedRunnersUrl = "hosted_runners_url"
        case networkConfigurationId = "network_configuration_id"
        case inheritedAllowsPublicRepositories = "inherited_allows_public_repositories"
        case workflowRestrictionsReadOnly = "workflow_restrictions_read_only"
        case restrictedToWorkflows = "restricted_to_workflows"
        case selectedWorkflows = "selected_workflows"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension RunnerGroupsOrg {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.id) else {
            throw SdkValidationError(
                field: "id",
                code: "required",
                message: "Validation failed for 'id': value is required"
            )
        }
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        guard container.contains(.visibility) else {
            throw SdkValidationError(
                field: "visibility",
                code: "required",
                message: "Validation failed for 'visibility': value is required"
            )
        }
        guard container.contains(.default) else {
            throw SdkValidationError(
                field: "default",
                code: "required",
                message: "Validation failed for 'default': value is required"
            )
        }
        guard container.contains(.runnersUrl) else {
            throw SdkValidationError(
                field: "runners_url",
                code: "required",
                message: "Validation failed for 'runners_url': value is required"
            )
        }
        guard container.contains(.inherited) else {
            throw SdkValidationError(
                field: "inherited",
                code: "required",
                message: "Validation failed for 'inherited': value is required"
            )
        }
        guard container.contains(.allowsPublicRepositories) else {
            throw SdkValidationError(
                field: "allows_public_repositories",
                code: "required",
                message: "Validation failed for 'allows_public_repositories': value is required"
            )
        }
        id = try container.sdkDecodeRequired(.id)
        name = try container.sdkDecodeRequired(.name)
        visibility = try container.sdkDecodeRequired(.visibility)
        self.default = try container.sdkDecodeRequired(.default)
        runnersUrl = try container.sdkDecodeRequired(.runnersUrl)
        inherited = try container.sdkDecodeRequired(.inherited)
        allowsPublicRepositories = try container.sdkDecodeRequired(.allowsPublicRepositories)
        selectedRepositoriesUrl = try container.sdkDecodeIfPresent(.selectedRepositoriesUrl)
        hostedRunnersUrl = try container.sdkDecodeIfPresent(.hostedRunnersUrl)
        networkConfigurationId = try container.sdkDecodeIfPresent(.networkConfigurationId)
        inheritedAllowsPublicRepositories = try container.sdkDecodeIfPresent(.inheritedAllowsPublicRepositories)
        workflowRestrictionsReadOnly = try container.sdkDecodeIfPresent(.workflowRestrictionsReadOnly)
        restrictedToWorkflows = try container.sdkDecodeIfPresent(.restrictedToWorkflows)
        selectedWorkflows = try container.sdkDecodeIfPresent(.selectedWorkflows)
    }
}

public extension RunnerGroupsOrg {
    init(
        id: Double,
        name: String,
        visibility: String,
        default: Bool,
        runnersUrl: String,
        inherited: Bool,
        allowsPublicRepositories: Bool,
        selectedRepositoriesUrl: String? = nil,
        hostedRunnersUrl: String? = nil,
        networkConfigurationId: String? = nil,
        inheritedAllowsPublicRepositories: Bool? = nil,
        workflowRestrictionsReadOnly: Bool? = nil,
        restrictedToWorkflows: Bool? = nil,
        selectedWorkflows: [String]? = nil
    ) {
        (self.id, self.name) = (id, name)
        (self.visibility, self.default) = (visibility, `default`)
        (self.runnersUrl, self.inherited) = (runnersUrl, inherited)
        self.allowsPublicRepositories = allowsPublicRepositories
        (self.selectedRepositoriesUrl, self.hostedRunnersUrl) = (selectedRepositoriesUrl, hostedRunnersUrl)
        self.networkConfigurationId = networkConfigurationId
        self.inheritedAllowsPublicRepositories = inheritedAllowsPublicRepositories
        self.workflowRestrictionsReadOnly = workflowRestrictionsReadOnly
        (self.restrictedToWorkflows, self.selectedWorkflows) = (restrictedToWorkflows, selectedWorkflows)
    }
}

/// A label for a self hosted runner
public struct RunnerLabel: Codable {
    /// Name of the label.
    public var name: String
    /// Unique identifier of the label.
    public var id: Int?
    /// The type of label. Read-only labels are applied automatically when the runner is configured.
    public var type: RunnerLabelType?

    enum CodingKeys: String, CodingKey {
        case name
        case id
        case type
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension RunnerLabel {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.name) else {
            throw SdkValidationError(
                field: "name",
                code: "required",
                message: "Validation failed for 'name': value is required"
            )
        }
        name = try container.sdkDecodeRequired(.name)
        id = try container.sdkDecodeIfPresent(.id)
        type = try container.sdkDecodeIfPresent(.type)
    }
}

public extension RunnerLabel {
    init(name: String, id: Int? = nil, type: RunnerLabelType? = nil) {
        (self.name, self.id) = (name, id)
        self.type = type
    }
}

/// The type of label. Read-only labels are applied automatically when the runner is configured.
public struct RunnerLabelType: RawRepresentable, Hashable, Codable, Sendable, SdkWireConvertible {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    public static let readOnly = RunnerLabelType(rawValue: "read-only")
    public static let custom = RunnerLabelType(rawValue: "custom")

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        try self.init(rawValue: container.decode(String.self))
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
