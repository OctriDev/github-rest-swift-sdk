// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// SecretScanning domain models
/// A custom pattern to delete in a bulk operation.
public struct SecretScanningCustomPatternToDelete: Codable {
    /// The ID of the custom pattern to delete.
    public var patternId: Int
    /// The version of the entity. This is used to confirm you're updating the current version of the entity and
    /// mitigate unintentionally overriding someone else's update.
    public var customPatternVersion: SecretScanningRowVersion?

    enum CodingKeys: String, CodingKey {
        case patternId = "pattern_id"
        case customPatternVersion = "custom_pattern_version"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningCustomPatternToDelete {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.patternId) else {
            throw SdkValidationError(
                field: "pattern_id",
                code: "required",
                message: "Validation failed for 'pattern_id': value is required"
            )
        }
        patternId = try container.sdkDecodeRequired(.patternId)
        customPatternVersion = try container.sdkDecodeIfPresent(.customPatternVersion)
    }
}

public extension SecretScanningCustomPatternToDelete {
    init(patternId: Int, customPatternVersion: SecretScanningRowVersion? = nil) {
        (self.patternId, self.customPatternVersion) = (patternId, customPatternVersion)
    }
}

/// A validation error for a custom pattern in a batch operation.
public struct SecretScanningCustomPatternValidationError: Codable {
    /// A machine-readable code describing the error.
    public var code: SecretScanningCustomPatternValidationErrorCode?
    /// A human-readable description of the error.
    public var message: String?

    enum CodingKeys: String, CodingKey {
        case code
        case message
    }

    init() {
        (code, message) = (nil, nil)
    }
}

public extension SecretScanningCustomPatternValidationError {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        code = try container.sdkDecodeIfPresent(.code)
        message = try container.sdkDecodeIfPresent(.message)
    }
}

public extension SecretScanningCustomPatternValidationError {
    init(code: SecretScanningCustomPatternValidationErrorCode? = nil, message: String? = nil) {
        self.init()
        (self.code, self.message) = (code, message)
    }
}

/// Typed representation of the `SecretScanningLocation` API schema.
public struct SecretScanningLocation: Codable {
    /// The location type. Because secrets may be found in different types of resources (ie. code, comments, issues,
    /// pull requests, discussions), this field identifies the type of resource where the secret was found.
    /// - Example: "commit"
    public var type: SecretScanningLocationType?
    /// Optional API value serialized in the `details` wire field.
    public var details: SecretScanningLocationDetails?

    enum CodingKeys: String, CodingKey {
        case type
        case details
    }

    init() {
        (type, details) = (nil, nil)
    }
}

public extension SecretScanningLocation {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        type = try container.sdkDecodeIfPresent(.type)
        details = try container.sdkDecodeIfPresent(.details)
    }
}

public extension SecretScanningLocation {
    init(type: SecretScanningLocationType? = nil, details: SecretScanningLocationDetails? = nil) {
        self.init()
        (self.type, self.details) = (type, details)
    }
}

public enum SecretScanningLocationDetails {
    case secretScanningLocationCommit(SecretScanningLocationCommit)
    case secretScanningLocationWikiCommit(SecretScanningLocationWikiCommit)
    case secretScanningLocationIssueTitle(SecretScanningLocationIssueTitle)
    case secretScanningLocationIssueBody(SecretScanningLocationIssueBody)
    case secretScanningLocationIssueComment(SecretScanningLocationIssueComment)
    case secretScanningLocationDiscussionTitle(SecretScanningLocationDiscussionTitle)
    case secretScanningLocationDiscussionBody(SecretScanningLocationDiscussionBody)
    case secretScanningLocationDiscussionComment(SecretScanningLocationDiscussionComment)
    case secretScanningLocationPullRequestTitle(SecretScanningLocationPullRequestTitle)
    case secretScanningLocationPullRequestBody(SecretScanningLocationPullRequestBody)
    case secretScanningLocationPullRequestComment(SecretScanningLocationPullRequestComment)
    case secretScanningLocationPullRequestReview(SecretScanningLocationPullRequestReview)
    case secretScanningLocationPullRequestReviewComment(SecretScanningLocationPullRequestReviewComment)
}

extension SecretScanningLocationDetails: Codable {
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = Self.decodeGroup1(from: container) {
            self = value; return
        }
        if let value = Self.decodeGroup2(from: container) {
            self = value; return
        }
        throw DecodingError.dataCorruptedError(
            in: container,
            debugDescription: "No variant matched for SecretScanningLocationDetails"
        )
    }

    private static func decodeGroup1(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container
            .decode(SecretScanningLocationCommit.self) {
            return .secretScanningLocationCommit(value)
        }
        if let value = try? container
            .decode(SecretScanningLocationWikiCommit.self) {
            return .secretScanningLocationWikiCommit(value)
        }
        if let value = try? container
            .decode(SecretScanningLocationIssueTitle.self) {
            return .secretScanningLocationIssueTitle(value)
        }
        if let value = try? container
            .decode(SecretScanningLocationIssueBody.self) {
            return .secretScanningLocationIssueBody(value)
        }
        if let value = try? container
            .decode(SecretScanningLocationIssueComment.self) {
            return .secretScanningLocationIssueComment(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationDiscussionTitle.self
        ) {
            return .secretScanningLocationDiscussionTitle(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationDiscussionBody.self
        ) {
            return .secretScanningLocationDiscussionBody(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationDiscussionComment.self
        ) {
            return .secretScanningLocationDiscussionComment(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationPullRequestTitle.self
        ) {
            return .secretScanningLocationPullRequestTitle(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationPullRequestBody.self
        ) {
            return .secretScanningLocationPullRequestBody(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationPullRequestComment.self
        ) {
            return .secretScanningLocationPullRequestComment(value)
        }
        if let value = try? container.decode(
            SecretScanningLocationPullRequestReview.self
        ) {
            return .secretScanningLocationPullRequestReview(value)
        }
        return nil
    }

    private static func decodeGroup2(from container: SingleValueDecodingContainer) -> Self? {
        if let value = try? container.decode(
            SecretScanningLocationPullRequestReviewComment.self
        ) {
            return .secretScanningLocationPullRequestReviewComment(value)
        }
        return nil
    }

    public func encode(to encoder: Encoder) throws {
        if try encodeGroup1(to: encoder) {
            return
        }
        if try encodeGroup2(to: encoder) {
            return
        }
    }

    private func encodeGroup1(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .secretScanningLocationCommit(value): try container.encode(value); return true
        case let .secretScanningLocationWikiCommit(value): try container.encode(value); return true
        case let .secretScanningLocationIssueTitle(value): try container.encode(value); return true
        case let .secretScanningLocationIssueBody(value): try container.encode(value); return true
        case let .secretScanningLocationIssueComment(value): try container.encode(value); return true
        case let .secretScanningLocationDiscussionTitle(value): try container.encode(value); return true
        case let .secretScanningLocationDiscussionBody(value): try container.encode(value); return true
        case let .secretScanningLocationDiscussionComment(value): try container.encode(value); return true
        case let .secretScanningLocationPullRequestTitle(value): try container.encode(value); return true
        case let .secretScanningLocationPullRequestBody(value): try container.encode(value); return true
        case let .secretScanningLocationPullRequestComment(value): try container.encode(value); return true
        case let .secretScanningLocationPullRequestReview(value): try container.encode(value); return true
        default: return false
        }
    }

    private func encodeGroup2(to encoder: Encoder) throws -> Bool {
        var container = encoder.singleValueContainer()
        switch self {
        case let .secretScanningLocationPullRequestReviewComment(value): try container.encode(value); return true
        default: return false
        }
    }
}

/// Represents a 'commit' secret scanning location type. This location type shows that a secret was detected inside
/// a commit to a repository.
public struct SecretScanningLocationCommit: Codable {
    /// The file path in the repository
    /// - Example: "/example/secrets.txt"
    public var path: String
    /// Line number at which the secret starts in the file
    public var startLine: Double
    /// Line number at which the secret ends in the file
    public var endLine: Double
    /// The column at which the secret starts within the start line when the file is interpreted as 8BIT ASCII
    public var startColumn: Double
    /// The column at which the secret ends within the end line when the file is interpreted as 8BIT ASCII
    public var endColumn: Double
    /// SHA-1 hash ID of the associated blob
    /// - Example: "af5626b4a114abcb82d63db7c8082c3c4756e51b"
    public var blobSha: String
    /// The API URL to get the associated blob resource
    public var blobUrl: String
    /// SHA-1 hash ID of the associated commit
    /// - Example: "af5626b4a114abcb82d63db7c8082c3c4756e51b"
    public var commitSha: String
    /// The API URL to get the associated commit resource
    public var commitUrl: String
    /// The GitHub URL to get the associated commit resource.
    /// - Example: "https://github.com/octocat/Hello-World/blob/af5626b/example/secrets.txt#L1-L1"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case path
        case startLine = "start_line"
        case endLine = "end_line"
        case startColumn = "start_column"
        case endColumn = "end_column"
        case blobSha = "blob_sha"
        case blobUrl = "blob_url"
        case commitSha = "commit_sha"
        case commitUrl = "commit_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationCommit {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        path = try container.sdkDecodeRequired(.path)
        startLine = try container.sdkDecodeRequired(.startLine)
        endLine = try container.sdkDecodeRequired(.endLine)
        startColumn = try container.sdkDecodeRequired(.startColumn)
        endColumn = try container.sdkDecodeRequired(.endColumn)
        blobSha = try container.sdkDecodeRequired(.blobSha)
        blobUrl = try container.sdkDecodeRequired(.blobUrl)
        commitSha = try container.sdkDecodeRequired(.commitSha)
        commitUrl = try container.sdkDecodeRequired(.commitUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationCommit {
    init(
        path: String,
        startLine: Double,
        endLine: Double,
        startColumn: Double,
        endColumn: Double,
        blobSha: String,
        blobUrl: String,
        commitSha: String,
        commitUrl: String,
        htmlUrl: String? = nil
    ) throws {
        (self.path, self.startLine) = (path, startLine)
        (self.endLine, self.startColumn) = (endLine, startColumn)
        (self.endColumn, self.blobSha) = (endColumn, blobSha)
        (self.blobUrl, self.commitSha) = (blobUrl, commitSha)
        (self.commitUrl, self.htmlUrl) = (commitUrl, htmlUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents a 'discussion_body' secret scanning location type. This location type shows that a secret was
/// detected in the body of a discussion.
public struct SecretScanningLocationDiscussionBody: Codable {
    /// The URL to the discussion where the secret was detected.
    /// - Example: "https://github.com/community/community/discussions/39082#discussion-4566270"
    public var discussionBodyUrl: String

    enum CodingKeys: String, CodingKey {
        case discussionBodyUrl = "discussion_body_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationDiscussionBody {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.discussionBodyUrl) else {
            throw SdkValidationError(
                field: "discussion_body_url",
                code: "required",
                message: "Validation failed for 'discussion_body_url': value is required"
            )
        }
        discussionBodyUrl = try container.sdkDecodeRequired(.discussionBodyUrl)
        try sdkValidateUri("discussion_body_url", discussionBodyUrl)
    }
}

public extension SecretScanningLocationDiscussionBody {
    init(discussionBodyUrl: String) throws {
        self.discussionBodyUrl = discussionBodyUrl
        try sdkValidateUri("discussion_body_url", self.discussionBodyUrl)
    }
}

/// Represents a 'discussion_comment' secret scanning location type. This location type shows that a secret was
/// detected in a comment on a discussion.
public struct SecretScanningLocationDiscussionComment: Codable {
    /// The API URL to get the discussion comment where the secret was detected.
    /// - Example: "https://github.com/community/community/discussions/39082#discussioncomment-4158232"
    public var discussionCommentUrl: String

    enum CodingKeys: String, CodingKey {
        case discussionCommentUrl = "discussion_comment_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationDiscussionComment {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.discussionCommentUrl) else {
            throw SdkValidationError(
                field: "discussion_comment_url",
                code: "required",
                message: "Validation failed for 'discussion_comment_url': value is required"
            )
        }
        discussionCommentUrl = try container.sdkDecodeRequired(.discussionCommentUrl)
        try sdkValidateUri("discussion_comment_url", discussionCommentUrl)
    }
}

public extension SecretScanningLocationDiscussionComment {
    init(discussionCommentUrl: String) throws {
        self.discussionCommentUrl = discussionCommentUrl
        try sdkValidateUri("discussion_comment_url", self.discussionCommentUrl)
    }
}

/// Represents a 'discussion_title' secret scanning location type. This location type shows that a secret was
/// detected in the title of a discussion.
public struct SecretScanningLocationDiscussionTitle: Codable {
    /// The URL to the discussion where the secret was detected.
    /// - Example: "https://github.com/community/community/discussions/39082"
    public var discussionTitleUrl: String

    enum CodingKeys: String, CodingKey {
        case discussionTitleUrl = "discussion_title_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationDiscussionTitle {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.discussionTitleUrl) else {
            throw SdkValidationError(
                field: "discussion_title_url",
                code: "required",
                message: "Validation failed for 'discussion_title_url': value is required"
            )
        }
        discussionTitleUrl = try container.sdkDecodeRequired(.discussionTitleUrl)
        try sdkValidateUri("discussion_title_url", discussionTitleUrl)
    }
}

public extension SecretScanningLocationDiscussionTitle {
    init(discussionTitleUrl: String) throws {
        self.discussionTitleUrl = discussionTitleUrl
        try sdkValidateUri("discussion_title_url", self.discussionTitleUrl)
    }
}

/// Represents an 'issue_body' secret scanning location type. This location type shows that a secret was detected in
/// the body of an issue.
public struct SecretScanningLocationIssueBody: Codable {
    /// The API URL to get the issue where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/issues/1347"
    public var issueBodyUrl: String
    /// The GitHub URL for the issue where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/issues/1"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case issueBodyUrl = "issue_body_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationIssueBody {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.issueBodyUrl) else {
            throw SdkValidationError(
                field: "issue_body_url",
                code: "required",
                message: "Validation failed for 'issue_body_url': value is required"
            )
        }
        issueBodyUrl = try container.sdkDecodeRequired(.issueBodyUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        try sdkValidateUri("issue_body_url", issueBodyUrl)
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationIssueBody {
    init(issueBodyUrl: String, htmlUrl: String? = nil) throws {
        (self.issueBodyUrl, self.htmlUrl) = (issueBodyUrl, htmlUrl)
        try sdkValidateUri("issue_body_url", self.issueBodyUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents an 'issue_comment' secret scanning location type. This location type shows that a secret was detected
/// in a comment on an issue.
public struct SecretScanningLocationIssueComment: Codable {
    /// The API URL to get the issue comment where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/issues/comments/1081119451"
    public var issueCommentUrl: String
    /// The GitHub URL for the issue comment where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/issues/1#issuecomment-1081119451"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case issueCommentUrl = "issue_comment_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationIssueComment {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.issueCommentUrl) else {
            throw SdkValidationError(
                field: "issue_comment_url",
                code: "required",
                message: "Validation failed for 'issue_comment_url': value is required"
            )
        }
        issueCommentUrl = try container.sdkDecodeRequired(.issueCommentUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        try sdkValidateUri("issue_comment_url", issueCommentUrl)
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationIssueComment {
    init(issueCommentUrl: String, htmlUrl: String? = nil) throws {
        (self.issueCommentUrl, self.htmlUrl) = (issueCommentUrl, htmlUrl)
        try sdkValidateUri("issue_comment_url", self.issueCommentUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents an 'issue_title' secret scanning location type. This location type shows that a secret was detected
/// in the title of an issue.
public struct SecretScanningLocationIssueTitle: Codable {
    /// The API URL to get the issue where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/issues/1347"
    public var issueTitleUrl: String
    /// The GitHub URL for the issue where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/issues/1"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case issueTitleUrl = "issue_title_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationIssueTitle {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.issueTitleUrl) else {
            throw SdkValidationError(
                field: "issue_title_url",
                code: "required",
                message: "Validation failed for 'issue_title_url': value is required"
            )
        }
        issueTitleUrl = try container.sdkDecodeRequired(.issueTitleUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        try sdkValidateUri("issue_title_url", issueTitleUrl)
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationIssueTitle {
    init(issueTitleUrl: String, htmlUrl: String? = nil) throws {
        (self.issueTitleUrl, self.htmlUrl) = (issueTitleUrl, htmlUrl)
        try sdkValidateUri("issue_title_url", self.issueTitleUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents a 'pull_request_body' secret scanning location type. This location type shows that a secret was
/// detected in the body of a pull request.
public struct SecretScanningLocationPullRequestBody: Codable {
    /// The API URL to get the pull request where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/pulls/2846"
    public var pullRequestBodyUrl: String
    /// The GitHub URL for the pull request where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/pull/2846"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case pullRequestBodyUrl = "pull_request_body_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension SecretScanningLocationPullRequestBody {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.pullRequestBodyUrl) else {
            throw SdkValidationError(
                field: "pull_request_body_url",
                code: "required",
                message: "Validation failed for 'pull_request_body_url': value is required"
            )
        }
        pullRequestBodyUrl = try container.sdkDecodeRequired(.pullRequestBodyUrl)
        htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
        try sdkValidateUri("pull_request_body_url", pullRequestBodyUrl)
        if let value = htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationPullRequestBody {
    init(pullRequestBodyUrl: String, htmlUrl: String? = nil) throws {
        (self.pullRequestBodyUrl, self.htmlUrl) = (pullRequestBodyUrl, htmlUrl)
        try sdkValidateUri("pull_request_body_url", self.pullRequestBodyUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}
