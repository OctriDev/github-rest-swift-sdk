// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

/// Gists domain models
/// Base Gist
public struct BaseGist: Codable {
    /// Required `uri`-formatted value serialized in the `url` wire field.
    public var url: String
    /// Required `uri`-formatted value serialized in the `forks_url` wire field.
    public var forksUrl: String
    /// Required `uri`-formatted value serialized in the `commits_url` wire field.
    public var commitsUrl: String
    /// Required `string` value serialized in the `id` wire field.
    public var id: String
    /// Required `string` value serialized in the `node_id` wire field.
    public var nodeId: String
    /// Required `uri`-formatted value serialized in the `git_pull_url` wire field.
    public var gitPullUrl: String
    /// Required `uri`-formatted value serialized in the `git_push_url` wire field.
    public var gitPushUrl: String
    /// Required `uri`-formatted value serialized in the `html_url` wire field.
    public var htmlUrl: String
    /// Required object value serialized in the `files` wire field.
    public var files: [String: BaseGistFilesValue]
    /// Required `boolean` value serialized in the `public` wire field.
    public var `public`: Bool
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    public var createdAt: Date
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    public var updatedAt: Date
    /// Required `string` value serialized in the `description` wire field.
    public var description: String?
    /// Required `integer` value serialized in the `comments` wire field.
    public var comments: Int
    /// A GitHub user.
    public var user: NullableSimpleUser?
    /// Required `uri`-formatted value serialized in the `comments_url` wire field.
    public var commentsUrl: String
    /// Optional `boolean` value serialized in the `comments_enabled` wire field.
    public var commentsEnabled: Bool?
    /// A GitHub user.
    public var owner: SimpleUser?
    /// Optional `boolean` value serialized in the `truncated` wire field.
    public var truncated: Bool?
    /// Optional array value serialized in the `forks` wire field.
    public var forks: [JSONValue]?
    /// Optional array value serialized in the `history` wire field.
    public var history: [JSONValue]?

    enum CodingKeys: String, CodingKey {
        case url
        case forksUrl = "forks_url"
        case commitsUrl = "commits_url"
        case id
        case nodeId = "node_id"
        case gitPullUrl = "git_pull_url"
        case gitPushUrl = "git_push_url"
        case htmlUrl = "html_url"
        case files
        case `public`
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case description
        case comments
        case user
        case commentsUrl = "comments_url"
        case commentsEnabled = "comments_enabled"
        case owner
        case truncated
        case forks
        case history
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension BaseGist {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        url = try container.sdkDecodeRequired(.url)
        forksUrl = try container.sdkDecodeRequired(.forksUrl)
        commitsUrl = try container.sdkDecodeRequired(.commitsUrl)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        gitPullUrl = try container.sdkDecodeRequired(.gitPullUrl)
        gitPushUrl = try container.sdkDecodeRequired(.gitPushUrl)
        htmlUrl = try container.sdkDecodeRequired(.htmlUrl)
        files = try container.sdkDecodeRequired(.files)
        self.public = try container.sdkDecodeRequired(.public)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        updatedAt = try container.sdkDecodeRequired(.updatedAt)
        description = try container.sdkDecodeIfPresent(.description)
        comments = try container.sdkDecodeRequired(.comments)
        user = try container.sdkDecodeIfPresent(.user)
        commentsUrl = try container.sdkDecodeRequired(.commentsUrl)
        commentsEnabled = try container.sdkDecodeIfPresent(.commentsEnabled)
        owner = try container.sdkDecodeIfPresent(.owner)
        truncated = try container.sdkDecodeIfPresent(.truncated)
        forks = try container.sdkDecodeIfPresent(.forks)
        history = try container.sdkDecodeIfPresent(.history)
        try sdkValidateConstraints()
    }
}

public extension BaseGist {
    init(
        url: String,
        forksUrl: String,
        commitsUrl: String,
        id: String,
        nodeId: String,
        gitPullUrl: String,
        gitPushUrl: String,
        htmlUrl: String,
        files: [String: BaseGistFilesValue],
        public: Bool,
        createdAt: Date,
        updatedAt: Date,
        description: String?,
        comments: Int,
        user: NullableSimpleUser?,
        commentsUrl: String,
        commentsEnabled: Bool? = nil,
        owner: SimpleUser? = nil,
        truncated: Bool? = nil,
        forks: [JSONValue]? = nil,
        history: [JSONValue]? = nil
    ) throws {
        (self.url, self.forksUrl) = (url, forksUrl)
        (self.commitsUrl, self.id) = (commitsUrl, id)
        (self.nodeId, self.gitPullUrl) = (nodeId, gitPullUrl)
        (self.gitPushUrl, self.htmlUrl) = (gitPushUrl, htmlUrl)
        (self.files, self.public) = (files, `public`)
        (self.createdAt, self.updatedAt) = (createdAt, updatedAt)
        (self.description, self.comments) = (description, comments)
        (self.user, self.commentsUrl) = (user, commentsUrl)
        (self.commentsEnabled, self.owner) = (commentsEnabled, owner)
        (self.truncated, self.forks) = (truncated, forks)
        self.history = history
        try sdkValidateConstraints()
    }
}

extension BaseGist {
    func sdkValidateConstraints() throws {
        try sdkValidateUri("url", url)
        try sdkValidateUri("forks_url", forksUrl)
        try sdkValidateUri("commits_url", commitsUrl)
        try sdkValidateUri("git_pull_url", gitPullUrl)
        try sdkValidateUri("git_push_url", gitPushUrl)
        try sdkValidateUri("html_url", htmlUrl)
        try sdkValidateDateTime("created_at", sdkWireString(createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(updatedAt))
        try sdkValidateUri("comments_url", commentsUrl)
    }
}

public struct BaseGistFilesValue: Codable {
    public var filename: String?
    public var type: String?
    public var language: String?
    public var rawUrl: String?
    public var size: Int?
    /// The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported.
    public var encoding: String?

    enum CodingKeys: String, CodingKey {
        case filename
        case type
        case language
        case rawUrl = "raw_url"
        case size
        case encoding
    }

    init() {
        (filename, type, language, rawUrl, size) = (nil, nil, nil, nil, nil)
        encoding = nil
    }
}

public extension BaseGistFilesValue {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        filename = try container.sdkDecodeIfPresent(.filename)
        type = try container.sdkDecodeIfPresent(.type)
        language = try container.sdkDecodeIfPresent(.language)
        rawUrl = try container.sdkDecodeIfPresent(.rawUrl)
        size = try container.sdkDecodeIfPresent(.size)
        encoding = try container.sdkDecodeIfPresent(.encoding)
    }
}

public extension BaseGistFilesValue {
    init(
        filename: String? = nil,
        type: String? = nil,
        language: String? = nil,
        rawUrl: String? = nil,
        size: Int? = nil,
        encoding: String? = nil
    ) {
        self.init()
        (self.filename, self.type) = (filename, type)
        (self.language, self.rawUrl) = (language, rawUrl)
        (self.size, self.encoding) = (size, encoding)
    }
}

/// A comment made to a gist.
public struct GistComment: Codable {
    /// Required `integer` value serialized in the `id` wire field.
    /// - Example: 1
    public var id: Int
    /// Required `string` value serialized in the `node_id` wire field.
    /// - Example: "MDExOkdpc3RDb21tZW50MQ=="
    public var nodeId: String
    /// Required `uri`-formatted value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/gists/a6db0bec360bb87e9418/comments/1"
    public var url: String
    /// The comment text.
    /// - Example: "Body of the attachment"
    public var body: String
    /// A GitHub user.
    public var user: NullableSimpleUser?
    /// Required `date-time`-formatted value serialized in the `created_at` wire field.
    /// - Example: "2011-04-18T23:23:56Z"
    public var createdAt: Date
    /// Required `date-time`-formatted value serialized in the `updated_at` wire field.
    /// - Example: "2011-04-18T23:23:56Z"
    public var updatedAt: Date
    /// How the author is associated with the repository.
    /// - Example: "OWNER"
    public var authorAssociation: AuthorAssociation

    enum CodingKeys: String, CodingKey {
        case id
        case nodeId = "node_id"
        case url
        case body
        case user
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case authorAssociation = "author_association"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension GistComment {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        id = try container.sdkDecodeRequired(.id)
        nodeId = try container.sdkDecodeRequired(.nodeId)
        url = try container.sdkDecodeRequired(.url)
        body = try container.sdkDecodeRequired(.body)
        user = try container.sdkDecodeIfPresent(.user)
        createdAt = try container.sdkDecodeRequired(.createdAt)
        updatedAt = try container.sdkDecodeRequired(.updatedAt)
        authorAssociation = try container.sdkDecodeRequired(.authorAssociation)
        try sdkValidateUri("url", url)
        try validateLength("body", body, min: nil, max: 65535)
        try sdkValidateDateTime("created_at", sdkWireString(createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(updatedAt))
    }
}

public extension GistComment {
    init(
        id: Int,
        nodeId: String,
        url: String,
        body: String,
        user: NullableSimpleUser?,
        createdAt: Date,
        updatedAt: Date,
        authorAssociation: AuthorAssociation
    ) throws {
        (self.id, self.nodeId) = (id, nodeId)
        (self.url, self.body) = (url, body)
        (self.user, self.createdAt) = (user, createdAt)
        (self.updatedAt, self.authorAssociation) = (updatedAt, authorAssociation)
        try sdkValidateUri("url", self.url)
        try validateLength("body", self.body, min: nil, max: 65535)
        try sdkValidateDateTime("created_at", sdkWireString(self.createdAt))
        try sdkValidateDateTime("updated_at", sdkWireString(self.updatedAt))
    }
}

/// Gist Commit
public struct GistCommit: Codable {
    /// Required `uri`-formatted value serialized in the `url` wire field.
    /// - Example: "https://api.github.com/gists/aa5a315d61ae9438b18d/57a7f021a713b1c5a6a199b54cc514735d2d462f"
    public var url: String
    /// Required `string` value serialized in the `version` wire field.
    /// - Example: "57a7f021a713b1c5a6a199b54cc514735d2d462f"
    public var version: String
    /// A GitHub user.
    public var user: NullableSimpleUser?
    /// Required object value serialized in the `change_status` wire field.
    public var changeStatus: GistCommitChangeStatus
    /// Required `date-time`-formatted value serialized in the `committed_at` wire field.
    /// - Example: "2010-04-14T02:15:15Z"
    public var committedAt: Date

    enum CodingKeys: String, CodingKey {
        case url
        case version
        case user
        case changeStatus = "change_status"
        case committedAt = "committed_at"
    }

    private init(sdkCopy value: Self) {
        self = value
    }
}

public extension GistCommit {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.url) else {
            throw SdkValidationError(
                field: "url",
                code: "required",
                message: "Validation failed for 'url': value is required"
            )
        }
        guard container.contains(.version) else {
            throw SdkValidationError(
                field: "version",
                code: "required",
                message: "Validation failed for 'version': value is required"
            )
        }
        guard container.contains(.user) else {
            throw SdkValidationError(
                field: "user",
                code: "required",
                message: "Validation failed for 'user': value is required"
            )
        }
        guard container.contains(.changeStatus) else {
            throw SdkValidationError(
                field: "change_status",
                code: "required",
                message: "Validation failed for 'change_status': value is required"
            )
        }
        guard container.contains(.committedAt) else {
            throw SdkValidationError(
                field: "committed_at",
                code: "required",
                message: "Validation failed for 'committed_at': value is required"
            )
        }
        url = try container.sdkDecodeRequired(.url)
        version = try container.sdkDecodeRequired(.version)
        user = try container.sdkDecodeIfPresent(.user)
        changeStatus = try container.sdkDecodeRequired(.changeStatus)
        committedAt = try container.sdkDecodeRequired(.committedAt)
        try sdkValidateUri("url", url)
        try sdkValidateDateTime("committed_at", sdkWireString(committedAt))
    }
}

public extension GistCommit {
    init(
        url: String,
        version: String,
        user: NullableSimpleUser?,
        changeStatus: GistCommitChangeStatus,
        committedAt: Date
    ) throws {
        (self.url, self.version) = (url, version)
        (self.user, self.changeStatus) = (user, changeStatus)
        self.committedAt = committedAt
        try sdkValidateUri("url", self.url)
        try sdkValidateDateTime("committed_at", sdkWireString(self.committedAt))
    }
}

/// Required object value serialized in the `change_status` wire field.
public struct GistCommitChangeStatus: Codable {
    /// Optional `integer` value serialized in the `total` wire field.
    public var total: Int?
    /// Optional `integer` value serialized in the `additions` wire field.
    public var additions: Int?
    /// Optional `integer` value serialized in the `deletions` wire field.
    public var deletions: Int?

    enum CodingKeys: String, CodingKey {
        case total
        case additions
        case deletions
    }

    init() {
        (total, additions, deletions) = (nil, nil, nil)
    }
}

public extension GistCommitChangeStatus {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        total = try container.sdkDecodeIfPresent(.total)
        additions = try container.sdkDecodeIfPresent(.additions)
        deletions = try container.sdkDecodeIfPresent(.deletions)
    }
}

public extension GistCommitChangeStatus {
    init(total: Int? = nil, additions: Int? = nil, deletions: Int? = nil) {
        self.init()
        (self.total, self.additions) = (total, additions)
        self.deletions = deletions
    }
}

/// Gist History
public struct GistHistory: Codable {
    /// A GitHub user.
    public var user: NullableSimpleUser?
    /// Optional `string` value serialized in the `version` wire field.
    public var version: String?
    /// Optional `date-time`-formatted value serialized in the `committed_at` wire field.
    public var committedAt: Date?
    /// Optional object value serialized in the `change_status` wire field.
    public var changeStatus: GistHistoryChangeStatus?
    /// Optional `uri`-formatted value serialized in the `url` wire field.
    public var url: String?

    enum CodingKeys: String, CodingKey {
        case user
        case version
        case committedAt = "committed_at"
        case changeStatus = "change_status"
        case url
    }

    init() {
        (user, version, committedAt, changeStatus, url) = (nil, nil, nil, nil, nil)
    }
}

public extension GistHistory {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        user = try container.sdkDecodeIfPresent(.user)
        version = try container.sdkDecodeIfPresent(.version)
        committedAt = try container.sdkDecodeIfPresent(.committedAt)
        changeStatus = try container.sdkDecodeIfPresent(.changeStatus)
        url = try container.sdkDecodeIfPresent(.url)
        if let value = committedAt {
            try sdkValidateDateTime("committed_at", sdkWireString(value))
        }
        if let value = url {
            try sdkValidateUri("url", value)
        }
    }
}

public extension GistHistory {
    init(
        user: NullableSimpleUser? = nil,
        version: String? = nil,
        committedAt: Date? = nil,
        changeStatus: GistHistoryChangeStatus? = nil,
        url: String? = nil
    ) throws {
        self.init()
        (self.user, self.version) = (user, version)
        (self.committedAt, self.changeStatus) = (committedAt, changeStatus)
        self.url = url
        if let value = self.committedAt {
            try sdkValidateDateTime("committed_at", sdkWireString(value))
        }
        if let value = self.url {
            try sdkValidateUri("url", value)
        }
    }
}

/// Optional object value serialized in the `change_status` wire field.
public struct GistHistoryChangeStatus: Codable {
    /// Optional `integer` value serialized in the `total` wire field.
    public var total: Int?
    /// Optional `integer` value serialized in the `additions` wire field.
    public var additions: Int?
    /// Optional `integer` value serialized in the `deletions` wire field.
    public var deletions: Int?

    enum CodingKeys: String, CodingKey {
        case total
        case additions
        case deletions
    }

    init() {
        (total, additions, deletions) = (nil, nil, nil)
    }
}

public extension GistHistoryChangeStatus {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        total = try container.sdkDecodeIfPresent(.total)
        additions = try container.sdkDecodeIfPresent(.additions)
        deletions = try container.sdkDecodeIfPresent(.deletions)
    }
}

public extension GistHistoryChangeStatus {
    init(total: Int? = nil, additions: Int? = nil, deletions: Int? = nil) {
        self.init()
        (self.total, self.additions) = (total, additions)
        self.deletions = deletions
    }
}

/// Gist Simple
public struct GistSimple: Codable {
    /// Optional array value serialized in the `forks` wire field.
    public var forks: [GistSimpleForksItem]?
    /// Optional array value serialized in the `history` wire field.
    public var history: [GistHistory]?
    /// Gist
    public var forkOf: GistSimpleForkOf?
    /// Optional `string` value serialized in the `url` wire field.
    public var url: String?
    /// Optional `string` value serialized in the `forks_url` wire field.
    public var forksUrl: String?
    /// Optional `string` value serialized in the `commits_url` wire field.
    public var commitsUrl: String?
    /// Optional `string` value serialized in the `id` wire field.
    public var id: String?
    /// Optional `string` value serialized in the `node_id` wire field.
    public var nodeId: String?
    /// Optional `string` value serialized in the `git_pull_url` wire field.
    public var gitPullUrl: String?
    /// Optional `string` value serialized in the `git_push_url` wire field.
    public var gitPushUrl: String?
    /// Optional `string` value serialized in the `html_url` wire field.
    public var htmlUrl: String?
    /// Optional object value serialized in the `files` wire field.
    public var files: [String: GistSimpleFilesValue?]?
    /// Optional `boolean` value serialized in the `public` wire field.
    public var `public`: Bool?
    /// Optional `string` value serialized in the `created_at` wire field.
    public var createdAt: String?
    /// Optional `string` value serialized in the `updated_at` wire field.
    public var updatedAt: String?
    /// Optional `string` value serialized in the `description` wire field.
    public var description: String?
    /// Optional `integer` value serialized in the `comments` wire field.
    public var comments: Int?
    /// Optional `boolean` value serialized in the `comments_enabled` wire field.
    public var commentsEnabled: Bool?
    /// Optional `string` value serialized in the `user` wire field.
    public var user: String?
    /// Optional `string` value serialized in the `comments_url` wire field.
    public var commentsUrl: String?
    /// A GitHub user.
    public var owner: SimpleUser?
    /// Optional `boolean` value serialized in the `truncated` wire field.
    public var truncated: Bool?

    enum CodingKeys: String, CodingKey {
        case forks
        case history
        case forkOf = "fork_of"
        case url
        case forksUrl = "forks_url"
        case commitsUrl = "commits_url"
        case id
        case nodeId = "node_id"
        case gitPullUrl = "git_pull_url"
        case gitPushUrl = "git_push_url"
        case htmlUrl = "html_url"
        case files
        case `public`
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case description
        case comments
        case commentsEnabled = "comments_enabled"
        case user
        case commentsUrl = "comments_url"
        case owner
        case truncated
    }

    init() {
        (forks, history, forkOf, url, forksUrl) = (nil, nil, nil, nil, nil)
        (commitsUrl, id, nodeId, gitPullUrl, gitPushUrl) = (nil, nil, nil, nil, nil)
        (htmlUrl, files, self.public, createdAt, updatedAt) = (nil, nil, nil, nil, nil)
        (description, comments, commentsEnabled, user, commentsUrl) = (nil, nil, nil, nil, nil)
        (owner, truncated) = (nil, nil)
    }
}

public extension GistSimple {
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.init()
        try sdkDecodeFieldsPart1(container)
        try sdkDecodeFieldsPart2(container)
    }
}
