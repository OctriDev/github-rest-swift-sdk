// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

// SecretScanning domain models
/// Represents a 'pull_request_comment' secret scanning location type. This location type shows that a secret was
/// detected in a comment on a pull request.
public struct SecretScanningLocationPullRequestComment: Codable {
    /// The API URL to get the pull request comment where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/issues/comments/1081119451"
    public var pullRequestCommentUrl: String
    /// The GitHub URL for the pull request comment where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/pull/2846#issuecomment-1081119451"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case pullRequestCommentUrl = "pull_request_comment_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension SecretScanningLocationPullRequestComment {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.pullRequestCommentUrl) else {
            throw SdkValidationError(field: "pull_request_comment_url", code: "required", message: "Validation failed for 'pull_request_comment_url': value is required")
        }
        self.pullRequestCommentUrl = try container.sdkDecodeRequired(.pullRequestCommentUrl)
        self.htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
            try sdkValidateUri("pull_request_comment_url", self.pullRequestCommentUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationPullRequestComment {
    public init(pullRequestCommentUrl: String, htmlUrl: String? = nil) throws {
        (self.pullRequestCommentUrl, self.htmlUrl) = (pullRequestCommentUrl, htmlUrl)
            try sdkValidateUri("pull_request_comment_url", self.pullRequestCommentUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents a 'pull_request_review' secret scanning location type. This location type shows that a secret was
/// detected in a review on a pull request.
public struct SecretScanningLocationPullRequestReview: Codable {
    /// The API URL to get the pull request review where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/pulls/2846/reviews/80"
    public var pullRequestReviewUrl: String
    /// The GitHub URL for the pull request review where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/pull/2846#pullrequestreview-80"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case pullRequestReviewUrl = "pull_request_review_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension SecretScanningLocationPullRequestReview {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.pullRequestReviewUrl) else {
            throw SdkValidationError(field: "pull_request_review_url", code: "required", message: "Validation failed for 'pull_request_review_url': value is required")
        }
        self.pullRequestReviewUrl = try container.sdkDecodeRequired(.pullRequestReviewUrl)
        self.htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
            try sdkValidateUri("pull_request_review_url", self.pullRequestReviewUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationPullRequestReview {
    public init(pullRequestReviewUrl: String, htmlUrl: String? = nil) throws {
        (self.pullRequestReviewUrl, self.htmlUrl) = (pullRequestReviewUrl, htmlUrl)
            try sdkValidateUri("pull_request_review_url", self.pullRequestReviewUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents a 'pull_request_review_comment' secret scanning location type. This location type shows that a secret
/// was detected in a review comment on a pull request.
public struct SecretScanningLocationPullRequestReviewComment: Codable {
    /// The API URL to get the pull request review comment where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/pulls/comments/12"
    public var pullRequestReviewCommentUrl: String
    /// The GitHub URL for the pull request review comment where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/pull/2846#discussion_r12"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case pullRequestReviewCommentUrl = "pull_request_review_comment_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension SecretScanningLocationPullRequestReviewComment {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.pullRequestReviewCommentUrl) else {
            throw SdkValidationError(field: "pull_request_review_comment_url", code: "required", message: "Validation failed for 'pull_request_review_comment_url': value is required")
        }
        self.pullRequestReviewCommentUrl = try container.sdkDecodeRequired(.pullRequestReviewCommentUrl)
        self.htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
            try sdkValidateUri("pull_request_review_comment_url", self.pullRequestReviewCommentUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationPullRequestReviewComment {
    public init(pullRequestReviewCommentUrl: String, htmlUrl: String? = nil) throws {
        (self.pullRequestReviewCommentUrl, self.htmlUrl) = (pullRequestReviewCommentUrl, htmlUrl)
            try sdkValidateUri("pull_request_review_comment_url", self.pullRequestReviewCommentUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents a 'pull_request_title' secret scanning location type. This location type shows that a secret was
/// detected in the title of a pull request.
public struct SecretScanningLocationPullRequestTitle: Codable {
    /// The API URL to get the pull request where the secret was detected.
    /// - Example: "https://api.github.com/repos/octocat/Hello-World/pulls/2846"
    public var pullRequestTitleUrl: String
    /// The GitHub URL for the pull request where the secret was detected.
    /// - Example: "https://github.com/octocat/Hello-World/pull/2846"
    public var htmlUrl: String?

    enum CodingKeys: String, CodingKey {
        case pullRequestTitleUrl = "pull_request_title_url"
        case htmlUrl = "html_url"
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension SecretScanningLocationPullRequestTitle {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        guard container.contains(.pullRequestTitleUrl) else {
            throw SdkValidationError(field: "pull_request_title_url", code: "required", message: "Validation failed for 'pull_request_title_url': value is required")
        }
        self.pullRequestTitleUrl = try container.sdkDecodeRequired(.pullRequestTitleUrl)
        self.htmlUrl = try container.sdkDecodeIfPresent(.htmlUrl)
            try sdkValidateUri("pull_request_title_url", self.pullRequestTitleUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

public extension SecretScanningLocationPullRequestTitle {
    public init(pullRequestTitleUrl: String, htmlUrl: String? = nil) throws {
        (self.pullRequestTitleUrl, self.htmlUrl) = (pullRequestTitleUrl, htmlUrl)
            try sdkValidateUri("pull_request_title_url", self.pullRequestTitleUrl)
        if let value = self.htmlUrl {
            try sdkValidateUri("html_url", value)
        }
    }
}

/// Represents a 'wiki_commit' secret scanning location type. This location type shows that a secret was detected
/// inside a commit to a repository wiki.
public struct SecretScanningLocationWikiCommit: Codable {
    /// The file path of the wiki page
    /// - Example: "/example/Home.md"
    public var path: String
    /// Line number at which the secret starts in the file
    public var startLine: Double
    /// Line number at which the secret ends in the file
    public var endLine: Double
    /// The column at which the secret starts within the start line when the file is interpreted as 8-bit ASCII.
    public var startColumn: Double
    /// The column at which the secret ends within the end line when the file is interpreted as 8-bit ASCII.
    public var endColumn: Double
    /// SHA-1 hash ID of the associated blob
    /// - Example: "af5626b4a114abcb82d63db7c8082c3c4756e51b"
    public var blobSha: String
    /// The GitHub URL to get the associated wiki page
    /// - Example: "https://github.com/octocat/Hello-World/wiki/Home/302c0b7e200761c9dd9b57e57db540ee0b4293a5"
    public var pageUrl: String
    /// SHA-1 hash ID of the associated commit
    /// - Example: "302c0b7e200761c9dd9b57e57db540ee0b4293a5"
    public var commitSha: String
    /// The GitHub URL to get the associated wiki commit
    /// - Example: "https://github.com/octocat/Hello-World/wiki/_compare/302c0b7e200761c9dd9b57e57db540ee0b4293a5"
    public var commitUrl: String

    enum CodingKeys: String, CodingKey {
        case path
        case startLine = "start_line"
        case endLine = "end_line"
        case startColumn = "start_column"
        case endColumn = "end_column"
        case blobSha = "blob_sha"
        case pageUrl = "page_url"
        case commitSha = "commit_sha"
        case commitUrl = "commit_url"
    }

    private init(sdkCopy value: Self) { self = value }
}

public extension SecretScanningLocationWikiCommit {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.path = try container.sdkDecodeRequired(.path)
        self.startLine = try container.sdkDecodeRequired(.startLine)
        self.endLine = try container.sdkDecodeRequired(.endLine)
        self.startColumn = try container.sdkDecodeRequired(.startColumn)
        self.endColumn = try container.sdkDecodeRequired(.endColumn)
        self.blobSha = try container.sdkDecodeRequired(.blobSha)
        self.pageUrl = try container.sdkDecodeRequired(.pageUrl)
        self.commitSha = try container.sdkDecodeRequired(.commitSha)
        self.commitUrl = try container.sdkDecodeRequired(.commitUrl)
    }
}

public extension SecretScanningLocationWikiCommit {
    public init(path: String, startLine: Double, endLine: Double, startColumn: Double, endColumn: Double, blobSha: String, pageUrl: String, commitSha: String, commitUrl: String) {
        (self.path, self.startLine) = (path, startLine)
        (self.endLine, self.startColumn) = (endLine, startColumn)
        (self.endColumn, self.blobSha) = (endColumn, blobSha)
        (self.pageUrl, self.commitSha) = (pageUrl, commitSha)
        self.commitUrl = commitUrl
    }
}

/// A collection of secret scanning patterns and their settings related to push protection.
public struct SecretScanningPatternConfiguration: Codable {
    /// The version of the entity. This is used to confirm you're updating the current version of the entity and
    /// mitigate unintentionally overriding someone else's update.
    public var patternConfigVersion: SecretScanningRowVersion?
    /// Overrides for partner patterns.
    public var providerPatternOverrides: [SecretScanningPatternOverride]?
    /// Overrides for custom patterns defined by the organization.
    public var customPatternOverrides: [SecretScanningPatternOverride]?

    enum CodingKeys: String, CodingKey {
        case patternConfigVersion = "pattern_config_version"
        case providerPatternOverrides = "provider_pattern_overrides"
        case customPatternOverrides = "custom_pattern_overrides"
    }

    init() {
        (self.patternConfigVersion, self.providerPatternOverrides, self.customPatternOverrides) = (nil, nil, nil)
    }
}

public extension SecretScanningPatternConfiguration {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.patternConfigVersion = try container.sdkDecodeIfPresent(.patternConfigVersion)
        self.providerPatternOverrides = try container.sdkDecodeIfPresent(.providerPatternOverrides)
        self.customPatternOverrides = try container.sdkDecodeIfPresent(.customPatternOverrides)
    }
}

public extension SecretScanningPatternConfiguration {
    public init(patternConfigVersion: SecretScanningRowVersion? = nil, providerPatternOverrides: [SecretScanningPatternOverride]? = nil, customPatternOverrides: [SecretScanningPatternOverride]? = nil) {
        self.init()
        self.patternConfigVersion = patternConfigVersion
        self.providerPatternOverrides = providerPatternOverrides
        self.customPatternOverrides = customPatternOverrides
    }
}

/// Typed representation of the `SecretScanningPatternOverride` API schema.
public struct SecretScanningPatternOverride: Codable {
    /// The ID of the pattern.
    public var tokenType: String?
    /// The version of this pattern if it's a custom pattern.
    public var customPatternVersion: String?
    /// The slug of the pattern.
    public var slug: String?
    /// The user-friendly name for the pattern.
    public var displayName: String?
    /// The total number of alerts generated by this pattern.
    public var alertTotal: Int?
    /// The percentage of all alerts that this pattern represents, rounded to the nearest integer.
    public var alertTotalPercentage: Int?
    /// The number of false positive alerts generated by this pattern.
    public var falsePositives: Int?
    /// The percentage of alerts from this pattern that are false positives, rounded to the nearest integer.
    public var falsePositiveRate: Int?
    /// The percentage of blocks for this pattern that were bypassed, rounded to the nearest integer.
    public var bypassRate: Int?
    /// The default push protection setting for this pattern.
    public var defaultSetting: SecretScanningPatternOverrideDefaultSetting?
    /// The push protection setting for this pattern set at the enterprise level. Only present for partner patterns
    /// when the organization has a parent enterprise.
    public var enterpriseSetting: SecretScanningPatternOverrideEnterpriseSetting?
    /// The current push protection setting for this pattern. If this is `not-set`, then it inherits either the
    /// enterprise setting if it exists or the default setting.
    public var setting: SecretScanningPatternOverrideSetting?

    enum CodingKeys: String, CodingKey {
        case tokenType = "token_type"
        case customPatternVersion = "custom_pattern_version"
        case slug
        case displayName = "display_name"
        case alertTotal = "alert_total"
        case alertTotalPercentage = "alert_total_percentage"
        case falsePositives = "false_positives"
        case falsePositiveRate = "false_positive_rate"
        case bypassRate = "bypass_rate"
        case defaultSetting = "default_setting"
        case enterpriseSetting = "enterprise_setting"
        case setting
    }

    init() {
        (self.tokenType, self.customPatternVersion, self.slug, self.displayName, self.alertTotal) = (nil, nil, nil, nil, nil)
        (self.alertTotalPercentage, self.falsePositives, self.falsePositiveRate, self.bypassRate, self.defaultSetting) = (nil, nil, nil, nil, nil)
        (self.enterpriseSetting, self.setting) = (nil, nil)
    }
}

public extension SecretScanningPatternOverride {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.tokenType = try container.sdkDecodeIfPresent(.tokenType)
        self.customPatternVersion = try container.sdkDecodeIfPresent(.customPatternVersion)
        self.slug = try container.sdkDecodeIfPresent(.slug)
        self.displayName = try container.sdkDecodeIfPresent(.displayName)
        self.alertTotal = try container.sdkDecodeIfPresent(.alertTotal)
        self.alertTotalPercentage = try container.sdkDecodeIfPresent(.alertTotalPercentage)
        self.falsePositives = try container.sdkDecodeIfPresent(.falsePositives)
        self.falsePositiveRate = try container.sdkDecodeIfPresent(.falsePositiveRate)
        self.bypassRate = try container.sdkDecodeIfPresent(.bypassRate)
        self.defaultSetting = try container.sdkDecodeIfPresent(.defaultSetting)
        self.enterpriseSetting = try container.sdkDecodeIfPresent(.enterpriseSetting)
        self.setting = try container.sdkDecodeIfPresent(.setting)
    }
}

public extension SecretScanningPatternOverride {
    public init(tokenType: String? = nil, customPatternVersion: String? = nil, slug: String? = nil, displayName: String? = nil, alertTotal: Int? = nil, alertTotalPercentage: Int? = nil, falsePositives: Int? = nil, falsePositiveRate: Int? = nil, bypassRate: Int? = nil, defaultSetting: SecretScanningPatternOverrideDefaultSetting? = nil, enterpriseSetting: SecretScanningPatternOverrideEnterpriseSetting? = nil, setting: SecretScanningPatternOverrideSetting? = nil) {
        self.init()
        (self.tokenType, self.customPatternVersion) = (tokenType, customPatternVersion)
        (self.slug, self.displayName) = (slug, displayName)
        (self.alertTotal, self.alertTotalPercentage) = (alertTotal, alertTotalPercentage)
        (self.falsePositives, self.falsePositiveRate) = (falsePositives, falsePositiveRate)
        (self.bypassRate, self.defaultSetting) = (bypassRate, defaultSetting)
        (self.enterpriseSetting, self.setting) = (enterpriseSetting, setting)
    }
}

/// Typed representation of the `SecretScanningPushProtectionBypass` API schema.
public struct SecretScanningPushProtectionBypass: Codable {
    /// The reason for bypassing push protection.
    public var reason: SecretScanningPushProtectionBypassReason?
    /// The time that the bypass will expire in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
    public var expireAt: Date?
    /// The token type this bypass is for.
    public var tokenType: String?

    enum CodingKeys: String, CodingKey {
        case reason
        case expireAt = "expire_at"
        case tokenType = "token_type"
    }

    init() {
        (self.reason, self.expireAt, self.tokenType) = (nil, nil, nil)
    }
}

public extension SecretScanningPushProtectionBypass {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.reason = try container.sdkDecodeIfPresent(.reason)
        self.expireAt = try container.sdkDecodeIfPresent(.expireAt)
        self.tokenType = try container.sdkDecodeIfPresent(.tokenType)
        if let value = self.expireAt {
            try sdkValidateDateTime("expire_at", sdkWireString(value))
        }
    }
}

public extension SecretScanningPushProtectionBypass {
    public init(reason: SecretScanningPushProtectionBypassReason? = nil, expireAt: Date? = nil, tokenType: String? = nil) throws {
        self.init()
        (self.reason, self.expireAt) = (reason, expireAt)
        self.tokenType = tokenType
        if let value = self.expireAt {
            try sdkValidateDateTime("expire_at", sdkWireString(value))
        }
    }
}

/// Information on a single scan performed by secret scanning on the repository
public struct SecretScanningScan: Codable {
    /// The type of scan
    public var type: String?
    /// The state of the scan. Either "completed", "running", "pending", "failed", or "unknown". "failed": this scan
    /// type has hit its retry limit and has been marked permanently failed. This usually resolves on its own at the
    /// next scheduled scan attempt. If "failed" persists, contact Support. "unknown": the scan is in an
    /// unrecognized or currently unhandled state.
    public var status: String?
    /// The time that the scan was completed. Empty if the scan is running
    public var completedAt: Date?
    /// The time that the scan was started. Empty if the scan is pending
    public var startedAt: Date?

    enum CodingKeys: String, CodingKey {
        case type
        case status
        case completedAt = "completed_at"
        case startedAt = "started_at"
    }

    init() {
        (self.type, self.status, self.completedAt, self.startedAt) = (nil, nil, nil, nil)
    }
}

public extension SecretScanningScan {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.type = try container.sdkDecodeIfPresent(.type)
        self.status = try container.sdkDecodeIfPresent(.status)
        self.completedAt = try container.sdkDecodeIfPresent(.completedAt)
        self.startedAt = try container.sdkDecodeIfPresent(.startedAt)
        if let value = self.completedAt {
            try sdkValidateDateTime("completed_at", sdkWireString(value))
        }
        if let value = self.startedAt {
            try sdkValidateDateTime("started_at", sdkWireString(value))
        }
    }
}

public extension SecretScanningScan {
    public init(type: String? = nil, status: String? = nil, completedAt: Date? = nil, startedAt: Date? = nil) throws {
        self.init()
        (self.type, self.status) = (type, status)
        (self.completedAt, self.startedAt) = (completedAt, startedAt)
        if let value = self.completedAt {
            try sdkValidateDateTime("completed_at", sdkWireString(value))
        }
        if let value = self.startedAt {
            try sdkValidateDateTime("started_at", sdkWireString(value))
        }
    }
}

/// Typed representation of the `SecretScanningScanHistory` API schema.
public struct SecretScanningScanHistory: Codable {
    /// Optional array value serialized in the `incremental_scans` wire field.
    public var incrementalScans: [SecretScanningScan]?
    /// Optional array value serialized in the `pattern_update_scans` wire field.
    public var patternUpdateScans: [SecretScanningScan]?
    /// Optional array value serialized in the `backfill_scans` wire field.
    public var backfillScans: [SecretScanningScan]?
    /// Optional array value serialized in the `custom_pattern_backfill_scans` wire field.
    public var customPatternBackfillScans: [SecretScanningScanHistoryCustomPatternBackfillScansItem]?
    /// Optional array value serialized in the `generic_secrets_backfill_scans` wire field.
    public var genericSecretsBackfillScans: [SecretScanningScan]?

    enum CodingKeys: String, CodingKey {
        case incrementalScans = "incremental_scans"
        case patternUpdateScans = "pattern_update_scans"
        case backfillScans = "backfill_scans"
        case customPatternBackfillScans = "custom_pattern_backfill_scans"
        case genericSecretsBackfillScans = "generic_secrets_backfill_scans"
    }

    init() {
        (self.incrementalScans, self.patternUpdateScans, self.backfillScans, self.customPatternBackfillScans, self.genericSecretsBackfillScans) = (nil, nil, nil, nil, nil)
    }
}

public extension SecretScanningScanHistory {
    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.incrementalScans = try container.sdkDecodeIfPresent(.incrementalScans)
        self.patternUpdateScans = try container.sdkDecodeIfPresent(.patternUpdateScans)
        self.backfillScans = try container.sdkDecodeIfPresent(.backfillScans)
        self.customPatternBackfillScans = try container.sdkDecodeIfPresent(.customPatternBackfillScans)
        self.genericSecretsBackfillScans = try container.sdkDecodeIfPresent(.genericSecretsBackfillScans)
    }
}

public extension SecretScanningScanHistory {
    public init(incrementalScans: [SecretScanningScan]? = nil, patternUpdateScans: [SecretScanningScan]? = nil, backfillScans: [SecretScanningScan]? = nil, customPatternBackfillScans: [SecretScanningScanHistoryCustomPatternBackfillScansItem]? = nil, genericSecretsBackfillScans: [SecretScanningScan]? = nil) {
        self.init()
        (self.incrementalScans, self.patternUpdateScans) = (incrementalScans, patternUpdateScans)
        (self.backfillScans, self.customPatternBackfillScans) = (backfillScans, customPatternBackfillScans)
        self.genericSecretsBackfillScans = genericSecretsBackfillScans
    }
}

/// Required API value serialized in the `custom_pattern_backfill_scans[]` wire field.
public struct SecretScanningScanHistoryCustomPatternBackfillScansItem: Codable {
    /// The type of scan
    public var type: String?
    /// The state of the scan. Either "completed", "running", "pending", "failed", or "unknown". "failed": this scan
    /// type has hit its retry limit and has been marked permanently failed. This usually resolves on its own at the
    /// next scheduled scan attempt. If "failed" persists, contact Support. "unknown": the scan is in an
    /// unrecognized or currently unhandled state.
    public var status: String?
    /// The time that the scan was completed. Empty if the scan is running
    public var completedAt: Date?
    /// The time that the scan was started. Empty if the scan is pending
    public var startedAt: Date?
    /// Name of the custom pattern for custom pattern scans
    public var patternName: String?
    /// Level at which the custom pattern is defined, one of "repository", "organization", or "enterprise"
    public var patternScope: String?

    enum CodingKeys: String, CodingKey {
        case type
        case status
        case completedAt = "completed_at"
        case startedAt = "started_at"
        case patternName = "pattern_name"
        case patternScope = "pattern_scope"
    }

    init() {
        (self.type, self.status, self.completedAt, self.startedAt, self.patternName) = (nil, nil, nil, nil, nil)
        self.patternScope = nil
    }
}
