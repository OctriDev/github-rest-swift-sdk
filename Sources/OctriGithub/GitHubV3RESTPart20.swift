// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

public class CampaignsNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// Lists campaigns in an organization. The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
    public func campaignsListOrg(org: String, page: Int?, perPage: Int?, direction: SecurityAdvisoriesListGlobalAdvisoriesParameter?, state: CampaignState?, sort: CampaignsListOrgCampaignsParameter?) async throws -> [CampaignSummary] {
        return try await CampaignsMethods.campaignsListOrgCampaigns(config: config, org: org, page: page, perPage: perPage, direction: direction, state: state, sort: sort)
    }

/// Create a campaign for an organization. The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. Fine-grained tokens must have the "Code scanning alerts" repository permissions (read) on all repositories included in the campaign.
    public func createCampaign(options: CampaignsMethods.CampaignsCreateCampaignOptions) async throws -> CampaignSummary {
        return try await CampaignsMethods.campaignsCreateCampaign(config: config, options: options)
    }

/// Gets a campaign for an organization. The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
    public func getCampaignSummary(org: String, campaignNumber: Int) async throws -> CampaignSummary {
        return try await CampaignsMethods.campaignsGetCampaignSummary(config: config, org: org, campaignNumber: campaignNumber)
    }

/// Updates a campaign in an organization. The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
    public func updateCampaign(options: CampaignsMethods.CampaignsUpdateCampaignOptions) async throws -> CampaignSummary {
        return try await CampaignsMethods.campaignsUpdateCampaign(config: config, options: options)
    }

/// Deletes a campaign in an organization. The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
    public func deleteCampaign(org: String, campaignNumber: Int) async throws -> SdkEmptyResponse {
        return try await CampaignsMethods.campaignsDeleteCampaign(config: config, org: org, campaignNumber: campaignNumber)
    }
}

public class CodeScanningNamespace {
    let config: ClientConfig
    public init(config: ClientConfig) {
        self.config = config
    }

/// > [!NOTE] > This endpoint is in public preview and is subject to change. Gets the AI Scan setting stored on an organization. The response reports the value stored on the organization. Organization respects enterprise policy. The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org`, `repo`, or `write:org` scope to use this endpoint. Organization owners can use `admin:org` or `repo`; security managers need `write:org`.
    public func getAiScanEnablementForOrg(org: String) async throws -> CodeScanningAiScanOrgSettings {
        return try await CodeScanningMethods.codeScanningGetAiScanEnablementForOrg(config: config, org: org)
    }

/// > [!NOTE] > This endpoint is in public preview and is subject to change. Updates the AI Scan setting stored on an organization. The organization respects the enterprise policy, so enabling is rejected when the enterprise disallows AI Scan. OAuth app tokens and personal access tokens (classic) need the `admin:org`, `repo`, or `write:org` scope to use this endpoint. Organization owners can use `admin:org` or `repo`; security managers need `write:org`.
    public func updateAiScanEnablementForOrg(org: String, prScan: CodeScanningAiScanOrgEnablementUpdatePrScan?) async throws -> CodeScanningAiScanOrgSettings {
        return try await CodeScanningMethods.codeScanningUpdateAiScanEnablementForOrg(config: config, org: org, prScan: prScan)
    }

/// Lists code scanning alerts for the default branch for all eligible repositories in an organization. Eligible repositories are repositories that are owned by organizations that you own or for which you are a security manager. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)." The authenticated user must be an owner or security manager for the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `security_events` or `repo`s cope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func listAlertsForOrg(options: CodeScanningMethods.CodeScanningListAlertsForOrgOptions) async throws -> [CodeScanningOrganizationAlertItems] {
        return try await CodeScanningMethods.codeScanningListAlertsForOrg(config: config, options: options)
    }

/// > [!NOTE] > This endpoint is in public preview and is subject to change. Gets whether AI Scan is enabled for a repository. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getAiScanEnablement(owner: String, repo: String) async throws -> CodeScanningAiScanEnablement {
        return try await CodeScanningMethods.codeScanningGetAiScanEnablement(config: config, owner: owner, repo: repo)
    }

/// > [!NOTE] > This endpoint is in public preview and is subject to change. Updates whether AI Scan is enabled for a repository. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func updateAiScanEnablement(owner: String, repo: String, prScan: CodeScanningAiScanEnablementUpdatePrScan?) async throws -> CodeScanningAiScanEnablement {
        return try await CodeScanningMethods.codeScanningUpdateAiScanEnablement(config: config, owner: owner, repo: repo, prScan: prScan)
    }

/// Lists code scanning alerts. The response includes a `most_recent_instance` object. This provides details of the most recent instance of this alert for the default branch (or for the specified Git reference if you used `ref` in the request). OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func listAlertsForRepo(options: CodeScanningMethods.CodeScanningListAlertsForRepoOptions) async throws -> [CodeScanningAlertItems] {
        return try await CodeScanningMethods.codeScanningListAlertsForRepo(config: config, options: options)
    }

/// Gets a single code scanning alert. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getAlert(owner: String, repo: String, alertNumber: AlertNumber) async throws -> CodeScanningAlert {
        return try await CodeScanningMethods.codeScanningGetAlert(config: config, owner: owner, repo: repo, alertNumber: alertNumber)
    }

/// Updates the status of a single code scanning alert. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func updateAlert(options: CodeScanningMethods.CodeScanningUpdateAlertOptions) async throws -> CodeScanningAlert {
        return try await CodeScanningMethods.codeScanningUpdateAlert(config: config, options: options)
    }
}

extension CodeScanningNamespace {
/// Gets the status and description of an autofix for a code scanning alert on the repository's default branch. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getAutofix(owner: String, repo: String, alertNumber: AlertNumber) async throws -> CodeScanningAutofix {
        return try await CodeScanningMethods.codeScanningGetAutofix(config: config, owner: owner, repo: repo, alertNumber: alertNumber)
    }

/// Creates an autofix for a code scanning alert on a repository's default branch. Supply `owner`, `repo`, and `alert_number` to identify the repository and alert, and use the required authorization scope for the repository's visibility. A 202 response indicates that an autofix is being generated or newly created, while a 200 response indicates that an autofix already exists.
    ///
    /// Creates an autofix for a code scanning alert from the repository's default branch. If a new autofix is to be created as a result of this request or is currently being generated, then this endpoint will return a 202 Accepted response. If an autofix already exists for a given alert, then this endpoint will return a 200 OK response. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func createAutofix(owner: String, repo: String, alertNumber: AlertNumber) async throws -> CodeScanningAutofix {
        return try await CodeScanningMethods.codeScanningCreateAutofix(config: config, owner: owner, repo: repo, alertNumber: alertNumber)
    }

/// Commits an autofix for a code scanning alert from the repository's default branch. If an autofix is committed as a result of this request, then this endpoint will return a 201 Created response. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func commitAutofix(owner: String, repo: String, alertNumber: AlertNumber, targetRef: String?, message: String?) async throws -> CodeScanningAutofixCommitsResponse {
        return try await CodeScanningMethods.codeScanningCommitAutofix(config: config, owner: owner, repo: repo, alertNumber: alertNumber, targetRef: targetRef, message: message)
    }

/// Lists all instances of the specified code scanning alert. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func listAlertInstances(owner: String, repo: String, alertNumber: AlertNumber, page: Int?, perPage: Int?, ref: CodeScanningRef?, pr: Int?) async throws -> [CodeScanningAlertInstanceList] {
        return try await CodeScanningMethods.codeScanningListAlertInstances(config: config, owner: owner, repo: repo, alertNumber: alertNumber, page: page, perPage: perPage, ref: ref, pr: pr)
    }

/// Lists the details of all code scanning analyses for a repository, starting with the most recent. The response is paginated and you can use the `page` and `per_page` parameters to list the analyses you're interested in. By default 30 analyses are listed per page. The `rules_count` field in the response give the number of rules that were run in the analysis. For very old analyses this data is not available, and `0` is returned in this field. > [!WARNING] > **Closing down notice:** The `tool_name` field is closing down and will, in future, not be included in the response for this endpoint. The example response reflects this change. The tool name can now be found inside the `tool` field. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func listRecentAnalyses(options: CodeScanningMethods.CodeScanningListRecentAnalysesOptions) async throws -> [CodeScanningAnalysis] {
        return try await CodeScanningMethods.codeScanningListRecentAnalyses(config: config, options: options)
    }

/// Gets a specified code scanning analysis for a repository. The default JSON response contains fields that describe the analysis. This includes the Git reference and commit SHA to which the analysis relates, the datetime of the analysis, the name of the code scanning tool, and the number of alerts. The `rules_count` field in the default response give the number of rules that were run in the analysis. For very old analyses this data is not available, and `0` is returned in this field. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/sarif+json`**: Instead of returning a summary of the analysis, this endpoint returns a subset of the analysis data that was uploaded. The data is formatted as [SARIF version 2.1.0](https://docs.oasis-open.org/sarif/sarif/v2.1.0/cs01/sarif-v2.1.0-cs01.html). It also returns additional data such as the `github/alertNumber` and `github/alertUrl` properties. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getAnalysis(owner: String, repo: String, analysisId: Int) async throws -> CodeScanningAnalysis {
        return try await CodeScanningMethods.codeScanningGetAnalysis(config: config, owner: owner, repo: repo, analysisId: analysisId)
    }

/// Deletes a specified code scanning analysis from a repository. You can delete one analysis at a time. To delete a series of analyses, start with the most recent analysis and work backwards. Conceptually, the process is similar to the undo function in a text editor. When you list the analyses for a repository, one or more will be identified as deletable in the response: ``` "deletable": true ``` An analysis is deletable when it's the most recent in a set of analyses. Typically, a repository will have multiple sets of analyses for each enabled code scanning tool, where a set is determined by a unique combination of analysis values: * `ref` * `tool` * `category` If you attempt to delete an analysis that is not the most recent in a set, you'll get a 400 response with the message: ``` Analysis specified is not deletable. ``` The response from a successful `DELETE` operation provides you with two alternative URLs for deleting the next analysis in the set: `next_analysis_url` and `confirm_delete_url`. Use the `next_analysis_url` URL if you want to avoid accidentally deleting the final analysis in a set. This is a useful option if you want to preserve at least one analysis for the specified tool in your repository. Use the `confirm_delete_url` URL if you are content to remove all analyses for a tool. When you delete the last analysis in a set, the value of `next_analysis_url` and `confirm_delete_url` in the 200 response is `null`. As an example of the deletion process, let's imagine that you added a workflow that configured a particular code scanning tool to analyze the code in a repository. This tool has added 15 analyses: 10 on the default branch, and another 5 on a topic branch. You therefore have two separate sets of analyses for this tool. You've now decided that you want to remove all of the analyses for the tool. To do this you must make 15 separate deletion requests. To start, you must find an analysis that's identified as deletable. Each set of analyses always has…
    public func deleteAnalysis(owner: String, repo: String, analysisId: Int, confirmDelete: String?) async throws -> CodeScanningAnalysisDeletion {
        return try await CodeScanningMethods.codeScanningDeleteAnalysis(config: config, owner: owner, repo: repo, analysisId: analysisId, confirmDelete: confirmDelete)
    }

/// Lists the CodeQL databases that are available in a repository. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func listCodeqlDatabases(owner: String, repo: String) async throws -> [CodeScanningCodeqlDatabase] {
        return try await CodeScanningMethods.codeScanningListCodeqlDatabases(config: config, owner: owner, repo: repo)
    }
}

extension CodeScanningNamespace {
/// Gets a CodeQL database for a language in a repository. By default this endpoint returns JSON metadata about the CodeQL database. To download the CodeQL database binary content, set the `Accept` header of the request to [`application/zip`](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types), and make sure your HTTP client is configured to follow redirects or use the `Location` header to make a second request to get the redirect URL. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getCodeqlDatabase(owner: String, repo: String, language: String) async throws -> CodeScanningCodeqlDatabase {
        return try await CodeScanningMethods.codeScanningGetCodeqlDatabase(config: config, owner: owner, repo: repo, language: language)
    }

/// Deletes a CodeQL database for a language in a repository. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func deleteCodeqlDatabase(owner: String, repo: String, language: String) async throws -> SdkEmptyResponse {
        return try await CodeScanningMethods.codeScanningDeleteCodeqlDatabase(config: config, owner: owner, repo: repo, language: language)
    }

/// Creates a new CodeQL variant analysis, which will run a CodeQL query against one or more repositories. Get started by learning more about [running CodeQL queries at scale with Multi-Repository Variant Analysis](https://docs.github.com/code-security/codeql-for-vs-code/getting-started-with-codeql-for-vs-code/running-codeql-queries-at-scale-with-multi-repository-variant-analysis). Use the `owner` and `repo` parameters in the URL to specify the controller repository that will be used for running GitHub Actions workflows and storing the results of the CodeQL variant analysis. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
    public func createVariantAnalysis(owner: String, repo: String, language: CodeScanningVariantAnalysisLanguage, queryPack: String, repositories: [String]?, repositoryLists: [String]?, repositoryOwners: [String]?) async throws -> CodeScanningVariantAnalysis {
        return try await CodeScanningMethods.codeScanningCreateVariantAnalysis(config: config, owner: owner, repo: repo, language: language, queryPack: queryPack, repositories: repositories, repositoryLists: repositoryLists, repositoryOwners: repositoryOwners)
    }

/// Gets the summary of a CodeQL variant analysis. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getVariantAnalysis(owner: String, repo: String, codeqlVariantAnalysisId: Int) async throws -> CodeScanningVariantAnalysis {
        return try await CodeScanningMethods.codeScanningGetVariantAnalysis(config: config, owner: owner, repo: repo, codeqlVariantAnalysisId: codeqlVariantAnalysisId)
    }

/// Gets the analysis status of a repository in a CodeQL variant analysis. OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getVariantAnalysisRepoTask(owner: String, repo: String, codeqlVariantAnalysisId: Int, repoOwner: String, repoName: String) async throws -> CodeScanningVariantAnalysisRepoTask {
        return try await CodeScanningMethods.codeScanningGetVariantAnalysisRepoTask(config: config, owner: owner, repo: repo, codeqlVariantAnalysisId: codeqlVariantAnalysisId, repoOwner: repoOwner, repoName: repoName)
    }

/// Gets a code scanning default setup configuration. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getDefaultSetup(owner: String, repo: String) async throws -> CodeScanningDefaultSetup {
        return try await CodeScanningMethods.codeScanningGetDefaultSetup(config: config, owner: owner, repo: repo)
    }

/// Updates a code scanning default setup configuration. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func updateDefaultSetup(options: CodeScanningMethods.CodeScanningUpdateDefaultSetupOptions) async throws -> EmptyObject {
        return try await CodeScanningMethods.codeScanningUpdateDefaultSetup(config: config, options: options)
    }

/// Uploads SARIF data containing the results of a code scanning analysis to make the results available in a repository. For troubleshooting information, see "[Troubleshooting SARIF uploads](https://docs.github.com/code-security/code-scanning/troubleshooting-sarif)." There are two places where you can upload code scanning results. - If you upload to a pull request, for example `--ref refs/pull/42/merge` or `--ref refs/pull/42/head`, then the results appear as alerts in a pull request check. For more information, see "Triaging code scanning alerts in pull requests." - If you upload to a branch, for example `--ref refs/heads/my-branch`, then the results appear in the **Security** tab for your repository. For more information, see "Managing code scanning alerts for your repository." You must compress the SARIF-formatted analysis data that you want to upload, using `gzip`, and then encode it as a Base64 format string. For example: ``` gzip -c analysis-data.sarif | base64 -w0 ``` SARIF upload supports a maximum number of entries per the following data objects, and an analysis will be rejected if any of these objects is above its maximum value. For some objects, there are additional values over which the entries will be ignored while keeping the most important entries whenever applicable. To get the most out of your analysis when it includes data above the supported limits, try to optimize the analysis configuration. For example, for the CodeQL tool, identify and remove the most noisy queries. For more information, see "[SARIF results exceed one or more limits](https://docs.github.com/code-security/code-scanning/troubleshooting-sarif/results-exceed-limit)." | **SARIF data** | **Maximum values** | **Additional limits** | |----------------------------------|:------------------:|----------------------------------------------------------------------------------| | Runs per file | 20 | | | Results per run | 25,000 | Only the top 5,000 results will be included, prioritized by…
    public func uploadSarif(options: CodeScanningMethods.CodeScanningUploadSarifOptions) async throws -> CodeScanningSarifsReceipt {
        return try await CodeScanningMethods.codeScanningUploadSarif(config: config, options: options)
    }
}

extension CodeScanningNamespace {
/// Gets information about a SARIF upload, including the status and the URL of the analysis that was uploaded so that you can retrieve details of the analysis. For more information, see "Get a code scanning analysis for a repository." OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    public func getSarif(owner: String, repo: String, sarifId: String) async throws -> CodeScanningSarifsStatus {
        return try await CodeScanningMethods.codeScanningGetSarif(config: config, owner: owner, repo: repo, sarifId: sarifId)
    }
}
