// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension IssuesMethods {
    public struct IssuesListForOrgOptions: Codable {
        public var org: String
        public var filter: IssuesListForOrgParameter?
        public var state: IssuesListForOrgParameterX5317c381?
        public var labels: String?
        public var type: String?
        public var sort: IssuesListForOrgParameterX75c05bfe?
        public var direction: SecurityAdvisoriesListGlobalAdvisoriesParameter?
        public var since: Date?
        public var perPage: Int?
        public var page: Int?

        public init(org: String) {
            self.org = org
        }
    }

    /// List issues in an organization assigned to the authenticated user. > [!NOTE] > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type. - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`. - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`. - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - filter: Indicates which sorts of issues to return. `assigned` means issues
    ///   assigned to you. `created` means issues created by you. `mentioned` means
    ///   issues mentioning you. `subscribed` means issues you're subscribed to
    ///   updates for. `all` or `repos` means all issues you can see, regardless of
    ///   participation or creation.
    /// - state: Indicates the state of the issues to return.
    /// - labels: A list of comma separated label names. Example: `bug,ui,@high`
    /// - type: Can be the name of an issue type.
    /// - sort: What to sort results by.
    /// - direction: The direction to sort the results by.
    /// - since: Only show results that were last updated after the given time. This
    ///   is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format:
    ///   `YYYY-MM-DDTHH:MM:SSZ`.
    /// - perPage: The number of results per page (max 100). For more information,
    ///   see "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - page: The page number of the results to fetch. For more information, see
    ///   "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    public static func issuesListForOrg(config: ClientConfig, options: IssuesListForOrgOptions) async throws -> [Issue] {
        if let since = options.since {
            try sdkValidateDateTime("since", since)
        }

        return try (await sdkRequest("GET", ["/orgs/", sdkEncodePathSegment(sdkWireString(options.org)), "/issues"].joined(), config: config, query: [
            SdkQueryParameter("filter", value: options.filter),
            SdkQueryParameter("state", value: options.state),
            SdkQueryParameter("labels", value: options.labels),
            SdkQueryParameter("type", value: options.type),
            SdkQueryParameter("sort", value: options.sort),
            SdkQueryParameter("direction", value: options.direction),
            SdkQueryParameter("since", value: options.since),
            SdkQueryParameter("per_page", value: options.perPage),
            SdkQueryParameter("page", value: options.page),
        ], decoder: .json, operationId: "issuesListForOrg")).data
    }
}
