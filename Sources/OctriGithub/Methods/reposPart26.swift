// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension ReposMethods {
    struct ReposUpdatePullRequestReviewProtectionOptions: Codable {
        public var owner: String
        public var repo: String
        public var branch: String
        public var dismissalRestrictions: ReposUpdatePullRequestReviewProtectionRequestBodyDismissalRestrictions?
        public var dismissStaleReviews: Bool?
        public var requireCodeOwnerReviews: Bool?
        public var requiredApprovingReviewCount: Int?
        public var requireLastPushApproval: Bool?
        public var bypassPullRequestAllowances: ReposUpdatePullRequestReviewProtectionRequestBodyBypassPullReX4b5a4bd7de?

        public init(owner: String, repo: String, branch: String) {
            self.owner = owner
            self.repo = repo
            self.branch = branch
        }
    }

    /// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and
    /// in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise
    /// Server. For more information, see [GitHub's
    /// products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help
    /// documentation. Updating pull request review enforcement requires admin or owner permissions to the repository
    /// and branch protection to be enabled. > [!NOTE] > Passing new arrays of `users` and `teams` replaces their
    /// previous values.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    /// - branch: The name of the branch. Cannot contain wildcard characters. To use
    ///   wildcard characters in branch names, use [the GraphQL
    ///   API](https://docs.github.com/graphql).
    /// - dismissalRestrictions: Specify which users, teams, and apps can dismiss
    ///   pull request reviews. Pass an empty `dismissal_restrictions` object to
    ///   disable. User and team `dismissal_restrictions` are only available for
    ///   organization-owned repositories. Omit this parameter for personal
    ///   repositories.
    /// - dismissStaleReviews: Set to `true` if you want to automatically dismiss
    ///   approving reviews when someone pushes a new commit.
    /// - requireCodeOwnerReviews: Blocks merging pull requests until [code
    ///   owners](https://docs.github.com/articles/about-code-owners/) have reviewed.
    /// - requiredApprovingReviewCount: Specifies the number of reviewers required
    ///   to approve pull requests. Use a number between 1 and 6 or 0 to not require
    ///   reviewers.
    /// - requireLastPushApproval: Whether the most recent push must be approved by
    ///   someone other than the person who pushed it. Default: `false`
    /// - bypassPullRequestAllowances: Allow specific users, teams, or apps to
    ///   bypass pull request requirements.
    static func reposUpdatePullRequestReviewProtection(
        config: ClientConfig,
        options: ReposUpdatePullRequestReviewProtectionOptions
    ) async throws -> ProtectedBranchPullRequestReview {
        let requestBody = ReposUpdatePullRequestReviewProtectionRequestBody(options: options)

        return try await (sdkRequest(
            "PATCH",
            [
                "/repos/",
                sdkEncodePathSegment(sdkWireString(options.owner)),
                "/",
                sdkEncodePathSegment(sdkWireString(options.repo)),
                "/branches/",
                sdkEncodePathSegment(sdkWireString(options.branch)),
                "/protection/required_pull_request_reviews",
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "reposUpdatePullRequestReviewProtection"
        )).data
    }
}
