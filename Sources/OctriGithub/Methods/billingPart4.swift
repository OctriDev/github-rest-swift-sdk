// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension BillingMethods {
    struct BillingCreateOrganizationBudgetOptions: Codable {
        public var org: String
        public var budgetAmount: Int?
        public var preventFurtherUsage: Bool?
        public var budgetAlerting: BillingCreateOrganizationBudgetRequestBodyBudgetAlerting?
        public var budgetScope: BillingCreateOrganizationBudgetRequestBodyBudgetScope?
        public var budgetEntityName: String?
        public var budgetType: BillingCreateOrganizationBudgetRequestBodyBudgetTypeVariant0?
        public var budgetProductSku: String?
        public var user: String?
        public var expiresAt: String?

        public init(org: String) {
            self.org = org
        }
    }

    /// Creates a new budget for an organization. Supply the budget scope, amount, pricing information, and any
    /// applicable alerting or expiration settings; the authenticated user must be an organization administrator or
    /// billing manager. Use `budget_entity_name` to target a repository or user when the selected scope requires a
    /// specific entity.
    ///
    /// Creates a new budget for an organization. The authenticated user must be an organization admin or billing
    /// manager.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - budgetAmount: The budget amount in whole dollars. For license-based
    ///   products, this represents the number of licenses.
    /// - preventFurtherUsage: Whether to prevent additional spending once the
    ///   budget is exceeded. For `user` and `multi_user_customer` scopes, this must
    ///   be `true`.
    /// - budgetScope: The scope of the budget for this organization. -
    ///   `organization`: Apply the budget to the organization. - `repository`: Apply
    ///   the budget to a specific repository in the organization. -
    ///   `multi_user_customer`: Apply a universal budget to all users in the
    ///   organization. - `user`: Apply the budget to a single user in the
    ///   organization. `user` and `multi_user_customer` scopes are only supported
    ///   when `budget_product_sku` is `ai_credits` or `premium_requests`.
    /// - budgetEntityName: The name of the entity to apply the budget to
    /// - budgetType: The type of pricing model used by the budget. Determines how
    ///   `budget_product_sku` is interpreted. - `BundlePricing`: Covers all AI credit
    ///   SKUs. Set `budget_product_sku` to `ai_credits`. - `ProductPricing`: Covers
    ///   all SKUs that belong to a product. Set `budget_product_sku` to a product
    ///   such as `actions` or `packages`. - `SkuPricing`: Covers a single, specific
    ///   SKU. Set `budget_product_sku` to a SKU such as `actions_linux`.
    /// - budgetProductSku: A single product or SKU that will be covered in the
    ///   budget
    /// - user: The username of the user for `user` scope budgets. This field is
    ///   required when `budget_scope` is `user`.
    /// - expiresAt: The date the budget will expire in `YYYY-MM-DD` format. Only
    ///   dates in the future are accepted. If not provided, the budget will not
    ///   expire. Only supported for budgets with `budget_scope` of `user`
    static func billingCreateOrganizationBudget(
        config: ClientConfig,
        options: BillingCreateOrganizationBudgetOptions
    ) async throws -> CreateBudget {
        if let expiresAt = options.expiresAt {
            try sdkValidateDate("expires_at", expiresAt)
        }

        let requestBody = BillingCreateOrganizationBudgetRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            ["/organizations/", sdkEncodePathSegment(sdkWireString(options.org)), "/settings/billing/budgets"].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "billingCreateOrganizationBudget"
        )).data
    }
}
