// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension CodeScanningMethods {
    public struct CodeScanningListAlertsForRepoOptions: Codable {
        public var owner: String
        public var repo: String
        public var toolName: CodeScanningAnalysisToolName?
        public var toolGuid: CodeScanningAnalysisToolGuid?
        public var page: Int?
        public var perPage: Int?
        public var ref: CodeScanningRef?
        public var pr: Int?
        public var direction: SecurityAdvisoriesListGlobalAdvisoriesParameter?
        public var before: String?
        public var after: String?
        public var sort: CodeScanningListAlertsForRepoParameter?
        public var state: CodeScanningAlertStateQuery?
        public var severity: CodeScanningAlertSeverity?
        public var assignees: String?

        public init(owner: String, repo: String) {
            self.owner = owner
            self.repo = repo
        }
    }

    /// Lists code scanning alerts. The response includes a `most_recent_instance` object. This provides details of the most recent instance of this alert for the default branch (or for the specified Git reference if you used `ref` in the request). OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    /// - toolName: The name of a code scanning tool. Only results by this tool will
    ///   be listed. You can specify the tool by using either `tool_name` or
    ///   `tool_guid`, but not both.
    /// - toolGuid: The GUID of a code scanning tool. Only results by this tool will
    ///   be listed. Note that some code scanning tools may not include a GUID in
    ///   their analysis data. You can specify the tool by using either `tool_guid` or
    ///   `tool_name`, but not both.
    /// - page: The page number of the results to fetch. For more information, see
    ///   "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - perPage: The number of results per page (max 100). For more information,
    ///   see "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - ref: The Git reference for the results you want to list. The `ref` for a
    ///   branch can be formatted either as `refs/heads/<branch name>` or simply
    ///   `<branch name>`. To reference a pull request use `refs/pull/<number>/merge`.
    /// - pr: The number of the pull request for the results you want to list.
    /// - direction: The direction to sort the results by.
    /// - before: A cursor, as given in the [Link
    ///   header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api
    ///   #using-link-headers). If specified, the query only searches for results
    ///   before this cursor. For more information, see "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - after: A cursor, as given in the [Link
    ///   header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api
    ///   #using-link-headers). If specified, the query only searches for results
    ///   after this cursor. For more information, see "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - sort: The property by which to sort the results.
    /// - state: If specified, only code scanning alerts with this state will be
    ///   returned.
    /// - severity: If specified, only code scanning alerts with this severity will
    ///   be returned.
    /// - assignees: Filter alerts by assignees. Provide a comma-separated list of
    ///   user handles (e.g., `octocat` or `octocat,hubot`). Use `*` to list alerts
    ///   with at least one assignee or `none` to list alerts with no assignees.
    public static func codeScanningListAlertsForRepo(config: ClientConfig, options: CodeScanningListAlertsForRepoOptions) async throws -> [CodeScanningAlertItems] {
        return try (await sdkRequest("GET", ["/repos/", sdkEncodePathSegment(sdkWireString(options.owner)), "/", sdkEncodePathSegment(sdkWireString(options.repo)), "/code-scanning/alerts"].joined(), config: config, query: [
            SdkQueryParameter("tool_name", value: options.toolName),
            SdkQueryParameter("tool_guid", value: options.toolGuid),
            SdkQueryParameter("page", value: options.page),
            SdkQueryParameter("per_page", value: options.perPage),
            SdkQueryParameter("ref", value: options.ref),
            SdkQueryParameter("pr", value: options.pr),
            SdkQueryParameter("direction", value: options.direction),
            SdkQueryParameter("before", value: options.before),
            SdkQueryParameter("after", value: options.after),
            SdkQueryParameter("sort", value: options.sort),
            SdkQueryParameter("state", value: options.state),
            SdkQueryParameter("severity", value: options.severity),
            SdkQueryParameter("assignees", value: options.assignees),
        ], decoder: .json, operationId: "codeScanningListAlertsForRepo")).data
    }
}
