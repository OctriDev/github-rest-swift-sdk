// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension ReposMethods {
    struct ReposCreateDeploymentStatusOptions: Codable {
        public var owner: String
        public var repo: String
        public var deploymentId: Int
        public var state: ReposCreateDeploymentStatusRequestBodyState
        public var targetUrl: String?
        public var logUrl: String?
        public var description: String?
        public var environment: String?
        public var environmentUrl: String?
        public var autoInactive: Bool?

        public init(
            owner: String,
            repo: String,
            deploymentId: Int,
            state: ReposCreateDeploymentStatusRequestBodyState
        ) {
            self.owner = owner
            self.repo = repo
            self.deploymentId = deploymentId
            self.state = state
        }
    }

    /// Users with `push` access can create deployment statuses for a given deployment. OAuth app tokens and personal
    /// access tokens (classic) need the `repo_deployment` scope to use this endpoint.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    /// - deploymentId: deployment_id parameter
    /// - state: The state of the status. When you set a transient deployment to
    ///   `inactive`, the deployment will be shown as `destroyed` in GitHub.
    /// - targetUrl: The target URL to associate with this status. This URL should
    ///   contain output to keep the user updated while the task is running or serve
    ///   as historical information for what happened in the deployment. > [!NOTE] >
    ///   It's recommended to use the `log_url` parameter, which replaces
    ///   `target_url`.
    /// - logUrl: The full URL of the deployment's output. This parameter replaces
    ///   `target_url`. We will continue to accept `target_url` to support legacy
    ///   uses, but we recommend replacing `target_url` with `log_url`. Setting
    ///   `log_url` will automatically set `target_url` to the same value. Default:
    ///   `""`
    /// - description: A short description of the status. The maximum description
    ///   length is 140 characters.
    /// - environment: Name for the target deployment environment, which can be
    ///   changed when setting a deploy status. For example, `production`, `staging`,
    ///   or `qa`. If not defined, the environment of the previous status on the
    ///   deployment will be used, if it exists. Otherwise, the environment of the
    ///   deployment will be used.
    /// - environmentUrl: Sets the URL for accessing your environment. Default: `""`
    /// - autoInactive: Adds a new `inactive` status to all prior non-transient,
    ///   non-production environment deployments with the same repository and
    ///   `environment` name as the created status's deployment. An `inactive` status
    ///   is only added to deployments that had a `success` state. Default: `true`
    static func reposCreateDeploymentStatus(
        config: ClientConfig,
        options: ReposCreateDeploymentStatusOptions
    ) async throws -> DeploymentStatus {
        let requestBody = ReposCreateDeploymentStatusRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            [
                "/repos/",
                sdkEncodePathSegment(sdkWireString(options.owner)),
                "/",
                sdkEncodePathSegment(sdkWireString(options.repo)),
                "/deployments/",
                sdkEncodePathSegment(sdkWireString(options.deploymentId)),
                "/statuses",
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "reposCreateDeploymentStatus"
        )).data
    }
}
