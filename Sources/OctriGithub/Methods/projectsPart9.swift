// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension ProjectsMethods {
    struct ProjectsCreateViewForOrgOptions: Codable {
        public var org: String
        public var projectNumber: Int
        public var name: String
        public var layout: ProjectsCreateViewForOrgRequestBodyLayout
        public var filter: String?
        public var visibleFields: [Int]?
        public var sortBy: [[ProjectsCreateViewForOrgRequestBodySortByItemItem]]?
        public var groupBy: [Int]?
        public var verticalGroupBy: [Int]?

        public init(org: String, projectNumber: Int, name: String, layout: ProjectsCreateViewForOrgRequestBodyLayout) {
            self.org = org
            self.projectNumber = projectNumber
            self.name = name
            self.layout = layout
        }
    }

    /// Create a new view in an organization-owned project. Views allow you to customize how items in a project are
    /// displayed and filtered.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - projectNumber: The project's number.
    /// - name: The name of the view.
    /// - layout: The layout of the view.
    /// - filter: The filter query for the view. See [Filtering
    ///   projects](https://docs.github.com/issues/planning-and-tracking-with-projects
    ///   /customizing-views-in-your-project/filtering-projects) for more information.
    /// - visibleFields: `visible_fields` is not applicable to `roadmap` layout
    ///   views. For `table` and `board` layouts, this represents the field IDs that
    ///   should be visible in the view. If not provided, the default visible fields
    ///   will be used.
    /// - sortBy: Sorting configuration for the view. Each element is a two-element
    ///   array of `[field_id, direction]` where `direction` is `"asc"` or `"desc"`.
    ///   Supports multiple sort criteria applied in order.
    /// - groupBy: The field IDs to group items by (horizontal grouping). Supports a
    ///   single field. The field must support grouping; fields such as `Title`,
    ///   `Reviewers`, `Linked pull requests`, `Sub-issues progress`, `Tracked by`,
    ///   and `Tracks` cannot be grouped on.
    /// - verticalGroupBy: The field IDs to use as columns in `board` layout
    ///   (vertical grouping). Supports a single field. The field must support
    ///   grouping; fields such as `Title`, `Reviewers`, `Linked pull requests`,
    ///   `Sub-issues progress`, `Tracked by`, and `Tracks` cannot be grouped on.
    static func projectsCreateViewForOrg(
        config: ClientConfig,
        options: ProjectsCreateViewForOrgOptions
    ) async throws -> ProjectsV2View {
        if let groupBy = options.groupBy {
            try validateItems("group_by", groupBy, max: 1)
        }

        if let verticalGroupBy = options.verticalGroupBy {
            try validateItems("vertical_group_by", verticalGroupBy, max: 1)
        }

        let requestBody = ProjectsCreateViewForOrgRequestBody(options: options)

        return try await (sdkRequest(
            "POST",
            [
                "/orgs/",
                sdkEncodePathSegment(sdkWireString(options.org)),
                "/projectsV2/",
                sdkEncodePathSegment(sdkWireString(options.projectNumber)),
                "/views",
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "projectsCreateViewForOrg"
        )).data
    }
}
