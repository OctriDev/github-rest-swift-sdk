// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension OrgsMethods {
    struct OrgsListPatGrantsOptions: Codable {
        public var org: String
        public var perPage: Int?
        public var page: Int?
        public var sort: OrgsListPatGrantRequestsParameter?
        public var direction: SecurityAdvisoriesListGlobalAdvisoriesParameter?
        public var owner: [String]?
        public var repository: String?
        public var permission: String?
        public var lastUsedBefore: Date?
        public var lastUsedAfter: Date?
        public var tokenId: [String]?

        public init(org: String) {
            self.org = org
        }
    }

    /// Lists approved fine-grained personal access tokens owned by organization members that can access organization
    /// resources. Only GitHub Apps can use this endpoint.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - perPage: The number of results per page (max 100). For more information,
    ///   see "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - page: The page number of the results to fetch. For more information, see
    ///   "[Using pagination in the REST
    ///   API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the
    ///   -rest-api)."
    /// - sort: The property by which to sort the results.
    /// - direction: The direction to sort the results by.
    /// - owner: A list of owner usernames to use to filter the results.
    /// - repository: The name of the repository to use to filter the results.
    /// - permission: The permission to use to filter the results.
    /// - lastUsedBefore: Only show fine-grained personal access tokens used before
    ///   the given time. This is a timestamp in [ISO
    ///   8601](https://en.wikipedia.org/wiki/ISO_8601) format:
    ///   `YYYY-MM-DDTHH:MM:SSZ`.
    /// - lastUsedAfter: Only show fine-grained personal access tokens used after
    ///   the given time. This is a timestamp in [ISO
    ///   8601](https://en.wikipedia.org/wiki/ISO_8601) format:
    ///   `YYYY-MM-DDTHH:MM:SSZ`.
    /// - tokenId: The ID of the token
    static func orgsListPatGrants(
        config: ClientConfig,
        options: OrgsListPatGrantsOptions
    ) async throws -> [OrganizationProgrammaticAccessGrant] {
        if let owner = options.owner {
            try validateItems("owner", owner, max: 10)
        }

        if let lastUsedBefore = options.lastUsedBefore {
            try sdkValidateDateTime("last_used_before", lastUsedBefore)
        }

        if let lastUsedAfter = options.lastUsedAfter {
            try sdkValidateDateTime("last_used_after", lastUsedAfter)
        }

        if let tokenId = options.tokenId {
            try validateItems("token_id", tokenId, max: 50)
        }

        return try await (sdkRequest(
            "GET",
            ["/orgs/", sdkEncodePathSegment(sdkWireString(options.org)), "/personal-access-tokens"].joined(),
            config: config,
            query: [
                SdkQueryParameter("per_page", value: options.perPage),
                SdkQueryParameter("page", value: options.page),
                SdkQueryParameter("sort", value: options.sort),
                SdkQueryParameter("direction", value: options.direction),
                SdkQueryParameter("owner", values: options.owner, style: "form", explode: true),
                SdkQueryParameter("repository", value: options.repository),
                SdkQueryParameter("permission", value: options.permission),
                SdkQueryParameter("last_used_before", value: options.lastUsedBefore),
                SdkQueryParameter("last_used_after", value: options.lastUsedAfter),
                SdkQueryParameter("token_id", values: options.tokenId, style: "form", explode: true),
            ],
            decoder: .json,
            operationId: "orgsListPatGrants"
        )).data
    }
}
