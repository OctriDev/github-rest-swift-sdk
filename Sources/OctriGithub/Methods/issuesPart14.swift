// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension IssuesMethods {
    struct IssuesUpdateOptions: Codable {
        public var owner: String
        public var repo: String
        public var issueNumber: Int
        public var title: SdkOptional<IssuesUpdateRequestBodyTitle>?
        public var body: SdkOptional<String>?
        public var assignee: SdkOptional<String>?
        public var state: IssuesUpdateRequestBodyState?
        public var stateReason: SdkOptional<IssuesUpdateRequestBodyStateReason>?
        public var duplicateIssueId: Int?
        public var milestone: SdkOptional<IssuesUpdateRequestBodyMilestone>?
        public var labels: [IssuesUpdateRequestBodyLabelsItem]?
        public var assignees: [IssuesUpdateRequestBodyAssigneesItem]?
        public var issueFieldValues: [IssuesUpdateRequestBodyIssueFieldValuesItem]?
        public var type: SdkOptional<IssuesUpdateRequestBodyType>?

        public init(owner: String, repo: String, issueNumber: Int) {
            self.owner = owner
            self.repo = repo
            self.issueNumber = issueNumber
        }
    }

    /// Updates an existing issue or pull request in a repository. Supply only the fields you want to change, including
    /// `title`, `body`, `state`, labels, assignees, milestones, or issue field values. Changes to labels, assignees,
    /// milestones, and issue types may be silently dropped without push access.
    ///
    /// Issue owners and users with push access or Triage role can edit an issue. This endpoint supports the following
    /// custom media types. For more information, see "[Media
    /// types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." -
    /// **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the
    /// default if you do not pass any specific media type. - **`application/vnd.github.text+json`**: Returns a text
    /// only representation of the markdown body. Response will include `body_text`. -
    /// **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include
    /// `body_html`. - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response
    /// will include `body`, `body_text`, and `body_html`.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    /// - issueNumber: The number that identifies the issue.
    /// - title: The title of the issue.
    /// - body: The contents of the issue.
    /// - assignee: Username to assign to this issue. **This field is closing
    ///   down.**
    /// - state: The open or closed state of the issue.
    /// - stateReason: The reason for the state change. Ignored unless `state` is
    ///   changed.
    /// - duplicateIssueId: The ID of the issue to mark as the canonical duplicate
    ///   when `state_reason` is `duplicate`. The issue must exist and be accessible
    ///   to the authenticated user. Ignored when `state_reason` is not `duplicate`.
    /// - labels: Labels to associate with this issue. Pass one or more labels to
    ///   _replace_ the set of labels on this issue. Send an empty array (`[]`) to
    ///   clear all labels from the issue. Only users with push access can set labels
    ///   for issues. Without push access to the repository, label changes are
    ///   silently dropped.
    /// - assignees: Usernames to assign to this issue. Pass one or more user logins
    ///   to _replace_ the set of assignees on this issue. Send an empty array (`[]`)
    ///   to clear all assignees from the issue. Only users with push access can set
    ///   assignees for new issues. Without push access to the repository, assignee
    ///   changes are silently dropped.
    /// - issueFieldValues: An array of issue field values to set on this issue.
    ///   Each field value must include the field ID and the value to set. Only users
    ///   with push access can set field values for issues
    /// - type: The issue type to associate with this issue. Only users with push
    ///   access can set the type for issues. Without push access to the repository,
    ///   type changes are silently dropped.
    static func issuesUpdate(config: ClientConfig, options: IssuesUpdateOptions) async throws -> IssuesUpdateResponse {
        let requestBody = IssuesUpdateRequestBody(options: options)

        return try await (sdkRequest(
            "PATCH",
            [
                "/repos/",
                sdkEncodePathSegment(sdkWireString(options.owner)),
                "/",
                sdkEncodePathSegment(sdkWireString(options.repo)),
                "/issues/",
                sdkEncodePathSegment(sdkWireString(options.issueNumber)),
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "issuesUpdate"
        )).data
    }
}
