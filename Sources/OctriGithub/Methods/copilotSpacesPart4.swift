// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension CopilotSpacesMethods {
    /// Updates a Copilot Space owned by an organization. The authenticated user must have permissions to update spaces in the organization. Organization members with appropriate permissions can update Copilot Spaces owned by their organization. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space, including any being added or updated.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - spaceNumber: The unique identifier of the Copilot Space.
    /// - name: The name of the Copilot Space.
    /// - description: A description of the Copilot Space.
    /// - generalInstructions: General instructions for the Copilot Space.
    /// - baseRole: The base role that determines default permissions for
    ///   organization members. Changing this field requires admin permissions. -
    ///   `no_access`: No default access (default) - `reader`: Organization members
    ///   can read the space - `writer`: Organization members can read and edit the
    ///   space - `admin`: Organization members have full admin access to the space
    /// - resourcesAttributes: Resources to attach to the space.
    public static func copilotSpacesUpdateForOrg(config: ClientConfig, org: String, spaceNumber: Int, name: String?, description: String?, generalInstructions: String?, baseRole: CopilotSpacesUpdateForOrgRequestBodyBaseRole?, resourcesAttributes: [CopilotSpacesUpdateForOrgRequestBodyResourcesAttributesItem]?) async throws -> CopilotSpace {
        if let generalInstructions = generalInstructions {
            try validateLength("general_instructions", generalInstructions, max: 4000)
        }

        let requestBody = CopilotSpacesUpdateForOrgRequestBody(name: name, description: description, generalInstructions: generalInstructions, baseRole: baseRole, resourcesAttributes: resourcesAttributes)

        return try (await sdkRequest("PUT", ["/orgs/", sdkEncodePathSegment(sdkWireString(org)), "/copilot-spaces/", sdkEncodePathSegment(sdkWireString(spaceNumber))].joined(), config: config, body: requestBody, decoder: .json, operationId: "copilotSpacesUpdateForOrg")).data
    }

    /// Deletes a Copilot Space owned by an organization. The authenticated user must have permissions to delete spaces in the organization. **Warning:** This action is permanent and cannot be undone. Deleting a Copilot Space will remove all associated resources and configurations. Organization members with appropriate permissions can delete Copilot Spaces owned by their organization. OAuth app tokens and personal access tokens (classic) need both the `read:org` and `repo` scopes to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - spaceNumber: The unique identifier of the Copilot Space.
    public static func copilotSpacesDeleteForOrg(config: ClientConfig, org: String, spaceNumber: Int) async throws -> SdkEmptyResponse {
        return try (await sdkRequest("DELETE", ["/orgs/", sdkEncodePathSegment(sdkWireString(org)), "/copilot-spaces/", sdkEncodePathSegment(sdkWireString(spaceNumber))].joined(), config: config, decoder: .empty, operationId: "copilotSpacesDeleteForOrg")).data
    }
}
