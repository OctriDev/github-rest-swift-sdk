// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension ReposMethods {
    /// Merges a head branch or commit into a base branch in a repository. Supply `base` and `head` to identify the
    /// branches or commit involved, and optionally provide `commit_message` for the resulting merge commit. A
    /// successful merge returns the resulting commit, while an already completed merge returns no content.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    /// - base: The name of the base branch that the head will be merged into.
    /// - head: The head to merge. This can be a branch name or a commit SHA1.
    /// - commitMessage: Commit message to use for the merge commit. If omitted, a
    ///   default message will be used.
    static func reposMerge(
        config: ClientConfig,
        owner: String,
        repo: String,
        base: String,
        head: String,
        commitMessage: String?
    ) async throws -> Commit {
        let requestBody = ReposMergeRequestBody(base: base, head: head, commitMessage: commitMessage)

        return try await (sdkRequest(
            "POST",
            [
                "/repos/",
                sdkEncodePathSegment(sdkWireString(owner)),
                "/",
                sdkEncodePathSegment(sdkWireString(repo)),
                "/merges",
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "reposMerge"
        )).data
    }

    /// Gets information about a GitHub Pages site. OAuth app tokens and personal access tokens (classic) need the
    /// `repo` scope to use this endpoint.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    static func reposGetPages(config: ClientConfig, owner: String, repo: String) async throws -> Page {
        try await (sdkRequest(
            "GET",
            [
                "/repos/",
                sdkEncodePathSegment(sdkWireString(owner)),
                "/",
                sdkEncodePathSegment(sdkWireString(repo)),
                "/pages",
            ].joined(),
            config: config,
            decoder: .json,
            operationId: "reposGetPages"
        )).data
    }
}
