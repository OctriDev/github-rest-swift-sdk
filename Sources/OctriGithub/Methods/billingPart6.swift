// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension BillingMethods {
    struct BillingUpdateBudgetOrgOptions: Codable {
        public var org: String
        public var budgetId: String
        public var budgetAmount: Int?
        public var preventFurtherUsage: Bool?
        public var budgetAlerting: BillingUpdateBudgetOrgRequestBodyBudgetAlerting?
        public var budgetScope: BillingUpdateBudgetOrgRequestBodyBudgetScope?
        public var budgetEntityName: String?
        public var budgetType: BillingUpdateBudgetOrgRequestBodyBudgetTypeVariant0?
        public var budgetProductSku: String?
        public var user: String?
        public var expiresAt: BillingUpdateBudgetOrgRequestBodyExpiresAt?

        public init(org: String, budgetId: String) {
            self.org = org
            self.budgetId = budgetId
        }
    }

    /// Updates an existing budget for an organization. Supply the budget fields you want to change, including
    /// `budget_amount`, `budget_scope`, or `prevent_further_usage`, while preserving scope-specific constraints. The
    /// authenticated user must be an organization administrator or billing manager.
    ///
    /// Updates an existing budget for an organization. The authenticated user must be an organization admin or billing
    /// manager.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - budgetId: The ID corresponding to the budget.
    /// - budgetAmount: The budget amount in whole dollars. For license-based
    ///   products, this represents the number of licenses.
    /// - preventFurtherUsage: Whether to prevent additional spending once the
    ///   budget is exceeded. For budgets with `user` or `multi_user_customer` scope,
    ///   this must remain `true`.
    /// - budgetScope: The scope of the budget for this organization. -
    ///   `organization`: Apply the budget to the organization. - `repository`: Apply
    ///   the budget to a specific repository in the organization. -
    ///   `multi_user_customer`: Apply a universal budget to all users in the
    ///   organization. - `user`: Apply the budget to a single user in the
    ///   organization.
    /// - budgetEntityName: The name of the entity to apply the budget to
    /// - budgetType: The type of pricing model used by the budget. Determines how
    ///   `budget_product_sku` is interpreted. - `BundlePricing`: Covers all AI credit
    ///   SKUs. Set `budget_product_sku` to `ai_credits`. - `ProductPricing`: Covers
    ///   all SKUs that belong to a product. Set `budget_product_sku` to a product
    ///   such as `actions` or `packages`. - `SkuPricing`: Covers a single, specific
    ///   SKU. Set `budget_product_sku` to a SKU such as `actions_linux`.
    /// - budgetProductSku: A single product or SKU that will be covered in the
    ///   budget
    /// - user: The username of the user for `user` scope budgets.
    /// - expiresAt: The date the budget will expire in `YYYY-MM-DD` format. Only
    ///   dates in the future are accepted. If not set, the budget will not expire.
    ///   Setting to `null` or `0` will remove the expiration date from a budget if
    ///   set. Only supported for budgets with `budget_scope` of `user`
    static func billingUpdateBudgetOrg(
        config: ClientConfig,
        options: BillingUpdateBudgetOrgOptions
    ) async throws -> UpdateBudget {
        let requestBody = BillingUpdateBudgetOrgRequestBody(options: options)

        return try await (sdkRequest(
            "PATCH",
            [
                "/organizations/",
                sdkEncodePathSegment(sdkWireString(options.org)),
                "/settings/billing/budgets/",
                sdkEncodePathSegment(sdkWireString(options.budgetId)),
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .json,
            operationId: "billingUpdateBudgetOrg"
        )).data
    }
}
