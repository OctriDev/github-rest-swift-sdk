// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
public extension PrivateRegistriesMethods {
    struct PrivateRegistriesUpdateOrgPrivateRegistryOptions: Codable {
        public var org: String
        public var secretName: String
        public var registryType: PrivateRegistriesUpdateOrgPrivateRegistryRequestBodyRegistryType?
        public var url: String?
        public var username: SdkOptional<String>?
        public var replacesBase: Bool?
        public var encryptedValue: String?
        public var keyId: String?
        public var visibility: PrivateRegistriesUpdateOrgPrivateRegistryRequestBodyVisibility?
        public var selectedRepositoryIds: [Int]?
        public var authType: PrivateRegistriesUpdateOrgPrivateRegistryRequestBodyAuthType?
        public var tenantId: String?
        public var clientId: String?
        public var awsRegion: String?
        public var accountId: String?
        public var roleName: String?
        public var domain: String?
        public var domainOwner: String?
        public var jfrogOidcProviderName: String?
        public var audience: String?
        public var identityMappingName: String?
        public var namespace: String?
        public var serviceSlug: String?
        public var apiHost: String?
        public var workloadIdentityProvider: String?
        public var serviceAccount: String?

        public init(org: String, secretName: String) {
            self.org = org
            self.secretName = secretName
        }
    }

    /// Updates a private registry configuration with an encrypted value for an organization. Encrypt your secret using
    /// [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see
    /// "[Encrypting secrets for the REST
    /// API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)." For OIDC-based registries
    /// (`oidc_azure`, `oidc_aws`, `oidc_jfrog`, `oidc_cloudsmith`, or `oidc_gcp`), the `encrypted_value` and `key_id`
    /// fields should be omitted. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to
    /// use this endpoint.
    ///
    /// - Parameters:
    /// - org: The organization name. The name is not case sensitive.
    /// - secretName: The name of the secret.
    /// - registryType: The registry type.
    /// - url: The URL of the private registry.
    /// - username: The username to use when authenticating with the private
    ///   registry. This field should be omitted if the private registry does not
    ///   require a username for authentication.
    /// - replacesBase: Whether this private registry should replace the base
    ///   registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When set to
    ///   `true`, Dependabot will only use this registry and will not fall back to the
    ///   public registry. When set to `false` (default), Dependabot will use this
    ///   registry for scoped packages but may fall back to the public registry for
    ///   other packages.
    /// - encryptedValue: The value for your secret, encrypted with
    ///   [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages)
    ///   using the public key retrieved from the [Get private registries public key
    ///   for an
    ///   organization](https://docs.github.com/rest/private-registries/organization-c
    ///   onfigurations#get-private-registries-public-key-for-an-organization)
    ///   endpoint.
    /// - keyId: The ID of the key you used to encrypt the secret.
    /// - visibility: Which type of organization repositories have access to the
    ///   private registry. `selected` means only the repositories specified by
    ///   `selected_repository_ids` can access the private registry.
    /// - selectedRepositoryIds: An array of repository IDs that can access the
    ///   organization private registry. You can only provide a list of repository IDs
    ///   when `visibility` is set to `selected`. This field should be omitted if
    ///   `visibility` is set to `all` or `private`.
    /// - authType: The authentication type for the private registry. This field
    ///   cannot be changed after creation. If provided, it must match the existing
    ///   `auth_type` of the configuration. To change the authentication type, delete
    ///   and recreate the configuration.
    /// - tenantId: The tenant ID of the Azure AD application. Required when
    ///   `auth_type` is `oidc_azure`.
    /// - clientId: The client ID of the Azure AD application. Required when
    ///   `auth_type` is `oidc_azure`.
    /// - awsRegion: The AWS region. Required when `auth_type` is `oidc_aws`.
    /// - accountId: The AWS account ID. Required when `auth_type` is `oidc_aws`.
    /// - roleName: The AWS IAM role name. Required when `auth_type` is `oidc_aws`.
    /// - domain: The CodeArtifact domain. Required when `auth_type` is `oidc_aws`.
    /// - domainOwner: The CodeArtifact domain owner (AWS account ID). Required when
    ///   `auth_type` is `oidc_aws`.
    /// - jfrogOidcProviderName: The JFrog OIDC provider name. Required when
    ///   `auth_type` is `oidc_jfrog`.
    /// - audience: The OIDC audience. Optional for `oidc_aws`, `oidc_jfrog`, and
    ///   `oidc_gcp`, and required for `oidc_cloudsmith` auth types.
    /// - identityMappingName: The JFrog identity mapping name. Optional for
    ///   `oidc_jfrog` auth type.
    /// - namespace: The Cloudsmith organization namespace. Required when
    ///   `auth_type` is `oidc_cloudsmith`.
    /// - serviceSlug: The Cloudsmith service account slug. Required when
    ///   `auth_type` is `oidc_cloudsmith`.
    /// - apiHost: The Cloudsmith API host. Optional for `oidc_cloudsmith` auth
    ///   type. If omitted, `api.cloudsmith.io` is used by default.
    /// - workloadIdentityProvider: The full resource name of the GCP Workload
    ///   Identity Provider (e.g.
    ///   `projects/<NUM>/locations/global/workloadIdentityPools/<POOL>/providers/<PRO
    ///   VIDER>`). Required when `auth_type` is `oidc_gcp`.
    /// - serviceAccount: The GCP service account email to impersonate. Optional for
    ///   `oidc_gcp` auth type. If omitted, the federated token is used directly
    ///   (direct WIF).
    static func privateRegistriesUpdateOrgPrivateRegistry(
        config: ClientConfig,
        options: PrivateRegistriesUpdateOrgPrivateRegistryOptions
    ) async throws -> SdkEmptyResponse {
        if let url = options.url {
            try sdkValidateUri("url", url)
        }

        if let encryptedValue = options.encryptedValue {
            try sdkValidatePattern("encrypted_value", encryptedValue, sdkPattern21db07621cc5)
        }

        let requestBody = PrivateRegistriesUpdateOrgPrivateRegistryRequestBody(options: options)

        return try await (sdkRequest(
            "PATCH",
            [
                "/orgs/",
                sdkEncodePathSegment(sdkWireString(options.org)),
                "/private-registries/",
                sdkEncodePathSegment(sdkWireString(options.secretName)),
            ].joined(),
            config: config,
            body: requestBody,
            decoder: .empty,
            operationId: "privateRegistriesUpdateOrgPrivateRegistry"
        )).data
    }
}
