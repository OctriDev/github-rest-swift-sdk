// Code generated by Octri SDK Studio. DO NOT EDIT.
// This file is machine-generated from your OpenAPI spec.

import Foundation

#if canImport(FoundationNetworking)
    import FoundationNetworking
#endif
extension ReposMethods {
    public struct ReposUpdateBranchProtectionOptions: Codable {
        public var owner: String
        public var repo: String
        public var branch: String
        public var requiredStatusChecks: ReposUpdateBranchProtectionRequestBodyRequiredStatusChecks?
        public var enforceAdmins: Bool?
        public var requiredPullRequestReviews: ReposUpdateBranchProtectionRequestBodyRequiredPullRequestReviews?
        public var restrictions: ReposUpdateBranchProtectionRequestBodyRestrictions?
        public var requiredLinearHistory: Bool?
        public var allowForcePushes: SdkOptional<Bool>?
        public var allowDeletions: Bool?
        public var blockCreations: Bool?
        public var requiredConversationResolution: Bool?
        public var lockBranch: Bool?
        public var allowForkSyncing: Bool?

        public init(owner: String, repo: String, branch: String, requiredStatusChecks: ReposUpdateBranchProtectionRequestBodyRequiredStatusChecks?, enforceAdmins: Bool?, requiredPullRequestReviews: ReposUpdateBranchProtectionRequestBodyRequiredPullRequestReviews?, restrictions: ReposUpdateBranchProtectionRequestBodyRestrictions?) {
            self.owner = owner
            self.repo = repo
            self.branch = branch
            self.requiredStatusChecks = requiredStatusChecks
            self.enforceAdmins = enforceAdmins
            self.requiredPullRequestReviews = requiredPullRequestReviews
            self.restrictions = restrictions
        }
    }

    /// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation. Protecting a branch requires admin or owner permissions to the repository. > [!NOTE] > Passing new arrays of `users` and `teams` replaces their previous values. > [!NOTE] > The list of users, apps, and teams in total is limited to 100 items.
    ///
    /// - Parameters:
    /// - owner: The account owner of the repository. The name is not case
    ///   sensitive.
    /// - repo: The name of the repository without the `.git` extension. The name is
    ///   not case sensitive.
    /// - branch: The name of the branch. Cannot contain wildcard characters. To use
    ///   wildcard characters in branch names, use [the GraphQL
    ///   API](https://docs.github.com/graphql).
    /// - requiredStatusChecks: Require status checks to pass before merging. Set to
    ///   `null` to disable.
    /// - enforceAdmins: Enforce all configured restrictions for administrators. Set
    ///   to `true` to enforce required status checks for repository administrators.
    ///   Set to `null` to disable.
    /// - requiredPullRequestReviews: Require at least one approving review on a
    ///   pull request, before merging. Set to `null` to disable.
    /// - restrictions: Restrict who can push to the protected branch. User, app,
    ///   and team `restrictions` are only available for organization-owned
    ///   repositories. Set to `null` to disable.
    /// - requiredLinearHistory: Enforces a linear commit Git history, which
    ///   prevents anyone from pushing merge commits to a branch. Set to `true` to
    ///   enforce a linear commit history. Set to `false` to disable a linear commit
    ///   Git history. Your repository must allow squash merging or rebase merging
    ///   before you can enable a linear commit history. Default: `false`. For more
    ///   information, see "[Requiring a linear commit
    ///   history](https://docs.github.com/github/administering-a-repository/requiring
    ///   -a-linear-commit-history)" in the GitHub Help documentation.
    /// - allowForcePushes: Permits force pushes to the protected branch by anyone
    ///   with write access to the repository. Set to `true` to allow force pushes.
    ///   Set to `false` or `null` to block force pushes. Default: `false`. For more
    ///   information, see "[Enabling force pushes to a protected
    ///   branch](https://docs.github.com/github/administering-a-repository/enabling-f
    ///   orce-pushes-to-a-protected-branch)" in the GitHub Help documentation."
    /// - allowDeletions: Allows deletion of the protected branch by anyone with
    ///   write access to the repository. Set to `false` to prevent deletion of the
    ///   protected branch. Default: `false`. For more information, see "[Enabling
    ///   force pushes to a protected
    ///   branch](https://docs.github.com/github/administering-a-repository/enabling-f
    ///   orce-pushes-to-a-protected-branch)" in the GitHub Help documentation.
    /// - blockCreations: If set to `true`, the `restrictions` branch protection
    ///   settings which limits who can push will also block pushes which create new
    ///   branches, unless the push is initiated by a user, team, or app which has the
    ///   ability to push. Set to `true` to restrict new branch creation. Default:
    ///   `false`.
    /// - requiredConversationResolution: Requires all conversations on code to be
    ///   resolved before a pull request can be merged into a branch that matches this
    ///   rule. Set to `false` to disable. Default: `false`.
    /// - lockBranch: Whether to set the branch as read-only. If this is true, users
    ///   will not be able to push to the branch. Default: `false`.
    /// - allowForkSyncing: Whether users can pull changes from upstream when the
    ///   branch is locked. Set to `true` to allow fork syncing. Set to `false` to
    ///   prevent fork syncing. Default: `false`.
    public static func reposUpdateBranchProtection(config: ClientConfig, options: ReposUpdateBranchProtectionOptions) async throws -> ProtectedBranch {
        let requestBody = ReposUpdateBranchProtectionRequestBody(options: options)

        return try (await sdkRequest("PUT", ["/repos/", sdkEncodePathSegment(sdkWireString(options.owner)), "/", sdkEncodePathSegment(sdkWireString(options.repo)), "/branches/", sdkEncodePathSegment(sdkWireString(options.branch)), "/protection"].joined(), config: config, body: requestBody, decoder: .json, operationId: "reposUpdateBranchProtection")).data
    }
}
